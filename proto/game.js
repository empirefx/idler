/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-mixed-operators, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars, default-case, jsdoc/require-param*/
import $protobuf from "protobufjs/minimal.js";

// Common aliases
const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;
const $Object = $util.global.Object, $undefined = $util.global.undefined, $Error = $util.global.Error, $TypeError = $util.global.TypeError, $String = $util.global.String, $Array = $util.global.Array, $Number = $util.global.Number, $isFinite = $util.global.isFinite, $parseInt = $util.global.parseInt, $BigInt = $util.global.BigInt, $Boolean = $util.global.Boolean;

// Exported root namespace
const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

export const game = $root.game = (() => {

    /**
     * Namespace game.
     * @exports game
     * @namespace
     */
    const game = {};

    game.GameMessage = (function() {

        /**
         * Properties of a GameMessage.
         * @typedef {Object} game.GameMessage.$Properties
         * @property {string|null} [type] GameMessage type
         * @property {Uint8Array|null} [payload] GameMessage payload
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GameMessage.
         * @memberof game
         * @interface IGameMessage
         * @augments game.GameMessage.$Properties
         * @deprecated Use game.GameMessage.$Properties instead.
         */

        /**
         * Shape of a GameMessage.
         * @typedef {game.GameMessage.$Properties} game.GameMessage.$Shape
         */

        /**
         * Constructs a new GameMessage.
         * @memberof game
         * @classdesc Represents a GameMessage.
         * @constructor
         * @param {game.GameMessage.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GameMessage = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GameMessage type.
         * @member {string} type
         * @memberof game.GameMessage
         * @instance
         */
        GameMessage.prototype.type = "";

        /**
         * GameMessage payload.
         * @member {Uint8Array} payload
         * @memberof game.GameMessage
         * @instance
         */
        GameMessage.prototype.payload = $util.newBuffer([]);

        /**
         * Creates a new GameMessage instance using the specified properties.
         * @function create
         * @memberof game.GameMessage
         * @static
         * @param {game.GameMessage.$Properties=} [properties] Properties to set
         * @returns {game.GameMessage} GameMessage instance
         * @type {{
         *   (properties: game.GameMessage.$Shape): game.GameMessage & game.GameMessage.$Shape;
         *   (properties?: game.GameMessage.$Properties): game.GameMessage;
         * }}
         */
        GameMessage.create = function(properties) {
            return new GameMessage(properties);
        };

        /**
         * Encodes the specified GameMessage message. Does not implicitly {@link game.GameMessage.verify|verify} messages.
         * @function encode
         * @memberof game.GameMessage
         * @static
         * @param {game.GameMessage.$Properties} message GameMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameMessage.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.type);
            if (message.payload != null && $Object.hasOwnProperty.call(message, "payload") && message.payload.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.payload);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GameMessage message, length delimited. Does not implicitly {@link game.GameMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.GameMessage
         * @static
         * @param {game.GameMessage.$Properties} message GameMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameMessage.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GameMessage message from the specified reader or buffer.
         * @function decode
         * @memberof game.GameMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.GameMessage & game.GameMessage.$Shape} GameMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameMessage.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.GameMessage(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payload = value;
                        else
                            delete message.payload;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GameMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.GameMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.GameMessage & game.GameMessage.$Shape} GameMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameMessage.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameMessage message.
         * @function verify
         * @memberof game.GameMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameMessage.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            if (message.payload != null && $Object.hasOwnProperty.call(message, "payload"))
                if (!(message.payload && typeof message.payload.length === "number" || $util.isString(message.payload)))
                    return "payload: buffer expected";
            return null;
        };

        /**
         * Creates a GameMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.GameMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.GameMessage} GameMessage
         */
        GameMessage.fromObject = function (object, _depth) {
            if (object instanceof $root.game.GameMessage)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.GameMessage: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.GameMessage();
            if (object.type != null)
                if (typeof object.type !== "string" || object.type.length)
                    message.type = $String(object.type);
            if (object.payload != null)
                if (object.payload.length)
                    if (typeof object.payload === "string")
                        $util.base64.decode(object.payload, message.payload = $util.newBuffer($util.base64.length(object.payload)), 0);
                    else if (object.payload.length >= 0)
                        message.payload = object.payload;
            return message;
        };

        /**
         * Creates a plain object from a GameMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.GameMessage
         * @static
         * @param {game.GameMessage} message GameMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameMessage.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.type = "";
                if (options.bytes === $String)
                    object.payload = "";
                else {
                    object.payload = [];
                    if (options.bytes !== $Array)
                        object.payload = $util.newBuffer(object.payload);
                }
            }
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                object.type = message.type;
            if (message.payload != null && $Object.hasOwnProperty.call(message, "payload"))
                object.payload = options.bytes === $String ? $util.base64.encode(message.payload, 0, message.payload.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.payload) : message.payload;
            return object;
        };

        /**
         * Converts this GameMessage to JSON.
         * @function toJSON
         * @memberof game.GameMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameMessage.prototype.toJSON = function() {
            return GameMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GameMessage
         * @function getTypeUrl
         * @memberof game.GameMessage
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GameMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.GameMessage";
        };

        return GameMessage;
    })();

    game.ErrorResponse = (function() {

        /**
         * Properties of an ErrorResponse.
         * @typedef {Object} game.ErrorResponse.$Properties
         * @property {string|null} [code] ErrorResponse code
         * @property {string|null} [message] ErrorResponse message
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an ErrorResponse.
         * @memberof game
         * @interface IErrorResponse
         * @augments game.ErrorResponse.$Properties
         * @deprecated Use game.ErrorResponse.$Properties instead.
         */

        /**
         * Shape of an ErrorResponse.
         * @typedef {game.ErrorResponse.$Properties} game.ErrorResponse.$Shape
         */

        /**
         * Constructs a new ErrorResponse.
         * @memberof game
         * @classdesc Represents an ErrorResponse.
         * @constructor
         * @param {game.ErrorResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ErrorResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ErrorResponse code.
         * @member {string} code
         * @memberof game.ErrorResponse
         * @instance
         */
        ErrorResponse.prototype.code = "";

        /**
         * ErrorResponse message.
         * @member {string} message
         * @memberof game.ErrorResponse
         * @instance
         */
        ErrorResponse.prototype.message = "";

        /**
         * Creates a new ErrorResponse instance using the specified properties.
         * @function create
         * @memberof game.ErrorResponse
         * @static
         * @param {game.ErrorResponse.$Properties=} [properties] Properties to set
         * @returns {game.ErrorResponse} ErrorResponse instance
         * @type {{
         *   (properties: game.ErrorResponse.$Shape): game.ErrorResponse & game.ErrorResponse.$Shape;
         *   (properties?: game.ErrorResponse.$Properties): game.ErrorResponse;
         * }}
         */
        ErrorResponse.create = function(properties) {
            return new ErrorResponse(properties);
        };

        /**
         * Encodes the specified ErrorResponse message. Does not implicitly {@link game.ErrorResponse.verify|verify} messages.
         * @function encode
         * @memberof game.ErrorResponse
         * @static
         * @param {game.ErrorResponse.$Properties} message ErrorResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ErrorResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.code != null && $Object.hasOwnProperty.call(message, "code") && message.code !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.code);
            if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ErrorResponse message, length delimited. Does not implicitly {@link game.ErrorResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.ErrorResponse
         * @static
         * @param {game.ErrorResponse.$Properties} message ErrorResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ErrorResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an ErrorResponse message from the specified reader or buffer.
         * @function decode
         * @memberof game.ErrorResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.ErrorResponse & game.ErrorResponse.$Shape} ErrorResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ErrorResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.ErrorResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.message = value;
                        else
                            delete message.message;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an ErrorResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.ErrorResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.ErrorResponse & game.ErrorResponse.$Shape} ErrorResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ErrorResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ErrorResponse message.
         * @function verify
         * @memberof game.ErrorResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ErrorResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
                if (!$util.isString(message.code))
                    return "code: string expected";
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            return null;
        };

        /**
         * Creates an ErrorResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.ErrorResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.ErrorResponse} ErrorResponse
         */
        ErrorResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.game.ErrorResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.ErrorResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.ErrorResponse();
            if (object.code != null)
                if (typeof object.code !== "string" || object.code.length)
                    message.code = $String(object.code);
            if (object.message != null)
                if (typeof object.message !== "string" || object.message.length)
                    message.message = $String(object.message);
            return message;
        };

        /**
         * Creates a plain object from an ErrorResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.ErrorResponse
         * @static
         * @param {game.ErrorResponse} message ErrorResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ErrorResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.code = "";
                object.message = "";
            }
            if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
                object.code = message.code;
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            return object;
        };

        /**
         * Converts this ErrorResponse to JSON.
         * @function toJSON
         * @memberof game.ErrorResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ErrorResponse.prototype.toJSON = function() {
            return ErrorResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ErrorResponse
         * @function getTypeUrl
         * @memberof game.ErrorResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ErrorResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.ErrorResponse";
        };

        return ErrorResponse;
    })();

    game.Stats = (function() {

        /**
         * Properties of a Stats.
         * @typedef {Object} game.Stats.$Properties
         * @property {number|null} [strength] Stats strength
         * @property {number|null} [defense] Stats defense
         * @property {number|null} [agility] Stats agility
         * @property {number|null} [vitality] Stats vitality
         * @property {number|null} [intelligence] Stats intelligence
         * @property {number|null} [wisdom] Stats wisdom
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Stats.
         * @memberof game
         * @interface IStats
         * @augments game.Stats.$Properties
         * @deprecated Use game.Stats.$Properties instead.
         */

        /**
         * Shape of a Stats.
         * @typedef {game.Stats.$Properties} game.Stats.$Shape
         */

        /**
         * Constructs a new Stats.
         * @memberof game
         * @classdesc Represents a Stats.
         * @constructor
         * @param {game.Stats.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Stats = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Stats strength.
         * @member {number} strength
         * @memberof game.Stats
         * @instance
         */
        Stats.prototype.strength = 0;

        /**
         * Stats defense.
         * @member {number} defense
         * @memberof game.Stats
         * @instance
         */
        Stats.prototype.defense = 0;

        /**
         * Stats agility.
         * @member {number} agility
         * @memberof game.Stats
         * @instance
         */
        Stats.prototype.agility = 0;

        /**
         * Stats vitality.
         * @member {number} vitality
         * @memberof game.Stats
         * @instance
         */
        Stats.prototype.vitality = 0;

        /**
         * Stats intelligence.
         * @member {number} intelligence
         * @memberof game.Stats
         * @instance
         */
        Stats.prototype.intelligence = 0;

        /**
         * Stats wisdom.
         * @member {number} wisdom
         * @memberof game.Stats
         * @instance
         */
        Stats.prototype.wisdom = 0;

        /**
         * Creates a new Stats instance using the specified properties.
         * @function create
         * @memberof game.Stats
         * @static
         * @param {game.Stats.$Properties=} [properties] Properties to set
         * @returns {game.Stats} Stats instance
         * @type {{
         *   (properties: game.Stats.$Shape): game.Stats & game.Stats.$Shape;
         *   (properties?: game.Stats.$Properties): game.Stats;
         * }}
         */
        Stats.create = function(properties) {
            return new Stats(properties);
        };

        /**
         * Encodes the specified Stats message. Does not implicitly {@link game.Stats.verify|verify} messages.
         * @function encode
         * @memberof game.Stats
         * @static
         * @param {game.Stats.$Properties} message Stats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Stats.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.strength != null && $Object.hasOwnProperty.call(message, "strength") && message.strength !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.strength);
            if (message.defense != null && $Object.hasOwnProperty.call(message, "defense") && message.defense !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.defense);
            if (message.agility != null && $Object.hasOwnProperty.call(message, "agility") && message.agility !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.agility);
            if (message.vitality != null && $Object.hasOwnProperty.call(message, "vitality") && message.vitality !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.vitality);
            if (message.intelligence != null && $Object.hasOwnProperty.call(message, "intelligence") && message.intelligence !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.intelligence);
            if (message.wisdom != null && $Object.hasOwnProperty.call(message, "wisdom") && message.wisdom !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.wisdom);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Stats message, length delimited. Does not implicitly {@link game.Stats.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Stats
         * @static
         * @param {game.Stats.$Properties} message Stats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Stats.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Stats message from the specified reader or buffer.
         * @function decode
         * @memberof game.Stats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Stats & game.Stats.$Shape} Stats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Stats.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Stats(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.strength = value;
                        else
                            delete message.strength;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.defense = value;
                        else
                            delete message.defense;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.agility = value;
                        else
                            delete message.agility;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.vitality = value;
                        else
                            delete message.vitality;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.intelligence = value;
                        else
                            delete message.intelligence;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.wisdom = value;
                        else
                            delete message.wisdom;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Stats message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Stats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Stats & game.Stats.$Shape} Stats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Stats.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Stats message.
         * @function verify
         * @memberof game.Stats
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Stats.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.strength != null && $Object.hasOwnProperty.call(message, "strength"))
                if (!$util.isInteger(message.strength))
                    return "strength: integer expected";
            if (message.defense != null && $Object.hasOwnProperty.call(message, "defense"))
                if (!$util.isInteger(message.defense))
                    return "defense: integer expected";
            if (message.agility != null && $Object.hasOwnProperty.call(message, "agility"))
                if (!$util.isInteger(message.agility))
                    return "agility: integer expected";
            if (message.vitality != null && $Object.hasOwnProperty.call(message, "vitality"))
                if (!$util.isInteger(message.vitality))
                    return "vitality: integer expected";
            if (message.intelligence != null && $Object.hasOwnProperty.call(message, "intelligence"))
                if (!$util.isInteger(message.intelligence))
                    return "intelligence: integer expected";
            if (message.wisdom != null && $Object.hasOwnProperty.call(message, "wisdom"))
                if (!$util.isInteger(message.wisdom))
                    return "wisdom: integer expected";
            return null;
        };

        /**
         * Creates a Stats message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Stats
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Stats} Stats
         */
        Stats.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Stats)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Stats: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Stats();
            if (object.strength != null)
                if ($Number(object.strength) !== 0)
                    message.strength = object.strength | 0;
            if (object.defense != null)
                if ($Number(object.defense) !== 0)
                    message.defense = object.defense | 0;
            if (object.agility != null)
                if ($Number(object.agility) !== 0)
                    message.agility = object.agility | 0;
            if (object.vitality != null)
                if ($Number(object.vitality) !== 0)
                    message.vitality = object.vitality | 0;
            if (object.intelligence != null)
                if ($Number(object.intelligence) !== 0)
                    message.intelligence = object.intelligence | 0;
            if (object.wisdom != null)
                if ($Number(object.wisdom) !== 0)
                    message.wisdom = object.wisdom | 0;
            return message;
        };

        /**
         * Creates a plain object from a Stats message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Stats
         * @static
         * @param {game.Stats} message Stats
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Stats.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.strength = 0;
                object.defense = 0;
                object.agility = 0;
                object.vitality = 0;
                object.intelligence = 0;
                object.wisdom = 0;
            }
            if (message.strength != null && $Object.hasOwnProperty.call(message, "strength"))
                object.strength = message.strength;
            if (message.defense != null && $Object.hasOwnProperty.call(message, "defense"))
                object.defense = message.defense;
            if (message.agility != null && $Object.hasOwnProperty.call(message, "agility"))
                object.agility = message.agility;
            if (message.vitality != null && $Object.hasOwnProperty.call(message, "vitality"))
                object.vitality = message.vitality;
            if (message.intelligence != null && $Object.hasOwnProperty.call(message, "intelligence"))
                object.intelligence = message.intelligence;
            if (message.wisdom != null && $Object.hasOwnProperty.call(message, "wisdom"))
                object.wisdom = message.wisdom;
            return object;
        };

        /**
         * Converts this Stats to JSON.
         * @function toJSON
         * @memberof game.Stats
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Stats.prototype.toJSON = function() {
            return Stats.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Stats
         * @function getTypeUrl
         * @memberof game.Stats
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Stats.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Stats";
        };

        return Stats;
    })();

    game.DerivedStats = (function() {

        /**
         * Properties of a DerivedStats.
         * @typedef {Object} game.DerivedStats.$Properties
         * @property {number|null} [defense] DerivedStats defense
         * @property {string|null} [damageType] DerivedStats damageType
         * @property {number|null} [damage] DerivedStats damage
         * @property {number|null} [hitChance] DerivedStats hitChance
         * @property {number|null} [critChance] DerivedStats critChance
         * @property {Object.<string,number>|null} [equipmentBonus] DerivedStats equipmentBonus
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a DerivedStats.
         * @memberof game
         * @interface IDerivedStats
         * @augments game.DerivedStats.$Properties
         * @deprecated Use game.DerivedStats.$Properties instead.
         */

        /**
         * Shape of a DerivedStats.
         * @typedef {game.DerivedStats.$Properties} game.DerivedStats.$Shape
         */

        /**
         * Constructs a new DerivedStats.
         * @memberof game
         * @classdesc Represents a DerivedStats.
         * @constructor
         * @param {game.DerivedStats.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const DerivedStats = function (properties) {
            this.equipmentBonus = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DerivedStats defense.
         * @member {number} defense
         * @memberof game.DerivedStats
         * @instance
         */
        DerivedStats.prototype.defense = 0;

        /**
         * DerivedStats damageType.
         * @member {string} damageType
         * @memberof game.DerivedStats
         * @instance
         */
        DerivedStats.prototype.damageType = "";

        /**
         * DerivedStats damage.
         * @member {number} damage
         * @memberof game.DerivedStats
         * @instance
         */
        DerivedStats.prototype.damage = 0;

        /**
         * DerivedStats hitChance.
         * @member {number} hitChance
         * @memberof game.DerivedStats
         * @instance
         */
        DerivedStats.prototype.hitChance = 0;

        /**
         * DerivedStats critChance.
         * @member {number} critChance
         * @memberof game.DerivedStats
         * @instance
         */
        DerivedStats.prototype.critChance = 0;

        /**
         * DerivedStats equipmentBonus.
         * @member {Object.<string,number>} equipmentBonus
         * @memberof game.DerivedStats
         * @instance
         */
        DerivedStats.prototype.equipmentBonus = $util.emptyObject;

        /**
         * Creates a new DerivedStats instance using the specified properties.
         * @function create
         * @memberof game.DerivedStats
         * @static
         * @param {game.DerivedStats.$Properties=} [properties] Properties to set
         * @returns {game.DerivedStats} DerivedStats instance
         * @type {{
         *   (properties: game.DerivedStats.$Shape): game.DerivedStats & game.DerivedStats.$Shape;
         *   (properties?: game.DerivedStats.$Properties): game.DerivedStats;
         * }}
         */
        DerivedStats.create = function(properties) {
            return new DerivedStats(properties);
        };

        /**
         * Encodes the specified DerivedStats message. Does not implicitly {@link game.DerivedStats.verify|verify} messages.
         * @function encode
         * @memberof game.DerivedStats
         * @static
         * @param {game.DerivedStats.$Properties} message DerivedStats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DerivedStats.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.defense != null && $Object.hasOwnProperty.call(message, "defense") && !$Object.is(message.defense, 0))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.defense);
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType") && message.damageType !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.damageType);
            if (message.damage != null && $Object.hasOwnProperty.call(message, "damage") && !$Object.is(message.damage, 0))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.damage);
            if (message.hitChance != null && $Object.hasOwnProperty.call(message, "hitChance") && !$Object.is(message.hitChance, 0))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.hitChance);
            if (message.critChance != null && $Object.hasOwnProperty.call(message, "critChance") && !$Object.is(message.critChance, 0))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.critChance);
            if (message.equipmentBonus != null && $Object.hasOwnProperty.call(message, "equipmentBonus"))
                for (let keys = $Object.keys(message.equipmentBonus), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 1 =*/17).double(message.equipmentBonus[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DerivedStats message, length delimited. Does not implicitly {@link game.DerivedStats.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.DerivedStats
         * @static
         * @param {game.DerivedStats.$Properties} message DerivedStats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DerivedStats.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a DerivedStats message from the specified reader or buffer.
         * @function decode
         * @memberof game.DerivedStats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.DerivedStats & game.DerivedStats.$Shape} DerivedStats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DerivedStats.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.DerivedStats(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.defense = value;
                        else
                            delete message.defense;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.damageType = value;
                        else
                            delete message.damageType;
                        continue;
                    }
                case 3: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.damage = value;
                        else
                            delete message.damage;
                        continue;
                    }
                case 4: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.hitChance = value;
                        else
                            delete message.hitChance;
                        continue;
                    }
                case 5: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.critChance = value;
                        else
                            delete message.critChance;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (message.equipmentBonus === $util.emptyObject)
                            message.equipmentBonus = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 1)
                                    break;
                                value = reader.double();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.equipmentBonus, key);
                        message.equipmentBonus[key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DerivedStats message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.DerivedStats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.DerivedStats & game.DerivedStats.$Shape} DerivedStats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DerivedStats.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DerivedStats message.
         * @function verify
         * @memberof game.DerivedStats
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DerivedStats.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.defense != null && $Object.hasOwnProperty.call(message, "defense"))
                if (typeof message.defense !== "number")
                    return "defense: number expected";
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType"))
                if (!$util.isString(message.damageType))
                    return "damageType: string expected";
            if (message.damage != null && $Object.hasOwnProperty.call(message, "damage"))
                if (typeof message.damage !== "number")
                    return "damage: number expected";
            if (message.hitChance != null && $Object.hasOwnProperty.call(message, "hitChance"))
                if (typeof message.hitChance !== "number")
                    return "hitChance: number expected";
            if (message.critChance != null && $Object.hasOwnProperty.call(message, "critChance"))
                if (typeof message.critChance !== "number")
                    return "critChance: number expected";
            if (message.equipmentBonus != null && $Object.hasOwnProperty.call(message, "equipmentBonus")) {
                if (!$util.isObject(message.equipmentBonus))
                    return "equipmentBonus: object expected";
                let key = $Object.keys(message.equipmentBonus);
                for (let i = 0; i < key.length; ++i)
                    if (typeof message.equipmentBonus[key[i]] !== "number")
                        return "equipmentBonus: number{k:string} expected";
            }
            return null;
        };

        /**
         * Creates a DerivedStats message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.DerivedStats
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.DerivedStats} DerivedStats
         */
        DerivedStats.fromObject = function (object, _depth) {
            if (object instanceof $root.game.DerivedStats)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.DerivedStats: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.DerivedStats();
            if (object.defense != null)
                if (!$Object.is($Number(object.defense), 0))
                    message.defense = $Number(object.defense);
            if (object.damageType != null)
                if (typeof object.damageType !== "string" || object.damageType.length)
                    message.damageType = $String(object.damageType);
            if (object.damage != null)
                if (!$Object.is($Number(object.damage), 0))
                    message.damage = $Number(object.damage);
            if (object.hitChance != null)
                if (!$Object.is($Number(object.hitChance), 0))
                    message.hitChance = $Number(object.hitChance);
            if (object.critChance != null)
                if (!$Object.is($Number(object.critChance), 0))
                    message.critChance = $Number(object.critChance);
            if (object.equipmentBonus) {
                if (!$util.isObject(object.equipmentBonus))
                    throw $TypeError(".game.DerivedStats.equipmentBonus: object expected");
                message.equipmentBonus = {};
                for (let keys = $Object.keys(object.equipmentBonus), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.equipmentBonus, keys[i]);
                    message.equipmentBonus[keys[i]] = $Number(object.equipmentBonus[keys[i]]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a DerivedStats message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.DerivedStats
         * @static
         * @param {game.DerivedStats} message DerivedStats
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DerivedStats.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.equipmentBonus = {};
            if (options.defaults) {
                object.defense = 0;
                object.damageType = "";
                object.damage = 0;
                object.hitChance = 0;
                object.critChance = 0;
            }
            if (message.defense != null && $Object.hasOwnProperty.call(message, "defense"))
                object.defense = options.json && !$isFinite(message.defense) ? $String(message.defense) : message.defense;
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType"))
                object.damageType = message.damageType;
            if (message.damage != null && $Object.hasOwnProperty.call(message, "damage"))
                object.damage = options.json && !$isFinite(message.damage) ? $String(message.damage) : message.damage;
            if (message.hitChance != null && $Object.hasOwnProperty.call(message, "hitChance"))
                object.hitChance = options.json && !$isFinite(message.hitChance) ? $String(message.hitChance) : message.hitChance;
            if (message.critChance != null && $Object.hasOwnProperty.call(message, "critChance"))
                object.critChance = options.json && !$isFinite(message.critChance) ? $String(message.critChance) : message.critChance;
            let keys2;
            if (message.equipmentBonus && (keys2 = $Object.keys(message.equipmentBonus)).length) {
                object.equipmentBonus = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.equipmentBonus, keys2[j]);
                    object.equipmentBonus[keys2[j]] = options.json && !$isFinite(message.equipmentBonus[keys2[j]]) ? $String(message.equipmentBonus[keys2[j]]) : message.equipmentBonus[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this DerivedStats to JSON.
         * @function toJSON
         * @memberof game.DerivedStats
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DerivedStats.prototype.toJSON = function() {
            return DerivedStats.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DerivedStats
         * @function getTypeUrl
         * @memberof game.DerivedStats
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DerivedStats.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.DerivedStats";
        };

        return DerivedStats;
    })();

    game.Buff = (function() {

        /**
         * Properties of a Buff.
         * @typedef {Object} game.Buff.$Properties
         * @property {string|null} [skillId] Buff skillId
         * @property {string|null} [stat] Buff stat
         * @property {number|null} [value] Buff value
         * @property {number|Long|null} [duration] Buff duration
         * @property {number|Long|null} [expiresAt] Buff expiresAt
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Buff.
         * @memberof game
         * @interface IBuff
         * @augments game.Buff.$Properties
         * @deprecated Use game.Buff.$Properties instead.
         */

        /**
         * Shape of a Buff.
         * @typedef {game.Buff.$Properties} game.Buff.$Shape
         */

        /**
         * Constructs a new Buff.
         * @memberof game
         * @classdesc Represents a Buff.
         * @constructor
         * @param {game.Buff.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Buff = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Buff skillId.
         * @member {string} skillId
         * @memberof game.Buff
         * @instance
         */
        Buff.prototype.skillId = "";

        /**
         * Buff stat.
         * @member {string} stat
         * @memberof game.Buff
         * @instance
         */
        Buff.prototype.stat = "";

        /**
         * Buff value.
         * @member {number} value
         * @memberof game.Buff
         * @instance
         */
        Buff.prototype.value = 0;

        /**
         * Buff duration.
         * @member {number|Long} duration
         * @memberof game.Buff
         * @instance
         */
        Buff.prototype.duration = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Buff expiresAt.
         * @member {number|Long} expiresAt
         * @memberof game.Buff
         * @instance
         */
        Buff.prototype.expiresAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new Buff instance using the specified properties.
         * @function create
         * @memberof game.Buff
         * @static
         * @param {game.Buff.$Properties=} [properties] Properties to set
         * @returns {game.Buff} Buff instance
         * @type {{
         *   (properties: game.Buff.$Shape): game.Buff & game.Buff.$Shape;
         *   (properties?: game.Buff.$Properties): game.Buff;
         * }}
         */
        Buff.create = function(properties) {
            return new Buff(properties);
        };

        /**
         * Encodes the specified Buff message. Does not implicitly {@link game.Buff.verify|verify} messages.
         * @function encode
         * @memberof game.Buff
         * @static
         * @param {game.Buff.$Properties} message Buff message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Buff.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.skillId != null && $Object.hasOwnProperty.call(message, "skillId") && message.skillId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.skillId);
            if (message.stat != null && $Object.hasOwnProperty.call(message, "stat") && message.stat !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.stat);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && !$Object.is(message.value, 0))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.value);
            if (message.duration != null && $Object.hasOwnProperty.call(message, "duration") && (typeof message.duration === "object" ? message.duration.low || message.duration.high : message.duration !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.duration);
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt") && (typeof message.expiresAt === "object" ? message.expiresAt.low || message.expiresAt.high : message.expiresAt !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.expiresAt);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Buff message, length delimited. Does not implicitly {@link game.Buff.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Buff
         * @static
         * @param {game.Buff.$Properties} message Buff message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Buff.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Buff message from the specified reader or buffer.
         * @function decode
         * @memberof game.Buff
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Buff & game.Buff.$Shape} Buff
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Buff.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Buff(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.skillId = value;
                        else
                            delete message.skillId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.stat = value;
                        else
                            delete message.stat;
                        continue;
                    }
                case 3: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.duration = value;
                        else
                            delete message.duration;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expiresAt = value;
                        else
                            delete message.expiresAt;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Buff message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Buff
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Buff & game.Buff.$Shape} Buff
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Buff.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Buff message.
         * @function verify
         * @memberof game.Buff
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Buff.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.skillId != null && $Object.hasOwnProperty.call(message, "skillId"))
                if (!$util.isString(message.skillId))
                    return "skillId: string expected";
            if (message.stat != null && $Object.hasOwnProperty.call(message, "stat"))
                if (!$util.isString(message.stat))
                    return "stat: string expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (typeof message.value !== "number")
                    return "value: number expected";
            if (message.duration != null && $Object.hasOwnProperty.call(message, "duration"))
                if (!$util.isInteger(message.duration) && !(message.duration && $util.isInteger(message.duration.low) && $util.isInteger(message.duration.high)))
                    return "duration: integer|Long expected";
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt"))
                if (!$util.isInteger(message.expiresAt) && !(message.expiresAt && $util.isInteger(message.expiresAt.low) && $util.isInteger(message.expiresAt.high)))
                    return "expiresAt: integer|Long expected";
            return null;
        };

        /**
         * Creates a Buff message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Buff
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Buff} Buff
         */
        Buff.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Buff)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Buff: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Buff();
            if (object.skillId != null)
                if (typeof object.skillId !== "string" || object.skillId.length)
                    message.skillId = $String(object.skillId);
            if (object.stat != null)
                if (typeof object.stat !== "string" || object.stat.length)
                    message.stat = $String(object.stat);
            if (object.value != null)
                if (!$Object.is($Number(object.value), 0))
                    message.value = $Number(object.value);
            if (object.duration != null)
                if (typeof object.duration === "object" ? object.duration.low || object.duration.high : $Number(object.duration) !== 0)
                    if ($util.Long)
                        message.duration = $util.Long.fromValue(object.duration, false);
                    else if (typeof object.duration === "string")
                        message.duration = $parseInt(object.duration, 10);
                    else if (typeof object.duration === "number")
                        message.duration = object.duration;
                    else if (typeof object.duration === "object")
                        message.duration = new $util.LongBits(object.duration.low >>> 0, object.duration.high >>> 0).toNumber();
            if (object.expiresAt != null)
                if (typeof object.expiresAt === "object" ? object.expiresAt.low || object.expiresAt.high : $Number(object.expiresAt) !== 0)
                    if ($util.Long)
                        message.expiresAt = $util.Long.fromValue(object.expiresAt, false);
                    else if (typeof object.expiresAt === "string")
                        message.expiresAt = $parseInt(object.expiresAt, 10);
                    else if (typeof object.expiresAt === "number")
                        message.expiresAt = object.expiresAt;
                    else if (typeof object.expiresAt === "object")
                        message.expiresAt = new $util.LongBits(object.expiresAt.low >>> 0, object.expiresAt.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a Buff message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Buff
         * @static
         * @param {game.Buff} message Buff
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Buff.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.skillId = "";
                object.stat = "";
                object.value = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.duration = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.duration = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiresAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expiresAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.skillId != null && $Object.hasOwnProperty.call(message, "skillId"))
                object.skillId = message.skillId;
            if (message.stat != null && $Object.hasOwnProperty.call(message, "stat"))
                object.stat = message.stat;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = options.json && !$isFinite(message.value) ? $String(message.value) : message.value;
            if (message.duration != null && $Object.hasOwnProperty.call(message, "duration"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.duration = typeof message.duration === "number" ? $BigInt(message.duration) : $util.Long.fromBits(message.duration.low >>> 0, message.duration.high >>> 0, false).toBigInt();
                else if (typeof message.duration === "number")
                    object.duration = options.longs === $String ? $String(message.duration) : message.duration;
                else
                    object.duration = options.longs === $String ? $util.Long.prototype.toString.call(message.duration) : options.longs === $Number ? new $util.LongBits(message.duration.low >>> 0, message.duration.high >>> 0).toNumber() : message.duration;
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expiresAt = typeof message.expiresAt === "number" ? $BigInt(message.expiresAt) : $util.Long.fromBits(message.expiresAt.low >>> 0, message.expiresAt.high >>> 0, false).toBigInt();
                else if (typeof message.expiresAt === "number")
                    object.expiresAt = options.longs === $String ? $String(message.expiresAt) : message.expiresAt;
                else
                    object.expiresAt = options.longs === $String ? $util.Long.prototype.toString.call(message.expiresAt) : options.longs === $Number ? new $util.LongBits(message.expiresAt.low >>> 0, message.expiresAt.high >>> 0).toNumber() : message.expiresAt;
            return object;
        };

        /**
         * Converts this Buff to JSON.
         * @function toJSON
         * @memberof game.Buff
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Buff.prototype.toJSON = function() {
            return Buff.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Buff
         * @function getTypeUrl
         * @memberof game.Buff
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Buff.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Buff";
        };

        return Buff;
    })();

    game.Buffs = (function() {

        /**
         * Properties of a Buffs.
         * @typedef {Object} game.Buffs.$Properties
         * @property {Array.<game.Buff.$Properties>|null} [buffs] Buffs buffs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Buffs.
         * @memberof game
         * @interface IBuffs
         * @augments game.Buffs.$Properties
         * @deprecated Use game.Buffs.$Properties instead.
         */

        /**
         * Shape of a Buffs.
         * @typedef {game.Buffs.$Properties} game.Buffs.$Shape
         */

        /**
         * Constructs a new Buffs.
         * @memberof game
         * @classdesc Represents a Buffs.
         * @constructor
         * @param {game.Buffs.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Buffs = function (properties) {
            this.buffs = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Buffs buffs.
         * @member {Array.<game.Buff.$Properties>} buffs
         * @memberof game.Buffs
         * @instance
         */
        Buffs.prototype.buffs = $util.emptyArray;

        /**
         * Creates a new Buffs instance using the specified properties.
         * @function create
         * @memberof game.Buffs
         * @static
         * @param {game.Buffs.$Properties=} [properties] Properties to set
         * @returns {game.Buffs} Buffs instance
         * @type {{
         *   (properties: game.Buffs.$Shape): game.Buffs & game.Buffs.$Shape;
         *   (properties?: game.Buffs.$Properties): game.Buffs;
         * }}
         */
        Buffs.create = function(properties) {
            return new Buffs(properties);
        };

        /**
         * Encodes the specified Buffs message. Does not implicitly {@link game.Buffs.verify|verify} messages.
         * @function encode
         * @memberof game.Buffs
         * @static
         * @param {game.Buffs.$Properties} message Buffs message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Buffs.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.buffs != null && message.buffs.length)
                for (let i = 0; i < message.buffs.length; ++i)
                    $root.game.Buff.encode(message.buffs[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Buffs message, length delimited. Does not implicitly {@link game.Buffs.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Buffs
         * @static
         * @param {game.Buffs.$Properties} message Buffs message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Buffs.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Buffs message from the specified reader or buffer.
         * @function decode
         * @memberof game.Buffs
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Buffs & game.Buffs.$Shape} Buffs
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Buffs.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Buffs();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.buffs && message.buffs.length))
                            message.buffs = [];
                        message.buffs.push($root.game.Buff.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Buffs message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Buffs
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Buffs & game.Buffs.$Shape} Buffs
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Buffs.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Buffs message.
         * @function verify
         * @memberof game.Buffs
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Buffs.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.buffs != null && $Object.hasOwnProperty.call(message, "buffs")) {
                if (!$Array.isArray(message.buffs))
                    return "buffs: array expected";
                for (let i = 0; i < message.buffs.length; ++i) {
                    let error = $root.game.Buff.verify(message.buffs[i], _depth + 1);
                    if (error)
                        return "buffs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Buffs message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Buffs
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Buffs} Buffs
         */
        Buffs.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Buffs)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Buffs: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Buffs();
            if (object.buffs) {
                if (!$Array.isArray(object.buffs))
                    throw $TypeError(".game.Buffs.buffs: array expected");
                message.buffs = $Array(object.buffs.length);
                for (let i = 0; i < object.buffs.length; ++i) {
                    if (!$util.isObject(object.buffs[i]))
                        throw $TypeError(".game.Buffs.buffs: object expected");
                    message.buffs[i] = $root.game.Buff.fromObject(object.buffs[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Buffs message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Buffs
         * @static
         * @param {game.Buffs} message Buffs
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Buffs.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.buffs = [];
            if (message.buffs && message.buffs.length) {
                object.buffs = $Array(message.buffs.length);
                for (let j = 0; j < message.buffs.length; ++j)
                    object.buffs[j] = $root.game.Buff.toObject(message.buffs[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this Buffs to JSON.
         * @function toJSON
         * @memberof game.Buffs
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Buffs.prototype.toJSON = function() {
            return Buffs.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Buffs
         * @function getTypeUrl
         * @memberof game.Buffs
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Buffs.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Buffs";
        };

        return Buffs;
    })();

    game.Cooldowns = (function() {

        /**
         * Properties of a Cooldowns.
         * @typedef {Object} game.Cooldowns.$Properties
         * @property {Object.<string,number|Long>|null} [cooldowns] Cooldowns cooldowns
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Cooldowns.
         * @memberof game
         * @interface ICooldowns
         * @augments game.Cooldowns.$Properties
         * @deprecated Use game.Cooldowns.$Properties instead.
         */

        /**
         * Shape of a Cooldowns.
         * @typedef {game.Cooldowns.$Properties} game.Cooldowns.$Shape
         */

        /**
         * Constructs a new Cooldowns.
         * @memberof game
         * @classdesc Represents a Cooldowns.
         * @constructor
         * @param {game.Cooldowns.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Cooldowns = function (properties) {
            this.cooldowns = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Cooldowns cooldowns.
         * @member {Object.<string,number|Long>} cooldowns
         * @memberof game.Cooldowns
         * @instance
         */
        Cooldowns.prototype.cooldowns = $util.emptyObject;

        /**
         * Creates a new Cooldowns instance using the specified properties.
         * @function create
         * @memberof game.Cooldowns
         * @static
         * @param {game.Cooldowns.$Properties=} [properties] Properties to set
         * @returns {game.Cooldowns} Cooldowns instance
         * @type {{
         *   (properties: game.Cooldowns.$Shape): game.Cooldowns & game.Cooldowns.$Shape;
         *   (properties?: game.Cooldowns.$Properties): game.Cooldowns;
         * }}
         */
        Cooldowns.create = function(properties) {
            return new Cooldowns(properties);
        };

        /**
         * Encodes the specified Cooldowns message. Does not implicitly {@link game.Cooldowns.verify|verify} messages.
         * @function encode
         * @memberof game.Cooldowns
         * @static
         * @param {game.Cooldowns.$Properties} message Cooldowns message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cooldowns.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.cooldowns != null && $Object.hasOwnProperty.call(message, "cooldowns"))
                for (let keys = $Object.keys(message.cooldowns), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.cooldowns[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Cooldowns message, length delimited. Does not implicitly {@link game.Cooldowns.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Cooldowns
         * @static
         * @param {game.Cooldowns.$Properties} message Cooldowns message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cooldowns.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Cooldowns message from the specified reader or buffer.
         * @function decode
         * @memberof game.Cooldowns
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Cooldowns & game.Cooldowns.$Shape} Cooldowns
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cooldowns.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Cooldowns(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (message.cooldowns === $util.emptyObject)
                            message.cooldowns = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = $util.Long ? $util.Long.fromNumber(0, false) : 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 0)
                                    break;
                                value = reader.int64();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.cooldowns, key);
                        message.cooldowns[key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Cooldowns message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Cooldowns
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Cooldowns & game.Cooldowns.$Shape} Cooldowns
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cooldowns.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Cooldowns message.
         * @function verify
         * @memberof game.Cooldowns
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Cooldowns.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.cooldowns != null && $Object.hasOwnProperty.call(message, "cooldowns")) {
                if (!$util.isObject(message.cooldowns))
                    return "cooldowns: object expected";
                let key = $Object.keys(message.cooldowns);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.cooldowns[key[i]]) && !(message.cooldowns[key[i]] && $util.isInteger(message.cooldowns[key[i]].low) && $util.isInteger(message.cooldowns[key[i]].high)))
                        return "cooldowns: integer|Long{k:string} expected";
            }
            return null;
        };

        /**
         * Creates a Cooldowns message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Cooldowns
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Cooldowns} Cooldowns
         */
        Cooldowns.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Cooldowns)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Cooldowns: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Cooldowns();
            if (object.cooldowns) {
                if (!$util.isObject(object.cooldowns))
                    throw $TypeError(".game.Cooldowns.cooldowns: object expected");
                message.cooldowns = {};
                for (let keys = $Object.keys(object.cooldowns), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.cooldowns, keys[i]);
                    if ($util.Long)
                        message.cooldowns[keys[i]] = $util.Long.fromValue(object.cooldowns[keys[i]], false);
                    else if (typeof object.cooldowns[keys[i]] === "string")
                        message.cooldowns[keys[i]] = $parseInt(object.cooldowns[keys[i]], 10);
                    else if (typeof object.cooldowns[keys[i]] === "number")
                        message.cooldowns[keys[i]] = object.cooldowns[keys[i]];
                    else if (typeof object.cooldowns[keys[i]] === "object")
                        message.cooldowns[keys[i]] = new $util.LongBits(object.cooldowns[keys[i]].low >>> 0, object.cooldowns[keys[i]].high >>> 0).toNumber();
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Cooldowns message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Cooldowns
         * @static
         * @param {game.Cooldowns} message Cooldowns
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Cooldowns.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.cooldowns = {};
            let keys2;
            if (message.cooldowns && (keys2 = $Object.keys(message.cooldowns)).length) {
                object.cooldowns = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.cooldowns, keys2[j]);
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.cooldowns[keys2[j]] = typeof message.cooldowns[keys2[j]] === "number" ? $BigInt(message.cooldowns[keys2[j]]) : $util.Long.fromBits(message.cooldowns[keys2[j]].low >>> 0, message.cooldowns[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.cooldowns[keys2[j]] === "number")
                        object.cooldowns[keys2[j]] = options.longs === $String ? $String(message.cooldowns[keys2[j]]) : message.cooldowns[keys2[j]];
                    else
                        object.cooldowns[keys2[j]] = options.longs === $String ? $util.Long.prototype.toString.call(message.cooldowns[keys2[j]]) : options.longs === $Number ? new $util.LongBits(message.cooldowns[keys2[j]].low >>> 0, message.cooldowns[keys2[j]].high >>> 0).toNumber() : message.cooldowns[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this Cooldowns to JSON.
         * @function toJSON
         * @memberof game.Cooldowns
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Cooldowns.prototype.toJSON = function() {
            return Cooldowns.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Cooldowns
         * @function getTypeUrl
         * @memberof game.Cooldowns
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Cooldowns.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Cooldowns";
        };

        return Cooldowns;
    })();

    game.SkillsRanks = (function() {

        /**
         * Properties of a SkillsRanks.
         * @typedef {Object} game.SkillsRanks.$Properties
         * @property {Object.<string,number>|null} [ranks] SkillsRanks ranks
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SkillsRanks.
         * @memberof game
         * @interface ISkillsRanks
         * @augments game.SkillsRanks.$Properties
         * @deprecated Use game.SkillsRanks.$Properties instead.
         */

        /**
         * Shape of a SkillsRanks.
         * @typedef {game.SkillsRanks.$Properties} game.SkillsRanks.$Shape
         */

        /**
         * Constructs a new SkillsRanks.
         * @memberof game
         * @classdesc Represents a SkillsRanks.
         * @constructor
         * @param {game.SkillsRanks.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SkillsRanks = function (properties) {
            this.ranks = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SkillsRanks ranks.
         * @member {Object.<string,number>} ranks
         * @memberof game.SkillsRanks
         * @instance
         */
        SkillsRanks.prototype.ranks = $util.emptyObject;

        /**
         * Creates a new SkillsRanks instance using the specified properties.
         * @function create
         * @memberof game.SkillsRanks
         * @static
         * @param {game.SkillsRanks.$Properties=} [properties] Properties to set
         * @returns {game.SkillsRanks} SkillsRanks instance
         * @type {{
         *   (properties: game.SkillsRanks.$Shape): game.SkillsRanks & game.SkillsRanks.$Shape;
         *   (properties?: game.SkillsRanks.$Properties): game.SkillsRanks;
         * }}
         */
        SkillsRanks.create = function(properties) {
            return new SkillsRanks(properties);
        };

        /**
         * Encodes the specified SkillsRanks message. Does not implicitly {@link game.SkillsRanks.verify|verify} messages.
         * @function encode
         * @memberof game.SkillsRanks
         * @static
         * @param {game.SkillsRanks.$Properties} message SkillsRanks message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SkillsRanks.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.ranks != null && $Object.hasOwnProperty.call(message, "ranks"))
                for (let keys = $Object.keys(message.ranks), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.ranks[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SkillsRanks message, length delimited. Does not implicitly {@link game.SkillsRanks.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SkillsRanks
         * @static
         * @param {game.SkillsRanks.$Properties} message SkillsRanks message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SkillsRanks.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SkillsRanks message from the specified reader or buffer.
         * @function decode
         * @memberof game.SkillsRanks
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SkillsRanks & game.SkillsRanks.$Shape} SkillsRanks
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SkillsRanks.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.SkillsRanks(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (message.ranks === $util.emptyObject)
                            message.ranks = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 0)
                                    break;
                                value = reader.int32();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.ranks, key);
                        message.ranks[key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SkillsRanks message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SkillsRanks
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SkillsRanks & game.SkillsRanks.$Shape} SkillsRanks
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SkillsRanks.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SkillsRanks message.
         * @function verify
         * @memberof game.SkillsRanks
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SkillsRanks.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.ranks != null && $Object.hasOwnProperty.call(message, "ranks")) {
                if (!$util.isObject(message.ranks))
                    return "ranks: object expected";
                let key = $Object.keys(message.ranks);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.ranks[key[i]]))
                        return "ranks: integer{k:string} expected";
            }
            return null;
        };

        /**
         * Creates a SkillsRanks message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SkillsRanks
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SkillsRanks} SkillsRanks
         */
        SkillsRanks.fromObject = function (object, _depth) {
            if (object instanceof $root.game.SkillsRanks)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.SkillsRanks: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.SkillsRanks();
            if (object.ranks) {
                if (!$util.isObject(object.ranks))
                    throw $TypeError(".game.SkillsRanks.ranks: object expected");
                message.ranks = {};
                for (let keys = $Object.keys(object.ranks), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.ranks, keys[i]);
                    message.ranks[keys[i]] = object.ranks[keys[i]] | 0;
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SkillsRanks message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SkillsRanks
         * @static
         * @param {game.SkillsRanks} message SkillsRanks
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SkillsRanks.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.ranks = {};
            let keys2;
            if (message.ranks && (keys2 = $Object.keys(message.ranks)).length) {
                object.ranks = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.ranks, keys2[j]);
                    object.ranks[keys2[j]] = message.ranks[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this SkillsRanks to JSON.
         * @function toJSON
         * @memberof game.SkillsRanks
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SkillsRanks.prototype.toJSON = function() {
            return SkillsRanks.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SkillsRanks
         * @function getTypeUrl
         * @memberof game.SkillsRanks
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SkillsRanks.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.SkillsRanks";
        };

        return SkillsRanks;
    })();

    game.Player = (function() {

        /**
         * Properties of a Player.
         * @typedef {Object} game.Player.$Properties
         * @property {number|null} [level] Player level
         * @property {number|Long|null} [gold] Player gold
         * @property {number|Long|null} [exp] Player exp
         * @property {number|Long|null} [expToNext] Player expToNext
         * @property {number|Long|null} [hp] Player hp
         * @property {number|Long|null} [maxHp] Player maxHp
         * @property {string|null} [avatar] Player avatar
         * @property {game.Stats.$Properties|null} [stats] Player stats
         * @property {number|null} [skillPoints] Player skillPoints
         * @property {string|null} [currentPlaceId] Player currentPlaceId
         * @property {number|Long|null} [lastSeenAt] Player lastSeenAt
         * @property {number|Long|null} [attackCooldown] Player attackCooldown
         * @property {number|Long|null} [lastAttackTime] Player lastAttackTime
         * @property {Array.<game.Buff.$Properties>|null} [activeBuffs] Player activeBuffs
         * @property {Object.<string,number|Long>|null} [activeCooldowns] Player activeCooldowns
         * @property {Object.<string,number|Long>|null} [pausedCooldowns] Player pausedCooldowns
         * @property {Object.<string,string>|null} [skillJobIds] Player skillJobIds
         * @property {boolean|null} [autoCombat] Player autoCombat
         * @property {boolean|null} [isDead] Player isDead
         * @property {game.DerivedStats.$Properties|null} [derivedStats] Player derivedStats
         * @property {Object.<string,number>|null} [skills] Player skills
         * @property {string|null} [targetEnemyId] Player targetEnemyId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Player.
         * @memberof game
         * @interface IPlayer
         * @augments game.Player.$Properties
         * @deprecated Use game.Player.$Properties instead.
         */

        /**
         * Shape of a Player.
         * @typedef {game.Player.$Properties} game.Player.$Shape
         */

        /**
         * Constructs a new Player.
         * @memberof game
         * @classdesc Represents a Player.
         * @constructor
         * @param {game.Player.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Player = function (properties) {
            this.activeBuffs = [];
            this.activeCooldowns = {};
            this.pausedCooldowns = {};
            this.skillJobIds = {};
            this.skills = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Player level.
         * @member {number} level
         * @memberof game.Player
         * @instance
         */
        Player.prototype.level = 0;

        /**
         * Player gold.
         * @member {number|Long} gold
         * @memberof game.Player
         * @instance
         */
        Player.prototype.gold = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Player exp.
         * @member {number|Long} exp
         * @memberof game.Player
         * @instance
         */
        Player.prototype.exp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Player expToNext.
         * @member {number|Long} expToNext
         * @memberof game.Player
         * @instance
         */
        Player.prototype.expToNext = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Player hp.
         * @member {number|Long} hp
         * @memberof game.Player
         * @instance
         */
        Player.prototype.hp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Player maxHp.
         * @member {number|Long} maxHp
         * @memberof game.Player
         * @instance
         */
        Player.prototype.maxHp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Player avatar.
         * @member {string} avatar
         * @memberof game.Player
         * @instance
         */
        Player.prototype.avatar = "";

        /**
         * Player stats.
         * @member {game.Stats.$Properties|null|undefined} stats
         * @memberof game.Player
         * @instance
         */
        Player.prototype.stats = null;

        /**
         * Player skillPoints.
         * @member {number} skillPoints
         * @memberof game.Player
         * @instance
         */
        Player.prototype.skillPoints = 0;

        /**
         * Player currentPlaceId.
         * @member {string} currentPlaceId
         * @memberof game.Player
         * @instance
         */
        Player.prototype.currentPlaceId = "";

        /**
         * Player lastSeenAt.
         * @member {number|Long} lastSeenAt
         * @memberof game.Player
         * @instance
         */
        Player.prototype.lastSeenAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Player attackCooldown.
         * @member {number|Long} attackCooldown
         * @memberof game.Player
         * @instance
         */
        Player.prototype.attackCooldown = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Player lastAttackTime.
         * @member {number|Long} lastAttackTime
         * @memberof game.Player
         * @instance
         */
        Player.prototype.lastAttackTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Player activeBuffs.
         * @member {Array.<game.Buff.$Properties>} activeBuffs
         * @memberof game.Player
         * @instance
         */
        Player.prototype.activeBuffs = $util.emptyArray;

        /**
         * Player activeCooldowns.
         * @member {Object.<string,number|Long>} activeCooldowns
         * @memberof game.Player
         * @instance
         */
        Player.prototype.activeCooldowns = $util.emptyObject;

        /**
         * Player pausedCooldowns.
         * @member {Object.<string,number|Long>} pausedCooldowns
         * @memberof game.Player
         * @instance
         */
        Player.prototype.pausedCooldowns = $util.emptyObject;

        /**
         * Player skillJobIds.
         * @member {Object.<string,string>} skillJobIds
         * @memberof game.Player
         * @instance
         */
        Player.prototype.skillJobIds = $util.emptyObject;

        /**
         * Player autoCombat.
         * @member {boolean} autoCombat
         * @memberof game.Player
         * @instance
         */
        Player.prototype.autoCombat = false;

        /**
         * Player isDead.
         * @member {boolean} isDead
         * @memberof game.Player
         * @instance
         */
        Player.prototype.isDead = false;

        /**
         * Player derivedStats.
         * @member {game.DerivedStats.$Properties|null|undefined} derivedStats
         * @memberof game.Player
         * @instance
         */
        Player.prototype.derivedStats = null;

        /**
         * Player skills.
         * @member {Object.<string,number>} skills
         * @memberof game.Player
         * @instance
         */
        Player.prototype.skills = $util.emptyObject;

        /**
         * Player targetEnemyId.
         * @member {string} targetEnemyId
         * @memberof game.Player
         * @instance
         */
        Player.prototype.targetEnemyId = "";

        /**
         * Creates a new Player instance using the specified properties.
         * @function create
         * @memberof game.Player
         * @static
         * @param {game.Player.$Properties=} [properties] Properties to set
         * @returns {game.Player} Player instance
         * @type {{
         *   (properties: game.Player.$Shape): game.Player & game.Player.$Shape;
         *   (properties?: game.Player.$Properties): game.Player;
         * }}
         */
        Player.create = function(properties) {
            return new Player(properties);
        };

        /**
         * Encodes the specified Player message. Does not implicitly {@link game.Player.verify|verify} messages.
         * @function encode
         * @memberof game.Player
         * @static
         * @param {game.Player.$Properties} message Player message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Player.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.level != null && $Object.hasOwnProperty.call(message, "level") && message.level !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.level);
            if (message.gold != null && $Object.hasOwnProperty.call(message, "gold") && (typeof message.gold === "object" ? message.gold.low || message.gold.high : message.gold !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.gold);
            if (message.exp != null && $Object.hasOwnProperty.call(message, "exp") && (typeof message.exp === "object" ? message.exp.low || message.exp.high : message.exp !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.exp);
            if (message.expToNext != null && $Object.hasOwnProperty.call(message, "expToNext") && (typeof message.expToNext === "object" ? message.expToNext.low || message.expToNext.high : message.expToNext !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.expToNext);
            if (message.hp != null && $Object.hasOwnProperty.call(message, "hp") && (typeof message.hp === "object" ? message.hp.low || message.hp.high : message.hp !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.hp);
            if (message.maxHp != null && $Object.hasOwnProperty.call(message, "maxHp") && (typeof message.maxHp === "object" ? message.maxHp.low || message.maxHp.high : message.maxHp !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.maxHp);
            if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar") && message.avatar !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.avatar);
            if (message.stats != null && $Object.hasOwnProperty.call(message, "stats"))
                $root.game.Stats.encode(message.stats, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.skillPoints != null && $Object.hasOwnProperty.call(message, "skillPoints") && message.skillPoints !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.skillPoints);
            if (message.currentPlaceId != null && $Object.hasOwnProperty.call(message, "currentPlaceId") && message.currentPlaceId !== "")
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.currentPlaceId);
            if (message.lastSeenAt != null && $Object.hasOwnProperty.call(message, "lastSeenAt") && (typeof message.lastSeenAt === "object" ? message.lastSeenAt.low || message.lastSeenAt.high : message.lastSeenAt !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.lastSeenAt);
            if (message.attackCooldown != null && $Object.hasOwnProperty.call(message, "attackCooldown") && (typeof message.attackCooldown === "object" ? message.attackCooldown.low || message.attackCooldown.high : message.attackCooldown !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.attackCooldown);
            if (message.lastAttackTime != null && $Object.hasOwnProperty.call(message, "lastAttackTime") && (typeof message.lastAttackTime === "object" ? message.lastAttackTime.low || message.lastAttackTime.high : message.lastAttackTime !== 0))
                writer.uint32(/* id 13, wireType 0 =*/104).int64(message.lastAttackTime);
            if (message.activeBuffs != null && message.activeBuffs.length)
                for (let i = 0; i < message.activeBuffs.length; ++i)
                    $root.game.Buff.encode(message.activeBuffs[i], writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.activeCooldowns != null && $Object.hasOwnProperty.call(message, "activeCooldowns"))
                for (let keys = $Object.keys(message.activeCooldowns), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 15, wireType 2 =*/122).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.activeCooldowns[keys[i]]).ldelim();
            if (message.pausedCooldowns != null && $Object.hasOwnProperty.call(message, "pausedCooldowns"))
                for (let keys = $Object.keys(message.pausedCooldowns), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 16, wireType 2 =*/130).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.pausedCooldowns[keys[i]]).ldelim();
            if (message.skillJobIds != null && $Object.hasOwnProperty.call(message, "skillJobIds"))
                for (let keys = $Object.keys(message.skillJobIds), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 17, wireType 2 =*/138).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.skillJobIds[keys[i]]).ldelim();
            if (message.autoCombat != null && $Object.hasOwnProperty.call(message, "autoCombat") && message.autoCombat !== false)
                writer.uint32(/* id 18, wireType 0 =*/144).bool(message.autoCombat);
            if (message.isDead != null && $Object.hasOwnProperty.call(message, "isDead") && message.isDead !== false)
                writer.uint32(/* id 19, wireType 0 =*/152).bool(message.isDead);
            if (message.derivedStats != null && $Object.hasOwnProperty.call(message, "derivedStats"))
                $root.game.DerivedStats.encode(message.derivedStats, writer.uint32(/* id 20, wireType 2 =*/162).fork(), _depth + 1).ldelim();
            if (message.skills != null && $Object.hasOwnProperty.call(message, "skills"))
                for (let keys = $Object.keys(message.skills), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 21, wireType 2 =*/170).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.skills[keys[i]]).ldelim();
            if (message.targetEnemyId != null && $Object.hasOwnProperty.call(message, "targetEnemyId") && message.targetEnemyId !== "")
                writer.uint32(/* id 22, wireType 2 =*/178).string(message.targetEnemyId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Player message, length delimited. Does not implicitly {@link game.Player.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Player
         * @static
         * @param {game.Player.$Properties} message Player message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Player.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Player message from the specified reader or buffer.
         * @function decode
         * @memberof game.Player
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Player & game.Player.$Shape} Player
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Player.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Player(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.gold = value;
                        else
                            delete message.gold;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.exp = value;
                        else
                            delete message.exp;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expToNext = value;
                        else
                            delete message.expToNext;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.hp = value;
                        else
                            delete message.hp;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.maxHp = value;
                        else
                            delete message.maxHp;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.avatar = value;
                        else
                            delete message.avatar;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.stats = $root.game.Stats.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stats);
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.skillPoints = value;
                        else
                            delete message.skillPoints;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.currentPlaceId = value;
                        else
                            delete message.currentPlaceId;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.lastSeenAt = value;
                        else
                            delete message.lastSeenAt;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.attackCooldown = value;
                        else
                            delete message.attackCooldown;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.lastAttackTime = value;
                        else
                            delete message.lastAttackTime;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if (!(message.activeBuffs && message.activeBuffs.length))
                            message.activeBuffs = [];
                        message.activeBuffs.push($root.game.Buff.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if (message.activeCooldowns === $util.emptyObject)
                            message.activeCooldowns = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = $util.Long ? $util.Long.fromNumber(0, false) : 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 0)
                                    break;
                                value = reader.int64();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.activeCooldowns, key);
                        message.activeCooldowns[key] = value;
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if (message.pausedCooldowns === $util.emptyObject)
                            message.pausedCooldowns = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = $util.Long ? $util.Long.fromNumber(0, false) : 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 0)
                                    break;
                                value = reader.int64();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.pausedCooldowns, key);
                        message.pausedCooldowns[key] = value;
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        if (message.skillJobIds === $util.emptyObject)
                            message.skillJobIds = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = "";
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = reader.stringVerify();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.skillJobIds, key);
                        message.skillJobIds[key] = value;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.autoCombat = value;
                        else
                            delete message.autoCombat;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.isDead = value;
                        else
                            delete message.isDead;
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        message.derivedStats = $root.game.DerivedStats.decode(reader, reader.uint32(), $undefined, _depth + 1, message.derivedStats);
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        if (message.skills === $util.emptyObject)
                            message.skills = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 0)
                                    break;
                                value = reader.int32();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.skills, key);
                        message.skills[key] = value;
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.targetEnemyId = value;
                        else
                            delete message.targetEnemyId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Player message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Player
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Player & game.Player.$Shape} Player
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Player.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Player message.
         * @function verify
         * @memberof game.Player
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Player.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.level != null && $Object.hasOwnProperty.call(message, "level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.gold != null && $Object.hasOwnProperty.call(message, "gold"))
                if (!$util.isInteger(message.gold) && !(message.gold && $util.isInteger(message.gold.low) && $util.isInteger(message.gold.high)))
                    return "gold: integer|Long expected";
            if (message.exp != null && $Object.hasOwnProperty.call(message, "exp"))
                if (!$util.isInteger(message.exp) && !(message.exp && $util.isInteger(message.exp.low) && $util.isInteger(message.exp.high)))
                    return "exp: integer|Long expected";
            if (message.expToNext != null && $Object.hasOwnProperty.call(message, "expToNext"))
                if (!$util.isInteger(message.expToNext) && !(message.expToNext && $util.isInteger(message.expToNext.low) && $util.isInteger(message.expToNext.high)))
                    return "expToNext: integer|Long expected";
            if (message.hp != null && $Object.hasOwnProperty.call(message, "hp"))
                if (!$util.isInteger(message.hp) && !(message.hp && $util.isInteger(message.hp.low) && $util.isInteger(message.hp.high)))
                    return "hp: integer|Long expected";
            if (message.maxHp != null && $Object.hasOwnProperty.call(message, "maxHp"))
                if (!$util.isInteger(message.maxHp) && !(message.maxHp && $util.isInteger(message.maxHp.low) && $util.isInteger(message.maxHp.high)))
                    return "maxHp: integer|Long expected";
            if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar"))
                if (!$util.isString(message.avatar))
                    return "avatar: string expected";
            if (message.stats != null && $Object.hasOwnProperty.call(message, "stats")) {
                let error = $root.game.Stats.verify(message.stats, _depth + 1);
                if (error)
                    return "stats." + error;
            }
            if (message.skillPoints != null && $Object.hasOwnProperty.call(message, "skillPoints"))
                if (!$util.isInteger(message.skillPoints))
                    return "skillPoints: integer expected";
            if (message.currentPlaceId != null && $Object.hasOwnProperty.call(message, "currentPlaceId"))
                if (!$util.isString(message.currentPlaceId))
                    return "currentPlaceId: string expected";
            if (message.lastSeenAt != null && $Object.hasOwnProperty.call(message, "lastSeenAt"))
                if (!$util.isInteger(message.lastSeenAt) && !(message.lastSeenAt && $util.isInteger(message.lastSeenAt.low) && $util.isInteger(message.lastSeenAt.high)))
                    return "lastSeenAt: integer|Long expected";
            if (message.attackCooldown != null && $Object.hasOwnProperty.call(message, "attackCooldown"))
                if (!$util.isInteger(message.attackCooldown) && !(message.attackCooldown && $util.isInteger(message.attackCooldown.low) && $util.isInteger(message.attackCooldown.high)))
                    return "attackCooldown: integer|Long expected";
            if (message.lastAttackTime != null && $Object.hasOwnProperty.call(message, "lastAttackTime"))
                if (!$util.isInteger(message.lastAttackTime) && !(message.lastAttackTime && $util.isInteger(message.lastAttackTime.low) && $util.isInteger(message.lastAttackTime.high)))
                    return "lastAttackTime: integer|Long expected";
            if (message.activeBuffs != null && $Object.hasOwnProperty.call(message, "activeBuffs")) {
                if (!$Array.isArray(message.activeBuffs))
                    return "activeBuffs: array expected";
                for (let i = 0; i < message.activeBuffs.length; ++i) {
                    let error = $root.game.Buff.verify(message.activeBuffs[i], _depth + 1);
                    if (error)
                        return "activeBuffs." + error;
                }
            }
            if (message.activeCooldowns != null && $Object.hasOwnProperty.call(message, "activeCooldowns")) {
                if (!$util.isObject(message.activeCooldowns))
                    return "activeCooldowns: object expected";
                let key = $Object.keys(message.activeCooldowns);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.activeCooldowns[key[i]]) && !(message.activeCooldowns[key[i]] && $util.isInteger(message.activeCooldowns[key[i]].low) && $util.isInteger(message.activeCooldowns[key[i]].high)))
                        return "activeCooldowns: integer|Long{k:string} expected";
            }
            if (message.pausedCooldowns != null && $Object.hasOwnProperty.call(message, "pausedCooldowns")) {
                if (!$util.isObject(message.pausedCooldowns))
                    return "pausedCooldowns: object expected";
                let key = $Object.keys(message.pausedCooldowns);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.pausedCooldowns[key[i]]) && !(message.pausedCooldowns[key[i]] && $util.isInteger(message.pausedCooldowns[key[i]].low) && $util.isInteger(message.pausedCooldowns[key[i]].high)))
                        return "pausedCooldowns: integer|Long{k:string} expected";
            }
            if (message.skillJobIds != null && $Object.hasOwnProperty.call(message, "skillJobIds")) {
                if (!$util.isObject(message.skillJobIds))
                    return "skillJobIds: object expected";
                let key = $Object.keys(message.skillJobIds);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isString(message.skillJobIds[key[i]]))
                        return "skillJobIds: string{k:string} expected";
            }
            if (message.autoCombat != null && $Object.hasOwnProperty.call(message, "autoCombat"))
                if (typeof message.autoCombat !== "boolean")
                    return "autoCombat: boolean expected";
            if (message.isDead != null && $Object.hasOwnProperty.call(message, "isDead"))
                if (typeof message.isDead !== "boolean")
                    return "isDead: boolean expected";
            if (message.derivedStats != null && $Object.hasOwnProperty.call(message, "derivedStats")) {
                let error = $root.game.DerivedStats.verify(message.derivedStats, _depth + 1);
                if (error)
                    return "derivedStats." + error;
            }
            if (message.skills != null && $Object.hasOwnProperty.call(message, "skills")) {
                if (!$util.isObject(message.skills))
                    return "skills: object expected";
                let key = $Object.keys(message.skills);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.skills[key[i]]))
                        return "skills: integer{k:string} expected";
            }
            if (message.targetEnemyId != null && $Object.hasOwnProperty.call(message, "targetEnemyId"))
                if (!$util.isString(message.targetEnemyId))
                    return "targetEnemyId: string expected";
            return null;
        };

        /**
         * Creates a Player message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Player
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Player} Player
         */
        Player.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Player)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Player: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Player();
            if (object.level != null)
                if ($Number(object.level) !== 0)
                    message.level = object.level | 0;
            if (object.gold != null)
                if (typeof object.gold === "object" ? object.gold.low || object.gold.high : $Number(object.gold) !== 0)
                    if ($util.Long)
                        message.gold = $util.Long.fromValue(object.gold, false);
                    else if (typeof object.gold === "string")
                        message.gold = $parseInt(object.gold, 10);
                    else if (typeof object.gold === "number")
                        message.gold = object.gold;
                    else if (typeof object.gold === "object")
                        message.gold = new $util.LongBits(object.gold.low >>> 0, object.gold.high >>> 0).toNumber();
            if (object.exp != null)
                if (typeof object.exp === "object" ? object.exp.low || object.exp.high : $Number(object.exp) !== 0)
                    if ($util.Long)
                        message.exp = $util.Long.fromValue(object.exp, false);
                    else if (typeof object.exp === "string")
                        message.exp = $parseInt(object.exp, 10);
                    else if (typeof object.exp === "number")
                        message.exp = object.exp;
                    else if (typeof object.exp === "object")
                        message.exp = new $util.LongBits(object.exp.low >>> 0, object.exp.high >>> 0).toNumber();
            if (object.expToNext != null)
                if (typeof object.expToNext === "object" ? object.expToNext.low || object.expToNext.high : $Number(object.expToNext) !== 0)
                    if ($util.Long)
                        message.expToNext = $util.Long.fromValue(object.expToNext, false);
                    else if (typeof object.expToNext === "string")
                        message.expToNext = $parseInt(object.expToNext, 10);
                    else if (typeof object.expToNext === "number")
                        message.expToNext = object.expToNext;
                    else if (typeof object.expToNext === "object")
                        message.expToNext = new $util.LongBits(object.expToNext.low >>> 0, object.expToNext.high >>> 0).toNumber();
            if (object.hp != null)
                if (typeof object.hp === "object" ? object.hp.low || object.hp.high : $Number(object.hp) !== 0)
                    if ($util.Long)
                        message.hp = $util.Long.fromValue(object.hp, false);
                    else if (typeof object.hp === "string")
                        message.hp = $parseInt(object.hp, 10);
                    else if (typeof object.hp === "number")
                        message.hp = object.hp;
                    else if (typeof object.hp === "object")
                        message.hp = new $util.LongBits(object.hp.low >>> 0, object.hp.high >>> 0).toNumber();
            if (object.maxHp != null)
                if (typeof object.maxHp === "object" ? object.maxHp.low || object.maxHp.high : $Number(object.maxHp) !== 0)
                    if ($util.Long)
                        message.maxHp = $util.Long.fromValue(object.maxHp, false);
                    else if (typeof object.maxHp === "string")
                        message.maxHp = $parseInt(object.maxHp, 10);
                    else if (typeof object.maxHp === "number")
                        message.maxHp = object.maxHp;
                    else if (typeof object.maxHp === "object")
                        message.maxHp = new $util.LongBits(object.maxHp.low >>> 0, object.maxHp.high >>> 0).toNumber();
            if (object.avatar != null)
                if (typeof object.avatar !== "string" || object.avatar.length)
                    message.avatar = $String(object.avatar);
            if (object.stats != null) {
                if (!$util.isObject(object.stats))
                    throw $TypeError(".game.Player.stats: object expected");
                message.stats = $root.game.Stats.fromObject(object.stats, _depth + 1);
            }
            if (object.skillPoints != null)
                if ($Number(object.skillPoints) !== 0)
                    message.skillPoints = object.skillPoints | 0;
            if (object.currentPlaceId != null)
                if (typeof object.currentPlaceId !== "string" || object.currentPlaceId.length)
                    message.currentPlaceId = $String(object.currentPlaceId);
            if (object.lastSeenAt != null)
                if (typeof object.lastSeenAt === "object" ? object.lastSeenAt.low || object.lastSeenAt.high : $Number(object.lastSeenAt) !== 0)
                    if ($util.Long)
                        message.lastSeenAt = $util.Long.fromValue(object.lastSeenAt, false);
                    else if (typeof object.lastSeenAt === "string")
                        message.lastSeenAt = $parseInt(object.lastSeenAt, 10);
                    else if (typeof object.lastSeenAt === "number")
                        message.lastSeenAt = object.lastSeenAt;
                    else if (typeof object.lastSeenAt === "object")
                        message.lastSeenAt = new $util.LongBits(object.lastSeenAt.low >>> 0, object.lastSeenAt.high >>> 0).toNumber();
            if (object.attackCooldown != null)
                if (typeof object.attackCooldown === "object" ? object.attackCooldown.low || object.attackCooldown.high : $Number(object.attackCooldown) !== 0)
                    if ($util.Long)
                        message.attackCooldown = $util.Long.fromValue(object.attackCooldown, false);
                    else if (typeof object.attackCooldown === "string")
                        message.attackCooldown = $parseInt(object.attackCooldown, 10);
                    else if (typeof object.attackCooldown === "number")
                        message.attackCooldown = object.attackCooldown;
                    else if (typeof object.attackCooldown === "object")
                        message.attackCooldown = new $util.LongBits(object.attackCooldown.low >>> 0, object.attackCooldown.high >>> 0).toNumber();
            if (object.lastAttackTime != null)
                if (typeof object.lastAttackTime === "object" ? object.lastAttackTime.low || object.lastAttackTime.high : $Number(object.lastAttackTime) !== 0)
                    if ($util.Long)
                        message.lastAttackTime = $util.Long.fromValue(object.lastAttackTime, false);
                    else if (typeof object.lastAttackTime === "string")
                        message.lastAttackTime = $parseInt(object.lastAttackTime, 10);
                    else if (typeof object.lastAttackTime === "number")
                        message.lastAttackTime = object.lastAttackTime;
                    else if (typeof object.lastAttackTime === "object")
                        message.lastAttackTime = new $util.LongBits(object.lastAttackTime.low >>> 0, object.lastAttackTime.high >>> 0).toNumber();
            if (object.activeBuffs) {
                if (!$Array.isArray(object.activeBuffs))
                    throw $TypeError(".game.Player.activeBuffs: array expected");
                message.activeBuffs = $Array(object.activeBuffs.length);
                for (let i = 0; i < object.activeBuffs.length; ++i) {
                    if (!$util.isObject(object.activeBuffs[i]))
                        throw $TypeError(".game.Player.activeBuffs: object expected");
                    message.activeBuffs[i] = $root.game.Buff.fromObject(object.activeBuffs[i], _depth + 1);
                }
            }
            if (object.activeCooldowns) {
                if (!$util.isObject(object.activeCooldowns))
                    throw $TypeError(".game.Player.activeCooldowns: object expected");
                message.activeCooldowns = {};
                for (let keys = $Object.keys(object.activeCooldowns), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.activeCooldowns, keys[i]);
                    if ($util.Long)
                        message.activeCooldowns[keys[i]] = $util.Long.fromValue(object.activeCooldowns[keys[i]], false);
                    else if (typeof object.activeCooldowns[keys[i]] === "string")
                        message.activeCooldowns[keys[i]] = $parseInt(object.activeCooldowns[keys[i]], 10);
                    else if (typeof object.activeCooldowns[keys[i]] === "number")
                        message.activeCooldowns[keys[i]] = object.activeCooldowns[keys[i]];
                    else if (typeof object.activeCooldowns[keys[i]] === "object")
                        message.activeCooldowns[keys[i]] = new $util.LongBits(object.activeCooldowns[keys[i]].low >>> 0, object.activeCooldowns[keys[i]].high >>> 0).toNumber();
                }
            }
            if (object.pausedCooldowns) {
                if (!$util.isObject(object.pausedCooldowns))
                    throw $TypeError(".game.Player.pausedCooldowns: object expected");
                message.pausedCooldowns = {};
                for (let keys = $Object.keys(object.pausedCooldowns), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.pausedCooldowns, keys[i]);
                    if ($util.Long)
                        message.pausedCooldowns[keys[i]] = $util.Long.fromValue(object.pausedCooldowns[keys[i]], false);
                    else if (typeof object.pausedCooldowns[keys[i]] === "string")
                        message.pausedCooldowns[keys[i]] = $parseInt(object.pausedCooldowns[keys[i]], 10);
                    else if (typeof object.pausedCooldowns[keys[i]] === "number")
                        message.pausedCooldowns[keys[i]] = object.pausedCooldowns[keys[i]];
                    else if (typeof object.pausedCooldowns[keys[i]] === "object")
                        message.pausedCooldowns[keys[i]] = new $util.LongBits(object.pausedCooldowns[keys[i]].low >>> 0, object.pausedCooldowns[keys[i]].high >>> 0).toNumber();
                }
            }
            if (object.skillJobIds) {
                if (!$util.isObject(object.skillJobIds))
                    throw $TypeError(".game.Player.skillJobIds: object expected");
                message.skillJobIds = {};
                for (let keys = $Object.keys(object.skillJobIds), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.skillJobIds, keys[i]);
                    message.skillJobIds[keys[i]] = $String(object.skillJobIds[keys[i]]);
                }
            }
            if (object.autoCombat != null)
                if (object.autoCombat)
                    message.autoCombat = $Boolean(object.autoCombat);
            if (object.isDead != null)
                if (object.isDead)
                    message.isDead = $Boolean(object.isDead);
            if (object.derivedStats != null) {
                if (!$util.isObject(object.derivedStats))
                    throw $TypeError(".game.Player.derivedStats: object expected");
                message.derivedStats = $root.game.DerivedStats.fromObject(object.derivedStats, _depth + 1);
            }
            if (object.skills) {
                if (!$util.isObject(object.skills))
                    throw $TypeError(".game.Player.skills: object expected");
                message.skills = {};
                for (let keys = $Object.keys(object.skills), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.skills, keys[i]);
                    message.skills[keys[i]] = object.skills[keys[i]] | 0;
                }
            }
            if (object.targetEnemyId != null)
                if (typeof object.targetEnemyId !== "string" || object.targetEnemyId.length)
                    message.targetEnemyId = $String(object.targetEnemyId);
            return message;
        };

        /**
         * Creates a plain object from a Player message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Player
         * @static
         * @param {game.Player} message Player
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Player.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.activeBuffs = [];
            if (options.objects || options.defaults) {
                object.activeCooldowns = {};
                object.pausedCooldowns = {};
                object.skillJobIds = {};
                object.skills = {};
            }
            if (options.defaults) {
                object.level = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.gold = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gold = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.exp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.exp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expToNext = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expToNext = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.hp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.hp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.maxHp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.maxHp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.avatar = "";
                object.stats = null;
                object.skillPoints = 0;
                object.currentPlaceId = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastSeenAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.lastSeenAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.attackCooldown = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.attackCooldown = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastAttackTime = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.lastAttackTime = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.autoCombat = false;
                object.isDead = false;
                object.derivedStats = null;
                object.targetEnemyId = "";
            }
            if (message.level != null && $Object.hasOwnProperty.call(message, "level"))
                object.level = message.level;
            if (message.gold != null && $Object.hasOwnProperty.call(message, "gold"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gold = typeof message.gold === "number" ? $BigInt(message.gold) : $util.Long.fromBits(message.gold.low >>> 0, message.gold.high >>> 0, false).toBigInt();
                else if (typeof message.gold === "number")
                    object.gold = options.longs === $String ? $String(message.gold) : message.gold;
                else
                    object.gold = options.longs === $String ? $util.Long.prototype.toString.call(message.gold) : options.longs === $Number ? new $util.LongBits(message.gold.low >>> 0, message.gold.high >>> 0).toNumber() : message.gold;
            if (message.exp != null && $Object.hasOwnProperty.call(message, "exp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.exp = typeof message.exp === "number" ? $BigInt(message.exp) : $util.Long.fromBits(message.exp.low >>> 0, message.exp.high >>> 0, false).toBigInt();
                else if (typeof message.exp === "number")
                    object.exp = options.longs === $String ? $String(message.exp) : message.exp;
                else
                    object.exp = options.longs === $String ? $util.Long.prototype.toString.call(message.exp) : options.longs === $Number ? new $util.LongBits(message.exp.low >>> 0, message.exp.high >>> 0).toNumber() : message.exp;
            if (message.expToNext != null && $Object.hasOwnProperty.call(message, "expToNext"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expToNext = typeof message.expToNext === "number" ? $BigInt(message.expToNext) : $util.Long.fromBits(message.expToNext.low >>> 0, message.expToNext.high >>> 0, false).toBigInt();
                else if (typeof message.expToNext === "number")
                    object.expToNext = options.longs === $String ? $String(message.expToNext) : message.expToNext;
                else
                    object.expToNext = options.longs === $String ? $util.Long.prototype.toString.call(message.expToNext) : options.longs === $Number ? new $util.LongBits(message.expToNext.low >>> 0, message.expToNext.high >>> 0).toNumber() : message.expToNext;
            if (message.hp != null && $Object.hasOwnProperty.call(message, "hp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.hp = typeof message.hp === "number" ? $BigInt(message.hp) : $util.Long.fromBits(message.hp.low >>> 0, message.hp.high >>> 0, false).toBigInt();
                else if (typeof message.hp === "number")
                    object.hp = options.longs === $String ? $String(message.hp) : message.hp;
                else
                    object.hp = options.longs === $String ? $util.Long.prototype.toString.call(message.hp) : options.longs === $Number ? new $util.LongBits(message.hp.low >>> 0, message.hp.high >>> 0).toNumber() : message.hp;
            if (message.maxHp != null && $Object.hasOwnProperty.call(message, "maxHp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.maxHp = typeof message.maxHp === "number" ? $BigInt(message.maxHp) : $util.Long.fromBits(message.maxHp.low >>> 0, message.maxHp.high >>> 0, false).toBigInt();
                else if (typeof message.maxHp === "number")
                    object.maxHp = options.longs === $String ? $String(message.maxHp) : message.maxHp;
                else
                    object.maxHp = options.longs === $String ? $util.Long.prototype.toString.call(message.maxHp) : options.longs === $Number ? new $util.LongBits(message.maxHp.low >>> 0, message.maxHp.high >>> 0).toNumber() : message.maxHp;
            if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar"))
                object.avatar = message.avatar;
            if (message.stats != null && $Object.hasOwnProperty.call(message, "stats"))
                object.stats = $root.game.Stats.toObject(message.stats, options, _depth + 1);
            if (message.skillPoints != null && $Object.hasOwnProperty.call(message, "skillPoints"))
                object.skillPoints = message.skillPoints;
            if (message.currentPlaceId != null && $Object.hasOwnProperty.call(message, "currentPlaceId"))
                object.currentPlaceId = message.currentPlaceId;
            if (message.lastSeenAt != null && $Object.hasOwnProperty.call(message, "lastSeenAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.lastSeenAt = typeof message.lastSeenAt === "number" ? $BigInt(message.lastSeenAt) : $util.Long.fromBits(message.lastSeenAt.low >>> 0, message.lastSeenAt.high >>> 0, false).toBigInt();
                else if (typeof message.lastSeenAt === "number")
                    object.lastSeenAt = options.longs === $String ? $String(message.lastSeenAt) : message.lastSeenAt;
                else
                    object.lastSeenAt = options.longs === $String ? $util.Long.prototype.toString.call(message.lastSeenAt) : options.longs === $Number ? new $util.LongBits(message.lastSeenAt.low >>> 0, message.lastSeenAt.high >>> 0).toNumber() : message.lastSeenAt;
            if (message.attackCooldown != null && $Object.hasOwnProperty.call(message, "attackCooldown"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.attackCooldown = typeof message.attackCooldown === "number" ? $BigInt(message.attackCooldown) : $util.Long.fromBits(message.attackCooldown.low >>> 0, message.attackCooldown.high >>> 0, false).toBigInt();
                else if (typeof message.attackCooldown === "number")
                    object.attackCooldown = options.longs === $String ? $String(message.attackCooldown) : message.attackCooldown;
                else
                    object.attackCooldown = options.longs === $String ? $util.Long.prototype.toString.call(message.attackCooldown) : options.longs === $Number ? new $util.LongBits(message.attackCooldown.low >>> 0, message.attackCooldown.high >>> 0).toNumber() : message.attackCooldown;
            if (message.lastAttackTime != null && $Object.hasOwnProperty.call(message, "lastAttackTime"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.lastAttackTime = typeof message.lastAttackTime === "number" ? $BigInt(message.lastAttackTime) : $util.Long.fromBits(message.lastAttackTime.low >>> 0, message.lastAttackTime.high >>> 0, false).toBigInt();
                else if (typeof message.lastAttackTime === "number")
                    object.lastAttackTime = options.longs === $String ? $String(message.lastAttackTime) : message.lastAttackTime;
                else
                    object.lastAttackTime = options.longs === $String ? $util.Long.prototype.toString.call(message.lastAttackTime) : options.longs === $Number ? new $util.LongBits(message.lastAttackTime.low >>> 0, message.lastAttackTime.high >>> 0).toNumber() : message.lastAttackTime;
            if (message.activeBuffs && message.activeBuffs.length) {
                object.activeBuffs = $Array(message.activeBuffs.length);
                for (let j = 0; j < message.activeBuffs.length; ++j)
                    object.activeBuffs[j] = $root.game.Buff.toObject(message.activeBuffs[j], options, _depth + 1);
            }
            let keys2;
            if (message.activeCooldowns && (keys2 = $Object.keys(message.activeCooldowns)).length) {
                object.activeCooldowns = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.activeCooldowns, keys2[j]);
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.activeCooldowns[keys2[j]] = typeof message.activeCooldowns[keys2[j]] === "number" ? $BigInt(message.activeCooldowns[keys2[j]]) : $util.Long.fromBits(message.activeCooldowns[keys2[j]].low >>> 0, message.activeCooldowns[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.activeCooldowns[keys2[j]] === "number")
                        object.activeCooldowns[keys2[j]] = options.longs === $String ? $String(message.activeCooldowns[keys2[j]]) : message.activeCooldowns[keys2[j]];
                    else
                        object.activeCooldowns[keys2[j]] = options.longs === $String ? $util.Long.prototype.toString.call(message.activeCooldowns[keys2[j]]) : options.longs === $Number ? new $util.LongBits(message.activeCooldowns[keys2[j]].low >>> 0, message.activeCooldowns[keys2[j]].high >>> 0).toNumber() : message.activeCooldowns[keys2[j]];
                }
            }
            if (message.pausedCooldowns && (keys2 = $Object.keys(message.pausedCooldowns)).length) {
                object.pausedCooldowns = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.pausedCooldowns, keys2[j]);
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.pausedCooldowns[keys2[j]] = typeof message.pausedCooldowns[keys2[j]] === "number" ? $BigInt(message.pausedCooldowns[keys2[j]]) : $util.Long.fromBits(message.pausedCooldowns[keys2[j]].low >>> 0, message.pausedCooldowns[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.pausedCooldowns[keys2[j]] === "number")
                        object.pausedCooldowns[keys2[j]] = options.longs === $String ? $String(message.pausedCooldowns[keys2[j]]) : message.pausedCooldowns[keys2[j]];
                    else
                        object.pausedCooldowns[keys2[j]] = options.longs === $String ? $util.Long.prototype.toString.call(message.pausedCooldowns[keys2[j]]) : options.longs === $Number ? new $util.LongBits(message.pausedCooldowns[keys2[j]].low >>> 0, message.pausedCooldowns[keys2[j]].high >>> 0).toNumber() : message.pausedCooldowns[keys2[j]];
                }
            }
            if (message.skillJobIds && (keys2 = $Object.keys(message.skillJobIds)).length) {
                object.skillJobIds = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.skillJobIds, keys2[j]);
                    object.skillJobIds[keys2[j]] = message.skillJobIds[keys2[j]];
                }
            }
            if (message.autoCombat != null && $Object.hasOwnProperty.call(message, "autoCombat"))
                object.autoCombat = message.autoCombat;
            if (message.isDead != null && $Object.hasOwnProperty.call(message, "isDead"))
                object.isDead = message.isDead;
            if (message.derivedStats != null && $Object.hasOwnProperty.call(message, "derivedStats"))
                object.derivedStats = $root.game.DerivedStats.toObject(message.derivedStats, options, _depth + 1);
            if (message.skills && (keys2 = $Object.keys(message.skills)).length) {
                object.skills = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.skills, keys2[j]);
                    object.skills[keys2[j]] = message.skills[keys2[j]];
                }
            }
            if (message.targetEnemyId != null && $Object.hasOwnProperty.call(message, "targetEnemyId"))
                object.targetEnemyId = message.targetEnemyId;
            return object;
        };

        /**
         * Converts this Player to JSON.
         * @function toJSON
         * @memberof game.Player
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Player.prototype.toJSON = function() {
            return Player.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Player
         * @function getTypeUrl
         * @memberof game.Player
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Player.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Player";
        };

        return Player;
    })();

    game.Item = (function() {

        /**
         * Properties of an Item.
         * @typedef {Object} game.Item.$Properties
         * @property {number|Long|null} [id] Item id
         * @property {string|null} [templateId] Item templateId
         * @property {string|null} [name] Item name
         * @property {string|null} [icon] Item icon
         * @property {string|null} [description] Item description
         * @property {string|null} [type] Item type
         * @property {number|null} [quantity] Item quantity
         * @property {number|null} [weight] Item weight
         * @property {Object.<string,number>|null} [stats] Item stats
         * @property {Object.<string,number|Long>|null} [consumable] Item consumable
         * @property {string|null} [damageType] Item damageType
         * @property {string|null} [primaryStat] Item primaryStat
         * @property {string|null} [recipeId] Item recipeId
         * @property {Object.<string,number|Long>|null} [buy] Item buy
         * @property {Object.<string,number|Long>|null} [sellable] Item sellable
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an Item.
         * @memberof game
         * @interface IItem
         * @augments game.Item.$Properties
         * @deprecated Use game.Item.$Properties instead.
         */

        /**
         * Shape of an Item.
         * @typedef {game.Item.$Properties} game.Item.$Shape
         */

        /**
         * Constructs a new Item.
         * @memberof game
         * @classdesc Represents an Item.
         * @constructor
         * @param {game.Item.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Item = function (properties) {
            this.stats = {};
            this.consumable = {};
            this.buy = {};
            this.sellable = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Item id.
         * @member {number|Long} id
         * @memberof game.Item
         * @instance
         */
        Item.prototype.id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Item templateId.
         * @member {string} templateId
         * @memberof game.Item
         * @instance
         */
        Item.prototype.templateId = "";

        /**
         * Item name.
         * @member {string} name
         * @memberof game.Item
         * @instance
         */
        Item.prototype.name = "";

        /**
         * Item icon.
         * @member {string} icon
         * @memberof game.Item
         * @instance
         */
        Item.prototype.icon = "";

        /**
         * Item description.
         * @member {string} description
         * @memberof game.Item
         * @instance
         */
        Item.prototype.description = "";

        /**
         * Item type.
         * @member {string} type
         * @memberof game.Item
         * @instance
         */
        Item.prototype.type = "";

        /**
         * Item quantity.
         * @member {number} quantity
         * @memberof game.Item
         * @instance
         */
        Item.prototype.quantity = 0;

        /**
         * Item weight.
         * @member {number} weight
         * @memberof game.Item
         * @instance
         */
        Item.prototype.weight = 0;

        /**
         * Item stats.
         * @member {Object.<string,number>} stats
         * @memberof game.Item
         * @instance
         */
        Item.prototype.stats = $util.emptyObject;

        /**
         * Item consumable.
         * @member {Object.<string,number|Long>} consumable
         * @memberof game.Item
         * @instance
         */
        Item.prototype.consumable = $util.emptyObject;

        /**
         * Item damageType.
         * @member {string} damageType
         * @memberof game.Item
         * @instance
         */
        Item.prototype.damageType = "";

        /**
         * Item primaryStat.
         * @member {string} primaryStat
         * @memberof game.Item
         * @instance
         */
        Item.prototype.primaryStat = "";

        /**
         * Item recipeId.
         * @member {string} recipeId
         * @memberof game.Item
         * @instance
         */
        Item.prototype.recipeId = "";

        /**
         * Item buy.
         * @member {Object.<string,number|Long>} buy
         * @memberof game.Item
         * @instance
         */
        Item.prototype.buy = $util.emptyObject;

        /**
         * Item sellable.
         * @member {Object.<string,number|Long>} sellable
         * @memberof game.Item
         * @instance
         */
        Item.prototype.sellable = $util.emptyObject;

        /**
         * Creates a new Item instance using the specified properties.
         * @function create
         * @memberof game.Item
         * @static
         * @param {game.Item.$Properties=} [properties] Properties to set
         * @returns {game.Item} Item instance
         * @type {{
         *   (properties: game.Item.$Shape): game.Item & game.Item.$Shape;
         *   (properties?: game.Item.$Properties): game.Item;
         * }}
         */
        Item.create = function(properties) {
            return new Item(properties);
        };

        /**
         * Encodes the specified Item message. Does not implicitly {@link game.Item.verify|verify} messages.
         * @function encode
         * @memberof game.Item
         * @static
         * @param {game.Item.$Properties} message Item message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Item.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && (typeof message.id === "object" ? message.id.low || message.id.high : message.id !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.id);
            if (message.templateId != null && $Object.hasOwnProperty.call(message, "templateId") && message.templateId !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.templateId);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
            if (message.icon != null && $Object.hasOwnProperty.call(message, "icon") && message.icon !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.icon);
            if (message.description != null && $Object.hasOwnProperty.call(message, "description") && message.description !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.description);
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.type);
            if (message.quantity != null && $Object.hasOwnProperty.call(message, "quantity") && message.quantity !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.quantity);
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight") && !$Object.is(message.weight, 0))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.weight);
            if (message.stats != null && $Object.hasOwnProperty.call(message, "stats"))
                for (let keys = $Object.keys(message.stats), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 9, wireType 2 =*/74).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 1 =*/17).double(message.stats[keys[i]]).ldelim();
            if (message.consumable != null && $Object.hasOwnProperty.call(message, "consumable"))
                for (let keys = $Object.keys(message.consumable), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 10, wireType 2 =*/82).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.consumable[keys[i]]).ldelim();
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType") && message.damageType !== "")
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.damageType);
            if (message.primaryStat != null && $Object.hasOwnProperty.call(message, "primaryStat") && message.primaryStat !== "")
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.primaryStat);
            if (message.recipeId != null && $Object.hasOwnProperty.call(message, "recipeId") && message.recipeId !== "")
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.recipeId);
            if (message.buy != null && $Object.hasOwnProperty.call(message, "buy"))
                for (let keys = $Object.keys(message.buy), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 14, wireType 2 =*/114).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.buy[keys[i]]).ldelim();
            if (message.sellable != null && $Object.hasOwnProperty.call(message, "sellable"))
                for (let keys = $Object.keys(message.sellable), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 15, wireType 2 =*/122).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.sellable[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Item message, length delimited. Does not implicitly {@link game.Item.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Item
         * @static
         * @param {game.Item.$Properties} message Item message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Item.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an Item message from the specified reader or buffer.
         * @function decode
         * @memberof game.Item
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Item & game.Item.$Shape} Item
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Item.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Item(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.templateId = value;
                        else
                            delete message.templateId;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.icon = value;
                        else
                            delete message.icon;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.description = value;
                        else
                            delete message.description;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.quantity = value;
                        else
                            delete message.quantity;
                        continue;
                    }
                case 8: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.weight = value;
                        else
                            delete message.weight;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (message.stats === $util.emptyObject)
                            message.stats = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 1)
                                    break;
                                value = reader.double();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.stats, key);
                        message.stats[key] = value;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (message.consumable === $util.emptyObject)
                            message.consumable = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = $util.Long ? $util.Long.fromNumber(0, false) : 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 0)
                                    break;
                                value = reader.int64();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.consumable, key);
                        message.consumable[key] = value;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.damageType = value;
                        else
                            delete message.damageType;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.primaryStat = value;
                        else
                            delete message.primaryStat;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.recipeId = value;
                        else
                            delete message.recipeId;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if (message.buy === $util.emptyObject)
                            message.buy = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = $util.Long ? $util.Long.fromNumber(0, false) : 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 0)
                                    break;
                                value = reader.int64();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.buy, key);
                        message.buy[key] = value;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if (message.sellable === $util.emptyObject)
                            message.sellable = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = $util.Long ? $util.Long.fromNumber(0, false) : 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 0)
                                    break;
                                value = reader.int64();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.sellable, key);
                        message.sellable[key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an Item message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Item
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Item & game.Item.$Shape} Item
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Item.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Item message.
         * @function verify
         * @memberof game.Item
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Item.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isInteger(message.id) && !(message.id && $util.isInteger(message.id.low) && $util.isInteger(message.id.high)))
                    return "id: integer|Long expected";
            if (message.templateId != null && $Object.hasOwnProperty.call(message, "templateId"))
                if (!$util.isString(message.templateId))
                    return "templateId: string expected";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.icon != null && $Object.hasOwnProperty.call(message, "icon"))
                if (!$util.isString(message.icon))
                    return "icon: string expected";
            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                if (!$util.isString(message.description))
                    return "description: string expected";
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            if (message.quantity != null && $Object.hasOwnProperty.call(message, "quantity"))
                if (!$util.isInteger(message.quantity))
                    return "quantity: integer expected";
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                if (typeof message.weight !== "number")
                    return "weight: number expected";
            if (message.stats != null && $Object.hasOwnProperty.call(message, "stats")) {
                if (!$util.isObject(message.stats))
                    return "stats: object expected";
                let key = $Object.keys(message.stats);
                for (let i = 0; i < key.length; ++i)
                    if (typeof message.stats[key[i]] !== "number")
                        return "stats: number{k:string} expected";
            }
            if (message.consumable != null && $Object.hasOwnProperty.call(message, "consumable")) {
                if (!$util.isObject(message.consumable))
                    return "consumable: object expected";
                let key = $Object.keys(message.consumable);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.consumable[key[i]]) && !(message.consumable[key[i]] && $util.isInteger(message.consumable[key[i]].low) && $util.isInteger(message.consumable[key[i]].high)))
                        return "consumable: integer|Long{k:string} expected";
            }
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType"))
                if (!$util.isString(message.damageType))
                    return "damageType: string expected";
            if (message.primaryStat != null && $Object.hasOwnProperty.call(message, "primaryStat"))
                if (!$util.isString(message.primaryStat))
                    return "primaryStat: string expected";
            if (message.recipeId != null && $Object.hasOwnProperty.call(message, "recipeId"))
                if (!$util.isString(message.recipeId))
                    return "recipeId: string expected";
            if (message.buy != null && $Object.hasOwnProperty.call(message, "buy")) {
                if (!$util.isObject(message.buy))
                    return "buy: object expected";
                let key = $Object.keys(message.buy);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.buy[key[i]]) && !(message.buy[key[i]] && $util.isInteger(message.buy[key[i]].low) && $util.isInteger(message.buy[key[i]].high)))
                        return "buy: integer|Long{k:string} expected";
            }
            if (message.sellable != null && $Object.hasOwnProperty.call(message, "sellable")) {
                if (!$util.isObject(message.sellable))
                    return "sellable: object expected";
                let key = $Object.keys(message.sellable);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.sellable[key[i]]) && !(message.sellable[key[i]] && $util.isInteger(message.sellable[key[i]].low) && $util.isInteger(message.sellable[key[i]].high)))
                        return "sellable: integer|Long{k:string} expected";
            }
            return null;
        };

        /**
         * Creates an Item message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Item
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Item} Item
         */
        Item.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Item)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Item: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Item();
            if (object.id != null)
                if (typeof object.id === "object" ? object.id.low || object.id.high : $Number(object.id) !== 0)
                    if ($util.Long)
                        message.id = $util.Long.fromValue(object.id, false);
                    else if (typeof object.id === "string")
                        message.id = $parseInt(object.id, 10);
                    else if (typeof object.id === "number")
                        message.id = object.id;
                    else if (typeof object.id === "object")
                        message.id = new $util.LongBits(object.id.low >>> 0, object.id.high >>> 0).toNumber();
            if (object.templateId != null)
                if (typeof object.templateId !== "string" || object.templateId.length)
                    message.templateId = $String(object.templateId);
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.icon != null)
                if (typeof object.icon !== "string" || object.icon.length)
                    message.icon = $String(object.icon);
            if (object.description != null)
                if (typeof object.description !== "string" || object.description.length)
                    message.description = $String(object.description);
            if (object.type != null)
                if (typeof object.type !== "string" || object.type.length)
                    message.type = $String(object.type);
            if (object.quantity != null)
                if ($Number(object.quantity) !== 0)
                    message.quantity = object.quantity | 0;
            if (object.weight != null)
                if (!$Object.is($Number(object.weight), 0))
                    message.weight = $Number(object.weight);
            if (object.stats) {
                if (!$util.isObject(object.stats))
                    throw $TypeError(".game.Item.stats: object expected");
                message.stats = {};
                for (let keys = $Object.keys(object.stats), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.stats, keys[i]);
                    message.stats[keys[i]] = $Number(object.stats[keys[i]]);
                }
            }
            if (object.consumable) {
                if (!$util.isObject(object.consumable))
                    throw $TypeError(".game.Item.consumable: object expected");
                message.consumable = {};
                for (let keys = $Object.keys(object.consumable), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.consumable, keys[i]);
                    if ($util.Long)
                        message.consumable[keys[i]] = $util.Long.fromValue(object.consumable[keys[i]], false);
                    else if (typeof object.consumable[keys[i]] === "string")
                        message.consumable[keys[i]] = $parseInt(object.consumable[keys[i]], 10);
                    else if (typeof object.consumable[keys[i]] === "number")
                        message.consumable[keys[i]] = object.consumable[keys[i]];
                    else if (typeof object.consumable[keys[i]] === "object")
                        message.consumable[keys[i]] = new $util.LongBits(object.consumable[keys[i]].low >>> 0, object.consumable[keys[i]].high >>> 0).toNumber();
                }
            }
            if (object.damageType != null)
                if (typeof object.damageType !== "string" || object.damageType.length)
                    message.damageType = $String(object.damageType);
            if (object.primaryStat != null)
                if (typeof object.primaryStat !== "string" || object.primaryStat.length)
                    message.primaryStat = $String(object.primaryStat);
            if (object.recipeId != null)
                if (typeof object.recipeId !== "string" || object.recipeId.length)
                    message.recipeId = $String(object.recipeId);
            if (object.buy) {
                if (!$util.isObject(object.buy))
                    throw $TypeError(".game.Item.buy: object expected");
                message.buy = {};
                for (let keys = $Object.keys(object.buy), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.buy, keys[i]);
                    if ($util.Long)
                        message.buy[keys[i]] = $util.Long.fromValue(object.buy[keys[i]], false);
                    else if (typeof object.buy[keys[i]] === "string")
                        message.buy[keys[i]] = $parseInt(object.buy[keys[i]], 10);
                    else if (typeof object.buy[keys[i]] === "number")
                        message.buy[keys[i]] = object.buy[keys[i]];
                    else if (typeof object.buy[keys[i]] === "object")
                        message.buy[keys[i]] = new $util.LongBits(object.buy[keys[i]].low >>> 0, object.buy[keys[i]].high >>> 0).toNumber();
                }
            }
            if (object.sellable) {
                if (!$util.isObject(object.sellable))
                    throw $TypeError(".game.Item.sellable: object expected");
                message.sellable = {};
                for (let keys = $Object.keys(object.sellable), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.sellable, keys[i]);
                    if ($util.Long)
                        message.sellable[keys[i]] = $util.Long.fromValue(object.sellable[keys[i]], false);
                    else if (typeof object.sellable[keys[i]] === "string")
                        message.sellable[keys[i]] = $parseInt(object.sellable[keys[i]], 10);
                    else if (typeof object.sellable[keys[i]] === "number")
                        message.sellable[keys[i]] = object.sellable[keys[i]];
                    else if (typeof object.sellable[keys[i]] === "object")
                        message.sellable[keys[i]] = new $util.LongBits(object.sellable[keys[i]].low >>> 0, object.sellable[keys[i]].high >>> 0).toNumber();
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an Item message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Item
         * @static
         * @param {game.Item} message Item
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Item.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults) {
                object.stats = {};
                object.consumable = {};
                object.buy = {};
                object.sellable = {};
            }
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.templateId = "";
                object.name = "";
                object.icon = "";
                object.description = "";
                object.type = "";
                object.quantity = 0;
                object.weight = 0;
                object.damageType = "";
                object.primaryStat = "";
                object.recipeId = "";
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.id = typeof message.id === "number" ? $BigInt(message.id) : $util.Long.fromBits(message.id.low >>> 0, message.id.high >>> 0, false).toBigInt();
                else if (typeof message.id === "number")
                    object.id = options.longs === $String ? $String(message.id) : message.id;
                else
                    object.id = options.longs === $String ? $util.Long.prototype.toString.call(message.id) : options.longs === $Number ? new $util.LongBits(message.id.low >>> 0, message.id.high >>> 0).toNumber() : message.id;
            if (message.templateId != null && $Object.hasOwnProperty.call(message, "templateId"))
                object.templateId = message.templateId;
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.icon != null && $Object.hasOwnProperty.call(message, "icon"))
                object.icon = message.icon;
            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                object.description = message.description;
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                object.type = message.type;
            if (message.quantity != null && $Object.hasOwnProperty.call(message, "quantity"))
                object.quantity = message.quantity;
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                object.weight = options.json && !$isFinite(message.weight) ? $String(message.weight) : message.weight;
            let keys2;
            if (message.stats && (keys2 = $Object.keys(message.stats)).length) {
                object.stats = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.stats, keys2[j]);
                    object.stats[keys2[j]] = options.json && !$isFinite(message.stats[keys2[j]]) ? $String(message.stats[keys2[j]]) : message.stats[keys2[j]];
                }
            }
            if (message.consumable && (keys2 = $Object.keys(message.consumable)).length) {
                object.consumable = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.consumable, keys2[j]);
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.consumable[keys2[j]] = typeof message.consumable[keys2[j]] === "number" ? $BigInt(message.consumable[keys2[j]]) : $util.Long.fromBits(message.consumable[keys2[j]].low >>> 0, message.consumable[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.consumable[keys2[j]] === "number")
                        object.consumable[keys2[j]] = options.longs === $String ? $String(message.consumable[keys2[j]]) : message.consumable[keys2[j]];
                    else
                        object.consumable[keys2[j]] = options.longs === $String ? $util.Long.prototype.toString.call(message.consumable[keys2[j]]) : options.longs === $Number ? new $util.LongBits(message.consumable[keys2[j]].low >>> 0, message.consumable[keys2[j]].high >>> 0).toNumber() : message.consumable[keys2[j]];
                }
            }
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType"))
                object.damageType = message.damageType;
            if (message.primaryStat != null && $Object.hasOwnProperty.call(message, "primaryStat"))
                object.primaryStat = message.primaryStat;
            if (message.recipeId != null && $Object.hasOwnProperty.call(message, "recipeId"))
                object.recipeId = message.recipeId;
            if (message.buy && (keys2 = $Object.keys(message.buy)).length) {
                object.buy = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.buy, keys2[j]);
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.buy[keys2[j]] = typeof message.buy[keys2[j]] === "number" ? $BigInt(message.buy[keys2[j]]) : $util.Long.fromBits(message.buy[keys2[j]].low >>> 0, message.buy[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.buy[keys2[j]] === "number")
                        object.buy[keys2[j]] = options.longs === $String ? $String(message.buy[keys2[j]]) : message.buy[keys2[j]];
                    else
                        object.buy[keys2[j]] = options.longs === $String ? $util.Long.prototype.toString.call(message.buy[keys2[j]]) : options.longs === $Number ? new $util.LongBits(message.buy[keys2[j]].low >>> 0, message.buy[keys2[j]].high >>> 0).toNumber() : message.buy[keys2[j]];
                }
            }
            if (message.sellable && (keys2 = $Object.keys(message.sellable)).length) {
                object.sellable = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.sellable, keys2[j]);
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.sellable[keys2[j]] = typeof message.sellable[keys2[j]] === "number" ? $BigInt(message.sellable[keys2[j]]) : $util.Long.fromBits(message.sellable[keys2[j]].low >>> 0, message.sellable[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.sellable[keys2[j]] === "number")
                        object.sellable[keys2[j]] = options.longs === $String ? $String(message.sellable[keys2[j]]) : message.sellable[keys2[j]];
                    else
                        object.sellable[keys2[j]] = options.longs === $String ? $util.Long.prototype.toString.call(message.sellable[keys2[j]]) : options.longs === $Number ? new $util.LongBits(message.sellable[keys2[j]].low >>> 0, message.sellable[keys2[j]].high >>> 0).toNumber() : message.sellable[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this Item to JSON.
         * @function toJSON
         * @memberof game.Item
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Item.prototype.toJSON = function() {
            return Item.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Item
         * @function getTypeUrl
         * @memberof game.Item
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Item.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Item";
        };

        return Item;
    })();

    game.Inventory = (function() {

        /**
         * Properties of an Inventory.
         * @typedef {Object} game.Inventory.$Properties
         * @property {string|null} [id] Inventory id
         * @property {string|null} [type] Inventory type
         * @property {string|null} [placeId] Inventory placeId
         * @property {string|null} [npcId] Inventory npcId
         * @property {number|null} [maxSlots] Inventory maxSlots
         * @property {number|null} [maxWeight] Inventory maxWeight
         * @property {Array.<game.Item.$Properties>|null} [items] Inventory items
         * @property {Object.<string,game.Item.$Properties>|null} [equipment] Inventory equipment
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an Inventory.
         * @memberof game
         * @interface IInventory
         * @augments game.Inventory.$Properties
         * @deprecated Use game.Inventory.$Properties instead.
         */

        /**
         * Shape of an Inventory.
         * @typedef {game.Inventory.$Properties} game.Inventory.$Shape
         */

        /**
         * Constructs a new Inventory.
         * @memberof game
         * @classdesc Represents an Inventory.
         * @constructor
         * @param {game.Inventory.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Inventory = function (properties) {
            this.items = [];
            this.equipment = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Inventory id.
         * @member {string} id
         * @memberof game.Inventory
         * @instance
         */
        Inventory.prototype.id = "";

        /**
         * Inventory type.
         * @member {string} type
         * @memberof game.Inventory
         * @instance
         */
        Inventory.prototype.type = "";

        /**
         * Inventory placeId.
         * @member {string} placeId
         * @memberof game.Inventory
         * @instance
         */
        Inventory.prototype.placeId = "";

        /**
         * Inventory npcId.
         * @member {string} npcId
         * @memberof game.Inventory
         * @instance
         */
        Inventory.prototype.npcId = "";

        /**
         * Inventory maxSlots.
         * @member {number} maxSlots
         * @memberof game.Inventory
         * @instance
         */
        Inventory.prototype.maxSlots = 0;

        /**
         * Inventory maxWeight.
         * @member {number} maxWeight
         * @memberof game.Inventory
         * @instance
         */
        Inventory.prototype.maxWeight = 0;

        /**
         * Inventory items.
         * @member {Array.<game.Item.$Properties>} items
         * @memberof game.Inventory
         * @instance
         */
        Inventory.prototype.items = $util.emptyArray;

        /**
         * Inventory equipment.
         * @member {Object.<string,game.Item.$Properties>} equipment
         * @memberof game.Inventory
         * @instance
         */
        Inventory.prototype.equipment = $util.emptyObject;

        /**
         * Creates a new Inventory instance using the specified properties.
         * @function create
         * @memberof game.Inventory
         * @static
         * @param {game.Inventory.$Properties=} [properties] Properties to set
         * @returns {game.Inventory} Inventory instance
         * @type {{
         *   (properties: game.Inventory.$Shape): game.Inventory & game.Inventory.$Shape;
         *   (properties?: game.Inventory.$Properties): game.Inventory;
         * }}
         */
        Inventory.create = function(properties) {
            return new Inventory(properties);
        };

        /**
         * Encodes the specified Inventory message. Does not implicitly {@link game.Inventory.verify|verify} messages.
         * @function encode
         * @memberof game.Inventory
         * @static
         * @param {game.Inventory.$Properties} message Inventory message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Inventory.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.type);
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId") && message.placeId !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.placeId);
            if (message.npcId != null && $Object.hasOwnProperty.call(message, "npcId") && message.npcId !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.npcId);
            if (message.maxSlots != null && $Object.hasOwnProperty.call(message, "maxSlots") && message.maxSlots !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.maxSlots);
            if (message.maxWeight != null && $Object.hasOwnProperty.call(message, "maxWeight") && !$Object.is(message.maxWeight, 0))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.maxWeight);
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.game.Item.encode(message.items[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.equipment != null && $Object.hasOwnProperty.call(message, "equipment"))
                for (let keys = $Object.keys(message.equipment), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 8, wireType 2 =*/66).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.game.Item.encode(message.equipment[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Inventory message, length delimited. Does not implicitly {@link game.Inventory.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Inventory
         * @static
         * @param {game.Inventory.$Properties} message Inventory message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Inventory.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an Inventory message from the specified reader or buffer.
         * @function decode
         * @memberof game.Inventory
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Inventory & game.Inventory.$Shape} Inventory
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Inventory.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Inventory(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.placeId = value;
                        else
                            delete message.placeId;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.npcId = value;
                        else
                            delete message.npcId;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.maxSlots = value;
                        else
                            delete message.maxSlots;
                        continue;
                    }
                case 6: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.maxWeight = value;
                        else
                            delete message.maxWeight;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.game.Item.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (message.equipment === $util.emptyObject)
                            message.equipment = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.game.Item.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.equipment, key);
                        message.equipment[key] = value || new $root.game.Item();
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an Inventory message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Inventory
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Inventory & game.Inventory.$Shape} Inventory
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Inventory.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Inventory message.
         * @function verify
         * @memberof game.Inventory
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Inventory.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                if (!$util.isString(message.placeId))
                    return "placeId: string expected";
            if (message.npcId != null && $Object.hasOwnProperty.call(message, "npcId"))
                if (!$util.isString(message.npcId))
                    return "npcId: string expected";
            if (message.maxSlots != null && $Object.hasOwnProperty.call(message, "maxSlots"))
                if (!$util.isInteger(message.maxSlots))
                    return "maxSlots: integer expected";
            if (message.maxWeight != null && $Object.hasOwnProperty.call(message, "maxWeight"))
                if (typeof message.maxWeight !== "number")
                    return "maxWeight: number expected";
            if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                if (!$Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.game.Item.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            if (message.equipment != null && $Object.hasOwnProperty.call(message, "equipment")) {
                if (!$util.isObject(message.equipment))
                    return "equipment: object expected";
                let key = $Object.keys(message.equipment);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.game.Item.verify(message.equipment[key[i]], _depth + 1);
                    if (error)
                        return "equipment." + error;
                }
            }
            return null;
        };

        /**
         * Creates an Inventory message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Inventory
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Inventory} Inventory
         */
        Inventory.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Inventory)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Inventory: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Inventory();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.type != null)
                if (typeof object.type !== "string" || object.type.length)
                    message.type = $String(object.type);
            if (object.placeId != null)
                if (typeof object.placeId !== "string" || object.placeId.length)
                    message.placeId = $String(object.placeId);
            if (object.npcId != null)
                if (typeof object.npcId !== "string" || object.npcId.length)
                    message.npcId = $String(object.npcId);
            if (object.maxSlots != null)
                if ($Number(object.maxSlots) !== 0)
                    message.maxSlots = object.maxSlots | 0;
            if (object.maxWeight != null)
                if (!$Object.is($Number(object.maxWeight), 0))
                    message.maxWeight = $Number(object.maxWeight);
            if (object.items) {
                if (!$Array.isArray(object.items))
                    throw $TypeError(".game.Inventory.items: array expected");
                message.items = $Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (!$util.isObject(object.items[i]))
                        throw $TypeError(".game.Inventory.items: object expected");
                    message.items[i] = $root.game.Item.fromObject(object.items[i], _depth + 1);
                }
            }
            if (object.equipment) {
                if (!$util.isObject(object.equipment))
                    throw $TypeError(".game.Inventory.equipment: object expected");
                message.equipment = {};
                for (let keys = $Object.keys(object.equipment), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.equipment, keys[i]);
                    if (!$util.isObject(object.equipment[keys[i]]))
                        throw $TypeError(".game.Inventory.equipment: object expected");
                    message.equipment[keys[i]] = $root.game.Item.fromObject(object.equipment[keys[i]], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an Inventory message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Inventory
         * @static
         * @param {game.Inventory} message Inventory
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Inventory.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.objects || options.defaults)
                object.equipment = {};
            if (options.defaults) {
                object.id = "";
                object.type = "";
                object.placeId = "";
                object.npcId = "";
                object.maxSlots = 0;
                object.maxWeight = 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                object.type = message.type;
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                object.placeId = message.placeId;
            if (message.npcId != null && $Object.hasOwnProperty.call(message, "npcId"))
                object.npcId = message.npcId;
            if (message.maxSlots != null && $Object.hasOwnProperty.call(message, "maxSlots"))
                object.maxSlots = message.maxSlots;
            if (message.maxWeight != null && $Object.hasOwnProperty.call(message, "maxWeight"))
                object.maxWeight = options.json && !$isFinite(message.maxWeight) ? $String(message.maxWeight) : message.maxWeight;
            if (message.items && message.items.length) {
                object.items = $Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.game.Item.toObject(message.items[j], options, _depth + 1);
            }
            let keys2;
            if (message.equipment && (keys2 = $Object.keys(message.equipment)).length) {
                object.equipment = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.equipment, keys2[j]);
                    object.equipment[keys2[j]] = $root.game.Item.toObject(message.equipment[keys2[j]], options, _depth + 1);
                }
            }
            return object;
        };

        /**
         * Converts this Inventory to JSON.
         * @function toJSON
         * @memberof game.Inventory
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Inventory.prototype.toJSON = function() {
            return Inventory.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Inventory
         * @function getTypeUrl
         * @memberof game.Inventory
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Inventory.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Inventory";
        };

        return Inventory;
    })();

    game.Building = (function() {

        /**
         * Properties of a Building.
         * @typedef {Object} game.Building.$Properties
         * @property {string|null} [id] Building id
         * @property {number|null} [level] Building level
         * @property {string|null} [placeId] Building placeId
         * @property {number|null} [socketIndex] Building socketIndex
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Building.
         * @memberof game
         * @interface IBuilding
         * @augments game.Building.$Properties
         * @deprecated Use game.Building.$Properties instead.
         */

        /**
         * Shape of a Building.
         * @typedef {game.Building.$Properties} game.Building.$Shape
         */

        /**
         * Constructs a new Building.
         * @memberof game
         * @classdesc Represents a Building.
         * @constructor
         * @param {game.Building.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Building = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Building id.
         * @member {string} id
         * @memberof game.Building
         * @instance
         */
        Building.prototype.id = "";

        /**
         * Building level.
         * @member {number} level
         * @memberof game.Building
         * @instance
         */
        Building.prototype.level = 0;

        /**
         * Building placeId.
         * @member {string} placeId
         * @memberof game.Building
         * @instance
         */
        Building.prototype.placeId = "";

        /**
         * Building socketIndex.
         * @member {number} socketIndex
         * @memberof game.Building
         * @instance
         */
        Building.prototype.socketIndex = 0;

        /**
         * Creates a new Building instance using the specified properties.
         * @function create
         * @memberof game.Building
         * @static
         * @param {game.Building.$Properties=} [properties] Properties to set
         * @returns {game.Building} Building instance
         * @type {{
         *   (properties: game.Building.$Shape): game.Building & game.Building.$Shape;
         *   (properties?: game.Building.$Properties): game.Building;
         * }}
         */
        Building.create = function(properties) {
            return new Building(properties);
        };

        /**
         * Encodes the specified Building message. Does not implicitly {@link game.Building.verify|verify} messages.
         * @function encode
         * @memberof game.Building
         * @static
         * @param {game.Building.$Properties} message Building message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Building.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.level != null && $Object.hasOwnProperty.call(message, "level") && message.level !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.level);
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId") && message.placeId !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.placeId);
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex") && message.socketIndex !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.socketIndex);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Building message, length delimited. Does not implicitly {@link game.Building.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Building
         * @static
         * @param {game.Building.$Properties} message Building message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Building.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Building message from the specified reader or buffer.
         * @function decode
         * @memberof game.Building
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Building & game.Building.$Shape} Building
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Building.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Building(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.placeId = value;
                        else
                            delete message.placeId;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.socketIndex = value;
                        else
                            delete message.socketIndex;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Building message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Building
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Building & game.Building.$Shape} Building
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Building.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Building message.
         * @function verify
         * @memberof game.Building
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Building.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.level != null && $Object.hasOwnProperty.call(message, "level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                if (!$util.isString(message.placeId))
                    return "placeId: string expected";
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                if (!$util.isInteger(message.socketIndex))
                    return "socketIndex: integer expected";
            return null;
        };

        /**
         * Creates a Building message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Building
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Building} Building
         */
        Building.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Building)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Building: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Building();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.level != null)
                if ($Number(object.level) !== 0)
                    message.level = object.level | 0;
            if (object.placeId != null)
                if (typeof object.placeId !== "string" || object.placeId.length)
                    message.placeId = $String(object.placeId);
            if (object.socketIndex != null)
                if ($Number(object.socketIndex) !== 0)
                    message.socketIndex = object.socketIndex | 0;
            return message;
        };

        /**
         * Creates a plain object from a Building message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Building
         * @static
         * @param {game.Building} message Building
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Building.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.level = 0;
                object.placeId = "";
                object.socketIndex = 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.level != null && $Object.hasOwnProperty.call(message, "level"))
                object.level = message.level;
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                object.placeId = message.placeId;
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                object.socketIndex = message.socketIndex;
            return object;
        };

        /**
         * Converts this Building to JSON.
         * @function toJSON
         * @memberof game.Building
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Building.prototype.toJSON = function() {
            return Building.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Building
         * @function getTypeUrl
         * @memberof game.Building
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Building.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Building";
        };

        return Building;
    })();

    game.Socket = (function() {

        /**
         * Properties of a Socket.
         * @typedef {Object} game.Socket.$Properties
         * @property {string|null} [placeId] Socket placeId
         * @property {number|null} [socketIndex] Socket socketIndex
         * @property {string|null} [status] Socket status
         * @property {string|null} [buildingId] Socket buildingId
         * @property {number|null} [level] Socket level
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Socket.
         * @memberof game
         * @interface ISocket
         * @augments game.Socket.$Properties
         * @deprecated Use game.Socket.$Properties instead.
         */

        /**
         * Shape of a Socket.
         * @typedef {game.Socket.$Properties} game.Socket.$Shape
         */

        /**
         * Constructs a new Socket.
         * @memberof game
         * @classdesc Represents a Socket.
         * @constructor
         * @param {game.Socket.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Socket = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Socket placeId.
         * @member {string} placeId
         * @memberof game.Socket
         * @instance
         */
        Socket.prototype.placeId = "";

        /**
         * Socket socketIndex.
         * @member {number} socketIndex
         * @memberof game.Socket
         * @instance
         */
        Socket.prototype.socketIndex = 0;

        /**
         * Socket status.
         * @member {string} status
         * @memberof game.Socket
         * @instance
         */
        Socket.prototype.status = "";

        /**
         * Socket buildingId.
         * @member {string} buildingId
         * @memberof game.Socket
         * @instance
         */
        Socket.prototype.buildingId = "";

        /**
         * Socket level.
         * @member {number} level
         * @memberof game.Socket
         * @instance
         */
        Socket.prototype.level = 0;

        /**
         * Creates a new Socket instance using the specified properties.
         * @function create
         * @memberof game.Socket
         * @static
         * @param {game.Socket.$Properties=} [properties] Properties to set
         * @returns {game.Socket} Socket instance
         * @type {{
         *   (properties: game.Socket.$Shape): game.Socket & game.Socket.$Shape;
         *   (properties?: game.Socket.$Properties): game.Socket;
         * }}
         */
        Socket.create = function(properties) {
            return new Socket(properties);
        };

        /**
         * Encodes the specified Socket message. Does not implicitly {@link game.Socket.verify|verify} messages.
         * @function encode
         * @memberof game.Socket
         * @static
         * @param {game.Socket.$Properties} message Socket message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Socket.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId") && message.placeId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.placeId);
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex") && message.socketIndex !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.socketIndex);
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.status);
            if (message.buildingId != null && $Object.hasOwnProperty.call(message, "buildingId") && message.buildingId !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.buildingId);
            if (message.level != null && $Object.hasOwnProperty.call(message, "level") && message.level !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.level);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Socket message, length delimited. Does not implicitly {@link game.Socket.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Socket
         * @static
         * @param {game.Socket.$Properties} message Socket message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Socket.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Socket message from the specified reader or buffer.
         * @function decode
         * @memberof game.Socket
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Socket & game.Socket.$Shape} Socket
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Socket.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Socket(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.placeId = value;
                        else
                            delete message.placeId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.socketIndex = value;
                        else
                            delete message.socketIndex;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.buildingId = value;
                        else
                            delete message.buildingId;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Socket message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Socket
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Socket & game.Socket.$Shape} Socket
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Socket.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Socket message.
         * @function verify
         * @memberof game.Socket
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Socket.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                if (!$util.isString(message.placeId))
                    return "placeId: string expected";
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                if (!$util.isInteger(message.socketIndex))
                    return "socketIndex: integer expected";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            if (message.buildingId != null && $Object.hasOwnProperty.call(message, "buildingId"))
                if (!$util.isString(message.buildingId))
                    return "buildingId: string expected";
            if (message.level != null && $Object.hasOwnProperty.call(message, "level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            return null;
        };

        /**
         * Creates a Socket message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Socket
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Socket} Socket
         */
        Socket.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Socket)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Socket: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Socket();
            if (object.placeId != null)
                if (typeof object.placeId !== "string" || object.placeId.length)
                    message.placeId = $String(object.placeId);
            if (object.socketIndex != null)
                if ($Number(object.socketIndex) !== 0)
                    message.socketIndex = object.socketIndex | 0;
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            if (object.buildingId != null)
                if (typeof object.buildingId !== "string" || object.buildingId.length)
                    message.buildingId = $String(object.buildingId);
            if (object.level != null)
                if ($Number(object.level) !== 0)
                    message.level = object.level | 0;
            return message;
        };

        /**
         * Creates a plain object from a Socket message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Socket
         * @static
         * @param {game.Socket} message Socket
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Socket.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.placeId = "";
                object.socketIndex = 0;
                object.status = "";
                object.buildingId = "";
                object.level = 0;
            }
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                object.placeId = message.placeId;
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                object.socketIndex = message.socketIndex;
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            if (message.buildingId != null && $Object.hasOwnProperty.call(message, "buildingId"))
                object.buildingId = message.buildingId;
            if (message.level != null && $Object.hasOwnProperty.call(message, "level"))
                object.level = message.level;
            return object;
        };

        /**
         * Converts this Socket to JSON.
         * @function toJSON
         * @memberof game.Socket
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Socket.prototype.toJSON = function() {
            return Socket.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Socket
         * @function getTypeUrl
         * @memberof game.Socket
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Socket.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Socket";
        };

        return Socket;
    })();

    game.Enemy = (function() {

        /**
         * Properties of an Enemy.
         * @typedef {Object} game.Enemy.$Properties
         * @property {string|null} [id] Enemy id
         * @property {string|null} [placeId] Enemy placeId
         * @property {string|null} [type] Enemy type
         * @property {string|null} [name] Enemy name
         * @property {string|null} [avatar] Enemy avatar
         * @property {number|Long|null} [hp] Enemy hp
         * @property {number|Long|null} [maxHp] Enemy maxHp
         * @property {number|null} [strength] Enemy strength
         * @property {number|null} [defense] Enemy defense
         * @property {number|null} [agility] Enemy agility
         * @property {number|null} [wisdom] Enemy wisdom
         * @property {number|null} [intelligence] Enemy intelligence
         * @property {string|null} [damageType] Enemy damageType
         * @property {number|null} [attack] Enemy attack
         * @property {number|null} [speed] Enemy speed
         * @property {string|null} [attackPattern] Enemy attackPattern
         * @property {Array.<number>|null} [attackDelayRange] Enemy attackDelayRange
         * @property {number|Long|null} [exp] Enemy exp
         * @property {number|Long|null} [gold] Enemy gold
         * @property {boolean|null} [isDead] Enemy isDead
         * @property {number|Long|null} [nextAttackAt] Enemy nextAttackAt
         * @property {number|Long|null} [nextAttackDelay] Enemy nextAttackDelay
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an Enemy.
         * @memberof game
         * @interface IEnemy
         * @augments game.Enemy.$Properties
         * @deprecated Use game.Enemy.$Properties instead.
         */

        /**
         * Shape of an Enemy.
         * @typedef {game.Enemy.$Properties} game.Enemy.$Shape
         */

        /**
         * Constructs a new Enemy.
         * @memberof game
         * @classdesc Represents an Enemy.
         * @constructor
         * @param {game.Enemy.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Enemy = function (properties) {
            this.attackDelayRange = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Enemy id.
         * @member {string} id
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.id = "";

        /**
         * Enemy placeId.
         * @member {string} placeId
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.placeId = "";

        /**
         * Enemy type.
         * @member {string} type
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.type = "";

        /**
         * Enemy name.
         * @member {string} name
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.name = "";

        /**
         * Enemy avatar.
         * @member {string} avatar
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.avatar = "";

        /**
         * Enemy hp.
         * @member {number|Long} hp
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.hp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Enemy maxHp.
         * @member {number|Long} maxHp
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.maxHp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Enemy strength.
         * @member {number} strength
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.strength = 0;

        /**
         * Enemy defense.
         * @member {number} defense
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.defense = 0;

        /**
         * Enemy agility.
         * @member {number} agility
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.agility = 0;

        /**
         * Enemy wisdom.
         * @member {number} wisdom
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.wisdom = 0;

        /**
         * Enemy intelligence.
         * @member {number} intelligence
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.intelligence = 0;

        /**
         * Enemy damageType.
         * @member {string} damageType
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.damageType = "";

        /**
         * Enemy attack.
         * @member {number} attack
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.attack = 0;

        /**
         * Enemy speed.
         * @member {number} speed
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.speed = 0;

        /**
         * Enemy attackPattern.
         * @member {string} attackPattern
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.attackPattern = "";

        /**
         * Enemy attackDelayRange.
         * @member {Array.<number>} attackDelayRange
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.attackDelayRange = $util.emptyArray;

        /**
         * Enemy exp.
         * @member {number|Long} exp
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.exp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Enemy gold.
         * @member {number|Long} gold
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.gold = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Enemy isDead.
         * @member {boolean} isDead
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.isDead = false;

        /**
         * Enemy nextAttackAt.
         * @member {number|Long} nextAttackAt
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.nextAttackAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Enemy nextAttackDelay.
         * @member {number|Long} nextAttackDelay
         * @memberof game.Enemy
         * @instance
         */
        Enemy.prototype.nextAttackDelay = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new Enemy instance using the specified properties.
         * @function create
         * @memberof game.Enemy
         * @static
         * @param {game.Enemy.$Properties=} [properties] Properties to set
         * @returns {game.Enemy} Enemy instance
         * @type {{
         *   (properties: game.Enemy.$Shape): game.Enemy & game.Enemy.$Shape;
         *   (properties?: game.Enemy.$Properties): game.Enemy;
         * }}
         */
        Enemy.create = function(properties) {
            return new Enemy(properties);
        };

        /**
         * Encodes the specified Enemy message. Does not implicitly {@link game.Enemy.verify|verify} messages.
         * @function encode
         * @memberof game.Enemy
         * @static
         * @param {game.Enemy.$Properties} message Enemy message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Enemy.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId") && message.placeId !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.placeId);
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.type);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.name);
            if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar") && message.avatar !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.avatar);
            if (message.hp != null && $Object.hasOwnProperty.call(message, "hp") && (typeof message.hp === "object" ? message.hp.low || message.hp.high : message.hp !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.hp);
            if (message.maxHp != null && $Object.hasOwnProperty.call(message, "maxHp") && (typeof message.maxHp === "object" ? message.maxHp.low || message.maxHp.high : message.maxHp !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.maxHp);
            if (message.strength != null && $Object.hasOwnProperty.call(message, "strength") && message.strength !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.strength);
            if (message.defense != null && $Object.hasOwnProperty.call(message, "defense") && message.defense !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.defense);
            if (message.agility != null && $Object.hasOwnProperty.call(message, "agility") && message.agility !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.agility);
            if (message.wisdom != null && $Object.hasOwnProperty.call(message, "wisdom") && message.wisdom !== 0)
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.wisdom);
            if (message.intelligence != null && $Object.hasOwnProperty.call(message, "intelligence") && message.intelligence !== 0)
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.intelligence);
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType") && message.damageType !== "")
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.damageType);
            if (message.attack != null && $Object.hasOwnProperty.call(message, "attack") && !$Object.is(message.attack, 0))
                writer.uint32(/* id 14, wireType 1 =*/113).double(message.attack);
            if (message.speed != null && $Object.hasOwnProperty.call(message, "speed") && !$Object.is(message.speed, 0))
                writer.uint32(/* id 15, wireType 1 =*/121).double(message.speed);
            if (message.attackPattern != null && $Object.hasOwnProperty.call(message, "attackPattern") && message.attackPattern !== "")
                writer.uint32(/* id 16, wireType 2 =*/130).string(message.attackPattern);
            if (message.attackDelayRange != null && message.attackDelayRange.length)
                writer.uint32(/* id 17, wireType 2 =*/138).int32s(message.attackDelayRange);
            if (message.exp != null && $Object.hasOwnProperty.call(message, "exp") && (typeof message.exp === "object" ? message.exp.low || message.exp.high : message.exp !== 0))
                writer.uint32(/* id 18, wireType 0 =*/144).int64(message.exp);
            if (message.gold != null && $Object.hasOwnProperty.call(message, "gold") && (typeof message.gold === "object" ? message.gold.low || message.gold.high : message.gold !== 0))
                writer.uint32(/* id 19, wireType 0 =*/152).int64(message.gold);
            if (message.isDead != null && $Object.hasOwnProperty.call(message, "isDead") && message.isDead !== false)
                writer.uint32(/* id 20, wireType 0 =*/160).bool(message.isDead);
            if (message.nextAttackAt != null && $Object.hasOwnProperty.call(message, "nextAttackAt") && (typeof message.nextAttackAt === "object" ? message.nextAttackAt.low || message.nextAttackAt.high : message.nextAttackAt !== 0))
                writer.uint32(/* id 21, wireType 0 =*/168).int64(message.nextAttackAt);
            if (message.nextAttackDelay != null && $Object.hasOwnProperty.call(message, "nextAttackDelay") && (typeof message.nextAttackDelay === "object" ? message.nextAttackDelay.low || message.nextAttackDelay.high : message.nextAttackDelay !== 0))
                writer.uint32(/* id 22, wireType 0 =*/176).int64(message.nextAttackDelay);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Enemy message, length delimited. Does not implicitly {@link game.Enemy.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Enemy
         * @static
         * @param {game.Enemy.$Properties} message Enemy message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Enemy.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an Enemy message from the specified reader or buffer.
         * @function decode
         * @memberof game.Enemy
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Enemy & game.Enemy.$Shape} Enemy
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Enemy.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Enemy(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.placeId = value;
                        else
                            delete message.placeId;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.avatar = value;
                        else
                            delete message.avatar;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.hp = value;
                        else
                            delete message.hp;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.maxHp = value;
                        else
                            delete message.maxHp;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.strength = value;
                        else
                            delete message.strength;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.defense = value;
                        else
                            delete message.defense;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.agility = value;
                        else
                            delete message.agility;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.wisdom = value;
                        else
                            delete message.wisdom;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.intelligence = value;
                        else
                            delete message.intelligence;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.damageType = value;
                        else
                            delete message.damageType;
                        continue;
                    }
                case 14: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.attack = value;
                        else
                            delete message.attack;
                        continue;
                    }
                case 15: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.speed = value;
                        else
                            delete message.speed;
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.attackPattern = value;
                        else
                            delete message.attackPattern;
                        continue;
                    }
                case 17: {
                        if (wireType === 2) {
                            if (!(message.attackDelayRange && message.attackDelayRange.length))
                                message.attackDelayRange = [];
                            reader.int32s(message.attackDelayRange);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.attackDelayRange && message.attackDelayRange.length))
                            message.attackDelayRange = [];
                        message.attackDelayRange.push(reader.int32());
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.exp = value;
                        else
                            delete message.exp;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.gold = value;
                        else
                            delete message.gold;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.isDead = value;
                        else
                            delete message.isDead;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.nextAttackAt = value;
                        else
                            delete message.nextAttackAt;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.nextAttackDelay = value;
                        else
                            delete message.nextAttackDelay;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an Enemy message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Enemy
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Enemy & game.Enemy.$Shape} Enemy
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Enemy.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Enemy message.
         * @function verify
         * @memberof game.Enemy
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Enemy.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                if (!$util.isString(message.placeId))
                    return "placeId: string expected";
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar"))
                if (!$util.isString(message.avatar))
                    return "avatar: string expected";
            if (message.hp != null && $Object.hasOwnProperty.call(message, "hp"))
                if (!$util.isInteger(message.hp) && !(message.hp && $util.isInteger(message.hp.low) && $util.isInteger(message.hp.high)))
                    return "hp: integer|Long expected";
            if (message.maxHp != null && $Object.hasOwnProperty.call(message, "maxHp"))
                if (!$util.isInteger(message.maxHp) && !(message.maxHp && $util.isInteger(message.maxHp.low) && $util.isInteger(message.maxHp.high)))
                    return "maxHp: integer|Long expected";
            if (message.strength != null && $Object.hasOwnProperty.call(message, "strength"))
                if (!$util.isInteger(message.strength))
                    return "strength: integer expected";
            if (message.defense != null && $Object.hasOwnProperty.call(message, "defense"))
                if (!$util.isInteger(message.defense))
                    return "defense: integer expected";
            if (message.agility != null && $Object.hasOwnProperty.call(message, "agility"))
                if (!$util.isInteger(message.agility))
                    return "agility: integer expected";
            if (message.wisdom != null && $Object.hasOwnProperty.call(message, "wisdom"))
                if (!$util.isInteger(message.wisdom))
                    return "wisdom: integer expected";
            if (message.intelligence != null && $Object.hasOwnProperty.call(message, "intelligence"))
                if (!$util.isInteger(message.intelligence))
                    return "intelligence: integer expected";
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType"))
                if (!$util.isString(message.damageType))
                    return "damageType: string expected";
            if (message.attack != null && $Object.hasOwnProperty.call(message, "attack"))
                if (typeof message.attack !== "number")
                    return "attack: number expected";
            if (message.speed != null && $Object.hasOwnProperty.call(message, "speed"))
                if (typeof message.speed !== "number")
                    return "speed: number expected";
            if (message.attackPattern != null && $Object.hasOwnProperty.call(message, "attackPattern"))
                if (!$util.isString(message.attackPattern))
                    return "attackPattern: string expected";
            if (message.attackDelayRange != null && $Object.hasOwnProperty.call(message, "attackDelayRange")) {
                if (!$Array.isArray(message.attackDelayRange))
                    return "attackDelayRange: array expected";
                for (let i = 0; i < message.attackDelayRange.length; ++i)
                    if (!$util.isInteger(message.attackDelayRange[i]))
                        return "attackDelayRange: integer[] expected";
            }
            if (message.exp != null && $Object.hasOwnProperty.call(message, "exp"))
                if (!$util.isInteger(message.exp) && !(message.exp && $util.isInteger(message.exp.low) && $util.isInteger(message.exp.high)))
                    return "exp: integer|Long expected";
            if (message.gold != null && $Object.hasOwnProperty.call(message, "gold"))
                if (!$util.isInteger(message.gold) && !(message.gold && $util.isInteger(message.gold.low) && $util.isInteger(message.gold.high)))
                    return "gold: integer|Long expected";
            if (message.isDead != null && $Object.hasOwnProperty.call(message, "isDead"))
                if (typeof message.isDead !== "boolean")
                    return "isDead: boolean expected";
            if (message.nextAttackAt != null && $Object.hasOwnProperty.call(message, "nextAttackAt"))
                if (!$util.isInteger(message.nextAttackAt) && !(message.nextAttackAt && $util.isInteger(message.nextAttackAt.low) && $util.isInteger(message.nextAttackAt.high)))
                    return "nextAttackAt: integer|Long expected";
            if (message.nextAttackDelay != null && $Object.hasOwnProperty.call(message, "nextAttackDelay"))
                if (!$util.isInteger(message.nextAttackDelay) && !(message.nextAttackDelay && $util.isInteger(message.nextAttackDelay.low) && $util.isInteger(message.nextAttackDelay.high)))
                    return "nextAttackDelay: integer|Long expected";
            return null;
        };

        /**
         * Creates an Enemy message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Enemy
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Enemy} Enemy
         */
        Enemy.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Enemy)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Enemy: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Enemy();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.placeId != null)
                if (typeof object.placeId !== "string" || object.placeId.length)
                    message.placeId = $String(object.placeId);
            if (object.type != null)
                if (typeof object.type !== "string" || object.type.length)
                    message.type = $String(object.type);
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.avatar != null)
                if (typeof object.avatar !== "string" || object.avatar.length)
                    message.avatar = $String(object.avatar);
            if (object.hp != null)
                if (typeof object.hp === "object" ? object.hp.low || object.hp.high : $Number(object.hp) !== 0)
                    if ($util.Long)
                        message.hp = $util.Long.fromValue(object.hp, false);
                    else if (typeof object.hp === "string")
                        message.hp = $parseInt(object.hp, 10);
                    else if (typeof object.hp === "number")
                        message.hp = object.hp;
                    else if (typeof object.hp === "object")
                        message.hp = new $util.LongBits(object.hp.low >>> 0, object.hp.high >>> 0).toNumber();
            if (object.maxHp != null)
                if (typeof object.maxHp === "object" ? object.maxHp.low || object.maxHp.high : $Number(object.maxHp) !== 0)
                    if ($util.Long)
                        message.maxHp = $util.Long.fromValue(object.maxHp, false);
                    else if (typeof object.maxHp === "string")
                        message.maxHp = $parseInt(object.maxHp, 10);
                    else if (typeof object.maxHp === "number")
                        message.maxHp = object.maxHp;
                    else if (typeof object.maxHp === "object")
                        message.maxHp = new $util.LongBits(object.maxHp.low >>> 0, object.maxHp.high >>> 0).toNumber();
            if (object.strength != null)
                if ($Number(object.strength) !== 0)
                    message.strength = object.strength | 0;
            if (object.defense != null)
                if ($Number(object.defense) !== 0)
                    message.defense = object.defense | 0;
            if (object.agility != null)
                if ($Number(object.agility) !== 0)
                    message.agility = object.agility | 0;
            if (object.wisdom != null)
                if ($Number(object.wisdom) !== 0)
                    message.wisdom = object.wisdom | 0;
            if (object.intelligence != null)
                if ($Number(object.intelligence) !== 0)
                    message.intelligence = object.intelligence | 0;
            if (object.damageType != null)
                if (typeof object.damageType !== "string" || object.damageType.length)
                    message.damageType = $String(object.damageType);
            if (object.attack != null)
                if (!$Object.is($Number(object.attack), 0))
                    message.attack = $Number(object.attack);
            if (object.speed != null)
                if (!$Object.is($Number(object.speed), 0))
                    message.speed = $Number(object.speed);
            if (object.attackPattern != null)
                if (typeof object.attackPattern !== "string" || object.attackPattern.length)
                    message.attackPattern = $String(object.attackPattern);
            if (object.attackDelayRange) {
                if (!$Array.isArray(object.attackDelayRange))
                    throw $TypeError(".game.Enemy.attackDelayRange: array expected");
                message.attackDelayRange = $Array(object.attackDelayRange.length);
                for (let i = 0; i < object.attackDelayRange.length; ++i)
                    message.attackDelayRange[i] = object.attackDelayRange[i] | 0;
            }
            if (object.exp != null)
                if (typeof object.exp === "object" ? object.exp.low || object.exp.high : $Number(object.exp) !== 0)
                    if ($util.Long)
                        message.exp = $util.Long.fromValue(object.exp, false);
                    else if (typeof object.exp === "string")
                        message.exp = $parseInt(object.exp, 10);
                    else if (typeof object.exp === "number")
                        message.exp = object.exp;
                    else if (typeof object.exp === "object")
                        message.exp = new $util.LongBits(object.exp.low >>> 0, object.exp.high >>> 0).toNumber();
            if (object.gold != null)
                if (typeof object.gold === "object" ? object.gold.low || object.gold.high : $Number(object.gold) !== 0)
                    if ($util.Long)
                        message.gold = $util.Long.fromValue(object.gold, false);
                    else if (typeof object.gold === "string")
                        message.gold = $parseInt(object.gold, 10);
                    else if (typeof object.gold === "number")
                        message.gold = object.gold;
                    else if (typeof object.gold === "object")
                        message.gold = new $util.LongBits(object.gold.low >>> 0, object.gold.high >>> 0).toNumber();
            if (object.isDead != null)
                if (object.isDead)
                    message.isDead = $Boolean(object.isDead);
            if (object.nextAttackAt != null)
                if (typeof object.nextAttackAt === "object" ? object.nextAttackAt.low || object.nextAttackAt.high : $Number(object.nextAttackAt) !== 0)
                    if ($util.Long)
                        message.nextAttackAt = $util.Long.fromValue(object.nextAttackAt, false);
                    else if (typeof object.nextAttackAt === "string")
                        message.nextAttackAt = $parseInt(object.nextAttackAt, 10);
                    else if (typeof object.nextAttackAt === "number")
                        message.nextAttackAt = object.nextAttackAt;
                    else if (typeof object.nextAttackAt === "object")
                        message.nextAttackAt = new $util.LongBits(object.nextAttackAt.low >>> 0, object.nextAttackAt.high >>> 0).toNumber();
            if (object.nextAttackDelay != null)
                if (typeof object.nextAttackDelay === "object" ? object.nextAttackDelay.low || object.nextAttackDelay.high : $Number(object.nextAttackDelay) !== 0)
                    if ($util.Long)
                        message.nextAttackDelay = $util.Long.fromValue(object.nextAttackDelay, false);
                    else if (typeof object.nextAttackDelay === "string")
                        message.nextAttackDelay = $parseInt(object.nextAttackDelay, 10);
                    else if (typeof object.nextAttackDelay === "number")
                        message.nextAttackDelay = object.nextAttackDelay;
                    else if (typeof object.nextAttackDelay === "object")
                        message.nextAttackDelay = new $util.LongBits(object.nextAttackDelay.low >>> 0, object.nextAttackDelay.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an Enemy message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Enemy
         * @static
         * @param {game.Enemy} message Enemy
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Enemy.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.attackDelayRange = [];
            if (options.defaults) {
                object.id = "";
                object.placeId = "";
                object.type = "";
                object.name = "";
                object.avatar = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.hp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.hp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.maxHp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.maxHp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.strength = 0;
                object.defense = 0;
                object.agility = 0;
                object.wisdom = 0;
                object.intelligence = 0;
                object.damageType = "";
                object.attack = 0;
                object.speed = 0;
                object.attackPattern = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.exp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.exp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.gold = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gold = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.isDead = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nextAttackAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nextAttackAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nextAttackDelay = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nextAttackDelay = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                object.placeId = message.placeId;
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                object.type = message.type;
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar"))
                object.avatar = message.avatar;
            if (message.hp != null && $Object.hasOwnProperty.call(message, "hp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.hp = typeof message.hp === "number" ? $BigInt(message.hp) : $util.Long.fromBits(message.hp.low >>> 0, message.hp.high >>> 0, false).toBigInt();
                else if (typeof message.hp === "number")
                    object.hp = options.longs === $String ? $String(message.hp) : message.hp;
                else
                    object.hp = options.longs === $String ? $util.Long.prototype.toString.call(message.hp) : options.longs === $Number ? new $util.LongBits(message.hp.low >>> 0, message.hp.high >>> 0).toNumber() : message.hp;
            if (message.maxHp != null && $Object.hasOwnProperty.call(message, "maxHp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.maxHp = typeof message.maxHp === "number" ? $BigInt(message.maxHp) : $util.Long.fromBits(message.maxHp.low >>> 0, message.maxHp.high >>> 0, false).toBigInt();
                else if (typeof message.maxHp === "number")
                    object.maxHp = options.longs === $String ? $String(message.maxHp) : message.maxHp;
                else
                    object.maxHp = options.longs === $String ? $util.Long.prototype.toString.call(message.maxHp) : options.longs === $Number ? new $util.LongBits(message.maxHp.low >>> 0, message.maxHp.high >>> 0).toNumber() : message.maxHp;
            if (message.strength != null && $Object.hasOwnProperty.call(message, "strength"))
                object.strength = message.strength;
            if (message.defense != null && $Object.hasOwnProperty.call(message, "defense"))
                object.defense = message.defense;
            if (message.agility != null && $Object.hasOwnProperty.call(message, "agility"))
                object.agility = message.agility;
            if (message.wisdom != null && $Object.hasOwnProperty.call(message, "wisdom"))
                object.wisdom = message.wisdom;
            if (message.intelligence != null && $Object.hasOwnProperty.call(message, "intelligence"))
                object.intelligence = message.intelligence;
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType"))
                object.damageType = message.damageType;
            if (message.attack != null && $Object.hasOwnProperty.call(message, "attack"))
                object.attack = options.json && !$isFinite(message.attack) ? $String(message.attack) : message.attack;
            if (message.speed != null && $Object.hasOwnProperty.call(message, "speed"))
                object.speed = options.json && !$isFinite(message.speed) ? $String(message.speed) : message.speed;
            if (message.attackPattern != null && $Object.hasOwnProperty.call(message, "attackPattern"))
                object.attackPattern = message.attackPattern;
            if (message.attackDelayRange && message.attackDelayRange.length) {
                object.attackDelayRange = $Array(message.attackDelayRange.length);
                for (let j = 0; j < message.attackDelayRange.length; ++j)
                    object.attackDelayRange[j] = message.attackDelayRange[j];
            }
            if (message.exp != null && $Object.hasOwnProperty.call(message, "exp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.exp = typeof message.exp === "number" ? $BigInt(message.exp) : $util.Long.fromBits(message.exp.low >>> 0, message.exp.high >>> 0, false).toBigInt();
                else if (typeof message.exp === "number")
                    object.exp = options.longs === $String ? $String(message.exp) : message.exp;
                else
                    object.exp = options.longs === $String ? $util.Long.prototype.toString.call(message.exp) : options.longs === $Number ? new $util.LongBits(message.exp.low >>> 0, message.exp.high >>> 0).toNumber() : message.exp;
            if (message.gold != null && $Object.hasOwnProperty.call(message, "gold"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gold = typeof message.gold === "number" ? $BigInt(message.gold) : $util.Long.fromBits(message.gold.low >>> 0, message.gold.high >>> 0, false).toBigInt();
                else if (typeof message.gold === "number")
                    object.gold = options.longs === $String ? $String(message.gold) : message.gold;
                else
                    object.gold = options.longs === $String ? $util.Long.prototype.toString.call(message.gold) : options.longs === $Number ? new $util.LongBits(message.gold.low >>> 0, message.gold.high >>> 0).toNumber() : message.gold;
            if (message.isDead != null && $Object.hasOwnProperty.call(message, "isDead"))
                object.isDead = message.isDead;
            if (message.nextAttackAt != null && $Object.hasOwnProperty.call(message, "nextAttackAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nextAttackAt = typeof message.nextAttackAt === "number" ? $BigInt(message.nextAttackAt) : $util.Long.fromBits(message.nextAttackAt.low >>> 0, message.nextAttackAt.high >>> 0, false).toBigInt();
                else if (typeof message.nextAttackAt === "number")
                    object.nextAttackAt = options.longs === $String ? $String(message.nextAttackAt) : message.nextAttackAt;
                else
                    object.nextAttackAt = options.longs === $String ? $util.Long.prototype.toString.call(message.nextAttackAt) : options.longs === $Number ? new $util.LongBits(message.nextAttackAt.low >>> 0, message.nextAttackAt.high >>> 0).toNumber() : message.nextAttackAt;
            if (message.nextAttackDelay != null && $Object.hasOwnProperty.call(message, "nextAttackDelay"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nextAttackDelay = typeof message.nextAttackDelay === "number" ? $BigInt(message.nextAttackDelay) : $util.Long.fromBits(message.nextAttackDelay.low >>> 0, message.nextAttackDelay.high >>> 0, false).toBigInt();
                else if (typeof message.nextAttackDelay === "number")
                    object.nextAttackDelay = options.longs === $String ? $String(message.nextAttackDelay) : message.nextAttackDelay;
                else
                    object.nextAttackDelay = options.longs === $String ? $util.Long.prototype.toString.call(message.nextAttackDelay) : options.longs === $Number ? new $util.LongBits(message.nextAttackDelay.low >>> 0, message.nextAttackDelay.high >>> 0).toNumber() : message.nextAttackDelay;
            return object;
        };

        /**
         * Converts this Enemy to JSON.
         * @function toJSON
         * @memberof game.Enemy
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Enemy.prototype.toJSON = function() {
            return Enemy.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Enemy
         * @function getTypeUrl
         * @memberof game.Enemy
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Enemy.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Enemy";
        };

        return Enemy;
    })();

    game.Assignment = (function() {

        /**
         * Properties of an Assignment.
         * @typedef {Object} game.Assignment.$Properties
         * @property {string|null} [placeId] Assignment placeId
         * @property {number|null} [socketIndex] Assignment socketIndex
         * @property {string|null} [material] Assignment material
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an Assignment.
         * @memberof game
         * @interface IAssignment
         * @augments game.Assignment.$Properties
         * @deprecated Use game.Assignment.$Properties instead.
         */

        /**
         * Shape of an Assignment.
         * @typedef {game.Assignment.$Properties} game.Assignment.$Shape
         */

        /**
         * Constructs a new Assignment.
         * @memberof game
         * @classdesc Represents an Assignment.
         * @constructor
         * @param {game.Assignment.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Assignment = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Assignment placeId.
         * @member {string} placeId
         * @memberof game.Assignment
         * @instance
         */
        Assignment.prototype.placeId = "";

        /**
         * Assignment socketIndex.
         * @member {number} socketIndex
         * @memberof game.Assignment
         * @instance
         */
        Assignment.prototype.socketIndex = 0;

        /**
         * Assignment material.
         * @member {string} material
         * @memberof game.Assignment
         * @instance
         */
        Assignment.prototype.material = "";

        /**
         * Creates a new Assignment instance using the specified properties.
         * @function create
         * @memberof game.Assignment
         * @static
         * @param {game.Assignment.$Properties=} [properties] Properties to set
         * @returns {game.Assignment} Assignment instance
         * @type {{
         *   (properties: game.Assignment.$Shape): game.Assignment & game.Assignment.$Shape;
         *   (properties?: game.Assignment.$Properties): game.Assignment;
         * }}
         */
        Assignment.create = function(properties) {
            return new Assignment(properties);
        };

        /**
         * Encodes the specified Assignment message. Does not implicitly {@link game.Assignment.verify|verify} messages.
         * @function encode
         * @memberof game.Assignment
         * @static
         * @param {game.Assignment.$Properties} message Assignment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Assignment.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId") && message.placeId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.placeId);
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex") && message.socketIndex !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.socketIndex);
            if (message.material != null && $Object.hasOwnProperty.call(message, "material") && message.material !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.material);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Assignment message, length delimited. Does not implicitly {@link game.Assignment.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Assignment
         * @static
         * @param {game.Assignment.$Properties} message Assignment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Assignment.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an Assignment message from the specified reader or buffer.
         * @function decode
         * @memberof game.Assignment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Assignment & game.Assignment.$Shape} Assignment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Assignment.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Assignment(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.placeId = value;
                        else
                            delete message.placeId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.socketIndex = value;
                        else
                            delete message.socketIndex;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.material = value;
                        else
                            delete message.material;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an Assignment message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Assignment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Assignment & game.Assignment.$Shape} Assignment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Assignment.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Assignment message.
         * @function verify
         * @memberof game.Assignment
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Assignment.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                if (!$util.isString(message.placeId))
                    return "placeId: string expected";
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                if (!$util.isInteger(message.socketIndex))
                    return "socketIndex: integer expected";
            if (message.material != null && $Object.hasOwnProperty.call(message, "material"))
                if (!$util.isString(message.material))
                    return "material: string expected";
            return null;
        };

        /**
         * Creates an Assignment message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Assignment
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Assignment} Assignment
         */
        Assignment.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Assignment)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Assignment: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Assignment();
            if (object.placeId != null)
                if (typeof object.placeId !== "string" || object.placeId.length)
                    message.placeId = $String(object.placeId);
            if (object.socketIndex != null)
                if ($Number(object.socketIndex) !== 0)
                    message.socketIndex = object.socketIndex | 0;
            if (object.material != null)
                if (typeof object.material !== "string" || object.material.length)
                    message.material = $String(object.material);
            return message;
        };

        /**
         * Creates a plain object from an Assignment message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Assignment
         * @static
         * @param {game.Assignment} message Assignment
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Assignment.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.placeId = "";
                object.socketIndex = 0;
                object.material = "";
            }
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                object.placeId = message.placeId;
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                object.socketIndex = message.socketIndex;
            if (message.material != null && $Object.hasOwnProperty.call(message, "material"))
                object.material = message.material;
            return object;
        };

        /**
         * Converts this Assignment to JSON.
         * @function toJSON
         * @memberof game.Assignment
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Assignment.prototype.toJSON = function() {
            return Assignment.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Assignment
         * @function getTypeUrl
         * @memberof game.Assignment
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Assignment.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Assignment";
        };

        return Assignment;
    })();

    game.Worker = (function() {

        /**
         * Properties of a Worker.
         * @typedef {Object} game.Worker.$Properties
         * @property {string|null} [id] Worker id
         * @property {string|null} [firstName] Worker firstName
         * @property {string|null} [name] Worker name
         * @property {string|null} [gender] Worker gender
         * @property {string|null} [avatar] Worker avatar
         * @property {game.Assignment.$Properties|null} [assignment] Worker assignment
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Worker.
         * @memberof game
         * @interface IWorker
         * @augments game.Worker.$Properties
         * @deprecated Use game.Worker.$Properties instead.
         */

        /**
         * Shape of a Worker.
         * @typedef {game.Worker.$Properties} game.Worker.$Shape
         */

        /**
         * Constructs a new Worker.
         * @memberof game
         * @classdesc Represents a Worker.
         * @constructor
         * @param {game.Worker.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Worker = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Worker id.
         * @member {string} id
         * @memberof game.Worker
         * @instance
         */
        Worker.prototype.id = "";

        /**
         * Worker firstName.
         * @member {string} firstName
         * @memberof game.Worker
         * @instance
         */
        Worker.prototype.firstName = "";

        /**
         * Worker name.
         * @member {string} name
         * @memberof game.Worker
         * @instance
         */
        Worker.prototype.name = "";

        /**
         * Worker gender.
         * @member {string} gender
         * @memberof game.Worker
         * @instance
         */
        Worker.prototype.gender = "";

        /**
         * Worker avatar.
         * @member {string} avatar
         * @memberof game.Worker
         * @instance
         */
        Worker.prototype.avatar = "";

        /**
         * Worker assignment.
         * @member {game.Assignment.$Properties|null|undefined} assignment
         * @memberof game.Worker
         * @instance
         */
        Worker.prototype.assignment = null;

        /**
         * Creates a new Worker instance using the specified properties.
         * @function create
         * @memberof game.Worker
         * @static
         * @param {game.Worker.$Properties=} [properties] Properties to set
         * @returns {game.Worker} Worker instance
         * @type {{
         *   (properties: game.Worker.$Shape): game.Worker & game.Worker.$Shape;
         *   (properties?: game.Worker.$Properties): game.Worker;
         * }}
         */
        Worker.create = function(properties) {
            return new Worker(properties);
        };

        /**
         * Encodes the specified Worker message. Does not implicitly {@link game.Worker.verify|verify} messages.
         * @function encode
         * @memberof game.Worker
         * @static
         * @param {game.Worker.$Properties} message Worker message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Worker.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.firstName != null && $Object.hasOwnProperty.call(message, "firstName") && message.firstName !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.firstName);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
            if (message.gender != null && $Object.hasOwnProperty.call(message, "gender") && message.gender !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.gender);
            if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar") && message.avatar !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.avatar);
            if (message.assignment != null && $Object.hasOwnProperty.call(message, "assignment"))
                $root.game.Assignment.encode(message.assignment, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Worker message, length delimited. Does not implicitly {@link game.Worker.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Worker
         * @static
         * @param {game.Worker.$Properties} message Worker message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Worker.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Worker message from the specified reader or buffer.
         * @function decode
         * @memberof game.Worker
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Worker & game.Worker.$Shape} Worker
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Worker.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Worker(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.firstName = value;
                        else
                            delete message.firstName;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.gender = value;
                        else
                            delete message.gender;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.avatar = value;
                        else
                            delete message.avatar;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.assignment = $root.game.Assignment.decode(reader, reader.uint32(), $undefined, _depth + 1, message.assignment);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Worker message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Worker
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Worker & game.Worker.$Shape} Worker
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Worker.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Worker message.
         * @function verify
         * @memberof game.Worker
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Worker.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.firstName != null && $Object.hasOwnProperty.call(message, "firstName"))
                if (!$util.isString(message.firstName))
                    return "firstName: string expected";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.gender != null && $Object.hasOwnProperty.call(message, "gender"))
                if (!$util.isString(message.gender))
                    return "gender: string expected";
            if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar"))
                if (!$util.isString(message.avatar))
                    return "avatar: string expected";
            if (message.assignment != null && $Object.hasOwnProperty.call(message, "assignment")) {
                let error = $root.game.Assignment.verify(message.assignment, _depth + 1);
                if (error)
                    return "assignment." + error;
            }
            return null;
        };

        /**
         * Creates a Worker message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Worker
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Worker} Worker
         */
        Worker.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Worker)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Worker: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Worker();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.firstName != null)
                if (typeof object.firstName !== "string" || object.firstName.length)
                    message.firstName = $String(object.firstName);
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.gender != null)
                if (typeof object.gender !== "string" || object.gender.length)
                    message.gender = $String(object.gender);
            if (object.avatar != null)
                if (typeof object.avatar !== "string" || object.avatar.length)
                    message.avatar = $String(object.avatar);
            if (object.assignment != null) {
                if (!$util.isObject(object.assignment))
                    throw $TypeError(".game.Worker.assignment: object expected");
                message.assignment = $root.game.Assignment.fromObject(object.assignment, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Worker message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Worker
         * @static
         * @param {game.Worker} message Worker
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Worker.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.firstName = "";
                object.name = "";
                object.gender = "";
                object.avatar = "";
                object.assignment = null;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.firstName != null && $Object.hasOwnProperty.call(message, "firstName"))
                object.firstName = message.firstName;
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.gender != null && $Object.hasOwnProperty.call(message, "gender"))
                object.gender = message.gender;
            if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar"))
                object.avatar = message.avatar;
            if (message.assignment != null && $Object.hasOwnProperty.call(message, "assignment"))
                object.assignment = $root.game.Assignment.toObject(message.assignment, options, _depth + 1);
            return object;
        };

        /**
         * Converts this Worker to JSON.
         * @function toJSON
         * @memberof game.Worker
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Worker.prototype.toJSON = function() {
            return Worker.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Worker
         * @function getTypeUrl
         * @memberof game.Worker
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Worker.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Worker";
        };

        return Worker;
    })();

    game.Workers = (function() {

        /**
         * Properties of a Workers.
         * @typedef {Object} game.Workers.$Properties
         * @property {Array.<game.Worker.$Properties>|null} [hired] Workers hired
         * @property {Array.<game.Worker.$Properties>|null} [available] Workers available
         * @property {number|null} [workerSlots] Workers workerSlots
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Workers.
         * @memberof game
         * @interface IWorkers
         * @augments game.Workers.$Properties
         * @deprecated Use game.Workers.$Properties instead.
         */

        /**
         * Shape of a Workers.
         * @typedef {game.Workers.$Properties} game.Workers.$Shape
         */

        /**
         * Constructs a new Workers.
         * @memberof game
         * @classdesc Represents a Workers.
         * @constructor
         * @param {game.Workers.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Workers = function (properties) {
            this.hired = [];
            this.available = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Workers hired.
         * @member {Array.<game.Worker.$Properties>} hired
         * @memberof game.Workers
         * @instance
         */
        Workers.prototype.hired = $util.emptyArray;

        /**
         * Workers available.
         * @member {Array.<game.Worker.$Properties>} available
         * @memberof game.Workers
         * @instance
         */
        Workers.prototype.available = $util.emptyArray;

        /**
         * Workers workerSlots.
         * @member {number} workerSlots
         * @memberof game.Workers
         * @instance
         */
        Workers.prototype.workerSlots = 0;

        /**
         * Creates a new Workers instance using the specified properties.
         * @function create
         * @memberof game.Workers
         * @static
         * @param {game.Workers.$Properties=} [properties] Properties to set
         * @returns {game.Workers} Workers instance
         * @type {{
         *   (properties: game.Workers.$Shape): game.Workers & game.Workers.$Shape;
         *   (properties?: game.Workers.$Properties): game.Workers;
         * }}
         */
        Workers.create = function(properties) {
            return new Workers(properties);
        };

        /**
         * Encodes the specified Workers message. Does not implicitly {@link game.Workers.verify|verify} messages.
         * @function encode
         * @memberof game.Workers
         * @static
         * @param {game.Workers.$Properties} message Workers message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Workers.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hired != null && message.hired.length)
                for (let i = 0; i < message.hired.length; ++i)
                    $root.game.Worker.encode(message.hired[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.available != null && message.available.length)
                for (let i = 0; i < message.available.length; ++i)
                    $root.game.Worker.encode(message.available[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.workerSlots != null && $Object.hasOwnProperty.call(message, "workerSlots") && message.workerSlots !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.workerSlots);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Workers message, length delimited. Does not implicitly {@link game.Workers.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Workers
         * @static
         * @param {game.Workers.$Properties} message Workers message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Workers.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Workers message from the specified reader or buffer.
         * @function decode
         * @memberof game.Workers
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Workers & game.Workers.$Shape} Workers
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Workers.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Workers(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hired && message.hired.length))
                            message.hired = [];
                        message.hired.push($root.game.Worker.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.available && message.available.length))
                            message.available = [];
                        message.available.push($root.game.Worker.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.workerSlots = value;
                        else
                            delete message.workerSlots;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Workers message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Workers
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Workers & game.Workers.$Shape} Workers
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Workers.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Workers message.
         * @function verify
         * @memberof game.Workers
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Workers.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hired != null && $Object.hasOwnProperty.call(message, "hired")) {
                if (!$Array.isArray(message.hired))
                    return "hired: array expected";
                for (let i = 0; i < message.hired.length; ++i) {
                    let error = $root.game.Worker.verify(message.hired[i], _depth + 1);
                    if (error)
                        return "hired." + error;
                }
            }
            if (message.available != null && $Object.hasOwnProperty.call(message, "available")) {
                if (!$Array.isArray(message.available))
                    return "available: array expected";
                for (let i = 0; i < message.available.length; ++i) {
                    let error = $root.game.Worker.verify(message.available[i], _depth + 1);
                    if (error)
                        return "available." + error;
                }
            }
            if (message.workerSlots != null && $Object.hasOwnProperty.call(message, "workerSlots"))
                if (!$util.isInteger(message.workerSlots))
                    return "workerSlots: integer expected";
            return null;
        };

        /**
         * Creates a Workers message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Workers
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Workers} Workers
         */
        Workers.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Workers)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Workers: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Workers();
            if (object.hired) {
                if (!$Array.isArray(object.hired))
                    throw $TypeError(".game.Workers.hired: array expected");
                message.hired = $Array(object.hired.length);
                for (let i = 0; i < object.hired.length; ++i) {
                    if (!$util.isObject(object.hired[i]))
                        throw $TypeError(".game.Workers.hired: object expected");
                    message.hired[i] = $root.game.Worker.fromObject(object.hired[i], _depth + 1);
                }
            }
            if (object.available) {
                if (!$Array.isArray(object.available))
                    throw $TypeError(".game.Workers.available: array expected");
                message.available = $Array(object.available.length);
                for (let i = 0; i < object.available.length; ++i) {
                    if (!$util.isObject(object.available[i]))
                        throw $TypeError(".game.Workers.available: object expected");
                    message.available[i] = $root.game.Worker.fromObject(object.available[i], _depth + 1);
                }
            }
            if (object.workerSlots != null)
                if ($Number(object.workerSlots) !== 0)
                    message.workerSlots = object.workerSlots | 0;
            return message;
        };

        /**
         * Creates a plain object from a Workers message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Workers
         * @static
         * @param {game.Workers} message Workers
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Workers.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.hired = [];
                object.available = [];
            }
            if (options.defaults)
                object.workerSlots = 0;
            if (message.hired && message.hired.length) {
                object.hired = $Array(message.hired.length);
                for (let j = 0; j < message.hired.length; ++j)
                    object.hired[j] = $root.game.Worker.toObject(message.hired[j], options, _depth + 1);
            }
            if (message.available && message.available.length) {
                object.available = $Array(message.available.length);
                for (let j = 0; j < message.available.length; ++j)
                    object.available[j] = $root.game.Worker.toObject(message.available[j], options, _depth + 1);
            }
            if (message.workerSlots != null && $Object.hasOwnProperty.call(message, "workerSlots"))
                object.workerSlots = message.workerSlots;
            return object;
        };

        /**
         * Converts this Workers to JSON.
         * @function toJSON
         * @memberof game.Workers
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Workers.prototype.toJSON = function() {
            return Workers.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Workers
         * @function getTypeUrl
         * @memberof game.Workers
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Workers.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Workers";
        };

        return Workers;
    })();

    game.QuestEntry = (function() {

        /**
         * Properties of a QuestEntry.
         * @typedef {Object} game.QuestEntry.$Properties
         * @property {string|null} [questId] QuestEntry questId
         * @property {number|Long|null} [startedAt] QuestEntry startedAt
         * @property {Object.<string,number|Long>|null} [progress] QuestEntry progress
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a QuestEntry.
         * @memberof game
         * @interface IQuestEntry
         * @augments game.QuestEntry.$Properties
         * @deprecated Use game.QuestEntry.$Properties instead.
         */

        /**
         * Shape of a QuestEntry.
         * @typedef {game.QuestEntry.$Properties} game.QuestEntry.$Shape
         */

        /**
         * Constructs a new QuestEntry.
         * @memberof game
         * @classdesc Represents a QuestEntry.
         * @constructor
         * @param {game.QuestEntry.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const QuestEntry = function (properties) {
            this.progress = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * QuestEntry questId.
         * @member {string} questId
         * @memberof game.QuestEntry
         * @instance
         */
        QuestEntry.prototype.questId = "";

        /**
         * QuestEntry startedAt.
         * @member {number|Long} startedAt
         * @memberof game.QuestEntry
         * @instance
         */
        QuestEntry.prototype.startedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * QuestEntry progress.
         * @member {Object.<string,number|Long>} progress
         * @memberof game.QuestEntry
         * @instance
         */
        QuestEntry.prototype.progress = $util.emptyObject;

        /**
         * Creates a new QuestEntry instance using the specified properties.
         * @function create
         * @memberof game.QuestEntry
         * @static
         * @param {game.QuestEntry.$Properties=} [properties] Properties to set
         * @returns {game.QuestEntry} QuestEntry instance
         * @type {{
         *   (properties: game.QuestEntry.$Shape): game.QuestEntry & game.QuestEntry.$Shape;
         *   (properties?: game.QuestEntry.$Properties): game.QuestEntry;
         * }}
         */
        QuestEntry.create = function(properties) {
            return new QuestEntry(properties);
        };

        /**
         * Encodes the specified QuestEntry message. Does not implicitly {@link game.QuestEntry.verify|verify} messages.
         * @function encode
         * @memberof game.QuestEntry
         * @static
         * @param {game.QuestEntry.$Properties} message QuestEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QuestEntry.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.questId != null && $Object.hasOwnProperty.call(message, "questId") && message.questId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.questId);
            if (message.startedAt != null && $Object.hasOwnProperty.call(message, "startedAt") && (typeof message.startedAt === "object" ? message.startedAt.low || message.startedAt.high : message.startedAt !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.startedAt);
            if (message.progress != null && $Object.hasOwnProperty.call(message, "progress"))
                for (let keys = $Object.keys(message.progress), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.progress[keys[i]]).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified QuestEntry message, length delimited. Does not implicitly {@link game.QuestEntry.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.QuestEntry
         * @static
         * @param {game.QuestEntry.$Properties} message QuestEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QuestEntry.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a QuestEntry message from the specified reader or buffer.
         * @function decode
         * @memberof game.QuestEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.QuestEntry & game.QuestEntry.$Shape} QuestEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QuestEntry.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.QuestEntry(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.questId = value;
                        else
                            delete message.questId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.startedAt = value;
                        else
                            delete message.startedAt;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (message.progress === $util.emptyObject)
                            message.progress = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = $util.Long ? $util.Long.fromNumber(0, false) : 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 0)
                                    break;
                                value = reader.int64();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.progress, key);
                        message.progress[key] = value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a QuestEntry message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.QuestEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.QuestEntry & game.QuestEntry.$Shape} QuestEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QuestEntry.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a QuestEntry message.
         * @function verify
         * @memberof game.QuestEntry
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        QuestEntry.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.questId != null && $Object.hasOwnProperty.call(message, "questId"))
                if (!$util.isString(message.questId))
                    return "questId: string expected";
            if (message.startedAt != null && $Object.hasOwnProperty.call(message, "startedAt"))
                if (!$util.isInteger(message.startedAt) && !(message.startedAt && $util.isInteger(message.startedAt.low) && $util.isInteger(message.startedAt.high)))
                    return "startedAt: integer|Long expected";
            if (message.progress != null && $Object.hasOwnProperty.call(message, "progress")) {
                if (!$util.isObject(message.progress))
                    return "progress: object expected";
                let key = $Object.keys(message.progress);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.progress[key[i]]) && !(message.progress[key[i]] && $util.isInteger(message.progress[key[i]].low) && $util.isInteger(message.progress[key[i]].high)))
                        return "progress: integer|Long{k:string} expected";
            }
            return null;
        };

        /**
         * Creates a QuestEntry message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.QuestEntry
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.QuestEntry} QuestEntry
         */
        QuestEntry.fromObject = function (object, _depth) {
            if (object instanceof $root.game.QuestEntry)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.QuestEntry: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.QuestEntry();
            if (object.questId != null)
                if (typeof object.questId !== "string" || object.questId.length)
                    message.questId = $String(object.questId);
            if (object.startedAt != null)
                if (typeof object.startedAt === "object" ? object.startedAt.low || object.startedAt.high : $Number(object.startedAt) !== 0)
                    if ($util.Long)
                        message.startedAt = $util.Long.fromValue(object.startedAt, false);
                    else if (typeof object.startedAt === "string")
                        message.startedAt = $parseInt(object.startedAt, 10);
                    else if (typeof object.startedAt === "number")
                        message.startedAt = object.startedAt;
                    else if (typeof object.startedAt === "object")
                        message.startedAt = new $util.LongBits(object.startedAt.low >>> 0, object.startedAt.high >>> 0).toNumber();
            if (object.progress) {
                if (!$util.isObject(object.progress))
                    throw $TypeError(".game.QuestEntry.progress: object expected");
                message.progress = {};
                for (let keys = $Object.keys(object.progress), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.progress, keys[i]);
                    if ($util.Long)
                        message.progress[keys[i]] = $util.Long.fromValue(object.progress[keys[i]], false);
                    else if (typeof object.progress[keys[i]] === "string")
                        message.progress[keys[i]] = $parseInt(object.progress[keys[i]], 10);
                    else if (typeof object.progress[keys[i]] === "number")
                        message.progress[keys[i]] = object.progress[keys[i]];
                    else if (typeof object.progress[keys[i]] === "object")
                        message.progress[keys[i]] = new $util.LongBits(object.progress[keys[i]].low >>> 0, object.progress[keys[i]].high >>> 0).toNumber();
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a QuestEntry message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.QuestEntry
         * @static
         * @param {game.QuestEntry} message QuestEntry
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QuestEntry.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.progress = {};
            if (options.defaults) {
                object.questId = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.startedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.startedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.questId != null && $Object.hasOwnProperty.call(message, "questId"))
                object.questId = message.questId;
            if (message.startedAt != null && $Object.hasOwnProperty.call(message, "startedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.startedAt = typeof message.startedAt === "number" ? $BigInt(message.startedAt) : $util.Long.fromBits(message.startedAt.low >>> 0, message.startedAt.high >>> 0, false).toBigInt();
                else if (typeof message.startedAt === "number")
                    object.startedAt = options.longs === $String ? $String(message.startedAt) : message.startedAt;
                else
                    object.startedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.startedAt) : options.longs === $Number ? new $util.LongBits(message.startedAt.low >>> 0, message.startedAt.high >>> 0).toNumber() : message.startedAt;
            let keys2;
            if (message.progress && (keys2 = $Object.keys(message.progress)).length) {
                object.progress = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.progress, keys2[j]);
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.progress[keys2[j]] = typeof message.progress[keys2[j]] === "number" ? $BigInt(message.progress[keys2[j]]) : $util.Long.fromBits(message.progress[keys2[j]].low >>> 0, message.progress[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.progress[keys2[j]] === "number")
                        object.progress[keys2[j]] = options.longs === $String ? $String(message.progress[keys2[j]]) : message.progress[keys2[j]];
                    else
                        object.progress[keys2[j]] = options.longs === $String ? $util.Long.prototype.toString.call(message.progress[keys2[j]]) : options.longs === $Number ? new $util.LongBits(message.progress[keys2[j]].low >>> 0, message.progress[keys2[j]].high >>> 0).toNumber() : message.progress[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this QuestEntry to JSON.
         * @function toJSON
         * @memberof game.QuestEntry
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QuestEntry.prototype.toJSON = function() {
            return QuestEntry.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for QuestEntry
         * @function getTypeUrl
         * @memberof game.QuestEntry
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        QuestEntry.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.QuestEntry";
        };

        return QuestEntry;
    })();

    game.CompletedQuest = (function() {

        /**
         * Properties of a CompletedQuest.
         * @typedef {Object} game.CompletedQuest.$Properties
         * @property {number|Long|null} [completedAt] CompletedQuest completedAt
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CompletedQuest.
         * @memberof game
         * @interface ICompletedQuest
         * @augments game.CompletedQuest.$Properties
         * @deprecated Use game.CompletedQuest.$Properties instead.
         */

        /**
         * Shape of a CompletedQuest.
         * @typedef {game.CompletedQuest.$Properties} game.CompletedQuest.$Shape
         */

        /**
         * Constructs a new CompletedQuest.
         * @memberof game
         * @classdesc Represents a CompletedQuest.
         * @constructor
         * @param {game.CompletedQuest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CompletedQuest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CompletedQuest completedAt.
         * @member {number|Long} completedAt
         * @memberof game.CompletedQuest
         * @instance
         */
        CompletedQuest.prototype.completedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new CompletedQuest instance using the specified properties.
         * @function create
         * @memberof game.CompletedQuest
         * @static
         * @param {game.CompletedQuest.$Properties=} [properties] Properties to set
         * @returns {game.CompletedQuest} CompletedQuest instance
         * @type {{
         *   (properties: game.CompletedQuest.$Shape): game.CompletedQuest & game.CompletedQuest.$Shape;
         *   (properties?: game.CompletedQuest.$Properties): game.CompletedQuest;
         * }}
         */
        CompletedQuest.create = function(properties) {
            return new CompletedQuest(properties);
        };

        /**
         * Encodes the specified CompletedQuest message. Does not implicitly {@link game.CompletedQuest.verify|verify} messages.
         * @function encode
         * @memberof game.CompletedQuest
         * @static
         * @param {game.CompletedQuest.$Properties} message CompletedQuest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompletedQuest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.completedAt != null && $Object.hasOwnProperty.call(message, "completedAt") && (typeof message.completedAt === "object" ? message.completedAt.low || message.completedAt.high : message.completedAt !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.completedAt);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CompletedQuest message, length delimited. Does not implicitly {@link game.CompletedQuest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CompletedQuest
         * @static
         * @param {game.CompletedQuest.$Properties} message CompletedQuest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompletedQuest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CompletedQuest message from the specified reader or buffer.
         * @function decode
         * @memberof game.CompletedQuest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CompletedQuest & game.CompletedQuest.$Shape} CompletedQuest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompletedQuest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.CompletedQuest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.completedAt = value;
                        else
                            delete message.completedAt;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CompletedQuest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CompletedQuest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CompletedQuest & game.CompletedQuest.$Shape} CompletedQuest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompletedQuest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CompletedQuest message.
         * @function verify
         * @memberof game.CompletedQuest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CompletedQuest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.completedAt != null && $Object.hasOwnProperty.call(message, "completedAt"))
                if (!$util.isInteger(message.completedAt) && !(message.completedAt && $util.isInteger(message.completedAt.low) && $util.isInteger(message.completedAt.high)))
                    return "completedAt: integer|Long expected";
            return null;
        };

        /**
         * Creates a CompletedQuest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CompletedQuest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CompletedQuest} CompletedQuest
         */
        CompletedQuest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.CompletedQuest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.CompletedQuest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.CompletedQuest();
            if (object.completedAt != null)
                if (typeof object.completedAt === "object" ? object.completedAt.low || object.completedAt.high : $Number(object.completedAt) !== 0)
                    if ($util.Long)
                        message.completedAt = $util.Long.fromValue(object.completedAt, false);
                    else if (typeof object.completedAt === "string")
                        message.completedAt = $parseInt(object.completedAt, 10);
                    else if (typeof object.completedAt === "number")
                        message.completedAt = object.completedAt;
                    else if (typeof object.completedAt === "object")
                        message.completedAt = new $util.LongBits(object.completedAt.low >>> 0, object.completedAt.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a CompletedQuest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CompletedQuest
         * @static
         * @param {game.CompletedQuest} message CompletedQuest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CompletedQuest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.completedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.completedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.completedAt != null && $Object.hasOwnProperty.call(message, "completedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.completedAt = typeof message.completedAt === "number" ? $BigInt(message.completedAt) : $util.Long.fromBits(message.completedAt.low >>> 0, message.completedAt.high >>> 0, false).toBigInt();
                else if (typeof message.completedAt === "number")
                    object.completedAt = options.longs === $String ? $String(message.completedAt) : message.completedAt;
                else
                    object.completedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.completedAt) : options.longs === $Number ? new $util.LongBits(message.completedAt.low >>> 0, message.completedAt.high >>> 0).toNumber() : message.completedAt;
            return object;
        };

        /**
         * Converts this CompletedQuest to JSON.
         * @function toJSON
         * @memberof game.CompletedQuest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CompletedQuest.prototype.toJSON = function() {
            return CompletedQuest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CompletedQuest
         * @function getTypeUrl
         * @memberof game.CompletedQuest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CompletedQuest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.CompletedQuest";
        };

        return CompletedQuest;
    })();

    game.Quests = (function() {

        /**
         * Properties of a Quests.
         * @typedef {Object} game.Quests.$Properties
         * @property {Object.<string,game.QuestEntry.$Properties>|null} [active] Quests active
         * @property {Object.<string,game.CompletedQuest.$Properties>|null} [completed] Quests completed
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Quests.
         * @memberof game
         * @interface IQuests
         * @augments game.Quests.$Properties
         * @deprecated Use game.Quests.$Properties instead.
         */

        /**
         * Shape of a Quests.
         * @typedef {game.Quests.$Properties} game.Quests.$Shape
         */

        /**
         * Constructs a new Quests.
         * @memberof game
         * @classdesc Represents a Quests.
         * @constructor
         * @param {game.Quests.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Quests = function (properties) {
            this.active = {};
            this.completed = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Quests active.
         * @member {Object.<string,game.QuestEntry.$Properties>} active
         * @memberof game.Quests
         * @instance
         */
        Quests.prototype.active = $util.emptyObject;

        /**
         * Quests completed.
         * @member {Object.<string,game.CompletedQuest.$Properties>} completed
         * @memberof game.Quests
         * @instance
         */
        Quests.prototype.completed = $util.emptyObject;

        /**
         * Creates a new Quests instance using the specified properties.
         * @function create
         * @memberof game.Quests
         * @static
         * @param {game.Quests.$Properties=} [properties] Properties to set
         * @returns {game.Quests} Quests instance
         * @type {{
         *   (properties: game.Quests.$Shape): game.Quests & game.Quests.$Shape;
         *   (properties?: game.Quests.$Properties): game.Quests;
         * }}
         */
        Quests.create = function(properties) {
            return new Quests(properties);
        };

        /**
         * Encodes the specified Quests message. Does not implicitly {@link game.Quests.verify|verify} messages.
         * @function encode
         * @memberof game.Quests
         * @static
         * @param {game.Quests.$Properties} message Quests message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Quests.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.active != null && $Object.hasOwnProperty.call(message, "active"))
                for (let keys = $Object.keys(message.active), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.game.QuestEntry.encode(message.active[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.completed != null && $Object.hasOwnProperty.call(message, "completed"))
                for (let keys = $Object.keys(message.completed), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 2, wireType 2 =*/18).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.game.CompletedQuest.encode(message.completed[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Quests message, length delimited. Does not implicitly {@link game.Quests.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Quests
         * @static
         * @param {game.Quests.$Properties} message Quests message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Quests.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Quests message from the specified reader or buffer.
         * @function decode
         * @memberof game.Quests
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Quests & game.Quests.$Shape} Quests
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Quests.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Quests(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (message.active === $util.emptyObject)
                            message.active = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.game.QuestEntry.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.active, key);
                        message.active[key] = value || new $root.game.QuestEntry();
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (message.completed === $util.emptyObject)
                            message.completed = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.game.CompletedQuest.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.completed, key);
                        message.completed[key] = value || new $root.game.CompletedQuest();
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Quests message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Quests
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Quests & game.Quests.$Shape} Quests
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Quests.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Quests message.
         * @function verify
         * @memberof game.Quests
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Quests.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.active != null && $Object.hasOwnProperty.call(message, "active")) {
                if (!$util.isObject(message.active))
                    return "active: object expected";
                let key = $Object.keys(message.active);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.game.QuestEntry.verify(message.active[key[i]], _depth + 1);
                    if (error)
                        return "active." + error;
                }
            }
            if (message.completed != null && $Object.hasOwnProperty.call(message, "completed")) {
                if (!$util.isObject(message.completed))
                    return "completed: object expected";
                let key = $Object.keys(message.completed);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.game.CompletedQuest.verify(message.completed[key[i]], _depth + 1);
                    if (error)
                        return "completed." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Quests message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Quests
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Quests} Quests
         */
        Quests.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Quests)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Quests: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Quests();
            if (object.active) {
                if (!$util.isObject(object.active))
                    throw $TypeError(".game.Quests.active: object expected");
                message.active = {};
                for (let keys = $Object.keys(object.active), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.active, keys[i]);
                    if (!$util.isObject(object.active[keys[i]]))
                        throw $TypeError(".game.Quests.active: object expected");
                    message.active[keys[i]] = $root.game.QuestEntry.fromObject(object.active[keys[i]], _depth + 1);
                }
            }
            if (object.completed) {
                if (!$util.isObject(object.completed))
                    throw $TypeError(".game.Quests.completed: object expected");
                message.completed = {};
                for (let keys = $Object.keys(object.completed), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.completed, keys[i]);
                    if (!$util.isObject(object.completed[keys[i]]))
                        throw $TypeError(".game.Quests.completed: object expected");
                    message.completed[keys[i]] = $root.game.CompletedQuest.fromObject(object.completed[keys[i]], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Quests message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Quests
         * @static
         * @param {game.Quests} message Quests
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Quests.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults) {
                object.active = {};
                object.completed = {};
            }
            let keys2;
            if (message.active && (keys2 = $Object.keys(message.active)).length) {
                object.active = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.active, keys2[j]);
                    object.active[keys2[j]] = $root.game.QuestEntry.toObject(message.active[keys2[j]], options, _depth + 1);
                }
            }
            if (message.completed && (keys2 = $Object.keys(message.completed)).length) {
                object.completed = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.completed, keys2[j]);
                    object.completed[keys2[j]] = $root.game.CompletedQuest.toObject(message.completed[keys2[j]], options, _depth + 1);
                }
            }
            return object;
        };

        /**
         * Converts this Quests to JSON.
         * @function toJSON
         * @memberof game.Quests
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Quests.prototype.toJSON = function() {
            return Quests.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Quests
         * @function getTypeUrl
         * @memberof game.Quests
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Quests.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Quests";
        };

        return Quests;
    })();

    game.Rewards = (function() {

        /**
         * Properties of a Rewards.
         * @typedef {Object} game.Rewards.$Properties
         * @property {number|Long|null} [gold] Rewards gold
         * @property {number|Long|null} [exp] Rewards exp
         * @property {Array.<game.Item.$Properties>|null} [items] Rewards items
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Rewards.
         * @memberof game
         * @interface IRewards
         * @augments game.Rewards.$Properties
         * @deprecated Use game.Rewards.$Properties instead.
         */

        /**
         * Shape of a Rewards.
         * @typedef {game.Rewards.$Properties} game.Rewards.$Shape
         */

        /**
         * Constructs a new Rewards.
         * @memberof game
         * @classdesc Represents a Rewards.
         * @constructor
         * @param {game.Rewards.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Rewards = function (properties) {
            this.items = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Rewards gold.
         * @member {number|Long} gold
         * @memberof game.Rewards
         * @instance
         */
        Rewards.prototype.gold = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Rewards exp.
         * @member {number|Long} exp
         * @memberof game.Rewards
         * @instance
         */
        Rewards.prototype.exp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Rewards items.
         * @member {Array.<game.Item.$Properties>} items
         * @memberof game.Rewards
         * @instance
         */
        Rewards.prototype.items = $util.emptyArray;

        /**
         * Creates a new Rewards instance using the specified properties.
         * @function create
         * @memberof game.Rewards
         * @static
         * @param {game.Rewards.$Properties=} [properties] Properties to set
         * @returns {game.Rewards} Rewards instance
         * @type {{
         *   (properties: game.Rewards.$Shape): game.Rewards & game.Rewards.$Shape;
         *   (properties?: game.Rewards.$Properties): game.Rewards;
         * }}
         */
        Rewards.create = function(properties) {
            return new Rewards(properties);
        };

        /**
         * Encodes the specified Rewards message. Does not implicitly {@link game.Rewards.verify|verify} messages.
         * @function encode
         * @memberof game.Rewards
         * @static
         * @param {game.Rewards.$Properties} message Rewards message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Rewards.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.gold != null && $Object.hasOwnProperty.call(message, "gold") && (typeof message.gold === "object" ? message.gold.low || message.gold.high : message.gold !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.gold);
            if (message.exp != null && $Object.hasOwnProperty.call(message, "exp") && (typeof message.exp === "object" ? message.exp.low || message.exp.high : message.exp !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.exp);
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.game.Item.encode(message.items[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Rewards message, length delimited. Does not implicitly {@link game.Rewards.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Rewards
         * @static
         * @param {game.Rewards.$Properties} message Rewards message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Rewards.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Rewards message from the specified reader or buffer.
         * @function decode
         * @memberof game.Rewards
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Rewards & game.Rewards.$Shape} Rewards
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Rewards.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Rewards(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.gold = value;
                        else
                            delete message.gold;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.exp = value;
                        else
                            delete message.exp;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.game.Item.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Rewards message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Rewards
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Rewards & game.Rewards.$Shape} Rewards
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Rewards.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Rewards message.
         * @function verify
         * @memberof game.Rewards
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Rewards.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.gold != null && $Object.hasOwnProperty.call(message, "gold"))
                if (!$util.isInteger(message.gold) && !(message.gold && $util.isInteger(message.gold.low) && $util.isInteger(message.gold.high)))
                    return "gold: integer|Long expected";
            if (message.exp != null && $Object.hasOwnProperty.call(message, "exp"))
                if (!$util.isInteger(message.exp) && !(message.exp && $util.isInteger(message.exp.low) && $util.isInteger(message.exp.high)))
                    return "exp: integer|Long expected";
            if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                if (!$Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.game.Item.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Rewards message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Rewards
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Rewards} Rewards
         */
        Rewards.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Rewards)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Rewards: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Rewards();
            if (object.gold != null)
                if (typeof object.gold === "object" ? object.gold.low || object.gold.high : $Number(object.gold) !== 0)
                    if ($util.Long)
                        message.gold = $util.Long.fromValue(object.gold, false);
                    else if (typeof object.gold === "string")
                        message.gold = $parseInt(object.gold, 10);
                    else if (typeof object.gold === "number")
                        message.gold = object.gold;
                    else if (typeof object.gold === "object")
                        message.gold = new $util.LongBits(object.gold.low >>> 0, object.gold.high >>> 0).toNumber();
            if (object.exp != null)
                if (typeof object.exp === "object" ? object.exp.low || object.exp.high : $Number(object.exp) !== 0)
                    if ($util.Long)
                        message.exp = $util.Long.fromValue(object.exp, false);
                    else if (typeof object.exp === "string")
                        message.exp = $parseInt(object.exp, 10);
                    else if (typeof object.exp === "number")
                        message.exp = object.exp;
                    else if (typeof object.exp === "object")
                        message.exp = new $util.LongBits(object.exp.low >>> 0, object.exp.high >>> 0).toNumber();
            if (object.items) {
                if (!$Array.isArray(object.items))
                    throw $TypeError(".game.Rewards.items: array expected");
                message.items = $Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (!$util.isObject(object.items[i]))
                        throw $TypeError(".game.Rewards.items: object expected");
                    message.items[i] = $root.game.Item.fromObject(object.items[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Rewards message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Rewards
         * @static
         * @param {game.Rewards} message Rewards
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Rewards.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.gold = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gold = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.exp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.exp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.gold != null && $Object.hasOwnProperty.call(message, "gold"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gold = typeof message.gold === "number" ? $BigInt(message.gold) : $util.Long.fromBits(message.gold.low >>> 0, message.gold.high >>> 0, false).toBigInt();
                else if (typeof message.gold === "number")
                    object.gold = options.longs === $String ? $String(message.gold) : message.gold;
                else
                    object.gold = options.longs === $String ? $util.Long.prototype.toString.call(message.gold) : options.longs === $Number ? new $util.LongBits(message.gold.low >>> 0, message.gold.high >>> 0).toNumber() : message.gold;
            if (message.exp != null && $Object.hasOwnProperty.call(message, "exp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.exp = typeof message.exp === "number" ? $BigInt(message.exp) : $util.Long.fromBits(message.exp.low >>> 0, message.exp.high >>> 0, false).toBigInt();
                else if (typeof message.exp === "number")
                    object.exp = options.longs === $String ? $String(message.exp) : message.exp;
                else
                    object.exp = options.longs === $String ? $util.Long.prototype.toString.call(message.exp) : options.longs === $Number ? new $util.LongBits(message.exp.low >>> 0, message.exp.high >>> 0).toNumber() : message.exp;
            if (message.items && message.items.length) {
                object.items = $Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.game.Item.toObject(message.items[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this Rewards to JSON.
         * @function toJSON
         * @memberof game.Rewards
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Rewards.prototype.toJSON = function() {
            return Rewards.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Rewards
         * @function getTypeUrl
         * @memberof game.Rewards
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Rewards.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Rewards";
        };

        return Rewards;
    })();

    game.JoinRequest = (function() {

        /**
         * Properties of a JoinRequest.
         * @typedef {Object} game.JoinRequest.$Properties
         * @property {string|null} [nickname] JoinRequest nickname
         * @property {number|null} [protocolVersion] JoinRequest protocolVersion
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a JoinRequest.
         * @memberof game
         * @interface IJoinRequest
         * @augments game.JoinRequest.$Properties
         * @deprecated Use game.JoinRequest.$Properties instead.
         */

        /**
         * Shape of a JoinRequest.
         * @typedef {game.JoinRequest.$Properties} game.JoinRequest.$Shape
         */

        /**
         * Constructs a new JoinRequest.
         * @memberof game
         * @classdesc Represents a JoinRequest.
         * @constructor
         * @param {game.JoinRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const JoinRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * JoinRequest nickname.
         * @member {string} nickname
         * @memberof game.JoinRequest
         * @instance
         */
        JoinRequest.prototype.nickname = "";

        /**
         * JoinRequest protocolVersion.
         * @member {number} protocolVersion
         * @memberof game.JoinRequest
         * @instance
         */
        JoinRequest.prototype.protocolVersion = 0;

        /**
         * Creates a new JoinRequest instance using the specified properties.
         * @function create
         * @memberof game.JoinRequest
         * @static
         * @param {game.JoinRequest.$Properties=} [properties] Properties to set
         * @returns {game.JoinRequest} JoinRequest instance
         * @type {{
         *   (properties: game.JoinRequest.$Shape): game.JoinRequest & game.JoinRequest.$Shape;
         *   (properties?: game.JoinRequest.$Properties): game.JoinRequest;
         * }}
         */
        JoinRequest.create = function(properties) {
            return new JoinRequest(properties);
        };

        /**
         * Encodes the specified JoinRequest message. Does not implicitly {@link game.JoinRequest.verify|verify} messages.
         * @function encode
         * @memberof game.JoinRequest
         * @static
         * @param {game.JoinRequest.$Properties} message JoinRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JoinRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nickname != null && $Object.hasOwnProperty.call(message, "nickname") && message.nickname !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.nickname);
            if (message.protocolVersion != null && $Object.hasOwnProperty.call(message, "protocolVersion") && message.protocolVersion !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.protocolVersion);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified JoinRequest message, length delimited. Does not implicitly {@link game.JoinRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.JoinRequest
         * @static
         * @param {game.JoinRequest.$Properties} message JoinRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JoinRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a JoinRequest message from the specified reader or buffer.
         * @function decode
         * @memberof game.JoinRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.JoinRequest & game.JoinRequest.$Shape} JoinRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JoinRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.JoinRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.nickname = value;
                        else
                            delete message.nickname;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.protocolVersion = value;
                        else
                            delete message.protocolVersion;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a JoinRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.JoinRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.JoinRequest & game.JoinRequest.$Shape} JoinRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JoinRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JoinRequest message.
         * @function verify
         * @memberof game.JoinRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JoinRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nickname != null && $Object.hasOwnProperty.call(message, "nickname"))
                if (!$util.isString(message.nickname))
                    return "nickname: string expected";
            if (message.protocolVersion != null && $Object.hasOwnProperty.call(message, "protocolVersion"))
                if (!$util.isInteger(message.protocolVersion))
                    return "protocolVersion: integer expected";
            return null;
        };

        /**
         * Creates a JoinRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.JoinRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.JoinRequest} JoinRequest
         */
        JoinRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.JoinRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.JoinRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.JoinRequest();
            if (object.nickname != null)
                if (typeof object.nickname !== "string" || object.nickname.length)
                    message.nickname = $String(object.nickname);
            if (object.protocolVersion != null)
                if ($Number(object.protocolVersion) !== 0)
                    message.protocolVersion = object.protocolVersion >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a JoinRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.JoinRequest
         * @static
         * @param {game.JoinRequest} message JoinRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JoinRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.nickname = "";
                object.protocolVersion = 0;
            }
            if (message.nickname != null && $Object.hasOwnProperty.call(message, "nickname"))
                object.nickname = message.nickname;
            if (message.protocolVersion != null && $Object.hasOwnProperty.call(message, "protocolVersion"))
                object.protocolVersion = message.protocolVersion;
            return object;
        };

        /**
         * Converts this JoinRequest to JSON.
         * @function toJSON
         * @memberof game.JoinRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JoinRequest.prototype.toJSON = function() {
            return JoinRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for JoinRequest
         * @function getTypeUrl
         * @memberof game.JoinRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        JoinRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.JoinRequest";
        };

        return JoinRequest;
    })();

    game.ResumeRequest = (function() {

        /**
         * Properties of a ResumeRequest.
         * @typedef {Object} game.ResumeRequest.$Properties
         * @property {string|null} [sessionId] ResumeRequest sessionId
         * @property {string|null} [nickname] ResumeRequest nickname
         * @property {number|null} [protocolVersion] ResumeRequest protocolVersion
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ResumeRequest.
         * @memberof game
         * @interface IResumeRequest
         * @augments game.ResumeRequest.$Properties
         * @deprecated Use game.ResumeRequest.$Properties instead.
         */

        /**
         * Shape of a ResumeRequest.
         * @typedef {game.ResumeRequest.$Properties} game.ResumeRequest.$Shape
         */

        /**
         * Constructs a new ResumeRequest.
         * @memberof game
         * @classdesc Represents a ResumeRequest.
         * @constructor
         * @param {game.ResumeRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ResumeRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ResumeRequest sessionId.
         * @member {string} sessionId
         * @memberof game.ResumeRequest
         * @instance
         */
        ResumeRequest.prototype.sessionId = "";

        /**
         * ResumeRequest nickname.
         * @member {string} nickname
         * @memberof game.ResumeRequest
         * @instance
         */
        ResumeRequest.prototype.nickname = "";

        /**
         * ResumeRequest protocolVersion.
         * @member {number} protocolVersion
         * @memberof game.ResumeRequest
         * @instance
         */
        ResumeRequest.prototype.protocolVersion = 0;

        /**
         * Creates a new ResumeRequest instance using the specified properties.
         * @function create
         * @memberof game.ResumeRequest
         * @static
         * @param {game.ResumeRequest.$Properties=} [properties] Properties to set
         * @returns {game.ResumeRequest} ResumeRequest instance
         * @type {{
         *   (properties: game.ResumeRequest.$Shape): game.ResumeRequest & game.ResumeRequest.$Shape;
         *   (properties?: game.ResumeRequest.$Properties): game.ResumeRequest;
         * }}
         */
        ResumeRequest.create = function(properties) {
            return new ResumeRequest(properties);
        };

        /**
         * Encodes the specified ResumeRequest message. Does not implicitly {@link game.ResumeRequest.verify|verify} messages.
         * @function encode
         * @memberof game.ResumeRequest
         * @static
         * @param {game.ResumeRequest.$Properties} message ResumeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResumeRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sessionId != null && $Object.hasOwnProperty.call(message, "sessionId") && message.sessionId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.sessionId);
            if (message.nickname != null && $Object.hasOwnProperty.call(message, "nickname") && message.nickname !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.nickname);
            if (message.protocolVersion != null && $Object.hasOwnProperty.call(message, "protocolVersion") && message.protocolVersion !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.protocolVersion);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ResumeRequest message, length delimited. Does not implicitly {@link game.ResumeRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.ResumeRequest
         * @static
         * @param {game.ResumeRequest.$Properties} message ResumeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResumeRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ResumeRequest message from the specified reader or buffer.
         * @function decode
         * @memberof game.ResumeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.ResumeRequest & game.ResumeRequest.$Shape} ResumeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResumeRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.ResumeRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.sessionId = value;
                        else
                            delete message.sessionId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.nickname = value;
                        else
                            delete message.nickname;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.protocolVersion = value;
                        else
                            delete message.protocolVersion;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ResumeRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.ResumeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.ResumeRequest & game.ResumeRequest.$Shape} ResumeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResumeRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ResumeRequest message.
         * @function verify
         * @memberof game.ResumeRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ResumeRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sessionId != null && $Object.hasOwnProperty.call(message, "sessionId"))
                if (!$util.isString(message.sessionId))
                    return "sessionId: string expected";
            if (message.nickname != null && $Object.hasOwnProperty.call(message, "nickname"))
                if (!$util.isString(message.nickname))
                    return "nickname: string expected";
            if (message.protocolVersion != null && $Object.hasOwnProperty.call(message, "protocolVersion"))
                if (!$util.isInteger(message.protocolVersion))
                    return "protocolVersion: integer expected";
            return null;
        };

        /**
         * Creates a ResumeRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.ResumeRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.ResumeRequest} ResumeRequest
         */
        ResumeRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.ResumeRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.ResumeRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.ResumeRequest();
            if (object.sessionId != null)
                if (typeof object.sessionId !== "string" || object.sessionId.length)
                    message.sessionId = $String(object.sessionId);
            if (object.nickname != null)
                if (typeof object.nickname !== "string" || object.nickname.length)
                    message.nickname = $String(object.nickname);
            if (object.protocolVersion != null)
                if ($Number(object.protocolVersion) !== 0)
                    message.protocolVersion = object.protocolVersion >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a ResumeRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.ResumeRequest
         * @static
         * @param {game.ResumeRequest} message ResumeRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ResumeRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.sessionId = "";
                object.nickname = "";
                object.protocolVersion = 0;
            }
            if (message.sessionId != null && $Object.hasOwnProperty.call(message, "sessionId"))
                object.sessionId = message.sessionId;
            if (message.nickname != null && $Object.hasOwnProperty.call(message, "nickname"))
                object.nickname = message.nickname;
            if (message.protocolVersion != null && $Object.hasOwnProperty.call(message, "protocolVersion"))
                object.protocolVersion = message.protocolVersion;
            return object;
        };

        /**
         * Converts this ResumeRequest to JSON.
         * @function toJSON
         * @memberof game.ResumeRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ResumeRequest.prototype.toJSON = function() {
            return ResumeRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ResumeRequest
         * @function getTypeUrl
         * @memberof game.ResumeRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ResumeRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.ResumeRequest";
        };

        return ResumeRequest;
    })();

    game.ToggleAutoCombat = (function() {

        /**
         * Properties of a ToggleAutoCombat.
         * @typedef {Object} game.ToggleAutoCombat.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ToggleAutoCombat.
         * @memberof game
         * @interface IToggleAutoCombat
         * @augments game.ToggleAutoCombat.$Properties
         * @deprecated Use game.ToggleAutoCombat.$Properties instead.
         */

        /**
         * Shape of a ToggleAutoCombat.
         * @typedef {game.ToggleAutoCombat.$Properties} game.ToggleAutoCombat.$Shape
         */

        /**
         * Constructs a new ToggleAutoCombat.
         * @memberof game
         * @classdesc Represents a ToggleAutoCombat.
         * @constructor
         * @param {game.ToggleAutoCombat.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ToggleAutoCombat = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new ToggleAutoCombat instance using the specified properties.
         * @function create
         * @memberof game.ToggleAutoCombat
         * @static
         * @param {game.ToggleAutoCombat.$Properties=} [properties] Properties to set
         * @returns {game.ToggleAutoCombat} ToggleAutoCombat instance
         * @type {{
         *   (properties: game.ToggleAutoCombat.$Shape): game.ToggleAutoCombat & game.ToggleAutoCombat.$Shape;
         *   (properties?: game.ToggleAutoCombat.$Properties): game.ToggleAutoCombat;
         * }}
         */
        ToggleAutoCombat.create = function(properties) {
            return new ToggleAutoCombat(properties);
        };

        /**
         * Encodes the specified ToggleAutoCombat message. Does not implicitly {@link game.ToggleAutoCombat.verify|verify} messages.
         * @function encode
         * @memberof game.ToggleAutoCombat
         * @static
         * @param {game.ToggleAutoCombat.$Properties} message ToggleAutoCombat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ToggleAutoCombat.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ToggleAutoCombat message, length delimited. Does not implicitly {@link game.ToggleAutoCombat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.ToggleAutoCombat
         * @static
         * @param {game.ToggleAutoCombat.$Properties} message ToggleAutoCombat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ToggleAutoCombat.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ToggleAutoCombat message from the specified reader or buffer.
         * @function decode
         * @memberof game.ToggleAutoCombat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.ToggleAutoCombat & game.ToggleAutoCombat.$Shape} ToggleAutoCombat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ToggleAutoCombat.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.ToggleAutoCombat();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ToggleAutoCombat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.ToggleAutoCombat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.ToggleAutoCombat & game.ToggleAutoCombat.$Shape} ToggleAutoCombat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ToggleAutoCombat.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ToggleAutoCombat message.
         * @function verify
         * @memberof game.ToggleAutoCombat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ToggleAutoCombat.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a ToggleAutoCombat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.ToggleAutoCombat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.ToggleAutoCombat} ToggleAutoCombat
         */
        ToggleAutoCombat.fromObject = function (object, _depth) {
            if (object instanceof $root.game.ToggleAutoCombat)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.ToggleAutoCombat: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.game.ToggleAutoCombat();
        };

        /**
         * Creates a plain object from a ToggleAutoCombat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.ToggleAutoCombat
         * @static
         * @param {game.ToggleAutoCombat} message ToggleAutoCombat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ToggleAutoCombat.toObject = function () {
            return {};
        };

        /**
         * Converts this ToggleAutoCombat to JSON.
         * @function toJSON
         * @memberof game.ToggleAutoCombat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ToggleAutoCombat.prototype.toJSON = function() {
            return ToggleAutoCombat.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ToggleAutoCombat
         * @function getTypeUrl
         * @memberof game.ToggleAutoCombat
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ToggleAutoCombat.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.ToggleAutoCombat";
        };

        return ToggleAutoCombat;
    })();

    game.ReviveRequest = (function() {

        /**
         * Properties of a ReviveRequest.
         * @typedef {Object} game.ReviveRequest.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ReviveRequest.
         * @memberof game
         * @interface IReviveRequest
         * @augments game.ReviveRequest.$Properties
         * @deprecated Use game.ReviveRequest.$Properties instead.
         */

        /**
         * Shape of a ReviveRequest.
         * @typedef {game.ReviveRequest.$Properties} game.ReviveRequest.$Shape
         */

        /**
         * Constructs a new ReviveRequest.
         * @memberof game
         * @classdesc Represents a ReviveRequest.
         * @constructor
         * @param {game.ReviveRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ReviveRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new ReviveRequest instance using the specified properties.
         * @function create
         * @memberof game.ReviveRequest
         * @static
         * @param {game.ReviveRequest.$Properties=} [properties] Properties to set
         * @returns {game.ReviveRequest} ReviveRequest instance
         * @type {{
         *   (properties: game.ReviveRequest.$Shape): game.ReviveRequest & game.ReviveRequest.$Shape;
         *   (properties?: game.ReviveRequest.$Properties): game.ReviveRequest;
         * }}
         */
        ReviveRequest.create = function(properties) {
            return new ReviveRequest(properties);
        };

        /**
         * Encodes the specified ReviveRequest message. Does not implicitly {@link game.ReviveRequest.verify|verify} messages.
         * @function encode
         * @memberof game.ReviveRequest
         * @static
         * @param {game.ReviveRequest.$Properties} message ReviveRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReviveRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ReviveRequest message, length delimited. Does not implicitly {@link game.ReviveRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.ReviveRequest
         * @static
         * @param {game.ReviveRequest.$Properties} message ReviveRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReviveRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ReviveRequest message from the specified reader or buffer.
         * @function decode
         * @memberof game.ReviveRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.ReviveRequest & game.ReviveRequest.$Shape} ReviveRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReviveRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.ReviveRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ReviveRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.ReviveRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.ReviveRequest & game.ReviveRequest.$Shape} ReviveRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReviveRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReviveRequest message.
         * @function verify
         * @memberof game.ReviveRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReviveRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a ReviveRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.ReviveRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.ReviveRequest} ReviveRequest
         */
        ReviveRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.ReviveRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.ReviveRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.game.ReviveRequest();
        };

        /**
         * Creates a plain object from a ReviveRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.ReviveRequest
         * @static
         * @param {game.ReviveRequest} message ReviveRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReviveRequest.toObject = function () {
            return {};
        };

        /**
         * Converts this ReviveRequest to JSON.
         * @function toJSON
         * @memberof game.ReviveRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReviveRequest.prototype.toJSON = function() {
            return ReviveRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ReviveRequest
         * @function getTypeUrl
         * @memberof game.ReviveRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ReviveRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.ReviveRequest";
        };

        return ReviveRequest;
    })();

    game.SpendSkillPoint = (function() {

        /**
         * Properties of a SpendSkillPoint.
         * @typedef {Object} game.SpendSkillPoint.$Properties
         * @property {string|null} [skillId] SpendSkillPoint skillId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SpendSkillPoint.
         * @memberof game
         * @interface ISpendSkillPoint
         * @augments game.SpendSkillPoint.$Properties
         * @deprecated Use game.SpendSkillPoint.$Properties instead.
         */

        /**
         * Shape of a SpendSkillPoint.
         * @typedef {game.SpendSkillPoint.$Properties} game.SpendSkillPoint.$Shape
         */

        /**
         * Constructs a new SpendSkillPoint.
         * @memberof game
         * @classdesc Represents a SpendSkillPoint.
         * @constructor
         * @param {game.SpendSkillPoint.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SpendSkillPoint = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SpendSkillPoint skillId.
         * @member {string} skillId
         * @memberof game.SpendSkillPoint
         * @instance
         */
        SpendSkillPoint.prototype.skillId = "";

        /**
         * Creates a new SpendSkillPoint instance using the specified properties.
         * @function create
         * @memberof game.SpendSkillPoint
         * @static
         * @param {game.SpendSkillPoint.$Properties=} [properties] Properties to set
         * @returns {game.SpendSkillPoint} SpendSkillPoint instance
         * @type {{
         *   (properties: game.SpendSkillPoint.$Shape): game.SpendSkillPoint & game.SpendSkillPoint.$Shape;
         *   (properties?: game.SpendSkillPoint.$Properties): game.SpendSkillPoint;
         * }}
         */
        SpendSkillPoint.create = function(properties) {
            return new SpendSkillPoint(properties);
        };

        /**
         * Encodes the specified SpendSkillPoint message. Does not implicitly {@link game.SpendSkillPoint.verify|verify} messages.
         * @function encode
         * @memberof game.SpendSkillPoint
         * @static
         * @param {game.SpendSkillPoint.$Properties} message SpendSkillPoint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpendSkillPoint.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.skillId != null && $Object.hasOwnProperty.call(message, "skillId") && message.skillId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.skillId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SpendSkillPoint message, length delimited. Does not implicitly {@link game.SpendSkillPoint.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SpendSkillPoint
         * @static
         * @param {game.SpendSkillPoint.$Properties} message SpendSkillPoint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpendSkillPoint.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SpendSkillPoint message from the specified reader or buffer.
         * @function decode
         * @memberof game.SpendSkillPoint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SpendSkillPoint & game.SpendSkillPoint.$Shape} SpendSkillPoint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpendSkillPoint.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.SpendSkillPoint(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.skillId = value;
                        else
                            delete message.skillId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SpendSkillPoint message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SpendSkillPoint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SpendSkillPoint & game.SpendSkillPoint.$Shape} SpendSkillPoint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpendSkillPoint.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpendSkillPoint message.
         * @function verify
         * @memberof game.SpendSkillPoint
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpendSkillPoint.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.skillId != null && $Object.hasOwnProperty.call(message, "skillId"))
                if (!$util.isString(message.skillId))
                    return "skillId: string expected";
            return null;
        };

        /**
         * Creates a SpendSkillPoint message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SpendSkillPoint
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SpendSkillPoint} SpendSkillPoint
         */
        SpendSkillPoint.fromObject = function (object, _depth) {
            if (object instanceof $root.game.SpendSkillPoint)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.SpendSkillPoint: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.SpendSkillPoint();
            if (object.skillId != null)
                if (typeof object.skillId !== "string" || object.skillId.length)
                    message.skillId = $String(object.skillId);
            return message;
        };

        /**
         * Creates a plain object from a SpendSkillPoint message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SpendSkillPoint
         * @static
         * @param {game.SpendSkillPoint} message SpendSkillPoint
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpendSkillPoint.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.skillId = "";
            if (message.skillId != null && $Object.hasOwnProperty.call(message, "skillId"))
                object.skillId = message.skillId;
            return object;
        };

        /**
         * Converts this SpendSkillPoint to JSON.
         * @function toJSON
         * @memberof game.SpendSkillPoint
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpendSkillPoint.prototype.toJSON = function() {
            return SpendSkillPoint.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SpendSkillPoint
         * @function getTypeUrl
         * @memberof game.SpendSkillPoint
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SpendSkillPoint.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.SpendSkillPoint";
        };

        return SpendSkillPoint;
    })();

    game.LevelUpRequest = (function() {

        /**
         * Properties of a LevelUpRequest.
         * @typedef {Object} game.LevelUpRequest.$Properties
         * @property {game.Stats.$Properties|null} [bonuses] LevelUpRequest bonuses
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a LevelUpRequest.
         * @memberof game
         * @interface ILevelUpRequest
         * @augments game.LevelUpRequest.$Properties
         * @deprecated Use game.LevelUpRequest.$Properties instead.
         */

        /**
         * Shape of a LevelUpRequest.
         * @typedef {game.LevelUpRequest.$Properties} game.LevelUpRequest.$Shape
         */

        /**
         * Constructs a new LevelUpRequest.
         * @memberof game
         * @classdesc Represents a LevelUpRequest.
         * @constructor
         * @param {game.LevelUpRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const LevelUpRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LevelUpRequest bonuses.
         * @member {game.Stats.$Properties|null|undefined} bonuses
         * @memberof game.LevelUpRequest
         * @instance
         */
        LevelUpRequest.prototype.bonuses = null;

        /**
         * Creates a new LevelUpRequest instance using the specified properties.
         * @function create
         * @memberof game.LevelUpRequest
         * @static
         * @param {game.LevelUpRequest.$Properties=} [properties] Properties to set
         * @returns {game.LevelUpRequest} LevelUpRequest instance
         * @type {{
         *   (properties: game.LevelUpRequest.$Shape): game.LevelUpRequest & game.LevelUpRequest.$Shape;
         *   (properties?: game.LevelUpRequest.$Properties): game.LevelUpRequest;
         * }}
         */
        LevelUpRequest.create = function(properties) {
            return new LevelUpRequest(properties);
        };

        /**
         * Encodes the specified LevelUpRequest message. Does not implicitly {@link game.LevelUpRequest.verify|verify} messages.
         * @function encode
         * @memberof game.LevelUpRequest
         * @static
         * @param {game.LevelUpRequest.$Properties} message LevelUpRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LevelUpRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.bonuses != null && $Object.hasOwnProperty.call(message, "bonuses"))
                $root.game.Stats.encode(message.bonuses, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LevelUpRequest message, length delimited. Does not implicitly {@link game.LevelUpRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.LevelUpRequest
         * @static
         * @param {game.LevelUpRequest.$Properties} message LevelUpRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LevelUpRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a LevelUpRequest message from the specified reader or buffer.
         * @function decode
         * @memberof game.LevelUpRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.LevelUpRequest & game.LevelUpRequest.$Shape} LevelUpRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LevelUpRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.LevelUpRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.bonuses = $root.game.Stats.decode(reader, reader.uint32(), $undefined, _depth + 1, message.bonuses);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LevelUpRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.LevelUpRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.LevelUpRequest & game.LevelUpRequest.$Shape} LevelUpRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LevelUpRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LevelUpRequest message.
         * @function verify
         * @memberof game.LevelUpRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LevelUpRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.bonuses != null && $Object.hasOwnProperty.call(message, "bonuses")) {
                let error = $root.game.Stats.verify(message.bonuses, _depth + 1);
                if (error)
                    return "bonuses." + error;
            }
            return null;
        };

        /**
         * Creates a LevelUpRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.LevelUpRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.LevelUpRequest} LevelUpRequest
         */
        LevelUpRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.LevelUpRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.LevelUpRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.LevelUpRequest();
            if (object.bonuses != null) {
                if (!$util.isObject(object.bonuses))
                    throw $TypeError(".game.LevelUpRequest.bonuses: object expected");
                message.bonuses = $root.game.Stats.fromObject(object.bonuses, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a LevelUpRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.LevelUpRequest
         * @static
         * @param {game.LevelUpRequest} message LevelUpRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LevelUpRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.bonuses = null;
            if (message.bonuses != null && $Object.hasOwnProperty.call(message, "bonuses"))
                object.bonuses = $root.game.Stats.toObject(message.bonuses, options, _depth + 1);
            return object;
        };

        /**
         * Converts this LevelUpRequest to JSON.
         * @function toJSON
         * @memberof game.LevelUpRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LevelUpRequest.prototype.toJSON = function() {
            return LevelUpRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LevelUpRequest
         * @function getTypeUrl
         * @memberof game.LevelUpRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LevelUpRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.LevelUpRequest";
        };

        return LevelUpRequest;
    })();

    game.NavigateRequest = (function() {

        /**
         * Properties of a NavigateRequest.
         * @typedef {Object} game.NavigateRequest.$Properties
         * @property {string|null} [placeId] NavigateRequest placeId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NavigateRequest.
         * @memberof game
         * @interface INavigateRequest
         * @augments game.NavigateRequest.$Properties
         * @deprecated Use game.NavigateRequest.$Properties instead.
         */

        /**
         * Shape of a NavigateRequest.
         * @typedef {game.NavigateRequest.$Properties} game.NavigateRequest.$Shape
         */

        /**
         * Constructs a new NavigateRequest.
         * @memberof game
         * @classdesc Represents a NavigateRequest.
         * @constructor
         * @param {game.NavigateRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NavigateRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * NavigateRequest placeId.
         * @member {string} placeId
         * @memberof game.NavigateRequest
         * @instance
         */
        NavigateRequest.prototype.placeId = "";

        /**
         * Creates a new NavigateRequest instance using the specified properties.
         * @function create
         * @memberof game.NavigateRequest
         * @static
         * @param {game.NavigateRequest.$Properties=} [properties] Properties to set
         * @returns {game.NavigateRequest} NavigateRequest instance
         * @type {{
         *   (properties: game.NavigateRequest.$Shape): game.NavigateRequest & game.NavigateRequest.$Shape;
         *   (properties?: game.NavigateRequest.$Properties): game.NavigateRequest;
         * }}
         */
        NavigateRequest.create = function(properties) {
            return new NavigateRequest(properties);
        };

        /**
         * Encodes the specified NavigateRequest message. Does not implicitly {@link game.NavigateRequest.verify|verify} messages.
         * @function encode
         * @memberof game.NavigateRequest
         * @static
         * @param {game.NavigateRequest.$Properties} message NavigateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NavigateRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId") && message.placeId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.placeId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NavigateRequest message, length delimited. Does not implicitly {@link game.NavigateRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.NavigateRequest
         * @static
         * @param {game.NavigateRequest.$Properties} message NavigateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NavigateRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NavigateRequest message from the specified reader or buffer.
         * @function decode
         * @memberof game.NavigateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.NavigateRequest & game.NavigateRequest.$Shape} NavigateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NavigateRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.NavigateRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.placeId = value;
                        else
                            delete message.placeId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NavigateRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.NavigateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.NavigateRequest & game.NavigateRequest.$Shape} NavigateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NavigateRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NavigateRequest message.
         * @function verify
         * @memberof game.NavigateRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NavigateRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                if (!$util.isString(message.placeId))
                    return "placeId: string expected";
            return null;
        };

        /**
         * Creates a NavigateRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.NavigateRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.NavigateRequest} NavigateRequest
         */
        NavigateRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.NavigateRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.NavigateRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.NavigateRequest();
            if (object.placeId != null)
                if (typeof object.placeId !== "string" || object.placeId.length)
                    message.placeId = $String(object.placeId);
            return message;
        };

        /**
         * Creates a plain object from a NavigateRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.NavigateRequest
         * @static
         * @param {game.NavigateRequest} message NavigateRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NavigateRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.placeId = "";
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                object.placeId = message.placeId;
            return object;
        };

        /**
         * Converts this NavigateRequest to JSON.
         * @function toJSON
         * @memberof game.NavigateRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NavigateRequest.prototype.toJSON = function() {
            return NavigateRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NavigateRequest
         * @function getTypeUrl
         * @memberof game.NavigateRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NavigateRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.NavigateRequest";
        };

        return NavigateRequest;
    })();

    game.BuySocket = (function() {

        /**
         * Properties of a BuySocket.
         * @typedef {Object} game.BuySocket.$Properties
         * @property {string|null} [placeId] BuySocket placeId
         * @property {number|null} [socketIndex] BuySocket socketIndex
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a BuySocket.
         * @memberof game
         * @interface IBuySocket
         * @augments game.BuySocket.$Properties
         * @deprecated Use game.BuySocket.$Properties instead.
         */

        /**
         * Shape of a BuySocket.
         * @typedef {game.BuySocket.$Properties} game.BuySocket.$Shape
         */

        /**
         * Constructs a new BuySocket.
         * @memberof game
         * @classdesc Represents a BuySocket.
         * @constructor
         * @param {game.BuySocket.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const BuySocket = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BuySocket placeId.
         * @member {string} placeId
         * @memberof game.BuySocket
         * @instance
         */
        BuySocket.prototype.placeId = "";

        /**
         * BuySocket socketIndex.
         * @member {number} socketIndex
         * @memberof game.BuySocket
         * @instance
         */
        BuySocket.prototype.socketIndex = 0;

        /**
         * Creates a new BuySocket instance using the specified properties.
         * @function create
         * @memberof game.BuySocket
         * @static
         * @param {game.BuySocket.$Properties=} [properties] Properties to set
         * @returns {game.BuySocket} BuySocket instance
         * @type {{
         *   (properties: game.BuySocket.$Shape): game.BuySocket & game.BuySocket.$Shape;
         *   (properties?: game.BuySocket.$Properties): game.BuySocket;
         * }}
         */
        BuySocket.create = function(properties) {
            return new BuySocket(properties);
        };

        /**
         * Encodes the specified BuySocket message. Does not implicitly {@link game.BuySocket.verify|verify} messages.
         * @function encode
         * @memberof game.BuySocket
         * @static
         * @param {game.BuySocket.$Properties} message BuySocket message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuySocket.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId") && message.placeId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.placeId);
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex") && message.socketIndex !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.socketIndex);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BuySocket message, length delimited. Does not implicitly {@link game.BuySocket.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.BuySocket
         * @static
         * @param {game.BuySocket.$Properties} message BuySocket message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuySocket.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a BuySocket message from the specified reader or buffer.
         * @function decode
         * @memberof game.BuySocket
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.BuySocket & game.BuySocket.$Shape} BuySocket
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuySocket.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.BuySocket(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.placeId = value;
                        else
                            delete message.placeId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.socketIndex = value;
                        else
                            delete message.socketIndex;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BuySocket message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.BuySocket
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.BuySocket & game.BuySocket.$Shape} BuySocket
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuySocket.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BuySocket message.
         * @function verify
         * @memberof game.BuySocket
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BuySocket.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                if (!$util.isString(message.placeId))
                    return "placeId: string expected";
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                if (!$util.isInteger(message.socketIndex))
                    return "socketIndex: integer expected";
            return null;
        };

        /**
         * Creates a BuySocket message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.BuySocket
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.BuySocket} BuySocket
         */
        BuySocket.fromObject = function (object, _depth) {
            if (object instanceof $root.game.BuySocket)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.BuySocket: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.BuySocket();
            if (object.placeId != null)
                if (typeof object.placeId !== "string" || object.placeId.length)
                    message.placeId = $String(object.placeId);
            if (object.socketIndex != null)
                if ($Number(object.socketIndex) !== 0)
                    message.socketIndex = object.socketIndex | 0;
            return message;
        };

        /**
         * Creates a plain object from a BuySocket message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.BuySocket
         * @static
         * @param {game.BuySocket} message BuySocket
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BuySocket.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.placeId = "";
                object.socketIndex = 0;
            }
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                object.placeId = message.placeId;
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                object.socketIndex = message.socketIndex;
            return object;
        };

        /**
         * Converts this BuySocket to JSON.
         * @function toJSON
         * @memberof game.BuySocket
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BuySocket.prototype.toJSON = function() {
            return BuySocket.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BuySocket
         * @function getTypeUrl
         * @memberof game.BuySocket
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BuySocket.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.BuySocket";
        };

        return BuySocket;
    })();

    game.BuildRequest = (function() {

        /**
         * Properties of a BuildRequest.
         * @typedef {Object} game.BuildRequest.$Properties
         * @property {string|null} [placeId] BuildRequest placeId
         * @property {number|null} [socketIndex] BuildRequest socketIndex
         * @property {string|null} [buildingId] BuildRequest buildingId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a BuildRequest.
         * @memberof game
         * @interface IBuildRequest
         * @augments game.BuildRequest.$Properties
         * @deprecated Use game.BuildRequest.$Properties instead.
         */

        /**
         * Shape of a BuildRequest.
         * @typedef {game.BuildRequest.$Properties} game.BuildRequest.$Shape
         */

        /**
         * Constructs a new BuildRequest.
         * @memberof game
         * @classdesc Represents a BuildRequest.
         * @constructor
         * @param {game.BuildRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const BuildRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BuildRequest placeId.
         * @member {string} placeId
         * @memberof game.BuildRequest
         * @instance
         */
        BuildRequest.prototype.placeId = "";

        /**
         * BuildRequest socketIndex.
         * @member {number} socketIndex
         * @memberof game.BuildRequest
         * @instance
         */
        BuildRequest.prototype.socketIndex = 0;

        /**
         * BuildRequest buildingId.
         * @member {string} buildingId
         * @memberof game.BuildRequest
         * @instance
         */
        BuildRequest.prototype.buildingId = "";

        /**
         * Creates a new BuildRequest instance using the specified properties.
         * @function create
         * @memberof game.BuildRequest
         * @static
         * @param {game.BuildRequest.$Properties=} [properties] Properties to set
         * @returns {game.BuildRequest} BuildRequest instance
         * @type {{
         *   (properties: game.BuildRequest.$Shape): game.BuildRequest & game.BuildRequest.$Shape;
         *   (properties?: game.BuildRequest.$Properties): game.BuildRequest;
         * }}
         */
        BuildRequest.create = function(properties) {
            return new BuildRequest(properties);
        };

        /**
         * Encodes the specified BuildRequest message. Does not implicitly {@link game.BuildRequest.verify|verify} messages.
         * @function encode
         * @memberof game.BuildRequest
         * @static
         * @param {game.BuildRequest.$Properties} message BuildRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuildRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId") && message.placeId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.placeId);
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex") && message.socketIndex !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.socketIndex);
            if (message.buildingId != null && $Object.hasOwnProperty.call(message, "buildingId") && message.buildingId !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.buildingId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BuildRequest message, length delimited. Does not implicitly {@link game.BuildRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.BuildRequest
         * @static
         * @param {game.BuildRequest.$Properties} message BuildRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuildRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a BuildRequest message from the specified reader or buffer.
         * @function decode
         * @memberof game.BuildRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.BuildRequest & game.BuildRequest.$Shape} BuildRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuildRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.BuildRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.placeId = value;
                        else
                            delete message.placeId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.socketIndex = value;
                        else
                            delete message.socketIndex;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.buildingId = value;
                        else
                            delete message.buildingId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BuildRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.BuildRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.BuildRequest & game.BuildRequest.$Shape} BuildRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuildRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BuildRequest message.
         * @function verify
         * @memberof game.BuildRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BuildRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                if (!$util.isString(message.placeId))
                    return "placeId: string expected";
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                if (!$util.isInteger(message.socketIndex))
                    return "socketIndex: integer expected";
            if (message.buildingId != null && $Object.hasOwnProperty.call(message, "buildingId"))
                if (!$util.isString(message.buildingId))
                    return "buildingId: string expected";
            return null;
        };

        /**
         * Creates a BuildRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.BuildRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.BuildRequest} BuildRequest
         */
        BuildRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.BuildRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.BuildRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.BuildRequest();
            if (object.placeId != null)
                if (typeof object.placeId !== "string" || object.placeId.length)
                    message.placeId = $String(object.placeId);
            if (object.socketIndex != null)
                if ($Number(object.socketIndex) !== 0)
                    message.socketIndex = object.socketIndex | 0;
            if (object.buildingId != null)
                if (typeof object.buildingId !== "string" || object.buildingId.length)
                    message.buildingId = $String(object.buildingId);
            return message;
        };

        /**
         * Creates a plain object from a BuildRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.BuildRequest
         * @static
         * @param {game.BuildRequest} message BuildRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BuildRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.placeId = "";
                object.socketIndex = 0;
                object.buildingId = "";
            }
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                object.placeId = message.placeId;
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                object.socketIndex = message.socketIndex;
            if (message.buildingId != null && $Object.hasOwnProperty.call(message, "buildingId"))
                object.buildingId = message.buildingId;
            return object;
        };

        /**
         * Converts this BuildRequest to JSON.
         * @function toJSON
         * @memberof game.BuildRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BuildRequest.prototype.toJSON = function() {
            return BuildRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BuildRequest
         * @function getTypeUrl
         * @memberof game.BuildRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BuildRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.BuildRequest";
        };

        return BuildRequest;
    })();

    game.UpgradeBuilding = (function() {

        /**
         * Properties of an UpgradeBuilding.
         * @typedef {Object} game.UpgradeBuilding.$Properties
         * @property {string|null} [placeId] UpgradeBuilding placeId
         * @property {number|null} [socketIndex] UpgradeBuilding socketIndex
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an UpgradeBuilding.
         * @memberof game
         * @interface IUpgradeBuilding
         * @augments game.UpgradeBuilding.$Properties
         * @deprecated Use game.UpgradeBuilding.$Properties instead.
         */

        /**
         * Shape of an UpgradeBuilding.
         * @typedef {game.UpgradeBuilding.$Properties} game.UpgradeBuilding.$Shape
         */

        /**
         * Constructs a new UpgradeBuilding.
         * @memberof game
         * @classdesc Represents an UpgradeBuilding.
         * @constructor
         * @param {game.UpgradeBuilding.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const UpgradeBuilding = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * UpgradeBuilding placeId.
         * @member {string} placeId
         * @memberof game.UpgradeBuilding
         * @instance
         */
        UpgradeBuilding.prototype.placeId = "";

        /**
         * UpgradeBuilding socketIndex.
         * @member {number} socketIndex
         * @memberof game.UpgradeBuilding
         * @instance
         */
        UpgradeBuilding.prototype.socketIndex = 0;

        /**
         * Creates a new UpgradeBuilding instance using the specified properties.
         * @function create
         * @memberof game.UpgradeBuilding
         * @static
         * @param {game.UpgradeBuilding.$Properties=} [properties] Properties to set
         * @returns {game.UpgradeBuilding} UpgradeBuilding instance
         * @type {{
         *   (properties: game.UpgradeBuilding.$Shape): game.UpgradeBuilding & game.UpgradeBuilding.$Shape;
         *   (properties?: game.UpgradeBuilding.$Properties): game.UpgradeBuilding;
         * }}
         */
        UpgradeBuilding.create = function(properties) {
            return new UpgradeBuilding(properties);
        };

        /**
         * Encodes the specified UpgradeBuilding message. Does not implicitly {@link game.UpgradeBuilding.verify|verify} messages.
         * @function encode
         * @memberof game.UpgradeBuilding
         * @static
         * @param {game.UpgradeBuilding.$Properties} message UpgradeBuilding message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UpgradeBuilding.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId") && message.placeId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.placeId);
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex") && message.socketIndex !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.socketIndex);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UpgradeBuilding message, length delimited. Does not implicitly {@link game.UpgradeBuilding.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.UpgradeBuilding
         * @static
         * @param {game.UpgradeBuilding.$Properties} message UpgradeBuilding message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UpgradeBuilding.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an UpgradeBuilding message from the specified reader or buffer.
         * @function decode
         * @memberof game.UpgradeBuilding
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.UpgradeBuilding & game.UpgradeBuilding.$Shape} UpgradeBuilding
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UpgradeBuilding.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.UpgradeBuilding(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.placeId = value;
                        else
                            delete message.placeId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.socketIndex = value;
                        else
                            delete message.socketIndex;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an UpgradeBuilding message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.UpgradeBuilding
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.UpgradeBuilding & game.UpgradeBuilding.$Shape} UpgradeBuilding
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UpgradeBuilding.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an UpgradeBuilding message.
         * @function verify
         * @memberof game.UpgradeBuilding
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UpgradeBuilding.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                if (!$util.isString(message.placeId))
                    return "placeId: string expected";
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                if (!$util.isInteger(message.socketIndex))
                    return "socketIndex: integer expected";
            return null;
        };

        /**
         * Creates an UpgradeBuilding message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.UpgradeBuilding
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.UpgradeBuilding} UpgradeBuilding
         */
        UpgradeBuilding.fromObject = function (object, _depth) {
            if (object instanceof $root.game.UpgradeBuilding)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.UpgradeBuilding: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.UpgradeBuilding();
            if (object.placeId != null)
                if (typeof object.placeId !== "string" || object.placeId.length)
                    message.placeId = $String(object.placeId);
            if (object.socketIndex != null)
                if ($Number(object.socketIndex) !== 0)
                    message.socketIndex = object.socketIndex | 0;
            return message;
        };

        /**
         * Creates a plain object from an UpgradeBuilding message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.UpgradeBuilding
         * @static
         * @param {game.UpgradeBuilding} message UpgradeBuilding
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UpgradeBuilding.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.placeId = "";
                object.socketIndex = 0;
            }
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                object.placeId = message.placeId;
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                object.socketIndex = message.socketIndex;
            return object;
        };

        /**
         * Converts this UpgradeBuilding to JSON.
         * @function toJSON
         * @memberof game.UpgradeBuilding
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UpgradeBuilding.prototype.toJSON = function() {
            return UpgradeBuilding.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for UpgradeBuilding
         * @function getTypeUrl
         * @memberof game.UpgradeBuilding
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        UpgradeBuilding.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.UpgradeBuilding";
        };

        return UpgradeBuilding;
    })();

    game.Demolish = (function() {

        /**
         * Properties of a Demolish.
         * @typedef {Object} game.Demolish.$Properties
         * @property {string|null} [placeId] Demolish placeId
         * @property {number|null} [socketIndex] Demolish socketIndex
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Demolish.
         * @memberof game
         * @interface IDemolish
         * @augments game.Demolish.$Properties
         * @deprecated Use game.Demolish.$Properties instead.
         */

        /**
         * Shape of a Demolish.
         * @typedef {game.Demolish.$Properties} game.Demolish.$Shape
         */

        /**
         * Constructs a new Demolish.
         * @memberof game
         * @classdesc Represents a Demolish.
         * @constructor
         * @param {game.Demolish.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Demolish = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Demolish placeId.
         * @member {string} placeId
         * @memberof game.Demolish
         * @instance
         */
        Demolish.prototype.placeId = "";

        /**
         * Demolish socketIndex.
         * @member {number} socketIndex
         * @memberof game.Demolish
         * @instance
         */
        Demolish.prototype.socketIndex = 0;

        /**
         * Creates a new Demolish instance using the specified properties.
         * @function create
         * @memberof game.Demolish
         * @static
         * @param {game.Demolish.$Properties=} [properties] Properties to set
         * @returns {game.Demolish} Demolish instance
         * @type {{
         *   (properties: game.Demolish.$Shape): game.Demolish & game.Demolish.$Shape;
         *   (properties?: game.Demolish.$Properties): game.Demolish;
         * }}
         */
        Demolish.create = function(properties) {
            return new Demolish(properties);
        };

        /**
         * Encodes the specified Demolish message. Does not implicitly {@link game.Demolish.verify|verify} messages.
         * @function encode
         * @memberof game.Demolish
         * @static
         * @param {game.Demolish.$Properties} message Demolish message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Demolish.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId") && message.placeId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.placeId);
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex") && message.socketIndex !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.socketIndex);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Demolish message, length delimited. Does not implicitly {@link game.Demolish.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Demolish
         * @static
         * @param {game.Demolish.$Properties} message Demolish message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Demolish.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Demolish message from the specified reader or buffer.
         * @function decode
         * @memberof game.Demolish
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Demolish & game.Demolish.$Shape} Demolish
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Demolish.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Demolish(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.placeId = value;
                        else
                            delete message.placeId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.socketIndex = value;
                        else
                            delete message.socketIndex;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Demolish message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Demolish
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Demolish & game.Demolish.$Shape} Demolish
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Demolish.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Demolish message.
         * @function verify
         * @memberof game.Demolish
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Demolish.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                if (!$util.isString(message.placeId))
                    return "placeId: string expected";
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                if (!$util.isInteger(message.socketIndex))
                    return "socketIndex: integer expected";
            return null;
        };

        /**
         * Creates a Demolish message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Demolish
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Demolish} Demolish
         */
        Demolish.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Demolish)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Demolish: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Demolish();
            if (object.placeId != null)
                if (typeof object.placeId !== "string" || object.placeId.length)
                    message.placeId = $String(object.placeId);
            if (object.socketIndex != null)
                if ($Number(object.socketIndex) !== 0)
                    message.socketIndex = object.socketIndex | 0;
            return message;
        };

        /**
         * Creates a plain object from a Demolish message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Demolish
         * @static
         * @param {game.Demolish} message Demolish
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Demolish.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.placeId = "";
                object.socketIndex = 0;
            }
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                object.placeId = message.placeId;
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                object.socketIndex = message.socketIndex;
            return object;
        };

        /**
         * Converts this Demolish to JSON.
         * @function toJSON
         * @memberof game.Demolish
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Demolish.prototype.toJSON = function() {
            return Demolish.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Demolish
         * @function getTypeUrl
         * @memberof game.Demolish
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Demolish.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Demolish";
        };

        return Demolish;
    })();

    game.AssignWorker = (function() {

        /**
         * Properties of an AssignWorker.
         * @typedef {Object} game.AssignWorker.$Properties
         * @property {string|null} [placeId] AssignWorker placeId
         * @property {number|null} [socketIndex] AssignWorker socketIndex
         * @property {string|null} [workerId] AssignWorker workerId
         * @property {string|null} [material] AssignWorker material
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AssignWorker.
         * @memberof game
         * @interface IAssignWorker
         * @augments game.AssignWorker.$Properties
         * @deprecated Use game.AssignWorker.$Properties instead.
         */

        /**
         * Shape of an AssignWorker.
         * @typedef {game.AssignWorker.$Properties} game.AssignWorker.$Shape
         */

        /**
         * Constructs a new AssignWorker.
         * @memberof game
         * @classdesc Represents an AssignWorker.
         * @constructor
         * @param {game.AssignWorker.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AssignWorker = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AssignWorker placeId.
         * @member {string} placeId
         * @memberof game.AssignWorker
         * @instance
         */
        AssignWorker.prototype.placeId = "";

        /**
         * AssignWorker socketIndex.
         * @member {number} socketIndex
         * @memberof game.AssignWorker
         * @instance
         */
        AssignWorker.prototype.socketIndex = 0;

        /**
         * AssignWorker workerId.
         * @member {string} workerId
         * @memberof game.AssignWorker
         * @instance
         */
        AssignWorker.prototype.workerId = "";

        /**
         * AssignWorker material.
         * @member {string} material
         * @memberof game.AssignWorker
         * @instance
         */
        AssignWorker.prototype.material = "";

        /**
         * Creates a new AssignWorker instance using the specified properties.
         * @function create
         * @memberof game.AssignWorker
         * @static
         * @param {game.AssignWorker.$Properties=} [properties] Properties to set
         * @returns {game.AssignWorker} AssignWorker instance
         * @type {{
         *   (properties: game.AssignWorker.$Shape): game.AssignWorker & game.AssignWorker.$Shape;
         *   (properties?: game.AssignWorker.$Properties): game.AssignWorker;
         * }}
         */
        AssignWorker.create = function(properties) {
            return new AssignWorker(properties);
        };

        /**
         * Encodes the specified AssignWorker message. Does not implicitly {@link game.AssignWorker.verify|verify} messages.
         * @function encode
         * @memberof game.AssignWorker
         * @static
         * @param {game.AssignWorker.$Properties} message AssignWorker message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AssignWorker.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId") && message.placeId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.placeId);
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex") && message.socketIndex !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.socketIndex);
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId") && message.workerId !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.workerId);
            if (message.material != null && $Object.hasOwnProperty.call(message, "material") && message.material !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.material);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AssignWorker message, length delimited. Does not implicitly {@link game.AssignWorker.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.AssignWorker
         * @static
         * @param {game.AssignWorker.$Properties} message AssignWorker message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AssignWorker.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AssignWorker message from the specified reader or buffer.
         * @function decode
         * @memberof game.AssignWorker
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.AssignWorker & game.AssignWorker.$Shape} AssignWorker
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AssignWorker.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.AssignWorker(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.placeId = value;
                        else
                            delete message.placeId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.socketIndex = value;
                        else
                            delete message.socketIndex;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.workerId = value;
                        else
                            delete message.workerId;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.material = value;
                        else
                            delete message.material;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AssignWorker message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.AssignWorker
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.AssignWorker & game.AssignWorker.$Shape} AssignWorker
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AssignWorker.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AssignWorker message.
         * @function verify
         * @memberof game.AssignWorker
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AssignWorker.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                if (!$util.isString(message.placeId))
                    return "placeId: string expected";
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                if (!$util.isInteger(message.socketIndex))
                    return "socketIndex: integer expected";
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId"))
                if (!$util.isString(message.workerId))
                    return "workerId: string expected";
            if (message.material != null && $Object.hasOwnProperty.call(message, "material"))
                if (!$util.isString(message.material))
                    return "material: string expected";
            return null;
        };

        /**
         * Creates an AssignWorker message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.AssignWorker
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.AssignWorker} AssignWorker
         */
        AssignWorker.fromObject = function (object, _depth) {
            if (object instanceof $root.game.AssignWorker)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.AssignWorker: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.AssignWorker();
            if (object.placeId != null)
                if (typeof object.placeId !== "string" || object.placeId.length)
                    message.placeId = $String(object.placeId);
            if (object.socketIndex != null)
                if ($Number(object.socketIndex) !== 0)
                    message.socketIndex = object.socketIndex | 0;
            if (object.workerId != null)
                if (typeof object.workerId !== "string" || object.workerId.length)
                    message.workerId = $String(object.workerId);
            if (object.material != null)
                if (typeof object.material !== "string" || object.material.length)
                    message.material = $String(object.material);
            return message;
        };

        /**
         * Creates a plain object from an AssignWorker message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.AssignWorker
         * @static
         * @param {game.AssignWorker} message AssignWorker
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AssignWorker.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.placeId = "";
                object.socketIndex = 0;
                object.workerId = "";
                object.material = "";
            }
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                object.placeId = message.placeId;
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                object.socketIndex = message.socketIndex;
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId"))
                object.workerId = message.workerId;
            if (message.material != null && $Object.hasOwnProperty.call(message, "material"))
                object.material = message.material;
            return object;
        };

        /**
         * Converts this AssignWorker to JSON.
         * @function toJSON
         * @memberof game.AssignWorker
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AssignWorker.prototype.toJSON = function() {
            return AssignWorker.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AssignWorker
         * @function getTypeUrl
         * @memberof game.AssignWorker
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AssignWorker.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.AssignWorker";
        };

        return AssignWorker;
    })();

    game.UnassignWorker = (function() {

        /**
         * Properties of an UnassignWorker.
         * @typedef {Object} game.UnassignWorker.$Properties
         * @property {string|null} [placeId] UnassignWorker placeId
         * @property {number|null} [socketIndex] UnassignWorker socketIndex
         * @property {string|null} [workerId] UnassignWorker workerId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an UnassignWorker.
         * @memberof game
         * @interface IUnassignWorker
         * @augments game.UnassignWorker.$Properties
         * @deprecated Use game.UnassignWorker.$Properties instead.
         */

        /**
         * Shape of an UnassignWorker.
         * @typedef {game.UnassignWorker.$Properties} game.UnassignWorker.$Shape
         */

        /**
         * Constructs a new UnassignWorker.
         * @memberof game
         * @classdesc Represents an UnassignWorker.
         * @constructor
         * @param {game.UnassignWorker.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const UnassignWorker = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * UnassignWorker placeId.
         * @member {string} placeId
         * @memberof game.UnassignWorker
         * @instance
         */
        UnassignWorker.prototype.placeId = "";

        /**
         * UnassignWorker socketIndex.
         * @member {number} socketIndex
         * @memberof game.UnassignWorker
         * @instance
         */
        UnassignWorker.prototype.socketIndex = 0;

        /**
         * UnassignWorker workerId.
         * @member {string} workerId
         * @memberof game.UnassignWorker
         * @instance
         */
        UnassignWorker.prototype.workerId = "";

        /**
         * Creates a new UnassignWorker instance using the specified properties.
         * @function create
         * @memberof game.UnassignWorker
         * @static
         * @param {game.UnassignWorker.$Properties=} [properties] Properties to set
         * @returns {game.UnassignWorker} UnassignWorker instance
         * @type {{
         *   (properties: game.UnassignWorker.$Shape): game.UnassignWorker & game.UnassignWorker.$Shape;
         *   (properties?: game.UnassignWorker.$Properties): game.UnassignWorker;
         * }}
         */
        UnassignWorker.create = function(properties) {
            return new UnassignWorker(properties);
        };

        /**
         * Encodes the specified UnassignWorker message. Does not implicitly {@link game.UnassignWorker.verify|verify} messages.
         * @function encode
         * @memberof game.UnassignWorker
         * @static
         * @param {game.UnassignWorker.$Properties} message UnassignWorker message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UnassignWorker.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId") && message.placeId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.placeId);
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex") && message.socketIndex !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.socketIndex);
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId") && message.workerId !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.workerId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UnassignWorker message, length delimited. Does not implicitly {@link game.UnassignWorker.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.UnassignWorker
         * @static
         * @param {game.UnassignWorker.$Properties} message UnassignWorker message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UnassignWorker.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an UnassignWorker message from the specified reader or buffer.
         * @function decode
         * @memberof game.UnassignWorker
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.UnassignWorker & game.UnassignWorker.$Shape} UnassignWorker
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UnassignWorker.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.UnassignWorker(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.placeId = value;
                        else
                            delete message.placeId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.socketIndex = value;
                        else
                            delete message.socketIndex;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.workerId = value;
                        else
                            delete message.workerId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an UnassignWorker message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.UnassignWorker
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.UnassignWorker & game.UnassignWorker.$Shape} UnassignWorker
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UnassignWorker.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an UnassignWorker message.
         * @function verify
         * @memberof game.UnassignWorker
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UnassignWorker.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                if (!$util.isString(message.placeId))
                    return "placeId: string expected";
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                if (!$util.isInteger(message.socketIndex))
                    return "socketIndex: integer expected";
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId"))
                if (!$util.isString(message.workerId))
                    return "workerId: string expected";
            return null;
        };

        /**
         * Creates an UnassignWorker message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.UnassignWorker
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.UnassignWorker} UnassignWorker
         */
        UnassignWorker.fromObject = function (object, _depth) {
            if (object instanceof $root.game.UnassignWorker)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.UnassignWorker: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.UnassignWorker();
            if (object.placeId != null)
                if (typeof object.placeId !== "string" || object.placeId.length)
                    message.placeId = $String(object.placeId);
            if (object.socketIndex != null)
                if ($Number(object.socketIndex) !== 0)
                    message.socketIndex = object.socketIndex | 0;
            if (object.workerId != null)
                if (typeof object.workerId !== "string" || object.workerId.length)
                    message.workerId = $String(object.workerId);
            return message;
        };

        /**
         * Creates a plain object from an UnassignWorker message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.UnassignWorker
         * @static
         * @param {game.UnassignWorker} message UnassignWorker
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UnassignWorker.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.placeId = "";
                object.socketIndex = 0;
                object.workerId = "";
            }
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                object.placeId = message.placeId;
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                object.socketIndex = message.socketIndex;
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId"))
                object.workerId = message.workerId;
            return object;
        };

        /**
         * Converts this UnassignWorker to JSON.
         * @function toJSON
         * @memberof game.UnassignWorker
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UnassignWorker.prototype.toJSON = function() {
            return UnassignWorker.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for UnassignWorker
         * @function getTypeUrl
         * @memberof game.UnassignWorker
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        UnassignWorker.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.UnassignWorker";
        };

        return UnassignWorker;
    })();

    game.FireWorker = (function() {

        /**
         * Properties of a FireWorker.
         * @typedef {Object} game.FireWorker.$Properties
         * @property {string|null} [workerId] FireWorker workerId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FireWorker.
         * @memberof game
         * @interface IFireWorker
         * @augments game.FireWorker.$Properties
         * @deprecated Use game.FireWorker.$Properties instead.
         */

        /**
         * Shape of a FireWorker.
         * @typedef {game.FireWorker.$Properties} game.FireWorker.$Shape
         */

        /**
         * Constructs a new FireWorker.
         * @memberof game
         * @classdesc Represents a FireWorker.
         * @constructor
         * @param {game.FireWorker.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FireWorker = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * FireWorker workerId.
         * @member {string} workerId
         * @memberof game.FireWorker
         * @instance
         */
        FireWorker.prototype.workerId = "";

        /**
         * Creates a new FireWorker instance using the specified properties.
         * @function create
         * @memberof game.FireWorker
         * @static
         * @param {game.FireWorker.$Properties=} [properties] Properties to set
         * @returns {game.FireWorker} FireWorker instance
         * @type {{
         *   (properties: game.FireWorker.$Shape): game.FireWorker & game.FireWorker.$Shape;
         *   (properties?: game.FireWorker.$Properties): game.FireWorker;
         * }}
         */
        FireWorker.create = function(properties) {
            return new FireWorker(properties);
        };

        /**
         * Encodes the specified FireWorker message. Does not implicitly {@link game.FireWorker.verify|verify} messages.
         * @function encode
         * @memberof game.FireWorker
         * @static
         * @param {game.FireWorker.$Properties} message FireWorker message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FireWorker.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId") && message.workerId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.workerId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FireWorker message, length delimited. Does not implicitly {@link game.FireWorker.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.FireWorker
         * @static
         * @param {game.FireWorker.$Properties} message FireWorker message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FireWorker.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FireWorker message from the specified reader or buffer.
         * @function decode
         * @memberof game.FireWorker
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.FireWorker & game.FireWorker.$Shape} FireWorker
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FireWorker.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.FireWorker(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.workerId = value;
                        else
                            delete message.workerId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FireWorker message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.FireWorker
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.FireWorker & game.FireWorker.$Shape} FireWorker
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FireWorker.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FireWorker message.
         * @function verify
         * @memberof game.FireWorker
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FireWorker.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId"))
                if (!$util.isString(message.workerId))
                    return "workerId: string expected";
            return null;
        };

        /**
         * Creates a FireWorker message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.FireWorker
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.FireWorker} FireWorker
         */
        FireWorker.fromObject = function (object, _depth) {
            if (object instanceof $root.game.FireWorker)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.FireWorker: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.FireWorker();
            if (object.workerId != null)
                if (typeof object.workerId !== "string" || object.workerId.length)
                    message.workerId = $String(object.workerId);
            return message;
        };

        /**
         * Creates a plain object from a FireWorker message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.FireWorker
         * @static
         * @param {game.FireWorker} message FireWorker
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FireWorker.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.workerId = "";
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId"))
                object.workerId = message.workerId;
            return object;
        };

        /**
         * Converts this FireWorker to JSON.
         * @function toJSON
         * @memberof game.FireWorker
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FireWorker.prototype.toJSON = function() {
            return FireWorker.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FireWorker
         * @function getTypeUrl
         * @memberof game.FireWorker
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FireWorker.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.FireWorker";
        };

        return FireWorker;
    })();

    game.CraftRequest = (function() {

        /**
         * Properties of a CraftRequest.
         * @typedef {Object} game.CraftRequest.$Properties
         * @property {string|null} [recipeId] CraftRequest recipeId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CraftRequest.
         * @memberof game
         * @interface ICraftRequest
         * @augments game.CraftRequest.$Properties
         * @deprecated Use game.CraftRequest.$Properties instead.
         */

        /**
         * Shape of a CraftRequest.
         * @typedef {game.CraftRequest.$Properties} game.CraftRequest.$Shape
         */

        /**
         * Constructs a new CraftRequest.
         * @memberof game
         * @classdesc Represents a CraftRequest.
         * @constructor
         * @param {game.CraftRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CraftRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CraftRequest recipeId.
         * @member {string} recipeId
         * @memberof game.CraftRequest
         * @instance
         */
        CraftRequest.prototype.recipeId = "";

        /**
         * Creates a new CraftRequest instance using the specified properties.
         * @function create
         * @memberof game.CraftRequest
         * @static
         * @param {game.CraftRequest.$Properties=} [properties] Properties to set
         * @returns {game.CraftRequest} CraftRequest instance
         * @type {{
         *   (properties: game.CraftRequest.$Shape): game.CraftRequest & game.CraftRequest.$Shape;
         *   (properties?: game.CraftRequest.$Properties): game.CraftRequest;
         * }}
         */
        CraftRequest.create = function(properties) {
            return new CraftRequest(properties);
        };

        /**
         * Encodes the specified CraftRequest message. Does not implicitly {@link game.CraftRequest.verify|verify} messages.
         * @function encode
         * @memberof game.CraftRequest
         * @static
         * @param {game.CraftRequest.$Properties} message CraftRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CraftRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.recipeId != null && $Object.hasOwnProperty.call(message, "recipeId") && message.recipeId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.recipeId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CraftRequest message, length delimited. Does not implicitly {@link game.CraftRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CraftRequest
         * @static
         * @param {game.CraftRequest.$Properties} message CraftRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CraftRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CraftRequest message from the specified reader or buffer.
         * @function decode
         * @memberof game.CraftRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CraftRequest & game.CraftRequest.$Shape} CraftRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CraftRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.CraftRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.recipeId = value;
                        else
                            delete message.recipeId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CraftRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CraftRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CraftRequest & game.CraftRequest.$Shape} CraftRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CraftRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CraftRequest message.
         * @function verify
         * @memberof game.CraftRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CraftRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.recipeId != null && $Object.hasOwnProperty.call(message, "recipeId"))
                if (!$util.isString(message.recipeId))
                    return "recipeId: string expected";
            return null;
        };

        /**
         * Creates a CraftRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CraftRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CraftRequest} CraftRequest
         */
        CraftRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.CraftRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.CraftRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.CraftRequest();
            if (object.recipeId != null)
                if (typeof object.recipeId !== "string" || object.recipeId.length)
                    message.recipeId = $String(object.recipeId);
            return message;
        };

        /**
         * Creates a plain object from a CraftRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CraftRequest
         * @static
         * @param {game.CraftRequest} message CraftRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CraftRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.recipeId = "";
            if (message.recipeId != null && $Object.hasOwnProperty.call(message, "recipeId"))
                object.recipeId = message.recipeId;
            return object;
        };

        /**
         * Converts this CraftRequest to JSON.
         * @function toJSON
         * @memberof game.CraftRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CraftRequest.prototype.toJSON = function() {
            return CraftRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CraftRequest
         * @function getTypeUrl
         * @memberof game.CraftRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CraftRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.CraftRequest";
        };

        return CraftRequest;
    })();

    game.HireWorker = (function() {

        /**
         * Properties of a HireWorker.
         * @typedef {Object} game.HireWorker.$Properties
         * @property {string|null} [workerId] HireWorker workerId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a HireWorker.
         * @memberof game
         * @interface IHireWorker
         * @augments game.HireWorker.$Properties
         * @deprecated Use game.HireWorker.$Properties instead.
         */

        /**
         * Shape of a HireWorker.
         * @typedef {game.HireWorker.$Properties} game.HireWorker.$Shape
         */

        /**
         * Constructs a new HireWorker.
         * @memberof game
         * @classdesc Represents a HireWorker.
         * @constructor
         * @param {game.HireWorker.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const HireWorker = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * HireWorker workerId.
         * @member {string} workerId
         * @memberof game.HireWorker
         * @instance
         */
        HireWorker.prototype.workerId = "";

        /**
         * Creates a new HireWorker instance using the specified properties.
         * @function create
         * @memberof game.HireWorker
         * @static
         * @param {game.HireWorker.$Properties=} [properties] Properties to set
         * @returns {game.HireWorker} HireWorker instance
         * @type {{
         *   (properties: game.HireWorker.$Shape): game.HireWorker & game.HireWorker.$Shape;
         *   (properties?: game.HireWorker.$Properties): game.HireWorker;
         * }}
         */
        HireWorker.create = function(properties) {
            return new HireWorker(properties);
        };

        /**
         * Encodes the specified HireWorker message. Does not implicitly {@link game.HireWorker.verify|verify} messages.
         * @function encode
         * @memberof game.HireWorker
         * @static
         * @param {game.HireWorker.$Properties} message HireWorker message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HireWorker.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId") && message.workerId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.workerId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HireWorker message, length delimited. Does not implicitly {@link game.HireWorker.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.HireWorker
         * @static
         * @param {game.HireWorker.$Properties} message HireWorker message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HireWorker.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a HireWorker message from the specified reader or buffer.
         * @function decode
         * @memberof game.HireWorker
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.HireWorker & game.HireWorker.$Shape} HireWorker
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HireWorker.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.HireWorker(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.workerId = value;
                        else
                            delete message.workerId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a HireWorker message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.HireWorker
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.HireWorker & game.HireWorker.$Shape} HireWorker
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HireWorker.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HireWorker message.
         * @function verify
         * @memberof game.HireWorker
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HireWorker.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId"))
                if (!$util.isString(message.workerId))
                    return "workerId: string expected";
            return null;
        };

        /**
         * Creates a HireWorker message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.HireWorker
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.HireWorker} HireWorker
         */
        HireWorker.fromObject = function (object, _depth) {
            if (object instanceof $root.game.HireWorker)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.HireWorker: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.HireWorker();
            if (object.workerId != null)
                if (typeof object.workerId !== "string" || object.workerId.length)
                    message.workerId = $String(object.workerId);
            return message;
        };

        /**
         * Creates a plain object from a HireWorker message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.HireWorker
         * @static
         * @param {game.HireWorker} message HireWorker
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HireWorker.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.workerId = "";
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId"))
                object.workerId = message.workerId;
            return object;
        };

        /**
         * Converts this HireWorker to JSON.
         * @function toJSON
         * @memberof game.HireWorker
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HireWorker.prototype.toJSON = function() {
            return HireWorker.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for HireWorker
         * @function getTypeUrl
         * @memberof game.HireWorker
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        HireWorker.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.HireWorker";
        };

        return HireWorker;
    })();

    game.RerollWorkers = (function() {

        /**
         * Properties of a RerollWorkers.
         * @typedef {Object} game.RerollWorkers.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RerollWorkers.
         * @memberof game
         * @interface IRerollWorkers
         * @augments game.RerollWorkers.$Properties
         * @deprecated Use game.RerollWorkers.$Properties instead.
         */

        /**
         * Shape of a RerollWorkers.
         * @typedef {game.RerollWorkers.$Properties} game.RerollWorkers.$Shape
         */

        /**
         * Constructs a new RerollWorkers.
         * @memberof game
         * @classdesc Represents a RerollWorkers.
         * @constructor
         * @param {game.RerollWorkers.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RerollWorkers = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new RerollWorkers instance using the specified properties.
         * @function create
         * @memberof game.RerollWorkers
         * @static
         * @param {game.RerollWorkers.$Properties=} [properties] Properties to set
         * @returns {game.RerollWorkers} RerollWorkers instance
         * @type {{
         *   (properties: game.RerollWorkers.$Shape): game.RerollWorkers & game.RerollWorkers.$Shape;
         *   (properties?: game.RerollWorkers.$Properties): game.RerollWorkers;
         * }}
         */
        RerollWorkers.create = function(properties) {
            return new RerollWorkers(properties);
        };

        /**
         * Encodes the specified RerollWorkers message. Does not implicitly {@link game.RerollWorkers.verify|verify} messages.
         * @function encode
         * @memberof game.RerollWorkers
         * @static
         * @param {game.RerollWorkers.$Properties} message RerollWorkers message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RerollWorkers.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RerollWorkers message, length delimited. Does not implicitly {@link game.RerollWorkers.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.RerollWorkers
         * @static
         * @param {game.RerollWorkers.$Properties} message RerollWorkers message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RerollWorkers.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RerollWorkers message from the specified reader or buffer.
         * @function decode
         * @memberof game.RerollWorkers
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.RerollWorkers & game.RerollWorkers.$Shape} RerollWorkers
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RerollWorkers.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.RerollWorkers();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RerollWorkers message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.RerollWorkers
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.RerollWorkers & game.RerollWorkers.$Shape} RerollWorkers
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RerollWorkers.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RerollWorkers message.
         * @function verify
         * @memberof game.RerollWorkers
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RerollWorkers.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a RerollWorkers message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.RerollWorkers
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.RerollWorkers} RerollWorkers
         */
        RerollWorkers.fromObject = function (object, _depth) {
            if (object instanceof $root.game.RerollWorkers)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.RerollWorkers: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.game.RerollWorkers();
        };

        /**
         * Creates a plain object from a RerollWorkers message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.RerollWorkers
         * @static
         * @param {game.RerollWorkers} message RerollWorkers
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RerollWorkers.toObject = function () {
            return {};
        };

        /**
         * Converts this RerollWorkers to JSON.
         * @function toJSON
         * @memberof game.RerollWorkers
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RerollWorkers.prototype.toJSON = function() {
            return RerollWorkers.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RerollWorkers
         * @function getTypeUrl
         * @memberof game.RerollWorkers
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RerollWorkers.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.RerollWorkers";
        };

        return RerollWorkers;
    })();

    game.BuyWorkerSlot = (function() {

        /**
         * Properties of a BuyWorkerSlot.
         * @typedef {Object} game.BuyWorkerSlot.$Properties
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a BuyWorkerSlot.
         * @memberof game
         * @interface IBuyWorkerSlot
         * @augments game.BuyWorkerSlot.$Properties
         * @deprecated Use game.BuyWorkerSlot.$Properties instead.
         */

        /**
         * Shape of a BuyWorkerSlot.
         * @typedef {game.BuyWorkerSlot.$Properties} game.BuyWorkerSlot.$Shape
         */

        /**
         * Constructs a new BuyWorkerSlot.
         * @memberof game
         * @classdesc Represents a BuyWorkerSlot.
         * @constructor
         * @param {game.BuyWorkerSlot.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const BuyWorkerSlot = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Creates a new BuyWorkerSlot instance using the specified properties.
         * @function create
         * @memberof game.BuyWorkerSlot
         * @static
         * @param {game.BuyWorkerSlot.$Properties=} [properties] Properties to set
         * @returns {game.BuyWorkerSlot} BuyWorkerSlot instance
         * @type {{
         *   (properties: game.BuyWorkerSlot.$Shape): game.BuyWorkerSlot & game.BuyWorkerSlot.$Shape;
         *   (properties?: game.BuyWorkerSlot.$Properties): game.BuyWorkerSlot;
         * }}
         */
        BuyWorkerSlot.create = function(properties) {
            return new BuyWorkerSlot(properties);
        };

        /**
         * Encodes the specified BuyWorkerSlot message. Does not implicitly {@link game.BuyWorkerSlot.verify|verify} messages.
         * @function encode
         * @memberof game.BuyWorkerSlot
         * @static
         * @param {game.BuyWorkerSlot.$Properties} message BuyWorkerSlot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyWorkerSlot.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BuyWorkerSlot message, length delimited. Does not implicitly {@link game.BuyWorkerSlot.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.BuyWorkerSlot
         * @static
         * @param {game.BuyWorkerSlot.$Properties} message BuyWorkerSlot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyWorkerSlot.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a BuyWorkerSlot message from the specified reader or buffer.
         * @function decode
         * @memberof game.BuyWorkerSlot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.BuyWorkerSlot & game.BuyWorkerSlot.$Shape} BuyWorkerSlot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyWorkerSlot.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.BuyWorkerSlot();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BuyWorkerSlot message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.BuyWorkerSlot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.BuyWorkerSlot & game.BuyWorkerSlot.$Shape} BuyWorkerSlot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyWorkerSlot.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BuyWorkerSlot message.
         * @function verify
         * @memberof game.BuyWorkerSlot
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BuyWorkerSlot.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        /**
         * Creates a BuyWorkerSlot message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.BuyWorkerSlot
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.BuyWorkerSlot} BuyWorkerSlot
         */
        BuyWorkerSlot.fromObject = function (object, _depth) {
            if (object instanceof $root.game.BuyWorkerSlot)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.BuyWorkerSlot: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.game.BuyWorkerSlot();
        };

        /**
         * Creates a plain object from a BuyWorkerSlot message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.BuyWorkerSlot
         * @static
         * @param {game.BuyWorkerSlot} message BuyWorkerSlot
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BuyWorkerSlot.toObject = function () {
            return {};
        };

        /**
         * Converts this BuyWorkerSlot to JSON.
         * @function toJSON
         * @memberof game.BuyWorkerSlot
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BuyWorkerSlot.prototype.toJSON = function() {
            return BuyWorkerSlot.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BuyWorkerSlot
         * @function getTypeUrl
         * @memberof game.BuyWorkerSlot
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BuyWorkerSlot.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.BuyWorkerSlot";
        };

        return BuyWorkerSlot;
    })();

    game.BuyItem = (function() {

        /**
         * Properties of a BuyItem.
         * @typedef {Object} game.BuyItem.$Properties
         * @property {string|null} [itemId] BuyItem itemId
         * @property {number|null} [quantity] BuyItem quantity
         * @property {string|null} [npcId] BuyItem npcId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a BuyItem.
         * @memberof game
         * @interface IBuyItem
         * @augments game.BuyItem.$Properties
         * @deprecated Use game.BuyItem.$Properties instead.
         */

        /**
         * Shape of a BuyItem.
         * @typedef {game.BuyItem.$Properties} game.BuyItem.$Shape
         */

        /**
         * Constructs a new BuyItem.
         * @memberof game
         * @classdesc Represents a BuyItem.
         * @constructor
         * @param {game.BuyItem.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const BuyItem = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BuyItem itemId.
         * @member {string} itemId
         * @memberof game.BuyItem
         * @instance
         */
        BuyItem.prototype.itemId = "";

        /**
         * BuyItem quantity.
         * @member {number} quantity
         * @memberof game.BuyItem
         * @instance
         */
        BuyItem.prototype.quantity = 0;

        /**
         * BuyItem npcId.
         * @member {string} npcId
         * @memberof game.BuyItem
         * @instance
         */
        BuyItem.prototype.npcId = "";

        /**
         * Creates a new BuyItem instance using the specified properties.
         * @function create
         * @memberof game.BuyItem
         * @static
         * @param {game.BuyItem.$Properties=} [properties] Properties to set
         * @returns {game.BuyItem} BuyItem instance
         * @type {{
         *   (properties: game.BuyItem.$Shape): game.BuyItem & game.BuyItem.$Shape;
         *   (properties?: game.BuyItem.$Properties): game.BuyItem;
         * }}
         */
        BuyItem.create = function(properties) {
            return new BuyItem(properties);
        };

        /**
         * Encodes the specified BuyItem message. Does not implicitly {@link game.BuyItem.verify|verify} messages.
         * @function encode
         * @memberof game.BuyItem
         * @static
         * @param {game.BuyItem.$Properties} message BuyItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyItem.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId") && message.itemId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.itemId);
            if (message.quantity != null && $Object.hasOwnProperty.call(message, "quantity") && message.quantity !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.quantity);
            if (message.npcId != null && $Object.hasOwnProperty.call(message, "npcId") && message.npcId !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.npcId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BuyItem message, length delimited. Does not implicitly {@link game.BuyItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.BuyItem
         * @static
         * @param {game.BuyItem.$Properties} message BuyItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyItem.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a BuyItem message from the specified reader or buffer.
         * @function decode
         * @memberof game.BuyItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.BuyItem & game.BuyItem.$Shape} BuyItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyItem.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.BuyItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.itemId = value;
                        else
                            delete message.itemId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.quantity = value;
                        else
                            delete message.quantity;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.npcId = value;
                        else
                            delete message.npcId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BuyItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.BuyItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.BuyItem & game.BuyItem.$Shape} BuyItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyItem.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BuyItem message.
         * @function verify
         * @memberof game.BuyItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BuyItem.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId"))
                if (!$util.isString(message.itemId))
                    return "itemId: string expected";
            if (message.quantity != null && $Object.hasOwnProperty.call(message, "quantity"))
                if (!$util.isInteger(message.quantity))
                    return "quantity: integer expected";
            if (message.npcId != null && $Object.hasOwnProperty.call(message, "npcId"))
                if (!$util.isString(message.npcId))
                    return "npcId: string expected";
            return null;
        };

        /**
         * Creates a BuyItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.BuyItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.BuyItem} BuyItem
         */
        BuyItem.fromObject = function (object, _depth) {
            if (object instanceof $root.game.BuyItem)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.BuyItem: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.BuyItem();
            if (object.itemId != null)
                if (typeof object.itemId !== "string" || object.itemId.length)
                    message.itemId = $String(object.itemId);
            if (object.quantity != null)
                if ($Number(object.quantity) !== 0)
                    message.quantity = object.quantity | 0;
            if (object.npcId != null)
                if (typeof object.npcId !== "string" || object.npcId.length)
                    message.npcId = $String(object.npcId);
            return message;
        };

        /**
         * Creates a plain object from a BuyItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.BuyItem
         * @static
         * @param {game.BuyItem} message BuyItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BuyItem.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.itemId = "";
                object.quantity = 0;
                object.npcId = "";
            }
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId"))
                object.itemId = message.itemId;
            if (message.quantity != null && $Object.hasOwnProperty.call(message, "quantity"))
                object.quantity = message.quantity;
            if (message.npcId != null && $Object.hasOwnProperty.call(message, "npcId"))
                object.npcId = message.npcId;
            return object;
        };

        /**
         * Converts this BuyItem to JSON.
         * @function toJSON
         * @memberof game.BuyItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BuyItem.prototype.toJSON = function() {
            return BuyItem.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BuyItem
         * @function getTypeUrl
         * @memberof game.BuyItem
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BuyItem.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.BuyItem";
        };

        return BuyItem;
    })();

    game.SellItem = (function() {

        /**
         * Properties of a SellItem.
         * @typedef {Object} game.SellItem.$Properties
         * @property {string|null} [itemId] SellItem itemId
         * @property {number|null} [quantity] SellItem quantity
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SellItem.
         * @memberof game
         * @interface ISellItem
         * @augments game.SellItem.$Properties
         * @deprecated Use game.SellItem.$Properties instead.
         */

        /**
         * Shape of a SellItem.
         * @typedef {game.SellItem.$Properties} game.SellItem.$Shape
         */

        /**
         * Constructs a new SellItem.
         * @memberof game
         * @classdesc Represents a SellItem.
         * @constructor
         * @param {game.SellItem.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SellItem = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SellItem itemId.
         * @member {string} itemId
         * @memberof game.SellItem
         * @instance
         */
        SellItem.prototype.itemId = "";

        /**
         * SellItem quantity.
         * @member {number} quantity
         * @memberof game.SellItem
         * @instance
         */
        SellItem.prototype.quantity = 0;

        /**
         * Creates a new SellItem instance using the specified properties.
         * @function create
         * @memberof game.SellItem
         * @static
         * @param {game.SellItem.$Properties=} [properties] Properties to set
         * @returns {game.SellItem} SellItem instance
         * @type {{
         *   (properties: game.SellItem.$Shape): game.SellItem & game.SellItem.$Shape;
         *   (properties?: game.SellItem.$Properties): game.SellItem;
         * }}
         */
        SellItem.create = function(properties) {
            return new SellItem(properties);
        };

        /**
         * Encodes the specified SellItem message. Does not implicitly {@link game.SellItem.verify|verify} messages.
         * @function encode
         * @memberof game.SellItem
         * @static
         * @param {game.SellItem.$Properties} message SellItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SellItem.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId") && message.itemId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.itemId);
            if (message.quantity != null && $Object.hasOwnProperty.call(message, "quantity") && message.quantity !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.quantity);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SellItem message, length delimited. Does not implicitly {@link game.SellItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SellItem
         * @static
         * @param {game.SellItem.$Properties} message SellItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SellItem.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SellItem message from the specified reader or buffer.
         * @function decode
         * @memberof game.SellItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SellItem & game.SellItem.$Shape} SellItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SellItem.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.SellItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.itemId = value;
                        else
                            delete message.itemId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.quantity = value;
                        else
                            delete message.quantity;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SellItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SellItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SellItem & game.SellItem.$Shape} SellItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SellItem.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SellItem message.
         * @function verify
         * @memberof game.SellItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SellItem.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId"))
                if (!$util.isString(message.itemId))
                    return "itemId: string expected";
            if (message.quantity != null && $Object.hasOwnProperty.call(message, "quantity"))
                if (!$util.isInteger(message.quantity))
                    return "quantity: integer expected";
            return null;
        };

        /**
         * Creates a SellItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SellItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SellItem} SellItem
         */
        SellItem.fromObject = function (object, _depth) {
            if (object instanceof $root.game.SellItem)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.SellItem: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.SellItem();
            if (object.itemId != null)
                if (typeof object.itemId !== "string" || object.itemId.length)
                    message.itemId = $String(object.itemId);
            if (object.quantity != null)
                if ($Number(object.quantity) !== 0)
                    message.quantity = object.quantity | 0;
            return message;
        };

        /**
         * Creates a plain object from a SellItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SellItem
         * @static
         * @param {game.SellItem} message SellItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SellItem.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.itemId = "";
                object.quantity = 0;
            }
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId"))
                object.itemId = message.itemId;
            if (message.quantity != null && $Object.hasOwnProperty.call(message, "quantity"))
                object.quantity = message.quantity;
            return object;
        };

        /**
         * Converts this SellItem to JSON.
         * @function toJSON
         * @memberof game.SellItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SellItem.prototype.toJSON = function() {
            return SellItem.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SellItem
         * @function getTypeUrl
         * @memberof game.SellItem
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SellItem.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.SellItem";
        };

        return SellItem;
    })();

    game.AcceptQuest = (function() {

        /**
         * Properties of an AcceptQuest.
         * @typedef {Object} game.AcceptQuest.$Properties
         * @property {string|null} [questId] AcceptQuest questId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AcceptQuest.
         * @memberof game
         * @interface IAcceptQuest
         * @augments game.AcceptQuest.$Properties
         * @deprecated Use game.AcceptQuest.$Properties instead.
         */

        /**
         * Shape of an AcceptQuest.
         * @typedef {game.AcceptQuest.$Properties} game.AcceptQuest.$Shape
         */

        /**
         * Constructs a new AcceptQuest.
         * @memberof game
         * @classdesc Represents an AcceptQuest.
         * @constructor
         * @param {game.AcceptQuest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AcceptQuest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AcceptQuest questId.
         * @member {string} questId
         * @memberof game.AcceptQuest
         * @instance
         */
        AcceptQuest.prototype.questId = "";

        /**
         * Creates a new AcceptQuest instance using the specified properties.
         * @function create
         * @memberof game.AcceptQuest
         * @static
         * @param {game.AcceptQuest.$Properties=} [properties] Properties to set
         * @returns {game.AcceptQuest} AcceptQuest instance
         * @type {{
         *   (properties: game.AcceptQuest.$Shape): game.AcceptQuest & game.AcceptQuest.$Shape;
         *   (properties?: game.AcceptQuest.$Properties): game.AcceptQuest;
         * }}
         */
        AcceptQuest.create = function(properties) {
            return new AcceptQuest(properties);
        };

        /**
         * Encodes the specified AcceptQuest message. Does not implicitly {@link game.AcceptQuest.verify|verify} messages.
         * @function encode
         * @memberof game.AcceptQuest
         * @static
         * @param {game.AcceptQuest.$Properties} message AcceptQuest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AcceptQuest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.questId != null && $Object.hasOwnProperty.call(message, "questId") && message.questId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.questId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AcceptQuest message, length delimited. Does not implicitly {@link game.AcceptQuest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.AcceptQuest
         * @static
         * @param {game.AcceptQuest.$Properties} message AcceptQuest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AcceptQuest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AcceptQuest message from the specified reader or buffer.
         * @function decode
         * @memberof game.AcceptQuest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.AcceptQuest & game.AcceptQuest.$Shape} AcceptQuest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AcceptQuest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.AcceptQuest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.questId = value;
                        else
                            delete message.questId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AcceptQuest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.AcceptQuest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.AcceptQuest & game.AcceptQuest.$Shape} AcceptQuest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AcceptQuest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AcceptQuest message.
         * @function verify
         * @memberof game.AcceptQuest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AcceptQuest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.questId != null && $Object.hasOwnProperty.call(message, "questId"))
                if (!$util.isString(message.questId))
                    return "questId: string expected";
            return null;
        };

        /**
         * Creates an AcceptQuest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.AcceptQuest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.AcceptQuest} AcceptQuest
         */
        AcceptQuest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.AcceptQuest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.AcceptQuest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.AcceptQuest();
            if (object.questId != null)
                if (typeof object.questId !== "string" || object.questId.length)
                    message.questId = $String(object.questId);
            return message;
        };

        /**
         * Creates a plain object from an AcceptQuest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.AcceptQuest
         * @static
         * @param {game.AcceptQuest} message AcceptQuest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AcceptQuest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.questId = "";
            if (message.questId != null && $Object.hasOwnProperty.call(message, "questId"))
                object.questId = message.questId;
            return object;
        };

        /**
         * Converts this AcceptQuest to JSON.
         * @function toJSON
         * @memberof game.AcceptQuest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AcceptQuest.prototype.toJSON = function() {
            return AcceptQuest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AcceptQuest
         * @function getTypeUrl
         * @memberof game.AcceptQuest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AcceptQuest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.AcceptQuest";
        };

        return AcceptQuest;
    })();

    game.CompleteQuest = (function() {

        /**
         * Properties of a CompleteQuest.
         * @typedef {Object} game.CompleteQuest.$Properties
         * @property {string|null} [questId] CompleteQuest questId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CompleteQuest.
         * @memberof game
         * @interface ICompleteQuest
         * @augments game.CompleteQuest.$Properties
         * @deprecated Use game.CompleteQuest.$Properties instead.
         */

        /**
         * Shape of a CompleteQuest.
         * @typedef {game.CompleteQuest.$Properties} game.CompleteQuest.$Shape
         */

        /**
         * Constructs a new CompleteQuest.
         * @memberof game
         * @classdesc Represents a CompleteQuest.
         * @constructor
         * @param {game.CompleteQuest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CompleteQuest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CompleteQuest questId.
         * @member {string} questId
         * @memberof game.CompleteQuest
         * @instance
         */
        CompleteQuest.prototype.questId = "";

        /**
         * Creates a new CompleteQuest instance using the specified properties.
         * @function create
         * @memberof game.CompleteQuest
         * @static
         * @param {game.CompleteQuest.$Properties=} [properties] Properties to set
         * @returns {game.CompleteQuest} CompleteQuest instance
         * @type {{
         *   (properties: game.CompleteQuest.$Shape): game.CompleteQuest & game.CompleteQuest.$Shape;
         *   (properties?: game.CompleteQuest.$Properties): game.CompleteQuest;
         * }}
         */
        CompleteQuest.create = function(properties) {
            return new CompleteQuest(properties);
        };

        /**
         * Encodes the specified CompleteQuest message. Does not implicitly {@link game.CompleteQuest.verify|verify} messages.
         * @function encode
         * @memberof game.CompleteQuest
         * @static
         * @param {game.CompleteQuest.$Properties} message CompleteQuest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompleteQuest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.questId != null && $Object.hasOwnProperty.call(message, "questId") && message.questId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.questId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CompleteQuest message, length delimited. Does not implicitly {@link game.CompleteQuest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CompleteQuest
         * @static
         * @param {game.CompleteQuest.$Properties} message CompleteQuest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompleteQuest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CompleteQuest message from the specified reader or buffer.
         * @function decode
         * @memberof game.CompleteQuest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CompleteQuest & game.CompleteQuest.$Shape} CompleteQuest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompleteQuest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.CompleteQuest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.questId = value;
                        else
                            delete message.questId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CompleteQuest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CompleteQuest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CompleteQuest & game.CompleteQuest.$Shape} CompleteQuest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompleteQuest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CompleteQuest message.
         * @function verify
         * @memberof game.CompleteQuest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CompleteQuest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.questId != null && $Object.hasOwnProperty.call(message, "questId"))
                if (!$util.isString(message.questId))
                    return "questId: string expected";
            return null;
        };

        /**
         * Creates a CompleteQuest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CompleteQuest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CompleteQuest} CompleteQuest
         */
        CompleteQuest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.CompleteQuest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.CompleteQuest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.CompleteQuest();
            if (object.questId != null)
                if (typeof object.questId !== "string" || object.questId.length)
                    message.questId = $String(object.questId);
            return message;
        };

        /**
         * Creates a plain object from a CompleteQuest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CompleteQuest
         * @static
         * @param {game.CompleteQuest} message CompleteQuest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CompleteQuest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.questId = "";
            if (message.questId != null && $Object.hasOwnProperty.call(message, "questId"))
                object.questId = message.questId;
            return object;
        };

        /**
         * Converts this CompleteQuest to JSON.
         * @function toJSON
         * @memberof game.CompleteQuest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CompleteQuest.prototype.toJSON = function() {
            return CompleteQuest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CompleteQuest
         * @function getTypeUrl
         * @memberof game.CompleteQuest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CompleteQuest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.CompleteQuest";
        };

        return CompleteQuest;
    })();

    game.MoveItem = (function() {

        /**
         * Properties of a MoveItem.
         * @typedef {Object} game.MoveItem.$Properties
         * @property {string|null} [fromInventoryId] MoveItem fromInventoryId
         * @property {string|null} [toInventoryId] MoveItem toInventoryId
         * @property {string|null} [itemId] MoveItem itemId
         * @property {number|null} [quantity] MoveItem quantity
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a MoveItem.
         * @memberof game
         * @interface IMoveItem
         * @augments game.MoveItem.$Properties
         * @deprecated Use game.MoveItem.$Properties instead.
         */

        /**
         * Shape of a MoveItem.
         * @typedef {game.MoveItem.$Properties} game.MoveItem.$Shape
         */

        /**
         * Constructs a new MoveItem.
         * @memberof game
         * @classdesc Represents a MoveItem.
         * @constructor
         * @param {game.MoveItem.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const MoveItem = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MoveItem fromInventoryId.
         * @member {string} fromInventoryId
         * @memberof game.MoveItem
         * @instance
         */
        MoveItem.prototype.fromInventoryId = "";

        /**
         * MoveItem toInventoryId.
         * @member {string} toInventoryId
         * @memberof game.MoveItem
         * @instance
         */
        MoveItem.prototype.toInventoryId = "";

        /**
         * MoveItem itemId.
         * @member {string} itemId
         * @memberof game.MoveItem
         * @instance
         */
        MoveItem.prototype.itemId = "";

        /**
         * MoveItem quantity.
         * @member {number} quantity
         * @memberof game.MoveItem
         * @instance
         */
        MoveItem.prototype.quantity = 0;

        /**
         * Creates a new MoveItem instance using the specified properties.
         * @function create
         * @memberof game.MoveItem
         * @static
         * @param {game.MoveItem.$Properties=} [properties] Properties to set
         * @returns {game.MoveItem} MoveItem instance
         * @type {{
         *   (properties: game.MoveItem.$Shape): game.MoveItem & game.MoveItem.$Shape;
         *   (properties?: game.MoveItem.$Properties): game.MoveItem;
         * }}
         */
        MoveItem.create = function(properties) {
            return new MoveItem(properties);
        };

        /**
         * Encodes the specified MoveItem message. Does not implicitly {@link game.MoveItem.verify|verify} messages.
         * @function encode
         * @memberof game.MoveItem
         * @static
         * @param {game.MoveItem.$Properties} message MoveItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MoveItem.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fromInventoryId != null && $Object.hasOwnProperty.call(message, "fromInventoryId") && message.fromInventoryId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.fromInventoryId);
            if (message.toInventoryId != null && $Object.hasOwnProperty.call(message, "toInventoryId") && message.toInventoryId !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.toInventoryId);
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId") && message.itemId !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.itemId);
            if (message.quantity != null && $Object.hasOwnProperty.call(message, "quantity") && message.quantity !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.quantity);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MoveItem message, length delimited. Does not implicitly {@link game.MoveItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.MoveItem
         * @static
         * @param {game.MoveItem.$Properties} message MoveItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MoveItem.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a MoveItem message from the specified reader or buffer.
         * @function decode
         * @memberof game.MoveItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.MoveItem & game.MoveItem.$Shape} MoveItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MoveItem.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.MoveItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.fromInventoryId = value;
                        else
                            delete message.fromInventoryId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.toInventoryId = value;
                        else
                            delete message.toInventoryId;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.itemId = value;
                        else
                            delete message.itemId;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.quantity = value;
                        else
                            delete message.quantity;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MoveItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.MoveItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.MoveItem & game.MoveItem.$Shape} MoveItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MoveItem.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MoveItem message.
         * @function verify
         * @memberof game.MoveItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MoveItem.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fromInventoryId != null && $Object.hasOwnProperty.call(message, "fromInventoryId"))
                if (!$util.isString(message.fromInventoryId))
                    return "fromInventoryId: string expected";
            if (message.toInventoryId != null && $Object.hasOwnProperty.call(message, "toInventoryId"))
                if (!$util.isString(message.toInventoryId))
                    return "toInventoryId: string expected";
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId"))
                if (!$util.isString(message.itemId))
                    return "itemId: string expected";
            if (message.quantity != null && $Object.hasOwnProperty.call(message, "quantity"))
                if (!$util.isInteger(message.quantity))
                    return "quantity: integer expected";
            return null;
        };

        /**
         * Creates a MoveItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.MoveItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.MoveItem} MoveItem
         */
        MoveItem.fromObject = function (object, _depth) {
            if (object instanceof $root.game.MoveItem)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.MoveItem: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.MoveItem();
            if (object.fromInventoryId != null)
                if (typeof object.fromInventoryId !== "string" || object.fromInventoryId.length)
                    message.fromInventoryId = $String(object.fromInventoryId);
            if (object.toInventoryId != null)
                if (typeof object.toInventoryId !== "string" || object.toInventoryId.length)
                    message.toInventoryId = $String(object.toInventoryId);
            if (object.itemId != null)
                if (typeof object.itemId !== "string" || object.itemId.length)
                    message.itemId = $String(object.itemId);
            if (object.quantity != null)
                if ($Number(object.quantity) !== 0)
                    message.quantity = object.quantity | 0;
            return message;
        };

        /**
         * Creates a plain object from a MoveItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.MoveItem
         * @static
         * @param {game.MoveItem} message MoveItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MoveItem.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.fromInventoryId = "";
                object.toInventoryId = "";
                object.itemId = "";
                object.quantity = 0;
            }
            if (message.fromInventoryId != null && $Object.hasOwnProperty.call(message, "fromInventoryId"))
                object.fromInventoryId = message.fromInventoryId;
            if (message.toInventoryId != null && $Object.hasOwnProperty.call(message, "toInventoryId"))
                object.toInventoryId = message.toInventoryId;
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId"))
                object.itemId = message.itemId;
            if (message.quantity != null && $Object.hasOwnProperty.call(message, "quantity"))
                object.quantity = message.quantity;
            return object;
        };

        /**
         * Converts this MoveItem to JSON.
         * @function toJSON
         * @memberof game.MoveItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MoveItem.prototype.toJSON = function() {
            return MoveItem.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MoveItem
         * @function getTypeUrl
         * @memberof game.MoveItem
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MoveItem.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.MoveItem";
        };

        return MoveItem;
    })();

    game.EquipItem = (function() {

        /**
         * Properties of an EquipItem.
         * @typedef {Object} game.EquipItem.$Properties
         * @property {string|null} [inventoryId] EquipItem inventoryId
         * @property {string|null} [itemId] EquipItem itemId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an EquipItem.
         * @memberof game
         * @interface IEquipItem
         * @augments game.EquipItem.$Properties
         * @deprecated Use game.EquipItem.$Properties instead.
         */

        /**
         * Shape of an EquipItem.
         * @typedef {game.EquipItem.$Properties} game.EquipItem.$Shape
         */

        /**
         * Constructs a new EquipItem.
         * @memberof game
         * @classdesc Represents an EquipItem.
         * @constructor
         * @param {game.EquipItem.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const EquipItem = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * EquipItem inventoryId.
         * @member {string} inventoryId
         * @memberof game.EquipItem
         * @instance
         */
        EquipItem.prototype.inventoryId = "";

        /**
         * EquipItem itemId.
         * @member {string} itemId
         * @memberof game.EquipItem
         * @instance
         */
        EquipItem.prototype.itemId = "";

        /**
         * Creates a new EquipItem instance using the specified properties.
         * @function create
         * @memberof game.EquipItem
         * @static
         * @param {game.EquipItem.$Properties=} [properties] Properties to set
         * @returns {game.EquipItem} EquipItem instance
         * @type {{
         *   (properties: game.EquipItem.$Shape): game.EquipItem & game.EquipItem.$Shape;
         *   (properties?: game.EquipItem.$Properties): game.EquipItem;
         * }}
         */
        EquipItem.create = function(properties) {
            return new EquipItem(properties);
        };

        /**
         * Encodes the specified EquipItem message. Does not implicitly {@link game.EquipItem.verify|verify} messages.
         * @function encode
         * @memberof game.EquipItem
         * @static
         * @param {game.EquipItem.$Properties} message EquipItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EquipItem.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.inventoryId != null && $Object.hasOwnProperty.call(message, "inventoryId") && message.inventoryId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.inventoryId);
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId") && message.itemId !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.itemId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EquipItem message, length delimited. Does not implicitly {@link game.EquipItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.EquipItem
         * @static
         * @param {game.EquipItem.$Properties} message EquipItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EquipItem.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an EquipItem message from the specified reader or buffer.
         * @function decode
         * @memberof game.EquipItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.EquipItem & game.EquipItem.$Shape} EquipItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EquipItem.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.EquipItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.inventoryId = value;
                        else
                            delete message.inventoryId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.itemId = value;
                        else
                            delete message.itemId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an EquipItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.EquipItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.EquipItem & game.EquipItem.$Shape} EquipItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EquipItem.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EquipItem message.
         * @function verify
         * @memberof game.EquipItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EquipItem.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.inventoryId != null && $Object.hasOwnProperty.call(message, "inventoryId"))
                if (!$util.isString(message.inventoryId))
                    return "inventoryId: string expected";
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId"))
                if (!$util.isString(message.itemId))
                    return "itemId: string expected";
            return null;
        };

        /**
         * Creates an EquipItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.EquipItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.EquipItem} EquipItem
         */
        EquipItem.fromObject = function (object, _depth) {
            if (object instanceof $root.game.EquipItem)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.EquipItem: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.EquipItem();
            if (object.inventoryId != null)
                if (typeof object.inventoryId !== "string" || object.inventoryId.length)
                    message.inventoryId = $String(object.inventoryId);
            if (object.itemId != null)
                if (typeof object.itemId !== "string" || object.itemId.length)
                    message.itemId = $String(object.itemId);
            return message;
        };

        /**
         * Creates a plain object from an EquipItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.EquipItem
         * @static
         * @param {game.EquipItem} message EquipItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EquipItem.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.inventoryId = "";
                object.itemId = "";
            }
            if (message.inventoryId != null && $Object.hasOwnProperty.call(message, "inventoryId"))
                object.inventoryId = message.inventoryId;
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId"))
                object.itemId = message.itemId;
            return object;
        };

        /**
         * Converts this EquipItem to JSON.
         * @function toJSON
         * @memberof game.EquipItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EquipItem.prototype.toJSON = function() {
            return EquipItem.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for EquipItem
         * @function getTypeUrl
         * @memberof game.EquipItem
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        EquipItem.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.EquipItem";
        };

        return EquipItem;
    })();

    game.UnequipItem = (function() {

        /**
         * Properties of an UnequipItem.
         * @typedef {Object} game.UnequipItem.$Properties
         * @property {string|null} [inventoryId] UnequipItem inventoryId
         * @property {string|null} [slot] UnequipItem slot
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an UnequipItem.
         * @memberof game
         * @interface IUnequipItem
         * @augments game.UnequipItem.$Properties
         * @deprecated Use game.UnequipItem.$Properties instead.
         */

        /**
         * Shape of an UnequipItem.
         * @typedef {game.UnequipItem.$Properties} game.UnequipItem.$Shape
         */

        /**
         * Constructs a new UnequipItem.
         * @memberof game
         * @classdesc Represents an UnequipItem.
         * @constructor
         * @param {game.UnequipItem.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const UnequipItem = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * UnequipItem inventoryId.
         * @member {string} inventoryId
         * @memberof game.UnequipItem
         * @instance
         */
        UnequipItem.prototype.inventoryId = "";

        /**
         * UnequipItem slot.
         * @member {string} slot
         * @memberof game.UnequipItem
         * @instance
         */
        UnequipItem.prototype.slot = "";

        /**
         * Creates a new UnequipItem instance using the specified properties.
         * @function create
         * @memberof game.UnequipItem
         * @static
         * @param {game.UnequipItem.$Properties=} [properties] Properties to set
         * @returns {game.UnequipItem} UnequipItem instance
         * @type {{
         *   (properties: game.UnequipItem.$Shape): game.UnequipItem & game.UnequipItem.$Shape;
         *   (properties?: game.UnequipItem.$Properties): game.UnequipItem;
         * }}
         */
        UnequipItem.create = function(properties) {
            return new UnequipItem(properties);
        };

        /**
         * Encodes the specified UnequipItem message. Does not implicitly {@link game.UnequipItem.verify|verify} messages.
         * @function encode
         * @memberof game.UnequipItem
         * @static
         * @param {game.UnequipItem.$Properties} message UnequipItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UnequipItem.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.inventoryId != null && $Object.hasOwnProperty.call(message, "inventoryId") && message.inventoryId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.inventoryId);
            if (message.slot != null && $Object.hasOwnProperty.call(message, "slot") && message.slot !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.slot);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UnequipItem message, length delimited. Does not implicitly {@link game.UnequipItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.UnequipItem
         * @static
         * @param {game.UnequipItem.$Properties} message UnequipItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UnequipItem.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an UnequipItem message from the specified reader or buffer.
         * @function decode
         * @memberof game.UnequipItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.UnequipItem & game.UnequipItem.$Shape} UnequipItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UnequipItem.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.UnequipItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.inventoryId = value;
                        else
                            delete message.inventoryId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.slot = value;
                        else
                            delete message.slot;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an UnequipItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.UnequipItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.UnequipItem & game.UnequipItem.$Shape} UnequipItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UnequipItem.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an UnequipItem message.
         * @function verify
         * @memberof game.UnequipItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UnequipItem.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.inventoryId != null && $Object.hasOwnProperty.call(message, "inventoryId"))
                if (!$util.isString(message.inventoryId))
                    return "inventoryId: string expected";
            if (message.slot != null && $Object.hasOwnProperty.call(message, "slot"))
                if (!$util.isString(message.slot))
                    return "slot: string expected";
            return null;
        };

        /**
         * Creates an UnequipItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.UnequipItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.UnequipItem} UnequipItem
         */
        UnequipItem.fromObject = function (object, _depth) {
            if (object instanceof $root.game.UnequipItem)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.UnequipItem: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.UnequipItem();
            if (object.inventoryId != null)
                if (typeof object.inventoryId !== "string" || object.inventoryId.length)
                    message.inventoryId = $String(object.inventoryId);
            if (object.slot != null)
                if (typeof object.slot !== "string" || object.slot.length)
                    message.slot = $String(object.slot);
            return message;
        };

        /**
         * Creates a plain object from an UnequipItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.UnequipItem
         * @static
         * @param {game.UnequipItem} message UnequipItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UnequipItem.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.inventoryId = "";
                object.slot = "";
            }
            if (message.inventoryId != null && $Object.hasOwnProperty.call(message, "inventoryId"))
                object.inventoryId = message.inventoryId;
            if (message.slot != null && $Object.hasOwnProperty.call(message, "slot"))
                object.slot = message.slot;
            return object;
        };

        /**
         * Converts this UnequipItem to JSON.
         * @function toJSON
         * @memberof game.UnequipItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UnequipItem.prototype.toJSON = function() {
            return UnequipItem.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for UnequipItem
         * @function getTypeUrl
         * @memberof game.UnequipItem
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        UnequipItem.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.UnequipItem";
        };

        return UnequipItem;
    })();

    game.UseItemRequest = (function() {

        /**
         * Properties of a UseItemRequest.
         * @typedef {Object} game.UseItemRequest.$Properties
         * @property {string|null} [itemId] UseItemRequest itemId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a UseItemRequest.
         * @memberof game
         * @interface IUseItemRequest
         * @augments game.UseItemRequest.$Properties
         * @deprecated Use game.UseItemRequest.$Properties instead.
         */

        /**
         * Shape of a UseItemRequest.
         * @typedef {game.UseItemRequest.$Properties} game.UseItemRequest.$Shape
         */

        /**
         * Constructs a new UseItemRequest.
         * @memberof game
         * @classdesc Represents a UseItemRequest.
         * @constructor
         * @param {game.UseItemRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const UseItemRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * UseItemRequest itemId.
         * @member {string} itemId
         * @memberof game.UseItemRequest
         * @instance
         */
        UseItemRequest.prototype.itemId = "";

        /**
         * Creates a new UseItemRequest instance using the specified properties.
         * @function create
         * @memberof game.UseItemRequest
         * @static
         * @param {game.UseItemRequest.$Properties=} [properties] Properties to set
         * @returns {game.UseItemRequest} UseItemRequest instance
         * @type {{
         *   (properties: game.UseItemRequest.$Shape): game.UseItemRequest & game.UseItemRequest.$Shape;
         *   (properties?: game.UseItemRequest.$Properties): game.UseItemRequest;
         * }}
         */
        UseItemRequest.create = function(properties) {
            return new UseItemRequest(properties);
        };

        /**
         * Encodes the specified UseItemRequest message. Does not implicitly {@link game.UseItemRequest.verify|verify} messages.
         * @function encode
         * @memberof game.UseItemRequest
         * @static
         * @param {game.UseItemRequest.$Properties} message UseItemRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UseItemRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId") && message.itemId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.itemId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UseItemRequest message, length delimited. Does not implicitly {@link game.UseItemRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.UseItemRequest
         * @static
         * @param {game.UseItemRequest.$Properties} message UseItemRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UseItemRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a UseItemRequest message from the specified reader or buffer.
         * @function decode
         * @memberof game.UseItemRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.UseItemRequest & game.UseItemRequest.$Shape} UseItemRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UseItemRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.UseItemRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.itemId = value;
                        else
                            delete message.itemId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a UseItemRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.UseItemRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.UseItemRequest & game.UseItemRequest.$Shape} UseItemRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UseItemRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UseItemRequest message.
         * @function verify
         * @memberof game.UseItemRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UseItemRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId"))
                if (!$util.isString(message.itemId))
                    return "itemId: string expected";
            return null;
        };

        /**
         * Creates a UseItemRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.UseItemRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.UseItemRequest} UseItemRequest
         */
        UseItemRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.UseItemRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.UseItemRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.UseItemRequest();
            if (object.itemId != null)
                if (typeof object.itemId !== "string" || object.itemId.length)
                    message.itemId = $String(object.itemId);
            return message;
        };

        /**
         * Creates a plain object from a UseItemRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.UseItemRequest
         * @static
         * @param {game.UseItemRequest} message UseItemRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UseItemRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.itemId = "";
            if (message.itemId != null && $Object.hasOwnProperty.call(message, "itemId"))
                object.itemId = message.itemId;
            return object;
        };

        /**
         * Converts this UseItemRequest to JSON.
         * @function toJSON
         * @memberof game.UseItemRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UseItemRequest.prototype.toJSON = function() {
            return UseItemRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for UseItemRequest
         * @function getTypeUrl
         * @memberof game.UseItemRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        UseItemRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.UseItemRequest";
        };

        return UseItemRequest;
    })();

    game.SetTargetRequest = (function() {

        /**
         * Properties of a SetTargetRequest.
         * @typedef {Object} game.SetTargetRequest.$Properties
         * @property {string|null} [enemyId] SetTargetRequest enemyId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SetTargetRequest.
         * @memberof game
         * @interface ISetTargetRequest
         * @augments game.SetTargetRequest.$Properties
         * @deprecated Use game.SetTargetRequest.$Properties instead.
         */

        /**
         * Shape of a SetTargetRequest.
         * @typedef {game.SetTargetRequest.$Properties} game.SetTargetRequest.$Shape
         */

        /**
         * Constructs a new SetTargetRequest.
         * @memberof game
         * @classdesc Represents a SetTargetRequest.
         * @constructor
         * @param {game.SetTargetRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SetTargetRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SetTargetRequest enemyId.
         * @member {string} enemyId
         * @memberof game.SetTargetRequest
         * @instance
         */
        SetTargetRequest.prototype.enemyId = "";

        /**
         * Creates a new SetTargetRequest instance using the specified properties.
         * @function create
         * @memberof game.SetTargetRequest
         * @static
         * @param {game.SetTargetRequest.$Properties=} [properties] Properties to set
         * @returns {game.SetTargetRequest} SetTargetRequest instance
         * @type {{
         *   (properties: game.SetTargetRequest.$Shape): game.SetTargetRequest & game.SetTargetRequest.$Shape;
         *   (properties?: game.SetTargetRequest.$Properties): game.SetTargetRequest;
         * }}
         */
        SetTargetRequest.create = function(properties) {
            return new SetTargetRequest(properties);
        };

        /**
         * Encodes the specified SetTargetRequest message. Does not implicitly {@link game.SetTargetRequest.verify|verify} messages.
         * @function encode
         * @memberof game.SetTargetRequest
         * @static
         * @param {game.SetTargetRequest.$Properties} message SetTargetRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetTargetRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.enemyId != null && $Object.hasOwnProperty.call(message, "enemyId") && message.enemyId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.enemyId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SetTargetRequest message, length delimited. Does not implicitly {@link game.SetTargetRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SetTargetRequest
         * @static
         * @param {game.SetTargetRequest.$Properties} message SetTargetRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetTargetRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SetTargetRequest message from the specified reader or buffer.
         * @function decode
         * @memberof game.SetTargetRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SetTargetRequest & game.SetTargetRequest.$Shape} SetTargetRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetTargetRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.SetTargetRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.enemyId = value;
                        else
                            delete message.enemyId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SetTargetRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SetTargetRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SetTargetRequest & game.SetTargetRequest.$Shape} SetTargetRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetTargetRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SetTargetRequest message.
         * @function verify
         * @memberof game.SetTargetRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SetTargetRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.enemyId != null && $Object.hasOwnProperty.call(message, "enemyId"))
                if (!$util.isString(message.enemyId))
                    return "enemyId: string expected";
            return null;
        };

        /**
         * Creates a SetTargetRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SetTargetRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SetTargetRequest} SetTargetRequest
         */
        SetTargetRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.SetTargetRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.SetTargetRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.SetTargetRequest();
            if (object.enemyId != null)
                if (typeof object.enemyId !== "string" || object.enemyId.length)
                    message.enemyId = $String(object.enemyId);
            return message;
        };

        /**
         * Creates a plain object from a SetTargetRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SetTargetRequest
         * @static
         * @param {game.SetTargetRequest} message SetTargetRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SetTargetRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.enemyId = "";
            if (message.enemyId != null && $Object.hasOwnProperty.call(message, "enemyId"))
                object.enemyId = message.enemyId;
            return object;
        };

        /**
         * Converts this SetTargetRequest to JSON.
         * @function toJSON
         * @memberof game.SetTargetRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SetTargetRequest.prototype.toJSON = function() {
            return SetTargetRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SetTargetRequest
         * @function getTypeUrl
         * @memberof game.SetTargetRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SetTargetRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.SetTargetRequest";
        };

        return SetTargetRequest;
    })();

    game.StateSync = (function() {

        /**
         * Properties of a StateSync.
         * @typedef {Object} game.StateSync.$Properties
         * @property {string|null} [sessionId] StateSync sessionId
         * @property {game.Player.$Properties|null} [player] StateSync player
         * @property {Object.<string,game.Inventory.$Properties>|null} [inventory] StateSync inventory
         * @property {Object.<string,game.Building.$Properties>|null} [buildings] StateSync buildings
         * @property {Object.<string,game.Socket.$Properties>|null} [sockets] StateSync sockets
         * @property {game.Workers.$Properties|null} [workers] StateSync workers
         * @property {game.Quests.$Properties|null} [quests] StateSync quests
         * @property {Object.<string,number>|null} [skills] StateSync skills
         * @property {Array.<string>|null} [recipes] StateSync recipes
         * @property {Object.<string,game.Enemy.$Properties>|null} [enemies] StateSync enemies
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a StateSync.
         * @memberof game
         * @interface IStateSync
         * @augments game.StateSync.$Properties
         * @deprecated Use game.StateSync.$Properties instead.
         */

        /**
         * Shape of a StateSync.
         * @typedef {game.StateSync.$Properties} game.StateSync.$Shape
         */

        /**
         * Constructs a new StateSync.
         * @memberof game
         * @classdesc Represents a StateSync.
         * @constructor
         * @param {game.StateSync.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const StateSync = function (properties) {
            this.inventory = {};
            this.buildings = {};
            this.sockets = {};
            this.skills = {};
            this.recipes = [];
            this.enemies = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * StateSync sessionId.
         * @member {string} sessionId
         * @memberof game.StateSync
         * @instance
         */
        StateSync.prototype.sessionId = "";

        /**
         * StateSync player.
         * @member {game.Player.$Properties|null|undefined} player
         * @memberof game.StateSync
         * @instance
         */
        StateSync.prototype.player = null;

        /**
         * StateSync inventory.
         * @member {Object.<string,game.Inventory.$Properties>} inventory
         * @memberof game.StateSync
         * @instance
         */
        StateSync.prototype.inventory = $util.emptyObject;

        /**
         * StateSync buildings.
         * @member {Object.<string,game.Building.$Properties>} buildings
         * @memberof game.StateSync
         * @instance
         */
        StateSync.prototype.buildings = $util.emptyObject;

        /**
         * StateSync sockets.
         * @member {Object.<string,game.Socket.$Properties>} sockets
         * @memberof game.StateSync
         * @instance
         */
        StateSync.prototype.sockets = $util.emptyObject;

        /**
         * StateSync workers.
         * @member {game.Workers.$Properties|null|undefined} workers
         * @memberof game.StateSync
         * @instance
         */
        StateSync.prototype.workers = null;

        /**
         * StateSync quests.
         * @member {game.Quests.$Properties|null|undefined} quests
         * @memberof game.StateSync
         * @instance
         */
        StateSync.prototype.quests = null;

        /**
         * StateSync skills.
         * @member {Object.<string,number>} skills
         * @memberof game.StateSync
         * @instance
         */
        StateSync.prototype.skills = $util.emptyObject;

        /**
         * StateSync recipes.
         * @member {Array.<string>} recipes
         * @memberof game.StateSync
         * @instance
         */
        StateSync.prototype.recipes = $util.emptyArray;

        /**
         * StateSync enemies.
         * @member {Object.<string,game.Enemy.$Properties>} enemies
         * @memberof game.StateSync
         * @instance
         */
        StateSync.prototype.enemies = $util.emptyObject;

        /**
         * Creates a new StateSync instance using the specified properties.
         * @function create
         * @memberof game.StateSync
         * @static
         * @param {game.StateSync.$Properties=} [properties] Properties to set
         * @returns {game.StateSync} StateSync instance
         * @type {{
         *   (properties: game.StateSync.$Shape): game.StateSync & game.StateSync.$Shape;
         *   (properties?: game.StateSync.$Properties): game.StateSync;
         * }}
         */
        StateSync.create = function(properties) {
            return new StateSync(properties);
        };

        /**
         * Encodes the specified StateSync message. Does not implicitly {@link game.StateSync.verify|verify} messages.
         * @function encode
         * @memberof game.StateSync
         * @static
         * @param {game.StateSync.$Properties} message StateSync message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateSync.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sessionId != null && $Object.hasOwnProperty.call(message, "sessionId") && message.sessionId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.sessionId);
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                $root.game.Player.encode(message.player, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.inventory != null && $Object.hasOwnProperty.call(message, "inventory"))
                for (let keys = $Object.keys(message.inventory), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 3, wireType 2 =*/26).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.game.Inventory.encode(message.inventory[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.buildings != null && $Object.hasOwnProperty.call(message, "buildings"))
                for (let keys = $Object.keys(message.buildings), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 4, wireType 2 =*/34).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.game.Building.encode(message.buildings[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.sockets != null && $Object.hasOwnProperty.call(message, "sockets"))
                for (let keys = $Object.keys(message.sockets), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 5, wireType 2 =*/42).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.game.Socket.encode(message.sockets[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.workers != null && $Object.hasOwnProperty.call(message, "workers"))
                $root.game.Workers.encode(message.workers, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.quests != null && $Object.hasOwnProperty.call(message, "quests"))
                $root.game.Quests.encode(message.quests, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.skills != null && $Object.hasOwnProperty.call(message, "skills"))
                for (let keys = $Object.keys(message.skills), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 8, wireType 2 =*/66).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.skills[keys[i]]).ldelim();
            if (message.recipes != null && message.recipes.length)
                for (let i = 0; i < message.recipes.length; ++i)
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.recipes[i]);
            if (message.enemies != null && $Object.hasOwnProperty.call(message, "enemies"))
                for (let keys = $Object.keys(message.enemies), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 10, wireType 2 =*/82).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.game.Enemy.encode(message.enemies[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StateSync message, length delimited. Does not implicitly {@link game.StateSync.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.StateSync
         * @static
         * @param {game.StateSync.$Properties} message StateSync message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateSync.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a StateSync message from the specified reader or buffer.
         * @function decode
         * @memberof game.StateSync
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.StateSync & game.StateSync.$Shape} StateSync
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateSync.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.StateSync(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.sessionId = value;
                        else
                            delete message.sessionId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.player = $root.game.Player.decode(reader, reader.uint32(), $undefined, _depth + 1, message.player);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (message.inventory === $util.emptyObject)
                            message.inventory = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.game.Inventory.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.inventory, key);
                        message.inventory[key] = value || new $root.game.Inventory();
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (message.buildings === $util.emptyObject)
                            message.buildings = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.game.Building.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.buildings, key);
                        message.buildings[key] = value || new $root.game.Building();
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (message.sockets === $util.emptyObject)
                            message.sockets = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.game.Socket.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.sockets, key);
                        message.sockets[key] = value || new $root.game.Socket();
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.workers = $root.game.Workers.decode(reader, reader.uint32(), $undefined, _depth + 1, message.workers);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.quests = $root.game.Quests.decode(reader, reader.uint32(), $undefined, _depth + 1, message.quests);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (message.skills === $util.emptyObject)
                            message.skills = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 0)
                                    break;
                                value = reader.int32();
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.skills, key);
                        message.skills[key] = value;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.recipes && message.recipes.length))
                            message.recipes = [];
                        message.recipes.push(reader.stringVerify());
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (message.enemies === $util.emptyObject)
                            message.enemies = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.game.Enemy.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.enemies, key);
                        message.enemies[key] = value || new $root.game.Enemy();
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StateSync message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.StateSync
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.StateSync & game.StateSync.$Shape} StateSync
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateSync.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateSync message.
         * @function verify
         * @memberof game.StateSync
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateSync.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sessionId != null && $Object.hasOwnProperty.call(message, "sessionId"))
                if (!$util.isString(message.sessionId))
                    return "sessionId: string expected";
            if (message.player != null && $Object.hasOwnProperty.call(message, "player")) {
                let error = $root.game.Player.verify(message.player, _depth + 1);
                if (error)
                    return "player." + error;
            }
            if (message.inventory != null && $Object.hasOwnProperty.call(message, "inventory")) {
                if (!$util.isObject(message.inventory))
                    return "inventory: object expected";
                let key = $Object.keys(message.inventory);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.game.Inventory.verify(message.inventory[key[i]], _depth + 1);
                    if (error)
                        return "inventory." + error;
                }
            }
            if (message.buildings != null && $Object.hasOwnProperty.call(message, "buildings")) {
                if (!$util.isObject(message.buildings))
                    return "buildings: object expected";
                let key = $Object.keys(message.buildings);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.game.Building.verify(message.buildings[key[i]], _depth + 1);
                    if (error)
                        return "buildings." + error;
                }
            }
            if (message.sockets != null && $Object.hasOwnProperty.call(message, "sockets")) {
                if (!$util.isObject(message.sockets))
                    return "sockets: object expected";
                let key = $Object.keys(message.sockets);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.game.Socket.verify(message.sockets[key[i]], _depth + 1);
                    if (error)
                        return "sockets." + error;
                }
            }
            if (message.workers != null && $Object.hasOwnProperty.call(message, "workers")) {
                let error = $root.game.Workers.verify(message.workers, _depth + 1);
                if (error)
                    return "workers." + error;
            }
            if (message.quests != null && $Object.hasOwnProperty.call(message, "quests")) {
                let error = $root.game.Quests.verify(message.quests, _depth + 1);
                if (error)
                    return "quests." + error;
            }
            if (message.skills != null && $Object.hasOwnProperty.call(message, "skills")) {
                if (!$util.isObject(message.skills))
                    return "skills: object expected";
                let key = $Object.keys(message.skills);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.skills[key[i]]))
                        return "skills: integer{k:string} expected";
            }
            if (message.recipes != null && $Object.hasOwnProperty.call(message, "recipes")) {
                if (!$Array.isArray(message.recipes))
                    return "recipes: array expected";
                for (let i = 0; i < message.recipes.length; ++i)
                    if (!$util.isString(message.recipes[i]))
                        return "recipes: string[] expected";
            }
            if (message.enemies != null && $Object.hasOwnProperty.call(message, "enemies")) {
                if (!$util.isObject(message.enemies))
                    return "enemies: object expected";
                let key = $Object.keys(message.enemies);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.game.Enemy.verify(message.enemies[key[i]], _depth + 1);
                    if (error)
                        return "enemies." + error;
                }
            }
            return null;
        };

        /**
         * Creates a StateSync message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.StateSync
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.StateSync} StateSync
         */
        StateSync.fromObject = function (object, _depth) {
            if (object instanceof $root.game.StateSync)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.StateSync: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.StateSync();
            if (object.sessionId != null)
                if (typeof object.sessionId !== "string" || object.sessionId.length)
                    message.sessionId = $String(object.sessionId);
            if (object.player != null) {
                if (!$util.isObject(object.player))
                    throw $TypeError(".game.StateSync.player: object expected");
                message.player = $root.game.Player.fromObject(object.player, _depth + 1);
            }
            if (object.inventory) {
                if (!$util.isObject(object.inventory))
                    throw $TypeError(".game.StateSync.inventory: object expected");
                message.inventory = {};
                for (let keys = $Object.keys(object.inventory), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.inventory, keys[i]);
                    if (!$util.isObject(object.inventory[keys[i]]))
                        throw $TypeError(".game.StateSync.inventory: object expected");
                    message.inventory[keys[i]] = $root.game.Inventory.fromObject(object.inventory[keys[i]], _depth + 1);
                }
            }
            if (object.buildings) {
                if (!$util.isObject(object.buildings))
                    throw $TypeError(".game.StateSync.buildings: object expected");
                message.buildings = {};
                for (let keys = $Object.keys(object.buildings), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.buildings, keys[i]);
                    if (!$util.isObject(object.buildings[keys[i]]))
                        throw $TypeError(".game.StateSync.buildings: object expected");
                    message.buildings[keys[i]] = $root.game.Building.fromObject(object.buildings[keys[i]], _depth + 1);
                }
            }
            if (object.sockets) {
                if (!$util.isObject(object.sockets))
                    throw $TypeError(".game.StateSync.sockets: object expected");
                message.sockets = {};
                for (let keys = $Object.keys(object.sockets), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.sockets, keys[i]);
                    if (!$util.isObject(object.sockets[keys[i]]))
                        throw $TypeError(".game.StateSync.sockets: object expected");
                    message.sockets[keys[i]] = $root.game.Socket.fromObject(object.sockets[keys[i]], _depth + 1);
                }
            }
            if (object.workers != null) {
                if (!$util.isObject(object.workers))
                    throw $TypeError(".game.StateSync.workers: object expected");
                message.workers = $root.game.Workers.fromObject(object.workers, _depth + 1);
            }
            if (object.quests != null) {
                if (!$util.isObject(object.quests))
                    throw $TypeError(".game.StateSync.quests: object expected");
                message.quests = $root.game.Quests.fromObject(object.quests, _depth + 1);
            }
            if (object.skills) {
                if (!$util.isObject(object.skills))
                    throw $TypeError(".game.StateSync.skills: object expected");
                message.skills = {};
                for (let keys = $Object.keys(object.skills), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.skills, keys[i]);
                    message.skills[keys[i]] = object.skills[keys[i]] | 0;
                }
            }
            if (object.recipes) {
                if (!$Array.isArray(object.recipes))
                    throw $TypeError(".game.StateSync.recipes: array expected");
                message.recipes = $Array(object.recipes.length);
                for (let i = 0; i < object.recipes.length; ++i)
                    message.recipes[i] = $String(object.recipes[i]);
            }
            if (object.enemies) {
                if (!$util.isObject(object.enemies))
                    throw $TypeError(".game.StateSync.enemies: object expected");
                message.enemies = {};
                for (let keys = $Object.keys(object.enemies), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.enemies, keys[i]);
                    if (!$util.isObject(object.enemies[keys[i]]))
                        throw $TypeError(".game.StateSync.enemies: object expected");
                    message.enemies[keys[i]] = $root.game.Enemy.fromObject(object.enemies[keys[i]], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a StateSync message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.StateSync
         * @static
         * @param {game.StateSync} message StateSync
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateSync.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.recipes = [];
            if (options.objects || options.defaults) {
                object.inventory = {};
                object.buildings = {};
                object.sockets = {};
                object.skills = {};
                object.enemies = {};
            }
            if (options.defaults) {
                object.sessionId = "";
                object.player = null;
                object.workers = null;
                object.quests = null;
            }
            if (message.sessionId != null && $Object.hasOwnProperty.call(message, "sessionId"))
                object.sessionId = message.sessionId;
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                object.player = $root.game.Player.toObject(message.player, options, _depth + 1);
            let keys2;
            if (message.inventory && (keys2 = $Object.keys(message.inventory)).length) {
                object.inventory = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.inventory, keys2[j]);
                    object.inventory[keys2[j]] = $root.game.Inventory.toObject(message.inventory[keys2[j]], options, _depth + 1);
                }
            }
            if (message.buildings && (keys2 = $Object.keys(message.buildings)).length) {
                object.buildings = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.buildings, keys2[j]);
                    object.buildings[keys2[j]] = $root.game.Building.toObject(message.buildings[keys2[j]], options, _depth + 1);
                }
            }
            if (message.sockets && (keys2 = $Object.keys(message.sockets)).length) {
                object.sockets = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.sockets, keys2[j]);
                    object.sockets[keys2[j]] = $root.game.Socket.toObject(message.sockets[keys2[j]], options, _depth + 1);
                }
            }
            if (message.workers != null && $Object.hasOwnProperty.call(message, "workers"))
                object.workers = $root.game.Workers.toObject(message.workers, options, _depth + 1);
            if (message.quests != null && $Object.hasOwnProperty.call(message, "quests"))
                object.quests = $root.game.Quests.toObject(message.quests, options, _depth + 1);
            if (message.skills && (keys2 = $Object.keys(message.skills)).length) {
                object.skills = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.skills, keys2[j]);
                    object.skills[keys2[j]] = message.skills[keys2[j]];
                }
            }
            if (message.recipes && message.recipes.length) {
                object.recipes = $Array(message.recipes.length);
                for (let j = 0; j < message.recipes.length; ++j)
                    object.recipes[j] = message.recipes[j];
            }
            if (message.enemies && (keys2 = $Object.keys(message.enemies)).length) {
                object.enemies = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.enemies, keys2[j]);
                    object.enemies[keys2[j]] = $root.game.Enemy.toObject(message.enemies[keys2[j]], options, _depth + 1);
                }
            }
            return object;
        };

        /**
         * Converts this StateSync to JSON.
         * @function toJSON
         * @memberof game.StateSync
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateSync.prototype.toJSON = function() {
            return StateSync.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StateSync
         * @function getTypeUrl
         * @memberof game.StateSync
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StateSync.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.StateSync";
        };

        return StateSync;
    })();

    game.Diff = (function() {

        /**
         * Properties of a Diff.
         * @typedef {Object} game.Diff.$Properties
         * @property {string|null} [path] Diff path
         * @property {number|Long|null} [intValue] Diff intValue
         * @property {number|null} [doubleValue] Diff doubleValue
         * @property {boolean|null} [boolValue] Diff boolValue
         * @property {string|null} [stringValue] Diff stringValue
         * @property {game.Stats.$Properties|null} [stats] Diff stats
         * @property {game.DerivedStats.$Properties|null} [derivedStats] Diff derivedStats
         * @property {game.SkillsRanks.$Properties|null} [skills] Diff skills
         * @property {game.Buffs.$Properties|null} [activeBuffs] Diff activeBuffs
         * @property {game.Cooldowns.$Properties|null} [activeCooldowns] Diff activeCooldowns
         * @property {game.Cooldowns.$Properties|null} [pausedCooldowns] Diff pausedCooldowns
         * @property {game.Inventory.$Properties|null} [inventory] Diff inventory
         * @property {game.Workers.$Properties|null} [workers] Diff workers
         * @property {game.Socket.$Properties|null} [socket] Diff socket
         * @property {game.QuestEntry.$Properties|null} [questEntry] Diff questEntry
         * @property {"intValue"|"doubleValue"|"boolValue"|"stringValue"|"stats"|"derivedStats"|"skills"|"activeBuffs"|"activeCooldowns"|"pausedCooldowns"|"inventory"|"workers"|"socket"|"questEntry"} [value] Diff value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Diff.
         * @memberof game
         * @interface IDiff
         * @augments game.Diff.$Properties
         * @deprecated Use game.Diff.$Properties instead.
         */

        /**
         * Narrowed shape of a Diff.
         * @typedef {{
         *   path?: string|null;
         *   intValue?: number|Long|null;
         *   doubleValue?: number|null;
         *   boolValue?: boolean|null;
         *   stringValue?: string|null;
         *   stats?: game.Stats.$Shape|null;
         *   derivedStats?: game.DerivedStats.$Shape|null;
         *   skills?: game.SkillsRanks.$Shape|null;
         *   activeBuffs?: game.Buffs.$Shape|null;
         *   activeCooldowns?: game.Cooldowns.$Shape|null;
         *   pausedCooldowns?: game.Cooldowns.$Shape|null;
         *   inventory?: game.Inventory.$Shape|null;
         *   workers?: game.Workers.$Shape|null;
         *   socket?: game.Socket.$Shape|null;
         *   questEntry?: game.QuestEntry.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ value?: undefined; intValue?: null; doubleValue?: null; boolValue?: null; stringValue?: null; stats?: null; derivedStats?: null; skills?: null; activeBuffs?: null; activeCooldowns?: null; pausedCooldowns?: null; inventory?: null; workers?: null; socket?: null; questEntry?: null }|{ value?: "intValue"; intValue: number|Long; doubleValue?: null; boolValue?: null; stringValue?: null; stats?: null; derivedStats?: null; skills?: null; activeBuffs?: null; activeCooldowns?: null; pausedCooldowns?: null; inventory?: null; workers?: null; socket?: null; questEntry?: null }|{ value?: "doubleValue"; intValue?: null; doubleValue: number; boolValue?: null; stringValue?: null; stats?: null; derivedStats?: null; skills?: null; activeBuffs?: null; activeCooldowns?: null; pausedCooldowns?: null; inventory?: null; workers?: null; socket?: null; questEntry?: null }|{ value?: "boolValue"; intValue?: null; doubleValue?: null; boolValue: boolean; stringValue?: null; stats?: null; derivedStats?: null; skills?: null; activeBuffs?: null; activeCooldowns?: null; pausedCooldowns?: null; inventory?: null; workers?: null; socket?: null; questEntry?: null }|{ value?: "stringValue"; intValue?: null; doubleValue?: null; boolValue?: null; stringValue: string; stats?: null; derivedStats?: null; skills?: null; activeBuffs?: null; activeCooldowns?: null; pausedCooldowns?: null; inventory?: null; workers?: null; socket?: null; questEntry?: null }|{ value?: "stats"; intValue?: null; doubleValue?: null; boolValue?: null; stringValue?: null; stats: game.Stats.$Shape; derivedStats?: null; skills?: null; activeBuffs?: null; activeCooldowns?: null; pausedCooldowns?: null; inventory?: null; workers?: null; socket?: null; questEntry?: null }|{ value?: "derivedStats"; intValue?: null; doubleValue?: null; boolValue?: null; stringValue?: null; stats?: null; derivedStats: game.DerivedStats.$Shape; skills?: null; activeBuffs?: null; activeCooldowns?: null; pausedCooldowns?: null; inventory?: null; workers?: null; socket?: null; questEntry?: null }|{ value?: "skills"; intValue?: null; doubleValue?: null; boolValue?: null; stringValue?: null; stats?: null; derivedStats?: null; skills: game.SkillsRanks.$Shape; activeBuffs?: null; activeCooldowns?: null; pausedCooldowns?: null; inventory?: null; workers?: null; socket?: null; questEntry?: null }|{ value?: "activeBuffs"; intValue?: null; doubleValue?: null; boolValue?: null; stringValue?: null; stats?: null; derivedStats?: null; skills?: null; activeBuffs: game.Buffs.$Shape; activeCooldowns?: null; pausedCooldowns?: null; inventory?: null; workers?: null; socket?: null; questEntry?: null }|{ value?: "activeCooldowns"; intValue?: null; doubleValue?: null; boolValue?: null; stringValue?: null; stats?: null; derivedStats?: null; skills?: null; activeBuffs?: null; activeCooldowns: game.Cooldowns.$Shape; pausedCooldowns?: null; inventory?: null; workers?: null; socket?: null; questEntry?: null }|{ value?: "pausedCooldowns"; intValue?: null; doubleValue?: null; boolValue?: null; stringValue?: null; stats?: null; derivedStats?: null; skills?: null; activeBuffs?: null; activeCooldowns?: null; pausedCooldowns: game.Cooldowns.$Shape; inventory?: null; workers?: null; socket?: null; questEntry?: null }|{ value?: "inventory"; intValue?: null; doubleValue?: null; boolValue?: null; stringValue?: null; stats?: null; derivedStats?: null; skills?: null; activeBuffs?: null; activeCooldowns?: null; pausedCooldowns?: null; inventory: game.Inventory.$Shape; workers?: null; socket?: null; questEntry?: null }|{ value?: "workers"; intValue?: null; doubleValue?: null; boolValue?: null; stringValue?: null; stats?: null; derivedStats?: null; skills?: null; activeBuffs?: null; activeCooldowns?: null; pausedCooldowns?: null; inventory?: null; workers: game.Workers.$Shape; socket?: null; questEntry?: null }|{ value?: "socket"; intValue?: null; doubleValue?: null; boolValue?: null; stringValue?: null; stats?: null; derivedStats?: null; skills?: null; activeBuffs?: null; activeCooldowns?: null; pausedCooldowns?: null; inventory?: null; workers?: null; socket: game.Socket.$Shape; questEntry?: null }|{ value?: "questEntry"; intValue?: null; doubleValue?: null; boolValue?: null; stringValue?: null; stats?: null; derivedStats?: null; skills?: null; activeBuffs?: null; activeCooldowns?: null; pausedCooldowns?: null; inventory?: null; workers?: null; socket?: null; questEntry: game.QuestEntry.$Shape })
         * )} game.Diff.$Shape
         */

        /**
         * Constructs a new Diff.
         * @memberof game
         * @classdesc Represents a Diff.
         * @constructor
         * @param {game.Diff.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Diff = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Diff path.
         * @member {string} path
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.path = "";

        /**
         * Diff intValue.
         * @member {number|Long|null|undefined} intValue
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.intValue = null;

        /**
         * Diff doubleValue.
         * @member {number|null|undefined} doubleValue
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.doubleValue = null;

        /**
         * Diff boolValue.
         * @member {boolean|null|undefined} boolValue
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.boolValue = null;

        /**
         * Diff stringValue.
         * @member {string|null|undefined} stringValue
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.stringValue = null;

        /**
         * Diff stats.
         * @member {game.Stats.$Properties|null|undefined} stats
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.stats = null;

        /**
         * Diff derivedStats.
         * @member {game.DerivedStats.$Properties|null|undefined} derivedStats
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.derivedStats = null;

        /**
         * Diff skills.
         * @member {game.SkillsRanks.$Properties|null|undefined} skills
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.skills = null;

        /**
         * Diff activeBuffs.
         * @member {game.Buffs.$Properties|null|undefined} activeBuffs
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.activeBuffs = null;

        /**
         * Diff activeCooldowns.
         * @member {game.Cooldowns.$Properties|null|undefined} activeCooldowns
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.activeCooldowns = null;

        /**
         * Diff pausedCooldowns.
         * @member {game.Cooldowns.$Properties|null|undefined} pausedCooldowns
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.pausedCooldowns = null;

        /**
         * Diff inventory.
         * @member {game.Inventory.$Properties|null|undefined} inventory
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.inventory = null;

        /**
         * Diff workers.
         * @member {game.Workers.$Properties|null|undefined} workers
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.workers = null;

        /**
         * Diff socket.
         * @member {game.Socket.$Properties|null|undefined} socket
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.socket = null;

        /**
         * Diff questEntry.
         * @member {game.QuestEntry.$Properties|null|undefined} questEntry
         * @memberof game.Diff
         * @instance
         */
        Diff.prototype.questEntry = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * Diff value.
         * @member {"intValue"|"doubleValue"|"boolValue"|"stringValue"|"stats"|"derivedStats"|"skills"|"activeBuffs"|"activeCooldowns"|"pausedCooldowns"|"inventory"|"workers"|"socket"|"questEntry"|undefined} value
         * @memberof game.Diff
         * @instance
         */
        $Object.defineProperty(Diff.prototype, "value", {
            get: $util.oneOfGetter($oneOfFields = ["intValue", "doubleValue", "boolValue", "stringValue", "stats", "derivedStats", "skills", "activeBuffs", "activeCooldowns", "pausedCooldowns", "inventory", "workers", "socket", "questEntry"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Diff instance using the specified properties.
         * @function create
         * @memberof game.Diff
         * @static
         * @param {game.Diff.$Properties=} [properties] Properties to set
         * @returns {game.Diff} Diff instance
         * @type {{
         *   (properties: game.Diff.$Shape): game.Diff & game.Diff.$Shape;
         *   (properties?: game.Diff.$Properties): game.Diff;
         * }}
         */
        Diff.create = function(properties) {
            return new Diff(properties);
        };

        /**
         * Encodes the specified Diff message. Does not implicitly {@link game.Diff.verify|verify} messages.
         * @function encode
         * @memberof game.Diff
         * @static
         * @param {game.Diff.$Properties} message Diff message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Diff.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.path);
            if (message.intValue != null && $Object.hasOwnProperty.call(message, "intValue"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.intValue);
            if (message.doubleValue != null && $Object.hasOwnProperty.call(message, "doubleValue"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.doubleValue);
            if (message.boolValue != null && $Object.hasOwnProperty.call(message, "boolValue"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.boolValue);
            if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.stringValue);
            if (message.stats != null && $Object.hasOwnProperty.call(message, "stats"))
                $root.game.Stats.encode(message.stats, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.derivedStats != null && $Object.hasOwnProperty.call(message, "derivedStats"))
                $root.game.DerivedStats.encode(message.derivedStats, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.skills != null && $Object.hasOwnProperty.call(message, "skills"))
                $root.game.SkillsRanks.encode(message.skills, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.activeBuffs != null && $Object.hasOwnProperty.call(message, "activeBuffs"))
                $root.game.Buffs.encode(message.activeBuffs, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.activeCooldowns != null && $Object.hasOwnProperty.call(message, "activeCooldowns"))
                $root.game.Cooldowns.encode(message.activeCooldowns, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.pausedCooldowns != null && $Object.hasOwnProperty.call(message, "pausedCooldowns"))
                $root.game.Cooldowns.encode(message.pausedCooldowns, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.inventory != null && $Object.hasOwnProperty.call(message, "inventory"))
                $root.game.Inventory.encode(message.inventory, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.workers != null && $Object.hasOwnProperty.call(message, "workers"))
                $root.game.Workers.encode(message.workers, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.socket != null && $Object.hasOwnProperty.call(message, "socket"))
                $root.game.Socket.encode(message.socket, writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.questEntry != null && $Object.hasOwnProperty.call(message, "questEntry"))
                $root.game.QuestEntry.encode(message.questEntry, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Diff message, length delimited. Does not implicitly {@link game.Diff.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Diff
         * @static
         * @param {game.Diff.$Properties} message Diff message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Diff.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Diff message from the specified reader or buffer.
         * @function decode
         * @memberof game.Diff
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Diff & game.Diff.$Shape} Diff
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Diff.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Diff(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        message.intValue = reader.int64();
                        message.value = "intValue";
                        continue;
                    }
                case 3: {
                        if (wireType !== 1)
                            break;
                        message.doubleValue = reader.double();
                        message.value = "doubleValue";
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        message.boolValue = reader.bool();
                        message.value = "boolValue";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.stringValue = reader.stringVerify();
                        message.value = "stringValue";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.stats = $root.game.Stats.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stats);
                        message.value = "stats";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.derivedStats = $root.game.DerivedStats.decode(reader, reader.uint32(), $undefined, _depth + 1, message.derivedStats);
                        message.value = "derivedStats";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.skills = $root.game.SkillsRanks.decode(reader, reader.uint32(), $undefined, _depth + 1, message.skills);
                        message.value = "skills";
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.activeBuffs = $root.game.Buffs.decode(reader, reader.uint32(), $undefined, _depth + 1, message.activeBuffs);
                        message.value = "activeBuffs";
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.activeCooldowns = $root.game.Cooldowns.decode(reader, reader.uint32(), $undefined, _depth + 1, message.activeCooldowns);
                        message.value = "activeCooldowns";
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.pausedCooldowns = $root.game.Cooldowns.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pausedCooldowns);
                        message.value = "pausedCooldowns";
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.inventory = $root.game.Inventory.decode(reader, reader.uint32(), $undefined, _depth + 1, message.inventory);
                        message.value = "inventory";
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.workers = $root.game.Workers.decode(reader, reader.uint32(), $undefined, _depth + 1, message.workers);
                        message.value = "workers";
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.socket = $root.game.Socket.decode(reader, reader.uint32(), $undefined, _depth + 1, message.socket);
                        message.value = "socket";
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.questEntry = $root.game.QuestEntry.decode(reader, reader.uint32(), $undefined, _depth + 1, message.questEntry);
                        message.value = "questEntry";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Diff message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Diff
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Diff & game.Diff.$Shape} Diff
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Diff.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Diff message.
         * @function verify
         * @memberof game.Diff
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Diff.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                if (!$util.isString(message.path))
                    return "path: string expected";
            if (message.intValue != null && $Object.hasOwnProperty.call(message, "intValue")) {
                properties.value = 1;
                if (!$util.isInteger(message.intValue) && !(message.intValue && $util.isInteger(message.intValue.low) && $util.isInteger(message.intValue.high)))
                    return "intValue: integer|Long expected";
            }
            if (message.doubleValue != null && $Object.hasOwnProperty.call(message, "doubleValue")) {
                if (properties.value === 1)
                    return "value: multiple values";
                properties.value = 1;
                if (typeof message.doubleValue !== "number")
                    return "doubleValue: number expected";
            }
            if (message.boolValue != null && $Object.hasOwnProperty.call(message, "boolValue")) {
                if (properties.value === 1)
                    return "value: multiple values";
                properties.value = 1;
                if (typeof message.boolValue !== "boolean")
                    return "boolValue: boolean expected";
            }
            if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue")) {
                if (properties.value === 1)
                    return "value: multiple values";
                properties.value = 1;
                if (!$util.isString(message.stringValue))
                    return "stringValue: string expected";
            }
            if (message.stats != null && $Object.hasOwnProperty.call(message, "stats")) {
                if (properties.value === 1)
                    return "value: multiple values";
                properties.value = 1;
                {
                    let error = $root.game.Stats.verify(message.stats, _depth + 1);
                    if (error)
                        return "stats." + error;
                }
            }
            if (message.derivedStats != null && $Object.hasOwnProperty.call(message, "derivedStats")) {
                if (properties.value === 1)
                    return "value: multiple values";
                properties.value = 1;
                {
                    let error = $root.game.DerivedStats.verify(message.derivedStats, _depth + 1);
                    if (error)
                        return "derivedStats." + error;
                }
            }
            if (message.skills != null && $Object.hasOwnProperty.call(message, "skills")) {
                if (properties.value === 1)
                    return "value: multiple values";
                properties.value = 1;
                {
                    let error = $root.game.SkillsRanks.verify(message.skills, _depth + 1);
                    if (error)
                        return "skills." + error;
                }
            }
            if (message.activeBuffs != null && $Object.hasOwnProperty.call(message, "activeBuffs")) {
                if (properties.value === 1)
                    return "value: multiple values";
                properties.value = 1;
                {
                    let error = $root.game.Buffs.verify(message.activeBuffs, _depth + 1);
                    if (error)
                        return "activeBuffs." + error;
                }
            }
            if (message.activeCooldowns != null && $Object.hasOwnProperty.call(message, "activeCooldowns")) {
                if (properties.value === 1)
                    return "value: multiple values";
                properties.value = 1;
                {
                    let error = $root.game.Cooldowns.verify(message.activeCooldowns, _depth + 1);
                    if (error)
                        return "activeCooldowns." + error;
                }
            }
            if (message.pausedCooldowns != null && $Object.hasOwnProperty.call(message, "pausedCooldowns")) {
                if (properties.value === 1)
                    return "value: multiple values";
                properties.value = 1;
                {
                    let error = $root.game.Cooldowns.verify(message.pausedCooldowns, _depth + 1);
                    if (error)
                        return "pausedCooldowns." + error;
                }
            }
            if (message.inventory != null && $Object.hasOwnProperty.call(message, "inventory")) {
                if (properties.value === 1)
                    return "value: multiple values";
                properties.value = 1;
                {
                    let error = $root.game.Inventory.verify(message.inventory, _depth + 1);
                    if (error)
                        return "inventory." + error;
                }
            }
            if (message.workers != null && $Object.hasOwnProperty.call(message, "workers")) {
                if (properties.value === 1)
                    return "value: multiple values";
                properties.value = 1;
                {
                    let error = $root.game.Workers.verify(message.workers, _depth + 1);
                    if (error)
                        return "workers." + error;
                }
            }
            if (message.socket != null && $Object.hasOwnProperty.call(message, "socket")) {
                if (properties.value === 1)
                    return "value: multiple values";
                properties.value = 1;
                {
                    let error = $root.game.Socket.verify(message.socket, _depth + 1);
                    if (error)
                        return "socket." + error;
                }
            }
            if (message.questEntry != null && $Object.hasOwnProperty.call(message, "questEntry")) {
                if (properties.value === 1)
                    return "value: multiple values";
                properties.value = 1;
                {
                    let error = $root.game.QuestEntry.verify(message.questEntry, _depth + 1);
                    if (error)
                        return "questEntry." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Diff message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Diff
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Diff} Diff
         */
        Diff.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Diff)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Diff: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Diff();
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            if (object.intValue != null)
                if ($util.Long)
                    message.intValue = $util.Long.fromValue(object.intValue, false);
                else if (typeof object.intValue === "string")
                    message.intValue = $parseInt(object.intValue, 10);
                else if (typeof object.intValue === "number")
                    message.intValue = object.intValue;
                else if (typeof object.intValue === "object")
                    message.intValue = new $util.LongBits(object.intValue.low >>> 0, object.intValue.high >>> 0).toNumber();
            if (object.doubleValue != null)
                message.doubleValue = $Number(object.doubleValue);
            if (object.boolValue != null)
                message.boolValue = $Boolean(object.boolValue);
            if (object.stringValue != null)
                message.stringValue = $String(object.stringValue);
            if (object.stats != null) {
                if (!$util.isObject(object.stats))
                    throw $TypeError(".game.Diff.stats: object expected");
                message.stats = $root.game.Stats.fromObject(object.stats, _depth + 1);
            }
            if (object.derivedStats != null) {
                if (!$util.isObject(object.derivedStats))
                    throw $TypeError(".game.Diff.derivedStats: object expected");
                message.derivedStats = $root.game.DerivedStats.fromObject(object.derivedStats, _depth + 1);
            }
            if (object.skills != null) {
                if (!$util.isObject(object.skills))
                    throw $TypeError(".game.Diff.skills: object expected");
                message.skills = $root.game.SkillsRanks.fromObject(object.skills, _depth + 1);
            }
            if (object.activeBuffs != null) {
                if (!$util.isObject(object.activeBuffs))
                    throw $TypeError(".game.Diff.activeBuffs: object expected");
                message.activeBuffs = $root.game.Buffs.fromObject(object.activeBuffs, _depth + 1);
            }
            if (object.activeCooldowns != null) {
                if (!$util.isObject(object.activeCooldowns))
                    throw $TypeError(".game.Diff.activeCooldowns: object expected");
                message.activeCooldowns = $root.game.Cooldowns.fromObject(object.activeCooldowns, _depth + 1);
            }
            if (object.pausedCooldowns != null) {
                if (!$util.isObject(object.pausedCooldowns))
                    throw $TypeError(".game.Diff.pausedCooldowns: object expected");
                message.pausedCooldowns = $root.game.Cooldowns.fromObject(object.pausedCooldowns, _depth + 1);
            }
            if (object.inventory != null) {
                if (!$util.isObject(object.inventory))
                    throw $TypeError(".game.Diff.inventory: object expected");
                message.inventory = $root.game.Inventory.fromObject(object.inventory, _depth + 1);
            }
            if (object.workers != null) {
                if (!$util.isObject(object.workers))
                    throw $TypeError(".game.Diff.workers: object expected");
                message.workers = $root.game.Workers.fromObject(object.workers, _depth + 1);
            }
            if (object.socket != null) {
                if (!$util.isObject(object.socket))
                    throw $TypeError(".game.Diff.socket: object expected");
                message.socket = $root.game.Socket.fromObject(object.socket, _depth + 1);
            }
            if (object.questEntry != null) {
                if (!$util.isObject(object.questEntry))
                    throw $TypeError(".game.Diff.questEntry: object expected");
                message.questEntry = $root.game.QuestEntry.fromObject(object.questEntry, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Diff message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Diff
         * @static
         * @param {game.Diff} message Diff
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Diff.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.path = "";
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            if (message.intValue != null && $Object.hasOwnProperty.call(message, "intValue")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.intValue = typeof message.intValue === "number" ? $BigInt(message.intValue) : $util.Long.fromBits(message.intValue.low >>> 0, message.intValue.high >>> 0, false).toBigInt();
                else if (typeof message.intValue === "number")
                    object.intValue = options.longs === $String ? $String(message.intValue) : message.intValue;
                else
                    object.intValue = options.longs === $String ? $util.Long.prototype.toString.call(message.intValue) : options.longs === $Number ? new $util.LongBits(message.intValue.low >>> 0, message.intValue.high >>> 0).toNumber() : message.intValue;
                if (options.oneofs)
                    object.value = "intValue";
            }
            if (message.doubleValue != null && $Object.hasOwnProperty.call(message, "doubleValue")) {
                object.doubleValue = options.json && !$isFinite(message.doubleValue) ? $String(message.doubleValue) : message.doubleValue;
                if (options.oneofs)
                    object.value = "doubleValue";
            }
            if (message.boolValue != null && $Object.hasOwnProperty.call(message, "boolValue")) {
                object.boolValue = message.boolValue;
                if (options.oneofs)
                    object.value = "boolValue";
            }
            if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue")) {
                object.stringValue = message.stringValue;
                if (options.oneofs)
                    object.value = "stringValue";
            }
            if (message.stats != null && $Object.hasOwnProperty.call(message, "stats")) {
                object.stats = $root.game.Stats.toObject(message.stats, options, _depth + 1);
                if (options.oneofs)
                    object.value = "stats";
            }
            if (message.derivedStats != null && $Object.hasOwnProperty.call(message, "derivedStats")) {
                object.derivedStats = $root.game.DerivedStats.toObject(message.derivedStats, options, _depth + 1);
                if (options.oneofs)
                    object.value = "derivedStats";
            }
            if (message.skills != null && $Object.hasOwnProperty.call(message, "skills")) {
                object.skills = $root.game.SkillsRanks.toObject(message.skills, options, _depth + 1);
                if (options.oneofs)
                    object.value = "skills";
            }
            if (message.activeBuffs != null && $Object.hasOwnProperty.call(message, "activeBuffs")) {
                object.activeBuffs = $root.game.Buffs.toObject(message.activeBuffs, options, _depth + 1);
                if (options.oneofs)
                    object.value = "activeBuffs";
            }
            if (message.activeCooldowns != null && $Object.hasOwnProperty.call(message, "activeCooldowns")) {
                object.activeCooldowns = $root.game.Cooldowns.toObject(message.activeCooldowns, options, _depth + 1);
                if (options.oneofs)
                    object.value = "activeCooldowns";
            }
            if (message.pausedCooldowns != null && $Object.hasOwnProperty.call(message, "pausedCooldowns")) {
                object.pausedCooldowns = $root.game.Cooldowns.toObject(message.pausedCooldowns, options, _depth + 1);
                if (options.oneofs)
                    object.value = "pausedCooldowns";
            }
            if (message.inventory != null && $Object.hasOwnProperty.call(message, "inventory")) {
                object.inventory = $root.game.Inventory.toObject(message.inventory, options, _depth + 1);
                if (options.oneofs)
                    object.value = "inventory";
            }
            if (message.workers != null && $Object.hasOwnProperty.call(message, "workers")) {
                object.workers = $root.game.Workers.toObject(message.workers, options, _depth + 1);
                if (options.oneofs)
                    object.value = "workers";
            }
            if (message.socket != null && $Object.hasOwnProperty.call(message, "socket")) {
                object.socket = $root.game.Socket.toObject(message.socket, options, _depth + 1);
                if (options.oneofs)
                    object.value = "socket";
            }
            if (message.questEntry != null && $Object.hasOwnProperty.call(message, "questEntry")) {
                object.questEntry = $root.game.QuestEntry.toObject(message.questEntry, options, _depth + 1);
                if (options.oneofs)
                    object.value = "questEntry";
            }
            return object;
        };

        /**
         * Converts this Diff to JSON.
         * @function toJSON
         * @memberof game.Diff
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Diff.prototype.toJSON = function() {
            return Diff.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Diff
         * @function getTypeUrl
         * @memberof game.Diff
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Diff.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Diff";
        };

        return Diff;
    })();

    game.PlayerStats = (function() {

        /**
         * Properties of a PlayerStats.
         * @typedef {Object} game.PlayerStats.$Properties
         * @property {number|Long|null} [exp] PlayerStats exp
         * @property {number|Long|null} [gold] PlayerStats gold
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayerStats.
         * @memberof game
         * @interface IPlayerStats
         * @augments game.PlayerStats.$Properties
         * @deprecated Use game.PlayerStats.$Properties instead.
         */

        /**
         * Shape of a PlayerStats.
         * @typedef {game.PlayerStats.$Properties} game.PlayerStats.$Shape
         */

        /**
         * Constructs a new PlayerStats.
         * @memberof game
         * @classdesc Represents a PlayerStats.
         * @constructor
         * @param {game.PlayerStats.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayerStats = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayerStats exp.
         * @member {number|Long} exp
         * @memberof game.PlayerStats
         * @instance
         */
        PlayerStats.prototype.exp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PlayerStats gold.
         * @member {number|Long} gold
         * @memberof game.PlayerStats
         * @instance
         */
        PlayerStats.prototype.gold = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new PlayerStats instance using the specified properties.
         * @function create
         * @memberof game.PlayerStats
         * @static
         * @param {game.PlayerStats.$Properties=} [properties] Properties to set
         * @returns {game.PlayerStats} PlayerStats instance
         * @type {{
         *   (properties: game.PlayerStats.$Shape): game.PlayerStats & game.PlayerStats.$Shape;
         *   (properties?: game.PlayerStats.$Properties): game.PlayerStats;
         * }}
         */
        PlayerStats.create = function(properties) {
            return new PlayerStats(properties);
        };

        /**
         * Encodes the specified PlayerStats message. Does not implicitly {@link game.PlayerStats.verify|verify} messages.
         * @function encode
         * @memberof game.PlayerStats
         * @static
         * @param {game.PlayerStats.$Properties} message PlayerStats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerStats.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.exp != null && $Object.hasOwnProperty.call(message, "exp") && (typeof message.exp === "object" ? message.exp.low || message.exp.high : message.exp !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.exp);
            if (message.gold != null && $Object.hasOwnProperty.call(message, "gold") && (typeof message.gold === "object" ? message.gold.low || message.gold.high : message.gold !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.gold);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PlayerStats message, length delimited. Does not implicitly {@link game.PlayerStats.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.PlayerStats
         * @static
         * @param {game.PlayerStats.$Properties} message PlayerStats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerStats.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a PlayerStats message from the specified reader or buffer.
         * @function decode
         * @memberof game.PlayerStats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.PlayerStats & game.PlayerStats.$Shape} PlayerStats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerStats.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.PlayerStats(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.exp = value;
                        else
                            delete message.exp;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.gold = value;
                        else
                            delete message.gold;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PlayerStats message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.PlayerStats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.PlayerStats & game.PlayerStats.$Shape} PlayerStats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerStats.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PlayerStats message.
         * @function verify
         * @memberof game.PlayerStats
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerStats.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.exp != null && $Object.hasOwnProperty.call(message, "exp"))
                if (!$util.isInteger(message.exp) && !(message.exp && $util.isInteger(message.exp.low) && $util.isInteger(message.exp.high)))
                    return "exp: integer|Long expected";
            if (message.gold != null && $Object.hasOwnProperty.call(message, "gold"))
                if (!$util.isInteger(message.gold) && !(message.gold && $util.isInteger(message.gold.low) && $util.isInteger(message.gold.high)))
                    return "gold: integer|Long expected";
            return null;
        };

        /**
         * Creates a PlayerStats message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.PlayerStats
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.PlayerStats} PlayerStats
         */
        PlayerStats.fromObject = function (object, _depth) {
            if (object instanceof $root.game.PlayerStats)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.PlayerStats: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.PlayerStats();
            if (object.exp != null)
                if (typeof object.exp === "object" ? object.exp.low || object.exp.high : $Number(object.exp) !== 0)
                    if ($util.Long)
                        message.exp = $util.Long.fromValue(object.exp, false);
                    else if (typeof object.exp === "string")
                        message.exp = $parseInt(object.exp, 10);
                    else if (typeof object.exp === "number")
                        message.exp = object.exp;
                    else if (typeof object.exp === "object")
                        message.exp = new $util.LongBits(object.exp.low >>> 0, object.exp.high >>> 0).toNumber();
            if (object.gold != null)
                if (typeof object.gold === "object" ? object.gold.low || object.gold.high : $Number(object.gold) !== 0)
                    if ($util.Long)
                        message.gold = $util.Long.fromValue(object.gold, false);
                    else if (typeof object.gold === "string")
                        message.gold = $parseInt(object.gold, 10);
                    else if (typeof object.gold === "number")
                        message.gold = object.gold;
                    else if (typeof object.gold === "object")
                        message.gold = new $util.LongBits(object.gold.low >>> 0, object.gold.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a PlayerStats message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.PlayerStats
         * @static
         * @param {game.PlayerStats} message PlayerStats
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PlayerStats.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.exp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.exp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.gold = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gold = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.exp != null && $Object.hasOwnProperty.call(message, "exp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.exp = typeof message.exp === "number" ? $BigInt(message.exp) : $util.Long.fromBits(message.exp.low >>> 0, message.exp.high >>> 0, false).toBigInt();
                else if (typeof message.exp === "number")
                    object.exp = options.longs === $String ? $String(message.exp) : message.exp;
                else
                    object.exp = options.longs === $String ? $util.Long.prototype.toString.call(message.exp) : options.longs === $Number ? new $util.LongBits(message.exp.low >>> 0, message.exp.high >>> 0).toNumber() : message.exp;
            if (message.gold != null && $Object.hasOwnProperty.call(message, "gold"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gold = typeof message.gold === "number" ? $BigInt(message.gold) : $util.Long.fromBits(message.gold.low >>> 0, message.gold.high >>> 0, false).toBigInt();
                else if (typeof message.gold === "number")
                    object.gold = options.longs === $String ? $String(message.gold) : message.gold;
                else
                    object.gold = options.longs === $String ? $util.Long.prototype.toString.call(message.gold) : options.longs === $Number ? new $util.LongBits(message.gold.low >>> 0, message.gold.high >>> 0).toNumber() : message.gold;
            return object;
        };

        /**
         * Converts this PlayerStats to JSON.
         * @function toJSON
         * @memberof game.PlayerStats
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PlayerStats.prototype.toJSON = function() {
            return PlayerStats.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PlayerStats
         * @function getTypeUrl
         * @memberof game.PlayerStats
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PlayerStats.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.PlayerStats";
        };

        return PlayerStats;
    })();

    game.CombatDiff = (function() {

        /**
         * Properties of a CombatDiff.
         * @typedef {Object} game.CombatDiff.$Properties
         * @property {string|null} [enemyId] CombatDiff enemyId
         * @property {boolean|null} [hit] CombatDiff hit
         * @property {number|null} [damageDealt] CombatDiff damageDealt
         * @property {boolean|null} [crit] CombatDiff crit
         * @property {string|null} [damageType] CombatDiff damageType
         * @property {string|null} [skillId] CombatDiff skillId
         * @property {string|null} [skillType] CombatDiff skillType
         * @property {number|Long|null} [enemyHp] CombatDiff enemyHp
         * @property {boolean|null} [enemyDead] CombatDiff enemyDead
         * @property {number|Long|null} [expGained] CombatDiff expGained
         * @property {number|Long|null} [goldGained] CombatDiff goldGained
         * @property {game.PlayerStats.$Properties|null} [playerStats] CombatDiff playerStats
         * @property {game.Buff.$Properties|null} [buff] CombatDiff buff
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CombatDiff.
         * @memberof game
         * @interface ICombatDiff
         * @augments game.CombatDiff.$Properties
         * @deprecated Use game.CombatDiff.$Properties instead.
         */

        /**
         * Shape of a CombatDiff.
         * @typedef {game.CombatDiff.$Properties} game.CombatDiff.$Shape
         */

        /**
         * Constructs a new CombatDiff.
         * @memberof game
         * @classdesc Represents a CombatDiff.
         * @constructor
         * @param {game.CombatDiff.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CombatDiff = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CombatDiff enemyId.
         * @member {string} enemyId
         * @memberof game.CombatDiff
         * @instance
         */
        CombatDiff.prototype.enemyId = "";

        /**
         * CombatDiff hit.
         * @member {boolean} hit
         * @memberof game.CombatDiff
         * @instance
         */
        CombatDiff.prototype.hit = false;

        /**
         * CombatDiff damageDealt.
         * @member {number} damageDealt
         * @memberof game.CombatDiff
         * @instance
         */
        CombatDiff.prototype.damageDealt = 0;

        /**
         * CombatDiff crit.
         * @member {boolean} crit
         * @memberof game.CombatDiff
         * @instance
         */
        CombatDiff.prototype.crit = false;

        /**
         * CombatDiff damageType.
         * @member {string} damageType
         * @memberof game.CombatDiff
         * @instance
         */
        CombatDiff.prototype.damageType = "";

        /**
         * CombatDiff skillId.
         * @member {string} skillId
         * @memberof game.CombatDiff
         * @instance
         */
        CombatDiff.prototype.skillId = "";

        /**
         * CombatDiff skillType.
         * @member {string} skillType
         * @memberof game.CombatDiff
         * @instance
         */
        CombatDiff.prototype.skillType = "";

        /**
         * CombatDiff enemyHp.
         * @member {number|Long} enemyHp
         * @memberof game.CombatDiff
         * @instance
         */
        CombatDiff.prototype.enemyHp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * CombatDiff enemyDead.
         * @member {boolean} enemyDead
         * @memberof game.CombatDiff
         * @instance
         */
        CombatDiff.prototype.enemyDead = false;

        /**
         * CombatDiff expGained.
         * @member {number|Long} expGained
         * @memberof game.CombatDiff
         * @instance
         */
        CombatDiff.prototype.expGained = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * CombatDiff goldGained.
         * @member {number|Long} goldGained
         * @memberof game.CombatDiff
         * @instance
         */
        CombatDiff.prototype.goldGained = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * CombatDiff playerStats.
         * @member {game.PlayerStats.$Properties|null|undefined} playerStats
         * @memberof game.CombatDiff
         * @instance
         */
        CombatDiff.prototype.playerStats = null;

        /**
         * CombatDiff buff.
         * @member {game.Buff.$Properties|null|undefined} buff
         * @memberof game.CombatDiff
         * @instance
         */
        CombatDiff.prototype.buff = null;

        /**
         * Creates a new CombatDiff instance using the specified properties.
         * @function create
         * @memberof game.CombatDiff
         * @static
         * @param {game.CombatDiff.$Properties=} [properties] Properties to set
         * @returns {game.CombatDiff} CombatDiff instance
         * @type {{
         *   (properties: game.CombatDiff.$Shape): game.CombatDiff & game.CombatDiff.$Shape;
         *   (properties?: game.CombatDiff.$Properties): game.CombatDiff;
         * }}
         */
        CombatDiff.create = function(properties) {
            return new CombatDiff(properties);
        };

        /**
         * Encodes the specified CombatDiff message. Does not implicitly {@link game.CombatDiff.verify|verify} messages.
         * @function encode
         * @memberof game.CombatDiff
         * @static
         * @param {game.CombatDiff.$Properties} message CombatDiff message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CombatDiff.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.enemyId != null && $Object.hasOwnProperty.call(message, "enemyId") && message.enemyId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.enemyId);
            if (message.hit != null && $Object.hasOwnProperty.call(message, "hit") && message.hit !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.hit);
            if (message.damageDealt != null && $Object.hasOwnProperty.call(message, "damageDealt") && !$Object.is(message.damageDealt, 0))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.damageDealt);
            if (message.crit != null && $Object.hasOwnProperty.call(message, "crit") && message.crit !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.crit);
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType") && message.damageType !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.damageType);
            if (message.skillId != null && $Object.hasOwnProperty.call(message, "skillId") && message.skillId !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.skillId);
            if (message.skillType != null && $Object.hasOwnProperty.call(message, "skillType") && message.skillType !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.skillType);
            if (message.enemyHp != null && $Object.hasOwnProperty.call(message, "enemyHp") && (typeof message.enemyHp === "object" ? message.enemyHp.low || message.enemyHp.high : message.enemyHp !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.enemyHp);
            if (message.enemyDead != null && $Object.hasOwnProperty.call(message, "enemyDead") && message.enemyDead !== false)
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.enemyDead);
            if (message.expGained != null && $Object.hasOwnProperty.call(message, "expGained") && (typeof message.expGained === "object" ? message.expGained.low || message.expGained.high : message.expGained !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.expGained);
            if (message.goldGained != null && $Object.hasOwnProperty.call(message, "goldGained") && (typeof message.goldGained === "object" ? message.goldGained.low || message.goldGained.high : message.goldGained !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.goldGained);
            if (message.playerStats != null && $Object.hasOwnProperty.call(message, "playerStats"))
                $root.game.PlayerStats.encode(message.playerStats, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.buff != null && $Object.hasOwnProperty.call(message, "buff"))
                $root.game.Buff.encode(message.buff, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CombatDiff message, length delimited. Does not implicitly {@link game.CombatDiff.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CombatDiff
         * @static
         * @param {game.CombatDiff.$Properties} message CombatDiff message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CombatDiff.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CombatDiff message from the specified reader or buffer.
         * @function decode
         * @memberof game.CombatDiff
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CombatDiff & game.CombatDiff.$Shape} CombatDiff
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CombatDiff.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.CombatDiff(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.enemyId = value;
                        else
                            delete message.enemyId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.hit = value;
                        else
                            delete message.hit;
                        continue;
                    }
                case 3: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.damageDealt = value;
                        else
                            delete message.damageDealt;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.crit = value;
                        else
                            delete message.crit;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.damageType = value;
                        else
                            delete message.damageType;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.skillId = value;
                        else
                            delete message.skillId;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.skillType = value;
                        else
                            delete message.skillType;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.enemyHp = value;
                        else
                            delete message.enemyHp;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enemyDead = value;
                        else
                            delete message.enemyDead;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expGained = value;
                        else
                            delete message.expGained;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.goldGained = value;
                        else
                            delete message.goldGained;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.playerStats = $root.game.PlayerStats.decode(reader, reader.uint32(), $undefined, _depth + 1, message.playerStats);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.buff = $root.game.Buff.decode(reader, reader.uint32(), $undefined, _depth + 1, message.buff);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CombatDiff message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CombatDiff
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CombatDiff & game.CombatDiff.$Shape} CombatDiff
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CombatDiff.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CombatDiff message.
         * @function verify
         * @memberof game.CombatDiff
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CombatDiff.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.enemyId != null && $Object.hasOwnProperty.call(message, "enemyId"))
                if (!$util.isString(message.enemyId))
                    return "enemyId: string expected";
            if (message.hit != null && $Object.hasOwnProperty.call(message, "hit"))
                if (typeof message.hit !== "boolean")
                    return "hit: boolean expected";
            if (message.damageDealt != null && $Object.hasOwnProperty.call(message, "damageDealt"))
                if (typeof message.damageDealt !== "number")
                    return "damageDealt: number expected";
            if (message.crit != null && $Object.hasOwnProperty.call(message, "crit"))
                if (typeof message.crit !== "boolean")
                    return "crit: boolean expected";
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType"))
                if (!$util.isString(message.damageType))
                    return "damageType: string expected";
            if (message.skillId != null && $Object.hasOwnProperty.call(message, "skillId"))
                if (!$util.isString(message.skillId))
                    return "skillId: string expected";
            if (message.skillType != null && $Object.hasOwnProperty.call(message, "skillType"))
                if (!$util.isString(message.skillType))
                    return "skillType: string expected";
            if (message.enemyHp != null && $Object.hasOwnProperty.call(message, "enemyHp"))
                if (!$util.isInteger(message.enemyHp) && !(message.enemyHp && $util.isInteger(message.enemyHp.low) && $util.isInteger(message.enemyHp.high)))
                    return "enemyHp: integer|Long expected";
            if (message.enemyDead != null && $Object.hasOwnProperty.call(message, "enemyDead"))
                if (typeof message.enemyDead !== "boolean")
                    return "enemyDead: boolean expected";
            if (message.expGained != null && $Object.hasOwnProperty.call(message, "expGained"))
                if (!$util.isInteger(message.expGained) && !(message.expGained && $util.isInteger(message.expGained.low) && $util.isInteger(message.expGained.high)))
                    return "expGained: integer|Long expected";
            if (message.goldGained != null && $Object.hasOwnProperty.call(message, "goldGained"))
                if (!$util.isInteger(message.goldGained) && !(message.goldGained && $util.isInteger(message.goldGained.low) && $util.isInteger(message.goldGained.high)))
                    return "goldGained: integer|Long expected";
            if (message.playerStats != null && $Object.hasOwnProperty.call(message, "playerStats")) {
                let error = $root.game.PlayerStats.verify(message.playerStats, _depth + 1);
                if (error)
                    return "playerStats." + error;
            }
            if (message.buff != null && $Object.hasOwnProperty.call(message, "buff")) {
                let error = $root.game.Buff.verify(message.buff, _depth + 1);
                if (error)
                    return "buff." + error;
            }
            return null;
        };

        /**
         * Creates a CombatDiff message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CombatDiff
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CombatDiff} CombatDiff
         */
        CombatDiff.fromObject = function (object, _depth) {
            if (object instanceof $root.game.CombatDiff)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.CombatDiff: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.CombatDiff();
            if (object.enemyId != null)
                if (typeof object.enemyId !== "string" || object.enemyId.length)
                    message.enemyId = $String(object.enemyId);
            if (object.hit != null)
                if (object.hit)
                    message.hit = $Boolean(object.hit);
            if (object.damageDealt != null)
                if (!$Object.is($Number(object.damageDealt), 0))
                    message.damageDealt = $Number(object.damageDealt);
            if (object.crit != null)
                if (object.crit)
                    message.crit = $Boolean(object.crit);
            if (object.damageType != null)
                if (typeof object.damageType !== "string" || object.damageType.length)
                    message.damageType = $String(object.damageType);
            if (object.skillId != null)
                if (typeof object.skillId !== "string" || object.skillId.length)
                    message.skillId = $String(object.skillId);
            if (object.skillType != null)
                if (typeof object.skillType !== "string" || object.skillType.length)
                    message.skillType = $String(object.skillType);
            if (object.enemyHp != null)
                if (typeof object.enemyHp === "object" ? object.enemyHp.low || object.enemyHp.high : $Number(object.enemyHp) !== 0)
                    if ($util.Long)
                        message.enemyHp = $util.Long.fromValue(object.enemyHp, false);
                    else if (typeof object.enemyHp === "string")
                        message.enemyHp = $parseInt(object.enemyHp, 10);
                    else if (typeof object.enemyHp === "number")
                        message.enemyHp = object.enemyHp;
                    else if (typeof object.enemyHp === "object")
                        message.enemyHp = new $util.LongBits(object.enemyHp.low >>> 0, object.enemyHp.high >>> 0).toNumber();
            if (object.enemyDead != null)
                if (object.enemyDead)
                    message.enemyDead = $Boolean(object.enemyDead);
            if (object.expGained != null)
                if (typeof object.expGained === "object" ? object.expGained.low || object.expGained.high : $Number(object.expGained) !== 0)
                    if ($util.Long)
                        message.expGained = $util.Long.fromValue(object.expGained, false);
                    else if (typeof object.expGained === "string")
                        message.expGained = $parseInt(object.expGained, 10);
                    else if (typeof object.expGained === "number")
                        message.expGained = object.expGained;
                    else if (typeof object.expGained === "object")
                        message.expGained = new $util.LongBits(object.expGained.low >>> 0, object.expGained.high >>> 0).toNumber();
            if (object.goldGained != null)
                if (typeof object.goldGained === "object" ? object.goldGained.low || object.goldGained.high : $Number(object.goldGained) !== 0)
                    if ($util.Long)
                        message.goldGained = $util.Long.fromValue(object.goldGained, false);
                    else if (typeof object.goldGained === "string")
                        message.goldGained = $parseInt(object.goldGained, 10);
                    else if (typeof object.goldGained === "number")
                        message.goldGained = object.goldGained;
                    else if (typeof object.goldGained === "object")
                        message.goldGained = new $util.LongBits(object.goldGained.low >>> 0, object.goldGained.high >>> 0).toNumber();
            if (object.playerStats != null) {
                if (!$util.isObject(object.playerStats))
                    throw $TypeError(".game.CombatDiff.playerStats: object expected");
                message.playerStats = $root.game.PlayerStats.fromObject(object.playerStats, _depth + 1);
            }
            if (object.buff != null) {
                if (!$util.isObject(object.buff))
                    throw $TypeError(".game.CombatDiff.buff: object expected");
                message.buff = $root.game.Buff.fromObject(object.buff, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CombatDiff message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CombatDiff
         * @static
         * @param {game.CombatDiff} message CombatDiff
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CombatDiff.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.enemyId = "";
                object.hit = false;
                object.damageDealt = 0;
                object.crit = false;
                object.damageType = "";
                object.skillId = "";
                object.skillType = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enemyHp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.enemyHp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.enemyDead = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expGained = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expGained = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.goldGained = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.goldGained = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.playerStats = null;
                object.buff = null;
            }
            if (message.enemyId != null && $Object.hasOwnProperty.call(message, "enemyId"))
                object.enemyId = message.enemyId;
            if (message.hit != null && $Object.hasOwnProperty.call(message, "hit"))
                object.hit = message.hit;
            if (message.damageDealt != null && $Object.hasOwnProperty.call(message, "damageDealt"))
                object.damageDealt = options.json && !$isFinite(message.damageDealt) ? $String(message.damageDealt) : message.damageDealt;
            if (message.crit != null && $Object.hasOwnProperty.call(message, "crit"))
                object.crit = message.crit;
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType"))
                object.damageType = message.damageType;
            if (message.skillId != null && $Object.hasOwnProperty.call(message, "skillId"))
                object.skillId = message.skillId;
            if (message.skillType != null && $Object.hasOwnProperty.call(message, "skillType"))
                object.skillType = message.skillType;
            if (message.enemyHp != null && $Object.hasOwnProperty.call(message, "enemyHp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.enemyHp = typeof message.enemyHp === "number" ? $BigInt(message.enemyHp) : $util.Long.fromBits(message.enemyHp.low >>> 0, message.enemyHp.high >>> 0, false).toBigInt();
                else if (typeof message.enemyHp === "number")
                    object.enemyHp = options.longs === $String ? $String(message.enemyHp) : message.enemyHp;
                else
                    object.enemyHp = options.longs === $String ? $util.Long.prototype.toString.call(message.enemyHp) : options.longs === $Number ? new $util.LongBits(message.enemyHp.low >>> 0, message.enemyHp.high >>> 0).toNumber() : message.enemyHp;
            if (message.enemyDead != null && $Object.hasOwnProperty.call(message, "enemyDead"))
                object.enemyDead = message.enemyDead;
            if (message.expGained != null && $Object.hasOwnProperty.call(message, "expGained"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expGained = typeof message.expGained === "number" ? $BigInt(message.expGained) : $util.Long.fromBits(message.expGained.low >>> 0, message.expGained.high >>> 0, false).toBigInt();
                else if (typeof message.expGained === "number")
                    object.expGained = options.longs === $String ? $String(message.expGained) : message.expGained;
                else
                    object.expGained = options.longs === $String ? $util.Long.prototype.toString.call(message.expGained) : options.longs === $Number ? new $util.LongBits(message.expGained.low >>> 0, message.expGained.high >>> 0).toNumber() : message.expGained;
            if (message.goldGained != null && $Object.hasOwnProperty.call(message, "goldGained"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.goldGained = typeof message.goldGained === "number" ? $BigInt(message.goldGained) : $util.Long.fromBits(message.goldGained.low >>> 0, message.goldGained.high >>> 0, false).toBigInt();
                else if (typeof message.goldGained === "number")
                    object.goldGained = options.longs === $String ? $String(message.goldGained) : message.goldGained;
                else
                    object.goldGained = options.longs === $String ? $util.Long.prototype.toString.call(message.goldGained) : options.longs === $Number ? new $util.LongBits(message.goldGained.low >>> 0, message.goldGained.high >>> 0).toNumber() : message.goldGained;
            if (message.playerStats != null && $Object.hasOwnProperty.call(message, "playerStats"))
                object.playerStats = $root.game.PlayerStats.toObject(message.playerStats, options, _depth + 1);
            if (message.buff != null && $Object.hasOwnProperty.call(message, "buff"))
                object.buff = $root.game.Buff.toObject(message.buff, options, _depth + 1);
            return object;
        };

        /**
         * Converts this CombatDiff to JSON.
         * @function toJSON
         * @memberof game.CombatDiff
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CombatDiff.prototype.toJSON = function() {
            return CombatDiff.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CombatDiff
         * @function getTypeUrl
         * @memberof game.CombatDiff
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CombatDiff.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.CombatDiff";
        };

        return CombatDiff;
    })();

    game.EnemyAttack = (function() {

        /**
         * Properties of an EnemyAttack.
         * @typedef {Object} game.EnemyAttack.$Properties
         * @property {string|null} [enemyId] EnemyAttack enemyId
         * @property {number|null} [damageDealt] EnemyAttack damageDealt
         * @property {boolean|null} [hit] EnemyAttack hit
         * @property {boolean|null} [crit] EnemyAttack crit
         * @property {string|null} [damageType] EnemyAttack damageType
         * @property {number|Long|null} [playerHp] EnemyAttack playerHp
         * @property {boolean|null} [playerDead] EnemyAttack playerDead
         * @property {number|Long|null} [nextAttackAt] EnemyAttack nextAttackAt
         * @property {number|Long|null} [nextAttackDelay] EnemyAttack nextAttackDelay
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an EnemyAttack.
         * @memberof game
         * @interface IEnemyAttack
         * @augments game.EnemyAttack.$Properties
         * @deprecated Use game.EnemyAttack.$Properties instead.
         */

        /**
         * Shape of an EnemyAttack.
         * @typedef {game.EnemyAttack.$Properties} game.EnemyAttack.$Shape
         */

        /**
         * Constructs a new EnemyAttack.
         * @memberof game
         * @classdesc Represents an EnemyAttack.
         * @constructor
         * @param {game.EnemyAttack.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const EnemyAttack = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * EnemyAttack enemyId.
         * @member {string} enemyId
         * @memberof game.EnemyAttack
         * @instance
         */
        EnemyAttack.prototype.enemyId = "";

        /**
         * EnemyAttack damageDealt.
         * @member {number} damageDealt
         * @memberof game.EnemyAttack
         * @instance
         */
        EnemyAttack.prototype.damageDealt = 0;

        /**
         * EnemyAttack hit.
         * @member {boolean} hit
         * @memberof game.EnemyAttack
         * @instance
         */
        EnemyAttack.prototype.hit = false;

        /**
         * EnemyAttack crit.
         * @member {boolean} crit
         * @memberof game.EnemyAttack
         * @instance
         */
        EnemyAttack.prototype.crit = false;

        /**
         * EnemyAttack damageType.
         * @member {string} damageType
         * @memberof game.EnemyAttack
         * @instance
         */
        EnemyAttack.prototype.damageType = "";

        /**
         * EnemyAttack playerHp.
         * @member {number|Long} playerHp
         * @memberof game.EnemyAttack
         * @instance
         */
        EnemyAttack.prototype.playerHp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EnemyAttack playerDead.
         * @member {boolean} playerDead
         * @memberof game.EnemyAttack
         * @instance
         */
        EnemyAttack.prototype.playerDead = false;

        /**
         * EnemyAttack nextAttackAt.
         * @member {number|Long} nextAttackAt
         * @memberof game.EnemyAttack
         * @instance
         */
        EnemyAttack.prototype.nextAttackAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EnemyAttack nextAttackDelay.
         * @member {number|Long} nextAttackDelay
         * @memberof game.EnemyAttack
         * @instance
         */
        EnemyAttack.prototype.nextAttackDelay = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new EnemyAttack instance using the specified properties.
         * @function create
         * @memberof game.EnemyAttack
         * @static
         * @param {game.EnemyAttack.$Properties=} [properties] Properties to set
         * @returns {game.EnemyAttack} EnemyAttack instance
         * @type {{
         *   (properties: game.EnemyAttack.$Shape): game.EnemyAttack & game.EnemyAttack.$Shape;
         *   (properties?: game.EnemyAttack.$Properties): game.EnemyAttack;
         * }}
         */
        EnemyAttack.create = function(properties) {
            return new EnemyAttack(properties);
        };

        /**
         * Encodes the specified EnemyAttack message. Does not implicitly {@link game.EnemyAttack.verify|verify} messages.
         * @function encode
         * @memberof game.EnemyAttack
         * @static
         * @param {game.EnemyAttack.$Properties} message EnemyAttack message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnemyAttack.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.enemyId != null && $Object.hasOwnProperty.call(message, "enemyId") && message.enemyId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.enemyId);
            if (message.damageDealt != null && $Object.hasOwnProperty.call(message, "damageDealt") && !$Object.is(message.damageDealt, 0))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.damageDealt);
            if (message.hit != null && $Object.hasOwnProperty.call(message, "hit") && message.hit !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.hit);
            if (message.crit != null && $Object.hasOwnProperty.call(message, "crit") && message.crit !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.crit);
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType") && message.damageType !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.damageType);
            if (message.playerHp != null && $Object.hasOwnProperty.call(message, "playerHp") && (typeof message.playerHp === "object" ? message.playerHp.low || message.playerHp.high : message.playerHp !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.playerHp);
            if (message.playerDead != null && $Object.hasOwnProperty.call(message, "playerDead") && message.playerDead !== false)
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.playerDead);
            if (message.nextAttackAt != null && $Object.hasOwnProperty.call(message, "nextAttackAt") && (typeof message.nextAttackAt === "object" ? message.nextAttackAt.low || message.nextAttackAt.high : message.nextAttackAt !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.nextAttackAt);
            if (message.nextAttackDelay != null && $Object.hasOwnProperty.call(message, "nextAttackDelay") && (typeof message.nextAttackDelay === "object" ? message.nextAttackDelay.low || message.nextAttackDelay.high : message.nextAttackDelay !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.nextAttackDelay);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EnemyAttack message, length delimited. Does not implicitly {@link game.EnemyAttack.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.EnemyAttack
         * @static
         * @param {game.EnemyAttack.$Properties} message EnemyAttack message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnemyAttack.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an EnemyAttack message from the specified reader or buffer.
         * @function decode
         * @memberof game.EnemyAttack
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.EnemyAttack & game.EnemyAttack.$Shape} EnemyAttack
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnemyAttack.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.EnemyAttack(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.enemyId = value;
                        else
                            delete message.enemyId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.damageDealt = value;
                        else
                            delete message.damageDealt;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.hit = value;
                        else
                            delete message.hit;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.crit = value;
                        else
                            delete message.crit;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.damageType = value;
                        else
                            delete message.damageType;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.playerHp = value;
                        else
                            delete message.playerHp;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.playerDead = value;
                        else
                            delete message.playerDead;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.nextAttackAt = value;
                        else
                            delete message.nextAttackAt;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.nextAttackDelay = value;
                        else
                            delete message.nextAttackDelay;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an EnemyAttack message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.EnemyAttack
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.EnemyAttack & game.EnemyAttack.$Shape} EnemyAttack
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnemyAttack.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EnemyAttack message.
         * @function verify
         * @memberof game.EnemyAttack
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EnemyAttack.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.enemyId != null && $Object.hasOwnProperty.call(message, "enemyId"))
                if (!$util.isString(message.enemyId))
                    return "enemyId: string expected";
            if (message.damageDealt != null && $Object.hasOwnProperty.call(message, "damageDealt"))
                if (typeof message.damageDealt !== "number")
                    return "damageDealt: number expected";
            if (message.hit != null && $Object.hasOwnProperty.call(message, "hit"))
                if (typeof message.hit !== "boolean")
                    return "hit: boolean expected";
            if (message.crit != null && $Object.hasOwnProperty.call(message, "crit"))
                if (typeof message.crit !== "boolean")
                    return "crit: boolean expected";
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType"))
                if (!$util.isString(message.damageType))
                    return "damageType: string expected";
            if (message.playerHp != null && $Object.hasOwnProperty.call(message, "playerHp"))
                if (!$util.isInteger(message.playerHp) && !(message.playerHp && $util.isInteger(message.playerHp.low) && $util.isInteger(message.playerHp.high)))
                    return "playerHp: integer|Long expected";
            if (message.playerDead != null && $Object.hasOwnProperty.call(message, "playerDead"))
                if (typeof message.playerDead !== "boolean")
                    return "playerDead: boolean expected";
            if (message.nextAttackAt != null && $Object.hasOwnProperty.call(message, "nextAttackAt"))
                if (!$util.isInteger(message.nextAttackAt) && !(message.nextAttackAt && $util.isInteger(message.nextAttackAt.low) && $util.isInteger(message.nextAttackAt.high)))
                    return "nextAttackAt: integer|Long expected";
            if (message.nextAttackDelay != null && $Object.hasOwnProperty.call(message, "nextAttackDelay"))
                if (!$util.isInteger(message.nextAttackDelay) && !(message.nextAttackDelay && $util.isInteger(message.nextAttackDelay.low) && $util.isInteger(message.nextAttackDelay.high)))
                    return "nextAttackDelay: integer|Long expected";
            return null;
        };

        /**
         * Creates an EnemyAttack message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.EnemyAttack
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.EnemyAttack} EnemyAttack
         */
        EnemyAttack.fromObject = function (object, _depth) {
            if (object instanceof $root.game.EnemyAttack)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.EnemyAttack: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.EnemyAttack();
            if (object.enemyId != null)
                if (typeof object.enemyId !== "string" || object.enemyId.length)
                    message.enemyId = $String(object.enemyId);
            if (object.damageDealt != null)
                if (!$Object.is($Number(object.damageDealt), 0))
                    message.damageDealt = $Number(object.damageDealt);
            if (object.hit != null)
                if (object.hit)
                    message.hit = $Boolean(object.hit);
            if (object.crit != null)
                if (object.crit)
                    message.crit = $Boolean(object.crit);
            if (object.damageType != null)
                if (typeof object.damageType !== "string" || object.damageType.length)
                    message.damageType = $String(object.damageType);
            if (object.playerHp != null)
                if (typeof object.playerHp === "object" ? object.playerHp.low || object.playerHp.high : $Number(object.playerHp) !== 0)
                    if ($util.Long)
                        message.playerHp = $util.Long.fromValue(object.playerHp, false);
                    else if (typeof object.playerHp === "string")
                        message.playerHp = $parseInt(object.playerHp, 10);
                    else if (typeof object.playerHp === "number")
                        message.playerHp = object.playerHp;
                    else if (typeof object.playerHp === "object")
                        message.playerHp = new $util.LongBits(object.playerHp.low >>> 0, object.playerHp.high >>> 0).toNumber();
            if (object.playerDead != null)
                if (object.playerDead)
                    message.playerDead = $Boolean(object.playerDead);
            if (object.nextAttackAt != null)
                if (typeof object.nextAttackAt === "object" ? object.nextAttackAt.low || object.nextAttackAt.high : $Number(object.nextAttackAt) !== 0)
                    if ($util.Long)
                        message.nextAttackAt = $util.Long.fromValue(object.nextAttackAt, false);
                    else if (typeof object.nextAttackAt === "string")
                        message.nextAttackAt = $parseInt(object.nextAttackAt, 10);
                    else if (typeof object.nextAttackAt === "number")
                        message.nextAttackAt = object.nextAttackAt;
                    else if (typeof object.nextAttackAt === "object")
                        message.nextAttackAt = new $util.LongBits(object.nextAttackAt.low >>> 0, object.nextAttackAt.high >>> 0).toNumber();
            if (object.nextAttackDelay != null)
                if (typeof object.nextAttackDelay === "object" ? object.nextAttackDelay.low || object.nextAttackDelay.high : $Number(object.nextAttackDelay) !== 0)
                    if ($util.Long)
                        message.nextAttackDelay = $util.Long.fromValue(object.nextAttackDelay, false);
                    else if (typeof object.nextAttackDelay === "string")
                        message.nextAttackDelay = $parseInt(object.nextAttackDelay, 10);
                    else if (typeof object.nextAttackDelay === "number")
                        message.nextAttackDelay = object.nextAttackDelay;
                    else if (typeof object.nextAttackDelay === "object")
                        message.nextAttackDelay = new $util.LongBits(object.nextAttackDelay.low >>> 0, object.nextAttackDelay.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an EnemyAttack message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.EnemyAttack
         * @static
         * @param {game.EnemyAttack} message EnemyAttack
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnemyAttack.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.enemyId = "";
                object.damageDealt = 0;
                object.hit = false;
                object.crit = false;
                object.damageType = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.playerHp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.playerHp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.playerDead = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nextAttackAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nextAttackAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nextAttackDelay = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nextAttackDelay = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.enemyId != null && $Object.hasOwnProperty.call(message, "enemyId"))
                object.enemyId = message.enemyId;
            if (message.damageDealt != null && $Object.hasOwnProperty.call(message, "damageDealt"))
                object.damageDealt = options.json && !$isFinite(message.damageDealt) ? $String(message.damageDealt) : message.damageDealt;
            if (message.hit != null && $Object.hasOwnProperty.call(message, "hit"))
                object.hit = message.hit;
            if (message.crit != null && $Object.hasOwnProperty.call(message, "crit"))
                object.crit = message.crit;
            if (message.damageType != null && $Object.hasOwnProperty.call(message, "damageType"))
                object.damageType = message.damageType;
            if (message.playerHp != null && $Object.hasOwnProperty.call(message, "playerHp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.playerHp = typeof message.playerHp === "number" ? $BigInt(message.playerHp) : $util.Long.fromBits(message.playerHp.low >>> 0, message.playerHp.high >>> 0, false).toBigInt();
                else if (typeof message.playerHp === "number")
                    object.playerHp = options.longs === $String ? $String(message.playerHp) : message.playerHp;
                else
                    object.playerHp = options.longs === $String ? $util.Long.prototype.toString.call(message.playerHp) : options.longs === $Number ? new $util.LongBits(message.playerHp.low >>> 0, message.playerHp.high >>> 0).toNumber() : message.playerHp;
            if (message.playerDead != null && $Object.hasOwnProperty.call(message, "playerDead"))
                object.playerDead = message.playerDead;
            if (message.nextAttackAt != null && $Object.hasOwnProperty.call(message, "nextAttackAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nextAttackAt = typeof message.nextAttackAt === "number" ? $BigInt(message.nextAttackAt) : $util.Long.fromBits(message.nextAttackAt.low >>> 0, message.nextAttackAt.high >>> 0, false).toBigInt();
                else if (typeof message.nextAttackAt === "number")
                    object.nextAttackAt = options.longs === $String ? $String(message.nextAttackAt) : message.nextAttackAt;
                else
                    object.nextAttackAt = options.longs === $String ? $util.Long.prototype.toString.call(message.nextAttackAt) : options.longs === $Number ? new $util.LongBits(message.nextAttackAt.low >>> 0, message.nextAttackAt.high >>> 0).toNumber() : message.nextAttackAt;
            if (message.nextAttackDelay != null && $Object.hasOwnProperty.call(message, "nextAttackDelay"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nextAttackDelay = typeof message.nextAttackDelay === "number" ? $BigInt(message.nextAttackDelay) : $util.Long.fromBits(message.nextAttackDelay.low >>> 0, message.nextAttackDelay.high >>> 0, false).toBigInt();
                else if (typeof message.nextAttackDelay === "number")
                    object.nextAttackDelay = options.longs === $String ? $String(message.nextAttackDelay) : message.nextAttackDelay;
                else
                    object.nextAttackDelay = options.longs === $String ? $util.Long.prototype.toString.call(message.nextAttackDelay) : options.longs === $Number ? new $util.LongBits(message.nextAttackDelay.low >>> 0, message.nextAttackDelay.high >>> 0).toNumber() : message.nextAttackDelay;
            return object;
        };

        /**
         * Converts this EnemyAttack to JSON.
         * @function toJSON
         * @memberof game.EnemyAttack
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnemyAttack.prototype.toJSON = function() {
            return EnemyAttack.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for EnemyAttack
         * @function getTypeUrl
         * @memberof game.EnemyAttack
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        EnemyAttack.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.EnemyAttack";
        };

        return EnemyAttack;
    })();

    game.EnemySpawn = (function() {

        /**
         * Properties of an EnemySpawn.
         * @typedef {Object} game.EnemySpawn.$Properties
         * @property {Array.<game.Enemy.$Properties>|null} [enemies] EnemySpawn enemies
         * @property {string|null} [placeId] EnemySpawn placeId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an EnemySpawn.
         * @memberof game
         * @interface IEnemySpawn
         * @augments game.EnemySpawn.$Properties
         * @deprecated Use game.EnemySpawn.$Properties instead.
         */

        /**
         * Shape of an EnemySpawn.
         * @typedef {game.EnemySpawn.$Properties} game.EnemySpawn.$Shape
         */

        /**
         * Constructs a new EnemySpawn.
         * @memberof game
         * @classdesc Represents an EnemySpawn.
         * @constructor
         * @param {game.EnemySpawn.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const EnemySpawn = function (properties) {
            this.enemies = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * EnemySpawn enemies.
         * @member {Array.<game.Enemy.$Properties>} enemies
         * @memberof game.EnemySpawn
         * @instance
         */
        EnemySpawn.prototype.enemies = $util.emptyArray;

        /**
         * EnemySpawn placeId.
         * @member {string} placeId
         * @memberof game.EnemySpawn
         * @instance
         */
        EnemySpawn.prototype.placeId = "";

        /**
         * Creates a new EnemySpawn instance using the specified properties.
         * @function create
         * @memberof game.EnemySpawn
         * @static
         * @param {game.EnemySpawn.$Properties=} [properties] Properties to set
         * @returns {game.EnemySpawn} EnemySpawn instance
         * @type {{
         *   (properties: game.EnemySpawn.$Shape): game.EnemySpawn & game.EnemySpawn.$Shape;
         *   (properties?: game.EnemySpawn.$Properties): game.EnemySpawn;
         * }}
         */
        EnemySpawn.create = function(properties) {
            return new EnemySpawn(properties);
        };

        /**
         * Encodes the specified EnemySpawn message. Does not implicitly {@link game.EnemySpawn.verify|verify} messages.
         * @function encode
         * @memberof game.EnemySpawn
         * @static
         * @param {game.EnemySpawn.$Properties} message EnemySpawn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnemySpawn.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.enemies != null && message.enemies.length)
                for (let i = 0; i < message.enemies.length; ++i)
                    $root.game.Enemy.encode(message.enemies[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId") && message.placeId !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.placeId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EnemySpawn message, length delimited. Does not implicitly {@link game.EnemySpawn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.EnemySpawn
         * @static
         * @param {game.EnemySpawn.$Properties} message EnemySpawn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnemySpawn.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an EnemySpawn message from the specified reader or buffer.
         * @function decode
         * @memberof game.EnemySpawn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.EnemySpawn & game.EnemySpawn.$Shape} EnemySpawn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnemySpawn.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.EnemySpawn(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.enemies && message.enemies.length))
                            message.enemies = [];
                        message.enemies.push($root.game.Enemy.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.placeId = value;
                        else
                            delete message.placeId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an EnemySpawn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.EnemySpawn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.EnemySpawn & game.EnemySpawn.$Shape} EnemySpawn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnemySpawn.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EnemySpawn message.
         * @function verify
         * @memberof game.EnemySpawn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EnemySpawn.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.enemies != null && $Object.hasOwnProperty.call(message, "enemies")) {
                if (!$Array.isArray(message.enemies))
                    return "enemies: array expected";
                for (let i = 0; i < message.enemies.length; ++i) {
                    let error = $root.game.Enemy.verify(message.enemies[i], _depth + 1);
                    if (error)
                        return "enemies." + error;
                }
            }
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                if (!$util.isString(message.placeId))
                    return "placeId: string expected";
            return null;
        };

        /**
         * Creates an EnemySpawn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.EnemySpawn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.EnemySpawn} EnemySpawn
         */
        EnemySpawn.fromObject = function (object, _depth) {
            if (object instanceof $root.game.EnemySpawn)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.EnemySpawn: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.EnemySpawn();
            if (object.enemies) {
                if (!$Array.isArray(object.enemies))
                    throw $TypeError(".game.EnemySpawn.enemies: array expected");
                message.enemies = $Array(object.enemies.length);
                for (let i = 0; i < object.enemies.length; ++i) {
                    if (!$util.isObject(object.enemies[i]))
                        throw $TypeError(".game.EnemySpawn.enemies: object expected");
                    message.enemies[i] = $root.game.Enemy.fromObject(object.enemies[i], _depth + 1);
                }
            }
            if (object.placeId != null)
                if (typeof object.placeId !== "string" || object.placeId.length)
                    message.placeId = $String(object.placeId);
            return message;
        };

        /**
         * Creates a plain object from an EnemySpawn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.EnemySpawn
         * @static
         * @param {game.EnemySpawn} message EnemySpawn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnemySpawn.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.enemies = [];
            if (options.defaults)
                object.placeId = "";
            if (message.enemies && message.enemies.length) {
                object.enemies = $Array(message.enemies.length);
                for (let j = 0; j < message.enemies.length; ++j)
                    object.enemies[j] = $root.game.Enemy.toObject(message.enemies[j], options, _depth + 1);
            }
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                object.placeId = message.placeId;
            return object;
        };

        /**
         * Converts this EnemySpawn to JSON.
         * @function toJSON
         * @memberof game.EnemySpawn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnemySpawn.prototype.toJSON = function() {
            return EnemySpawn.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for EnemySpawn
         * @function getTypeUrl
         * @memberof game.EnemySpawn
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        EnemySpawn.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.EnemySpawn";
        };

        return EnemySpawn;
    })();

    game.ProductionTick = (function() {

        /**
         * Properties of a ProductionTick.
         * @typedef {Object} game.ProductionTick.$Properties
         * @property {game.Item.$Properties|null} [item] ProductionTick item
         * @property {string|null} [placeId] ProductionTick placeId
         * @property {number|null} [socketIndex] ProductionTick socketIndex
         * @property {string|null} [targetPlaceId] ProductionTick targetPlaceId
         * @property {string|null} [workerId] ProductionTick workerId
         * @property {string|null} [workerName] ProductionTick workerName
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ProductionTick.
         * @memberof game
         * @interface IProductionTick
         * @augments game.ProductionTick.$Properties
         * @deprecated Use game.ProductionTick.$Properties instead.
         */

        /**
         * Shape of a ProductionTick.
         * @typedef {game.ProductionTick.$Properties} game.ProductionTick.$Shape
         */

        /**
         * Constructs a new ProductionTick.
         * @memberof game
         * @classdesc Represents a ProductionTick.
         * @constructor
         * @param {game.ProductionTick.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ProductionTick = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ProductionTick item.
         * @member {game.Item.$Properties|null|undefined} item
         * @memberof game.ProductionTick
         * @instance
         */
        ProductionTick.prototype.item = null;

        /**
         * ProductionTick placeId.
         * @member {string} placeId
         * @memberof game.ProductionTick
         * @instance
         */
        ProductionTick.prototype.placeId = "";

        /**
         * ProductionTick socketIndex.
         * @member {number} socketIndex
         * @memberof game.ProductionTick
         * @instance
         */
        ProductionTick.prototype.socketIndex = 0;

        /**
         * ProductionTick targetPlaceId.
         * @member {string} targetPlaceId
         * @memberof game.ProductionTick
         * @instance
         */
        ProductionTick.prototype.targetPlaceId = "";

        /**
         * ProductionTick workerId.
         * @member {string} workerId
         * @memberof game.ProductionTick
         * @instance
         */
        ProductionTick.prototype.workerId = "";

        /**
         * ProductionTick workerName.
         * @member {string} workerName
         * @memberof game.ProductionTick
         * @instance
         */
        ProductionTick.prototype.workerName = "";

        /**
         * Creates a new ProductionTick instance using the specified properties.
         * @function create
         * @memberof game.ProductionTick
         * @static
         * @param {game.ProductionTick.$Properties=} [properties] Properties to set
         * @returns {game.ProductionTick} ProductionTick instance
         * @type {{
         *   (properties: game.ProductionTick.$Shape): game.ProductionTick & game.ProductionTick.$Shape;
         *   (properties?: game.ProductionTick.$Properties): game.ProductionTick;
         * }}
         */
        ProductionTick.create = function(properties) {
            return new ProductionTick(properties);
        };

        /**
         * Encodes the specified ProductionTick message. Does not implicitly {@link game.ProductionTick.verify|verify} messages.
         * @function encode
         * @memberof game.ProductionTick
         * @static
         * @param {game.ProductionTick.$Properties} message ProductionTick message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProductionTick.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.item != null && $Object.hasOwnProperty.call(message, "item"))
                $root.game.Item.encode(message.item, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId") && message.placeId !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.placeId);
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex") && message.socketIndex !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.socketIndex);
            if (message.targetPlaceId != null && $Object.hasOwnProperty.call(message, "targetPlaceId") && message.targetPlaceId !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.targetPlaceId);
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId") && message.workerId !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.workerId);
            if (message.workerName != null && $Object.hasOwnProperty.call(message, "workerName") && message.workerName !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.workerName);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ProductionTick message, length delimited. Does not implicitly {@link game.ProductionTick.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.ProductionTick
         * @static
         * @param {game.ProductionTick.$Properties} message ProductionTick message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProductionTick.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ProductionTick message from the specified reader or buffer.
         * @function decode
         * @memberof game.ProductionTick
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.ProductionTick & game.ProductionTick.$Shape} ProductionTick
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProductionTick.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.ProductionTick(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.item = $root.game.Item.decode(reader, reader.uint32(), $undefined, _depth + 1, message.item);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.placeId = value;
                        else
                            delete message.placeId;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.socketIndex = value;
                        else
                            delete message.socketIndex;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.targetPlaceId = value;
                        else
                            delete message.targetPlaceId;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.workerId = value;
                        else
                            delete message.workerId;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.workerName = value;
                        else
                            delete message.workerName;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ProductionTick message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.ProductionTick
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.ProductionTick & game.ProductionTick.$Shape} ProductionTick
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProductionTick.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ProductionTick message.
         * @function verify
         * @memberof game.ProductionTick
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ProductionTick.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.item != null && $Object.hasOwnProperty.call(message, "item")) {
                let error = $root.game.Item.verify(message.item, _depth + 1);
                if (error)
                    return "item." + error;
            }
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                if (!$util.isString(message.placeId))
                    return "placeId: string expected";
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                if (!$util.isInteger(message.socketIndex))
                    return "socketIndex: integer expected";
            if (message.targetPlaceId != null && $Object.hasOwnProperty.call(message, "targetPlaceId"))
                if (!$util.isString(message.targetPlaceId))
                    return "targetPlaceId: string expected";
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId"))
                if (!$util.isString(message.workerId))
                    return "workerId: string expected";
            if (message.workerName != null && $Object.hasOwnProperty.call(message, "workerName"))
                if (!$util.isString(message.workerName))
                    return "workerName: string expected";
            return null;
        };

        /**
         * Creates a ProductionTick message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.ProductionTick
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.ProductionTick} ProductionTick
         */
        ProductionTick.fromObject = function (object, _depth) {
            if (object instanceof $root.game.ProductionTick)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.ProductionTick: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.ProductionTick();
            if (object.item != null) {
                if (!$util.isObject(object.item))
                    throw $TypeError(".game.ProductionTick.item: object expected");
                message.item = $root.game.Item.fromObject(object.item, _depth + 1);
            }
            if (object.placeId != null)
                if (typeof object.placeId !== "string" || object.placeId.length)
                    message.placeId = $String(object.placeId);
            if (object.socketIndex != null)
                if ($Number(object.socketIndex) !== 0)
                    message.socketIndex = object.socketIndex | 0;
            if (object.targetPlaceId != null)
                if (typeof object.targetPlaceId !== "string" || object.targetPlaceId.length)
                    message.targetPlaceId = $String(object.targetPlaceId);
            if (object.workerId != null)
                if (typeof object.workerId !== "string" || object.workerId.length)
                    message.workerId = $String(object.workerId);
            if (object.workerName != null)
                if (typeof object.workerName !== "string" || object.workerName.length)
                    message.workerName = $String(object.workerName);
            return message;
        };

        /**
         * Creates a plain object from a ProductionTick message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.ProductionTick
         * @static
         * @param {game.ProductionTick} message ProductionTick
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ProductionTick.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.item = null;
                object.placeId = "";
                object.socketIndex = 0;
                object.targetPlaceId = "";
                object.workerId = "";
                object.workerName = "";
            }
            if (message.item != null && $Object.hasOwnProperty.call(message, "item"))
                object.item = $root.game.Item.toObject(message.item, options, _depth + 1);
            if (message.placeId != null && $Object.hasOwnProperty.call(message, "placeId"))
                object.placeId = message.placeId;
            if (message.socketIndex != null && $Object.hasOwnProperty.call(message, "socketIndex"))
                object.socketIndex = message.socketIndex;
            if (message.targetPlaceId != null && $Object.hasOwnProperty.call(message, "targetPlaceId"))
                object.targetPlaceId = message.targetPlaceId;
            if (message.workerId != null && $Object.hasOwnProperty.call(message, "workerId"))
                object.workerId = message.workerId;
            if (message.workerName != null && $Object.hasOwnProperty.call(message, "workerName"))
                object.workerName = message.workerName;
            return object;
        };

        /**
         * Converts this ProductionTick to JSON.
         * @function toJSON
         * @memberof game.ProductionTick
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ProductionTick.prototype.toJSON = function() {
            return ProductionTick.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ProductionTick
         * @function getTypeUrl
         * @memberof game.ProductionTick
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ProductionTick.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.ProductionTick";
        };

        return ProductionTick;
    })();

    game.QuestUpdate = (function() {

        /**
         * Properties of a QuestUpdate.
         * @typedef {Object} game.QuestUpdate.$Properties
         * @property {string|null} [questId] QuestUpdate questId
         * @property {boolean|null} [accepted] QuestUpdate accepted
         * @property {boolean|null} [completed] QuestUpdate completed
         * @property {game.QuestEntry.$Properties|null} [progress] QuestUpdate progress
         * @property {boolean|null} [success] QuestUpdate success
         * @property {game.Rewards.$Properties|null} [rewards] QuestUpdate rewards
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a QuestUpdate.
         * @memberof game
         * @interface IQuestUpdate
         * @augments game.QuestUpdate.$Properties
         * @deprecated Use game.QuestUpdate.$Properties instead.
         */

        /**
         * Shape of a QuestUpdate.
         * @typedef {game.QuestUpdate.$Properties} game.QuestUpdate.$Shape
         */

        /**
         * Constructs a new QuestUpdate.
         * @memberof game
         * @classdesc Represents a QuestUpdate.
         * @constructor
         * @param {game.QuestUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const QuestUpdate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * QuestUpdate questId.
         * @member {string} questId
         * @memberof game.QuestUpdate
         * @instance
         */
        QuestUpdate.prototype.questId = "";

        /**
         * QuestUpdate accepted.
         * @member {boolean} accepted
         * @memberof game.QuestUpdate
         * @instance
         */
        QuestUpdate.prototype.accepted = false;

        /**
         * QuestUpdate completed.
         * @member {boolean} completed
         * @memberof game.QuestUpdate
         * @instance
         */
        QuestUpdate.prototype.completed = false;

        /**
         * QuestUpdate progress.
         * @member {game.QuestEntry.$Properties|null|undefined} progress
         * @memberof game.QuestUpdate
         * @instance
         */
        QuestUpdate.prototype.progress = null;

        /**
         * QuestUpdate success.
         * @member {boolean} success
         * @memberof game.QuestUpdate
         * @instance
         */
        QuestUpdate.prototype.success = false;

        /**
         * QuestUpdate rewards.
         * @member {game.Rewards.$Properties|null|undefined} rewards
         * @memberof game.QuestUpdate
         * @instance
         */
        QuestUpdate.prototype.rewards = null;

        /**
         * Creates a new QuestUpdate instance using the specified properties.
         * @function create
         * @memberof game.QuestUpdate
         * @static
         * @param {game.QuestUpdate.$Properties=} [properties] Properties to set
         * @returns {game.QuestUpdate} QuestUpdate instance
         * @type {{
         *   (properties: game.QuestUpdate.$Shape): game.QuestUpdate & game.QuestUpdate.$Shape;
         *   (properties?: game.QuestUpdate.$Properties): game.QuestUpdate;
         * }}
         */
        QuestUpdate.create = function(properties) {
            return new QuestUpdate(properties);
        };

        /**
         * Encodes the specified QuestUpdate message. Does not implicitly {@link game.QuestUpdate.verify|verify} messages.
         * @function encode
         * @memberof game.QuestUpdate
         * @static
         * @param {game.QuestUpdate.$Properties} message QuestUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QuestUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.questId != null && $Object.hasOwnProperty.call(message, "questId") && message.questId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.questId);
            if (message.accepted != null && $Object.hasOwnProperty.call(message, "accepted") && message.accepted !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.accepted);
            if (message.completed != null && $Object.hasOwnProperty.call(message, "completed") && message.completed !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.completed);
            if (message.progress != null && $Object.hasOwnProperty.call(message, "progress"))
                $root.game.QuestEntry.encode(message.progress, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.success != null && $Object.hasOwnProperty.call(message, "success") && message.success !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.success);
            if (message.rewards != null && $Object.hasOwnProperty.call(message, "rewards"))
                $root.game.Rewards.encode(message.rewards, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified QuestUpdate message, length delimited. Does not implicitly {@link game.QuestUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.QuestUpdate
         * @static
         * @param {game.QuestUpdate.$Properties} message QuestUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QuestUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a QuestUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof game.QuestUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.QuestUpdate & game.QuestUpdate.$Shape} QuestUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QuestUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.QuestUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.questId = value;
                        else
                            delete message.questId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.accepted = value;
                        else
                            delete message.accepted;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.completed = value;
                        else
                            delete message.completed;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.progress = $root.game.QuestEntry.decode(reader, reader.uint32(), $undefined, _depth + 1, message.progress);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.success = value;
                        else
                            delete message.success;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.rewards = $root.game.Rewards.decode(reader, reader.uint32(), $undefined, _depth + 1, message.rewards);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a QuestUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.QuestUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.QuestUpdate & game.QuestUpdate.$Shape} QuestUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QuestUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a QuestUpdate message.
         * @function verify
         * @memberof game.QuestUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        QuestUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.questId != null && $Object.hasOwnProperty.call(message, "questId"))
                if (!$util.isString(message.questId))
                    return "questId: string expected";
            if (message.accepted != null && $Object.hasOwnProperty.call(message, "accepted"))
                if (typeof message.accepted !== "boolean")
                    return "accepted: boolean expected";
            if (message.completed != null && $Object.hasOwnProperty.call(message, "completed"))
                if (typeof message.completed !== "boolean")
                    return "completed: boolean expected";
            if (message.progress != null && $Object.hasOwnProperty.call(message, "progress")) {
                let error = $root.game.QuestEntry.verify(message.progress, _depth + 1);
                if (error)
                    return "progress." + error;
            }
            if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                if (typeof message.success !== "boolean")
                    return "success: boolean expected";
            if (message.rewards != null && $Object.hasOwnProperty.call(message, "rewards")) {
                let error = $root.game.Rewards.verify(message.rewards, _depth + 1);
                if (error)
                    return "rewards." + error;
            }
            return null;
        };

        /**
         * Creates a QuestUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.QuestUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.QuestUpdate} QuestUpdate
         */
        QuestUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.game.QuestUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.QuestUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.QuestUpdate();
            if (object.questId != null)
                if (typeof object.questId !== "string" || object.questId.length)
                    message.questId = $String(object.questId);
            if (object.accepted != null)
                if (object.accepted)
                    message.accepted = $Boolean(object.accepted);
            if (object.completed != null)
                if (object.completed)
                    message.completed = $Boolean(object.completed);
            if (object.progress != null) {
                if (!$util.isObject(object.progress))
                    throw $TypeError(".game.QuestUpdate.progress: object expected");
                message.progress = $root.game.QuestEntry.fromObject(object.progress, _depth + 1);
            }
            if (object.success != null)
                if (object.success)
                    message.success = $Boolean(object.success);
            if (object.rewards != null) {
                if (!$util.isObject(object.rewards))
                    throw $TypeError(".game.QuestUpdate.rewards: object expected");
                message.rewards = $root.game.Rewards.fromObject(object.rewards, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a QuestUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.QuestUpdate
         * @static
         * @param {game.QuestUpdate} message QuestUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QuestUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.questId = "";
                object.accepted = false;
                object.completed = false;
                object.progress = null;
                object.success = false;
                object.rewards = null;
            }
            if (message.questId != null && $Object.hasOwnProperty.call(message, "questId"))
                object.questId = message.questId;
            if (message.accepted != null && $Object.hasOwnProperty.call(message, "accepted"))
                object.accepted = message.accepted;
            if (message.completed != null && $Object.hasOwnProperty.call(message, "completed"))
                object.completed = message.completed;
            if (message.progress != null && $Object.hasOwnProperty.call(message, "progress"))
                object.progress = $root.game.QuestEntry.toObject(message.progress, options, _depth + 1);
            if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                object.success = message.success;
            if (message.rewards != null && $Object.hasOwnProperty.call(message, "rewards"))
                object.rewards = $root.game.Rewards.toObject(message.rewards, options, _depth + 1);
            return object;
        };

        /**
         * Converts this QuestUpdate to JSON.
         * @function toJSON
         * @memberof game.QuestUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QuestUpdate.prototype.toJSON = function() {
            return QuestUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for QuestUpdate
         * @function getTypeUrl
         * @memberof game.QuestUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        QuestUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.QuestUpdate";
        };

        return QuestUpdate;
    })();

    game.InventoryUpdate = (function() {

        /**
         * Properties of an InventoryUpdate.
         * @typedef {Object} game.InventoryUpdate.$Properties
         * @property {Object.<string,game.Inventory.$Properties>|null} [inventories] InventoryUpdate inventories
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an InventoryUpdate.
         * @memberof game
         * @interface IInventoryUpdate
         * @augments game.InventoryUpdate.$Properties
         * @deprecated Use game.InventoryUpdate.$Properties instead.
         */

        /**
         * Shape of an InventoryUpdate.
         * @typedef {game.InventoryUpdate.$Properties} game.InventoryUpdate.$Shape
         */

        /**
         * Constructs a new InventoryUpdate.
         * @memberof game
         * @classdesc Represents an InventoryUpdate.
         * @constructor
         * @param {game.InventoryUpdate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const InventoryUpdate = function (properties) {
            this.inventories = {};
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * InventoryUpdate inventories.
         * @member {Object.<string,game.Inventory.$Properties>} inventories
         * @memberof game.InventoryUpdate
         * @instance
         */
        InventoryUpdate.prototype.inventories = $util.emptyObject;

        /**
         * Creates a new InventoryUpdate instance using the specified properties.
         * @function create
         * @memberof game.InventoryUpdate
         * @static
         * @param {game.InventoryUpdate.$Properties=} [properties] Properties to set
         * @returns {game.InventoryUpdate} InventoryUpdate instance
         * @type {{
         *   (properties: game.InventoryUpdate.$Shape): game.InventoryUpdate & game.InventoryUpdate.$Shape;
         *   (properties?: game.InventoryUpdate.$Properties): game.InventoryUpdate;
         * }}
         */
        InventoryUpdate.create = function(properties) {
            return new InventoryUpdate(properties);
        };

        /**
         * Encodes the specified InventoryUpdate message. Does not implicitly {@link game.InventoryUpdate.verify|verify} messages.
         * @function encode
         * @memberof game.InventoryUpdate
         * @static
         * @param {game.InventoryUpdate.$Properties} message InventoryUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InventoryUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.inventories != null && $Object.hasOwnProperty.call(message, "inventories"))
                for (let keys = $Object.keys(message.inventories), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.game.Inventory.encode(message.inventories[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                }
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InventoryUpdate message, length delimited. Does not implicitly {@link game.InventoryUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.InventoryUpdate
         * @static
         * @param {game.InventoryUpdate.$Properties} message InventoryUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InventoryUpdate.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an InventoryUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof game.InventoryUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.InventoryUpdate & game.InventoryUpdate.$Shape} InventoryUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InventoryUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.InventoryUpdate(), key, value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (message.inventories === $util.emptyObject)
                            message.inventories = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.tag();
                            wireType = tag2 & 7;
                            switch (tag2 >>>= 3) {
                            case 1:
                                if (wireType !== 2)
                                    break;
                                key = reader.stringVerify();
                                continue;
                            case 2:
                                if (wireType !== 2)
                                    break;
                                value = $root.game.Inventory.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                continue;
                            }
                            reader.skipType(wireType, _depth, tag2);
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.inventories, key);
                        message.inventories[key] = value || new $root.game.Inventory();
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an InventoryUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.InventoryUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.InventoryUpdate & game.InventoryUpdate.$Shape} InventoryUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InventoryUpdate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InventoryUpdate message.
         * @function verify
         * @memberof game.InventoryUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InventoryUpdate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.inventories != null && $Object.hasOwnProperty.call(message, "inventories")) {
                if (!$util.isObject(message.inventories))
                    return "inventories: object expected";
                let key = $Object.keys(message.inventories);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.game.Inventory.verify(message.inventories[key[i]], _depth + 1);
                    if (error)
                        return "inventories." + error;
                }
            }
            return null;
        };

        /**
         * Creates an InventoryUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.InventoryUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.InventoryUpdate} InventoryUpdate
         */
        InventoryUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.game.InventoryUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.InventoryUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.InventoryUpdate();
            if (object.inventories) {
                if (!$util.isObject(object.inventories))
                    throw $TypeError(".game.InventoryUpdate.inventories: object expected");
                message.inventories = {};
                for (let keys = $Object.keys(object.inventories), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.inventories, keys[i]);
                    if (!$util.isObject(object.inventories[keys[i]]))
                        throw $TypeError(".game.InventoryUpdate.inventories: object expected");
                    message.inventories[keys[i]] = $root.game.Inventory.fromObject(object.inventories[keys[i]], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an InventoryUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.InventoryUpdate
         * @static
         * @param {game.InventoryUpdate} message InventoryUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InventoryUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.inventories = {};
            let keys2;
            if (message.inventories && (keys2 = $Object.keys(message.inventories)).length) {
                object.inventories = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.inventories, keys2[j]);
                    object.inventories[keys2[j]] = $root.game.Inventory.toObject(message.inventories[keys2[j]], options, _depth + 1);
                }
            }
            return object;
        };

        /**
         * Converts this InventoryUpdate to JSON.
         * @function toJSON
         * @memberof game.InventoryUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InventoryUpdate.prototype.toJSON = function() {
            return InventoryUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for InventoryUpdate
         * @function getTypeUrl
         * @memberof game.InventoryUpdate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        InventoryUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.InventoryUpdate";
        };

        return InventoryUpdate;
    })();

    game.Notification = (function() {

        /**
         * Properties of a Notification.
         * @typedef {Object} game.Notification.$Properties
         * @property {string|null} [message] Notification message
         * @property {string|null} [type] Notification type
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Notification.
         * @memberof game
         * @interface INotification
         * @augments game.Notification.$Properties
         * @deprecated Use game.Notification.$Properties instead.
         */

        /**
         * Shape of a Notification.
         * @typedef {game.Notification.$Properties} game.Notification.$Shape
         */

        /**
         * Constructs a new Notification.
         * @memberof game
         * @classdesc Represents a Notification.
         * @constructor
         * @param {game.Notification.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Notification = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Notification message.
         * @member {string} message
         * @memberof game.Notification
         * @instance
         */
        Notification.prototype.message = "";

        /**
         * Notification type.
         * @member {string} type
         * @memberof game.Notification
         * @instance
         */
        Notification.prototype.type = "";

        /**
         * Creates a new Notification instance using the specified properties.
         * @function create
         * @memberof game.Notification
         * @static
         * @param {game.Notification.$Properties=} [properties] Properties to set
         * @returns {game.Notification} Notification instance
         * @type {{
         *   (properties: game.Notification.$Shape): game.Notification & game.Notification.$Shape;
         *   (properties?: game.Notification.$Properties): game.Notification;
         * }}
         */
        Notification.create = function(properties) {
            return new Notification(properties);
        };

        /**
         * Encodes the specified Notification message. Does not implicitly {@link game.Notification.verify|verify} messages.
         * @function encode
         * @memberof game.Notification
         * @static
         * @param {game.Notification.$Properties} message Notification message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Notification.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.message);
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.type);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Notification message, length delimited. Does not implicitly {@link game.Notification.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Notification
         * @static
         * @param {game.Notification.$Properties} message Notification message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Notification.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Notification message from the specified reader or buffer.
         * @function decode
         * @memberof game.Notification
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Notification & game.Notification.$Shape} Notification
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Notification.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Notification(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.message = value;
                        else
                            delete message.message;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Notification message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Notification
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Notification & game.Notification.$Shape} Notification
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Notification.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Notification message.
         * @function verify
         * @memberof game.Notification
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Notification.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            return null;
        };

        /**
         * Creates a Notification message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Notification
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Notification} Notification
         */
        Notification.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Notification)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Notification: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Notification();
            if (object.message != null)
                if (typeof object.message !== "string" || object.message.length)
                    message.message = $String(object.message);
            if (object.type != null)
                if (typeof object.type !== "string" || object.type.length)
                    message.type = $String(object.type);
            return message;
        };

        /**
         * Creates a plain object from a Notification message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Notification
         * @static
         * @param {game.Notification} message Notification
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Notification.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.message = "";
                object.type = "";
            }
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                object.type = message.type;
            return object;
        };

        /**
         * Converts this Notification to JSON.
         * @function toJSON
         * @memberof game.Notification
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Notification.prototype.toJSON = function() {
            return Notification.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Notification
         * @function getTypeUrl
         * @memberof game.Notification
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Notification.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Notification";
        };

        return Notification;
    })();

    game.TradeResult = (function() {

        /**
         * Properties of a TradeResult.
         * @typedef {Object} game.TradeResult.$Properties
         * @property {boolean|null} [success] TradeResult success
         * @property {string|null} [message] TradeResult message
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TradeResult.
         * @memberof game
         * @interface ITradeResult
         * @augments game.TradeResult.$Properties
         * @deprecated Use game.TradeResult.$Properties instead.
         */

        /**
         * Shape of a TradeResult.
         * @typedef {game.TradeResult.$Properties} game.TradeResult.$Shape
         */

        /**
         * Constructs a new TradeResult.
         * @memberof game
         * @classdesc Represents a TradeResult.
         * @constructor
         * @param {game.TradeResult.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TradeResult = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TradeResult success.
         * @member {boolean} success
         * @memberof game.TradeResult
         * @instance
         */
        TradeResult.prototype.success = false;

        /**
         * TradeResult message.
         * @member {string} message
         * @memberof game.TradeResult
         * @instance
         */
        TradeResult.prototype.message = "";

        /**
         * Creates a new TradeResult instance using the specified properties.
         * @function create
         * @memberof game.TradeResult
         * @static
         * @param {game.TradeResult.$Properties=} [properties] Properties to set
         * @returns {game.TradeResult} TradeResult instance
         * @type {{
         *   (properties: game.TradeResult.$Shape): game.TradeResult & game.TradeResult.$Shape;
         *   (properties?: game.TradeResult.$Properties): game.TradeResult;
         * }}
         */
        TradeResult.create = function(properties) {
            return new TradeResult(properties);
        };

        /**
         * Encodes the specified TradeResult message. Does not implicitly {@link game.TradeResult.verify|verify} messages.
         * @function encode
         * @memberof game.TradeResult
         * @static
         * @param {game.TradeResult.$Properties} message TradeResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TradeResult.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.success != null && $Object.hasOwnProperty.call(message, "success") && message.success !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.success);
            if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TradeResult message, length delimited. Does not implicitly {@link game.TradeResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.TradeResult
         * @static
         * @param {game.TradeResult.$Properties} message TradeResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TradeResult.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TradeResult message from the specified reader or buffer.
         * @function decode
         * @memberof game.TradeResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.TradeResult & game.TradeResult.$Shape} TradeResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TradeResult.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.TradeResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.success = value;
                        else
                            delete message.success;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.message = value;
                        else
                            delete message.message;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TradeResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.TradeResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.TradeResult & game.TradeResult.$Shape} TradeResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TradeResult.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TradeResult message.
         * @function verify
         * @memberof game.TradeResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TradeResult.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                if (typeof message.success !== "boolean")
                    return "success: boolean expected";
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            return null;
        };

        /**
         * Creates a TradeResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.TradeResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.TradeResult} TradeResult
         */
        TradeResult.fromObject = function (object, _depth) {
            if (object instanceof $root.game.TradeResult)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.TradeResult: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.TradeResult();
            if (object.success != null)
                if (object.success)
                    message.success = $Boolean(object.success);
            if (object.message != null)
                if (typeof object.message !== "string" || object.message.length)
                    message.message = $String(object.message);
            return message;
        };

        /**
         * Creates a plain object from a TradeResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.TradeResult
         * @static
         * @param {game.TradeResult} message TradeResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TradeResult.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.success = false;
                object.message = "";
            }
            if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                object.success = message.success;
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            return object;
        };

        /**
         * Converts this TradeResult to JSON.
         * @function toJSON
         * @memberof game.TradeResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TradeResult.prototype.toJSON = function() {
            return TradeResult.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TradeResult
         * @function getTypeUrl
         * @memberof game.TradeResult
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TradeResult.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.TradeResult";
        };

        return TradeResult;
    })();

    game.UseResult = (function() {

        /**
         * Properties of a UseResult.
         * @typedef {Object} game.UseResult.$Properties
         * @property {boolean|null} [success] UseResult success
         * @property {string|null} [message] UseResult message
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a UseResult.
         * @memberof game
         * @interface IUseResult
         * @augments game.UseResult.$Properties
         * @deprecated Use game.UseResult.$Properties instead.
         */

        /**
         * Shape of a UseResult.
         * @typedef {game.UseResult.$Properties} game.UseResult.$Shape
         */

        /**
         * Constructs a new UseResult.
         * @memberof game
         * @classdesc Represents a UseResult.
         * @constructor
         * @param {game.UseResult.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const UseResult = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * UseResult success.
         * @member {boolean} success
         * @memberof game.UseResult
         * @instance
         */
        UseResult.prototype.success = false;

        /**
         * UseResult message.
         * @member {string} message
         * @memberof game.UseResult
         * @instance
         */
        UseResult.prototype.message = "";

        /**
         * Creates a new UseResult instance using the specified properties.
         * @function create
         * @memberof game.UseResult
         * @static
         * @param {game.UseResult.$Properties=} [properties] Properties to set
         * @returns {game.UseResult} UseResult instance
         * @type {{
         *   (properties: game.UseResult.$Shape): game.UseResult & game.UseResult.$Shape;
         *   (properties?: game.UseResult.$Properties): game.UseResult;
         * }}
         */
        UseResult.create = function(properties) {
            return new UseResult(properties);
        };

        /**
         * Encodes the specified UseResult message. Does not implicitly {@link game.UseResult.verify|verify} messages.
         * @function encode
         * @memberof game.UseResult
         * @static
         * @param {game.UseResult.$Properties} message UseResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UseResult.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.success != null && $Object.hasOwnProperty.call(message, "success") && message.success !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.success);
            if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UseResult message, length delimited. Does not implicitly {@link game.UseResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.UseResult
         * @static
         * @param {game.UseResult.$Properties} message UseResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UseResult.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a UseResult message from the specified reader or buffer.
         * @function decode
         * @memberof game.UseResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.UseResult & game.UseResult.$Shape} UseResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UseResult.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.UseResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.success = value;
                        else
                            delete message.success;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.message = value;
                        else
                            delete message.message;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a UseResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.UseResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.UseResult & game.UseResult.$Shape} UseResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UseResult.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UseResult message.
         * @function verify
         * @memberof game.UseResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UseResult.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                if (typeof message.success !== "boolean")
                    return "success: boolean expected";
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            return null;
        };

        /**
         * Creates a UseResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.UseResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.UseResult} UseResult
         */
        UseResult.fromObject = function (object, _depth) {
            if (object instanceof $root.game.UseResult)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.UseResult: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.UseResult();
            if (object.success != null)
                if (object.success)
                    message.success = $Boolean(object.success);
            if (object.message != null)
                if (typeof object.message !== "string" || object.message.length)
                    message.message = $String(object.message);
            return message;
        };

        /**
         * Creates a plain object from a UseResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.UseResult
         * @static
         * @param {game.UseResult} message UseResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UseResult.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.success = false;
                object.message = "";
            }
            if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                object.success = message.success;
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            return object;
        };

        /**
         * Converts this UseResult to JSON.
         * @function toJSON
         * @memberof game.UseResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UseResult.prototype.toJSON = function() {
            return UseResult.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for UseResult
         * @function getTypeUrl
         * @memberof game.UseResult
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        UseResult.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.UseResult";
        };

        return UseResult;
    })();

    return game;
})();

export {
  $root as default
};
